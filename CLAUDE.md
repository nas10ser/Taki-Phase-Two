# TAKI — برومت Claude (يتحمّل تلقائياً كل جلسة)

## 👤 من أنا
أنا **ناصر**، مالك TAKI (منصة حجز التخفيضات في السعودية). **لست مبرمجاً** — لا أفهم الكود ولا الأوامر.

## ⚠️ أوامري الثابتة (لا تتغير)
- نفّذ كل شيء بنفسك. **لا تطلب مني أوامر تقنية إلا للضرورة القصوى** — وحين تضطر، أعطني أمراً واحداً جاهزاً للّصق، وقل ماذا أتوقّع أن أرى، وماذا يعني كل احتمال.
- ردّ بالعربي دائماً. وقل لي ماذا أفعل بالضبط (افتح هذا الرابط، اضغط هذا الزر).
- دقة عالية + احترافية + أحدث التقنيات + شكل عصري.
- **بدون دين برمجي** (no tech debt) + أحدث معايير الأمان ضد الاختراقات.
- **لا تقل «تمّ» إلا بعد قياس.** الفرق بين «الكود يبدو صحيحاً» و«جرّبته فنجح» هو ما كلّفنا إصدارات كاملة.

---

## 🏗 البنية التقنية
- React 18 + Parcel 2 + TypeScript + Supabase + Vercel
- المسار المحلي على ماك ناصر: `/Users/nasser/Desktop/TAKI`
- Repo: `https://github.com/nas10ser/Taki-Phase-Two` (فرع `main`) — ⚠️ **عام** فلا يُرفع إليه أي سرّ ولا بيانات عملاء
- الإنتاج: **`https://www.takisa.net`** (النطاق الرئيسي — الجذر `takisa.net` و`taki-test-eight.vercel.app` **يحوّلان إليه بـ308 مع حفظ المسار**)
- القاعدة: **`https://api.takisa.net`** (نفس خادم جدة؛ الاسم القديم `141-147-142-147.sslip.io` يبقى يعمل فلا تنكسر صور العروض القديمة)
- النطاق على **Cloudflare** — كل السجلات **DNS only (سحابة رمادية)**: `www`+`@` CNAME إلى Vercel، و`api` A إلى `141.147.142.147`
- Vercel: `nasser-projects1/taki-test` (مفوّض كـ `nalaumari-8916`، Env vars مشفّرة)
- **الإصدار الحالي: v14.63** (آخر إصدار في `progress.md` — الأحدث في الأعلى) · البوت `14.42.0`

### 🔴 قاعدتان: أيّهما الحقيقية؟
- **جدة = الإنتاج المعتمد** (`api.takisa.net` — وسابقاً `141-147-142-147.sslip.io`، مستضاف ذاتياً على أوراكل). **قرار ناصر (٨ أغسطس ٢٠٢٦): أي تعديل على القاعدة يُعتمد على جدة.**
- **طوكيو `kbmqzxcjdankdgiovctm`** (Supabase MCP) = **مختبر ونسخة احتياطية فقط**. لا يُعتمد إصلاح لأنه نجح عليه.
- **كيف تعرف أيّهما أمامك بيقين** — علامة داخل المخطط:
  ```sql
  SELECT obj_description('public'::regnamespace, 'pg_namespace');
  -- 'TAKI_LAB_TOKYO_MARKER_v1382' = المختبر
  ```
  🪤 **درس مكلف (٢٢ أغسطس):** هجرات أسابيع ذهبت للمختبر لا لجدة، لأن لوحة supabase.com سهلة الفتح وجدة تحتاج طريقاً خاصاً. **كل ملف هجرة يجب أن يبدأ بحارس يرفض التنفيذ على المختبر، وينتهي بجدول ✅/❌ أوّل سطر فيه اسم الخادم.**

### 🔐 مفاتيح الخدمات — مكانٌ واحد لا سجلّات المحادثات
`~/Desktop/TAKI-مفاتيح-الخدمات.txt` (صلاحيات ٦٠٠) يحوي: **مفتاح Render API**
(الخدمة `srv-d8m0eg3bc2fs73eenjb0` · `taki-bot`)، **توكن GitHub**، و**كلمة لوحة
Studio**. اقرأ منه — لا تبحث في سجلّات الجلسات.

🪤 **سجلّات المحادثات سطحُ أسرار.** فُحصت ٣٧ ملفاً في ١٥ سبتمبر ٢٠٢٦ فوُجد فيها
مفتاح Render حيّاً وتوكن GitHub وكلمة Studio وسرّ البوت الذي طبعتُه أنا بنفسي في
مخرجات `psql`. (ولم تظهر `POSTGRES_PASSWORD` ولا `JWT_SECRET` ولا `VAULT_ENC_KEY`.)
نُقلت إلى الملف أعلاه ومُسحت من ٥ ملفات. **لا تطبع قيمة سرّ أبداً**: اقرأها في
متغيّر واستعمل `$VAR`، وإن لزم العرض فاطبع `${V:0:6}…${V: -6}` وحدها.

**تدوير سرّ بوّابة البوت بلا انقطاع** (النمط صار دائماً):
`_bot_gate_ok()` تقبل `bot_gateway_secret` و`bot_gateway_secret_next` معاً، فتُنشأ
القيمة الجديدة في الخزنة (`vault.create_secret`، **تُولَّد على الخادم**) ثم يُحدَّث
متغيّر Render عبر `PUT /v1/services/{id}/env-vars/BOT_GATEWAY_SECRET` ثم
`POST …/deploys`. والبوت يُبلّغ عند كل إقلاع أيّ السرّين يحمل في
`public.bot_gate_usage` — **ولا يُتقاعد القديم بالتخمين**: هجرة التقاعد ترفض
التنفيذ ما لم تقل `next` وما لم يكن البلاغ أحدث من ساعتين.
🪤 `public.app_secrets` **عرضٌ لا جدول** — يقرأ من الخزنة، والكتابة بدوال `vault.*`.

### 🔑 الدخول لخادم جدة (من جهاز ناصر فقط)
```bash
ssh -i ~/.ssh/taki_oracle ubuntu@141.147.142.147
```
- المفتاح `~/.ssh/taki_oracle` على ماك ناصر وحده و**لا يُرفع للمستودع أبداً**.
- جهاز أوراكل: `taki-supabase-jeddah` في `me-jeddah-1` · حاوية القاعدة **`supabase-db`**
- الدور المالك **`supabase_admin`** — **لا `postgres`**، فهو لا يملك الدوال ويفشل بـ`must be owner of function`.
- ⚠️ **«Run Command» في أوراكل غير متاح على هذا الجهاز** (ليست ضمن إضافات الوكيل الإحدى عشرة) — لا تُضِع وقتاً فيها، SSH هو الطريق.
- ⚠️ لوحة Studio محمية بـBasic Auth و**كلمة سرّها مفقودة** (لم تعد لازمة بعد SSH؛ تُقرأ من `.env` على الخادم عند الحاجة).

**تشغيل ملف هجرة على جدة (الطريقة المُختبَرة):**
```bash
ssh -i ~/.ssh/taki_oracle ubuntu@141.147.142.147 \
  'curl -fsSL -o /tmp/m.sql <رابط raw ببصمة الكوميت> && sudo docker exec -i supabase-db \
   psql -U supabase_admin -d postgres -v ON_ERROR_STOP=1 -f - < /tmp/m.sql' \
  2>&1 | tee ~/Desktop/result.txt
```

### 💾 النسخة الاحتياطية اليومية (ضُبطت ٢٢ أغسطس ٢٠٢٦)
- `/usr/local/bin/taki-backup.sh` عبر `/etc/cron.d/taki-backup` — **٣:٣٠ فجراً بتوقيت الرياض**، تسبق تحديثات الأمان التلقائية (٤:٣٠).
- تحتفظ بآخر **١٤** نسخة في `/home/ubuntu/backups` + سجل `backup.log`، **وترفض أي نسخة أصغر من ٥٠٠ كيلوبايت** فلا تُستبدل نسخة سليمة بفاسدة.
- **اختبار الاستعادة أُجري فعلاً ونجح:** ٣٨٤ دالة · ٥٠ جدولاً · ٢١ عرضاً · ٨٩ حجزاً · ١١٢ رسالة — مطابقة للحيّ.
  ```bash
  ssh -i ~/.ssh/taki_oracle ubuntu@141.147.142.147 'sudo bash -s' <<'EOF'
  LATEST=$(ls -1t /home/ubuntu/backups/taki-*.dump | head -1)
  docker exec supabase-db psql -U supabase_admin -d postgres -c 'CREATE DATABASE taki_restore_test;'
  docker exec -i supabase-db pg_restore -U supabase_admin -d taki_restore_test --no-owner --no-privileges < "$LATEST"
  docker exec supabase-db psql -U supabase_admin -d taki_restore_test -c "SELECT count(*) FROM public.bookings;"
  docker exec supabase-db psql -U supabase_admin -d postgres -c 'DROP DATABASE taki_restore_test;'
  EOF
  ```
  (تحذيرات `pg_restore` عن `cron.*` طبيعية — عدّادات pg_cron لا بيانات تطبيق.)
- ✅ **النسخ الخارجية تعمل** (صُحّح في v14.25 — كنتُ قد قلتُ خطأً إنها ناقصة):
  `taki-backup.timer` ٣:١٨ فجراً (كامل: قاعدة + صور + إعدادات) و`taki-backup-hourly.timer`
  الدقيقة ٤٧ من كل ساعة (قاعدة فقط) → أوراكل `taki-backups` مشفّرة AES-256.
  🪤 **السكربتان في `/usr/local/sbin` ومؤقّتا systemd — لا في `/etc/cron.d` ولا
  `/usr/local/bin`.** البحث فيهما وحدهما يُوهم أن لا نسخ خارجية.
- **المراقبة:** `.github/workflows/backup-monitor.yml` يومياً ٥ صباحاً من خارج
  الخادم؛ فشلُه يُرسل بريداً من GitHub. خطّ الأساس في `supabase/ops/backup-baseline.json`
  — **حدِّثه عمداً عند أي تغيّر مقصود في الحجم** وإلا صرخ كل ليلة.
- **التعافي:** `supabase/ops/RECOVERY.md` (سبع خطوات، مُختبَرة ١٤ سبتمبر ٢٠٢٦: ١١/١١).
- **خطة الخادم الثاني:** `supabase/ops/STANDBY-PLAN.md` — بانتظار قرار ناصر.
- ⏰ **رابط رفع النسخ ينتهي ٦ أغسطس ٢٠٢٧** — يتوقّف الرفع بصمت، والمراقبة تصرخ في اليوم التالي.

### 🔍 فحص اكتمال الخادم
`supabase/JEDDAH_COMPLETENESS_CHECK.sql` — يستخرج آلياً كل ما يناديه الكود ويقارنه بالخادم. **يقرأ فقط.**
آخر تشغيل (٢٢ أغسطس): **٢١٦/٢١٦ دالة · ٢٨/٢٨ جدولاً · حارس البوت ✅**.
⚠️ لالتقاط دوال البوت لا تكتفِ بنمط `.rpc('اسم')` — `server/bot.js` يناديها عبر غلاف `rpc(fn, args)` بلا نقطة، وثلاث دوال تُسنَد لمتغيّر (`acknowledge/cancel/complete_booking`).

### 🌐 النطاق (٣١ أغسطس ٢٠٢٦)
`takisa.net` على Cloudflare. الموقع `www.takisa.net` (Vercel، شهادة تلقائية) والقاعدة `api.takisa.net`
(خادم جدة خلف **Caddy** الذي يجلب شهادة Let's Encrypt وحده). Caddy يخدم **الاسمين في كتلة واحدة**
(`{$PROXY_DOMAIN}, api.takisa.net {`) فيتشاركان نفس الإعداد بلا ازدواج.

**الشبكة لم تعد محجوبة في الجلسات** — يمكن الوصول مباشرة إلى الموقع والقاعدة بـ`curl`،
والفحص الأمني الحيّ يُشغَّل محلياً: `cd ~/Desktop/TAKI && bash scripts/security-probe.sh`.

⚠️ **الصور القديمة** في `deals.images` مخزَّنة بعنوان `sslip.io` كاملاً — لذلك يجب أن يبقى ذلك
الاسم يعمل، أو تُعاد كتابة العناوين في القاعدة.

### 📧 قوالب البريد (v13.92)
GoTrue **لا يدعم قالباً لكل لغة**، لكنه يمرّر `raw_user_meta_data` للقالب باسم `.Data`
و**يُمرّر العنوان (Subject) عبر نفس محرّك القوالب** — فقالب واحد يفرّع على `.Data.lang`
يكفي بلا Edge Function ولا خدمة وسيطة. **الافتراضي عربي.**
- القوالب: `supabase/email-templates/*.html` → `bash scripts/deploy-email-templates.sh`
  → يخدمها Caddy على `https://api.takisa.net/email-templates/` (خارج basic_auth عمداً).
  GoTrue يخزّنها **١٠ دقائق**، فأي تعديل يسري خلالها بلا إعادة تشغيل.
- العناوين: `GOTRUE_MAILER_SUBJECTS_*` في `/opt/taki/supabase/docker-compose.taki.yml`
  على جدة (نسخة في `supabase/email-templates/docker-compose.taki.yml` — **عدّلهما معاً**).
- مصدر اللغة: `Register.tsx` يرسل `lang` → `handle_new_user` يحفظها في
  `users.preferred_lang` → تريجر `tr_sync_auth_lang` يزامن أي تغيير لاحق لبيانات الحساب.

🪤 **ثلاثة فخاخ تُسقط القالب بصمت** إلى الإنجليزي المدمج (بلا أي خطأ ظاهر):
`html/template` يرفض اختلاف سياق HTML بين فرعَي `{{ if }}` · `eq .Data.lang "en"` وحدها
تنفجر عند غياب المفتاح فلا بد من حارسَي `{{ if .Data }}{{ if .Data.lang }}` ·
و`$` في ملفات compose تعني متغيّر بيئة فتُكتب `$$`
(و`docker compose config` يُعيد ترميزها فلا يصلح دليلاً — اقرأ المتغيّر من داخل حاوية).

---

## 🔄 آلية التنفيذ (كل تعديل)
1. عدّل الكود في الـworktree الحالي
2. ارفع `CACHE_NAME` في `sw.js` (`taki-cache-vXX.YY`) — **إلزامي لكل نشر**
3. `npm run typecheck` ثم `npm run build` — لازم ينجحا
4. commit برسالة `vX.YY: <الوصف>`
5. `git push origin HEAD:main` — **إذن دائم من ناصر: ادفع إلى `main` مباشرة بلا سؤال.**
6. حدّث `progress.md` (الأحدث في الأعلى)
7. **النشر تلقائي:** Vercel مربوط بـGitHub، فأي دفع إلى `main` يبني وينشر وحده.
8. تحقّق عبر **Vercel MCP** (`list_deployments` → `READY`) لا بـ`curl` (محجوب).
9. أخبر ناصر بالعربي بما فعلت + كيف يتأكّد على جواله

---

## 🪤 فخاخ يجب تجنّبها (دروس مدفوعة الثمن)

### قاعدة البيانات وجدة
- **خادم جدة على توقيت `+03:00` لا UTC** — تحقّق بـ`date` قبل ضبط أي جدولة. (ضُبطت النسخة أولاً على `23:30` ظناً أنها ٢:٣٠ فجراً فكانت ١١:٣٠ ليلاً، وقت ذروة.)
- **`raw.githubusercontent` يخزّن مؤقتاً باسم الفرع** فيعيد نسخة قديمة **بلا إنذار** — استعمل **بصمة الكوميت** في الرابط.
- **«آمن للتكرار» ليس صحيحاً تلقائياً:** كل `CREATE POLICY` يحتاج `DROP POLICY IF EXISTS` **لاسمه هو** لا لاسمه القديم فقط.
- **`CREATE OR REPLACE FUNCTION` لا يغيّر نوع الإرجاع** → `DROP FUNCTION IF EXISTS` أولاً (بلا `CASCADE`).
- **DB trigger `tr_guard_deal_publish`** يرفض أي `UPDATE OF status` حتى بنفس القيمة — استخدم `dealRepository.updateQuantity`.
- **PL/pgSQL `text[] || 'literal'`** ambiguous → cast صريح `::TEXT`.
- **RLS policies:** ممنوع `EXISTS (SELECT FROM T)` داخل policy على نفس الجدول — استخدم `is_admin()` SECURITY DEFINER.
- **admin RPCs مع `RETURNS TABLE`:** qualify الأعمدة (`u.user_type`) وإلا "column reference is ambiguous".
- **`is_admin()` تُرجع TRUE لأي أدمن حين تُنادى من سياسة RLS** — لتقييد أدمن فرعي استعمل `has_admin_permission('tab_*')` صراحةً في السياسة.

### الواجهة
- **iOS Safari يثبت على نسخة قديمة** إذا لم تُرفع `CACHE_NAME`. v10.25+ فيه UpdateBanner أخضر.
- **`useMemo` يستدعي `const`-arrow معرّف بعده** = TDZ على أول render → ErrorBoundary.
- **Notifications routing:** اقرأ `meta_data.audience` (يكتبه DB trigger)، **لا** `user.userType`.
- **`saveProfile`:** النسخة partial-aware — تكتب الحقول المُمرَّرة فقط.
- **Booking complete:** RPC `complete_booking` الذرّي، لا fire-and-forget.
- **`.book-cta` على DealDetails:** لا `bottom: 0` في `@supports (height: 100dvh)` — BottomNav يغطّي زر الحجز.
- **كل مرشِّح `user_type='seller'` يُخفي متجر ناصر** المملوك لحساب أدمن → استعمل `neq('buyer')`.
- **الأزرار الصامتة:** كل كتابة تُرجع `error` يجب فحصه؛ وحذفٌ ترفضه RLS يعود بـ`error=null` وصفر صفوف → أضف `.select()` وتحقّق من العدد.
- **Parcel preview لا يعمل في الجلسات** (EPERM) — اعتمد typecheck + build + Vercel.

### الريل‑تايم
- **افحص صحة القناة لا وجودها:** قناة ماتت بـ`CHANNEL_ERROR` تبقى في `getChannels()` بحالة `errored` — اشترط `state === 'joined'` مع `supabase.realtime.isConnected()`.
- **إعادة الاشتراك لا تُعيد بثّ ما فات** — كل إعادة بناء تحتاج إعادة جلب صريحة، وإلا بقيت الرسالة مفقودة رغم نجاح الاتصال.
- **iOS يقتل الـwebsocket عند التصغير** — لكن المستخدم قد يبقى داخل التطبيق فلا يقع أي حدث visibility؛ لذلك النبض الدوري ضروري.

---

## 📁 ملفات أساسية
```
src/App.tsx                          — Routes
src/pages/Home.tsx                   — الرئيسية
src/pages/DealsList.tsx              — قائمة العروض
src/pages/DealDetails.tsx            — تفاصيل + ImageZoomViewer
src/pages/SellerDashboard.tsx        — لوحة التاجر + chip-picker للمواقع
src/pages/AdminDashboard.tsx         — لوحة المدير
src/pages/Bookings.tsx               — حجوزات + محادثة
src/pages/Nearby.tsx                 — خريطة + GPS
src/pages/Profile.tsx · StoreDetails.tsx · Notifications.tsx
src/components/DealCard.tsx · BottomNav.tsx · BookingThread.tsx · UpdateBanner.tsx
src/context/AppContext.tsx           — الحالة + الفروع
src/repositories/                    — deal · booking · user · branch · contest · rating
src/services/realtimeService.ts      — القنوات + النبض + فحص الصحة (v13.83)
src/services/storageService.ts       — رفع الصور + المصغّرات
src/utils/helpers.ts · thumb.ts      — أدوات مشتركة
sw.js                                — Service Worker (CACHE_NAME)
server/bot.js                        — بوت تيليجرام + واتساب
vercel.json                          — headers + CSP
progress.md                          — سجل الإصدارات (الأحدث في الأعلى)

supabase/JEDDAH_CATCHUP_v13_67_to_82.sql          — هجرات مجمّعة (مطبَّقة ٢٢ أغسطس)
supabase/JEDDAH_COMPLETENESS_CHECK.sql            — فحص ٢١٦ دالة و٢٨ جدولاً
supabase/JEDDAH_EXPORT_db_logic.sql               — تصدير عقل القاعدة
supabase/audit_v13_75_jeddah_security_parity.sql  — فحص أمني شامل
```

## 🗄 جداول Supabase (الأهم)
- `users` — arrays: `notif_keywords` / `followed_merchants` · jsonb: `smart_alerts` · **RLS: صفّك أو أدمن فقط**
- `sellers_public` — **عرض** الدليل العام (حقول عامة فقط، للقراءة فقط بمشغّل `INSTEAD OF`)
- `deals` — `images text[]` · `region` + `city` مُسطَّحان
- `bookings` + `booking_messages` — `recipient_id` يملؤه مشغّل (v13.82)
- `store_branches` · `store_profiles` (`subscription_plan` + `max_branches`)
- `notifications` — `meta_data.audience` يحدّد الوجهة
- `regions` / `cities` / `locations` / `sa_cities_geo`

---

## 🧾 الضريبة والفواتير (v14.17)
**مفتاحان لا مفتاح واحد:** `merchant_vat` لنسبة ضريبة **طلبات التجار**، و
`tax_settings` لضريبة **اشتراكات تاكي** وحدها. لا يقرأ أحدهما مكان الآخر.
- الضريبة تظهر على فاتورة الطلب **برقم التاجر الضريبي وحده** — البائع هو التاجر،
  فتسجيلُ تاكي لا يخصّه. وطلبات التجزئة **مضمّنة الضريبة دائماً** (لا «فوق السعر»).
- `order_invoices` = **لقطة مجمّدة** لكل طلب تُكتب لحظة الحجز (نسبة · أساس ·
  ضريبة · اسم البائع ورقمه وسجلّه وعنوانه). **لا يُعاد حساب فاتورة أبداً.**
- رقم فاتورة **متسلسل لكل تاجر** (`TK-000001`) يُخصَّص عند اكتمال البيع أو دفعه،
  من `store_invoice_counters` بـ`ON CONFLICT DO UPDATE … RETURNING` (ذرّي، بلا فجوات).
- الموقع وملفّ البوتين ورمز زاتكا يقرؤون اللقطة. `taki_is_saudi_vat()` هي القاعدة
  الوحيدة لصيغة الرقم الضريبي.
🪤 أي شاشة جديدة تعرض إجمالاً أو ضريبة تقرأ `get_order_invoice` — لا تحسب.

## ↩️ الاسترداد (v14.18)
**تاكي وسيط ولا تملك المال.** الدفع يذهب لحساب التاجر مباشرة، فلا تستطيع المنصّة
ردّ مبلغ لا تحتفظ به. تُسجّل وتُبلّغ وتُثبت — **ولا تبتّ**.
- `cancel_booking` و`bot_cancel_booking` ترفضان الطلب المدفوع (`P0014` /
  `paid_needs_refund`). و`bookings_delete_own` مقيَّدة بغير المدفوع.
- `booking_refunds`: `requested → declined | approved → refunded | withdrawn`.
  و`resolve_booking_refund(action)` = `decline | refund | open`.
- الكمّية لا تعود للبيع إلا بعد تأكيد ردّ المال (`cancelled_by='refund'`).
- إشعار دائن `CN-000001` بسلسلة مستقلّة لكل تاجر، يُطبع على الفاتورة.
- سياسة التاجر: `store_profiles.refund_policy` و`store_terms` (١٥٠٠ حرفاً)،
  تُعرض في صفحة المتجر وصفحة العرض **فوق زرّ الحجز** وفي شاشة تأكيد البوتين.

## 🛡 حرّاس لا تُلتَفّ (v14.20 · v14.22)
سياسةُ RLS وحدها لا تكفي: الدوال المالكة تتجاوزها، والحذف المتسلسل يلتفّ عليها.
- `tr_guard_user_delete` يرفض حذف مستخدمٍ له طلبات (`P0015`) — كان نداءٌ واحد
  `DELETE /users?id=eq.me` يمسح طلبات كل مشتري التاجر وفواتيرهم الضريبية.
- `tr_guard_booking_delete` يرفض حذف حجزٍ صدرت فاتورته (`P0016`) — كان الحذف
  يُسقط الفاتورة ويترك فجوة دائمة في تسلسلٍ ضريبيّ يُفترض ألّا فجوة فيه.
🪤 كل مفتاح أجنبي إلى `users` أو `bookings` هو `ON DELETE CASCADE`. أي جدولٍ
جديد يحمل سجلّاً يجب أن يُحسب حسابُه هنا.

## 🔐 الخصوصية والموافقة (v14.19)
- تتبّع التوصيل يُحذف بعد **ساعة** من إغلاق الطلب (كرون كل ربع ساعة)، وعنوان
  الطلب داخل الطلب يبقى ما بقي (الفاتورة تحمله).
- حذف الحساب = تعطيل فوري ⇐ **تجهيل** بعد ٣٠ يوماً (`purge_expired_accounts`،
  كرون يومي). 🔴 كانت `DELETE FROM users` و`bookings.user_id/store_id` كلاهما
  CASCADE — أي أنها كانت ستمسح مبيعات التجار وفواتيرهم.
  ويشمل التجهيل: `auth.users` **و`auth.identities`** (تحمل البريد والاسم والجوال
  والعنوان) والجلسات، وجوّالَ المشتري وإحداثياته داخل `delivery_address`، واسمه
  تحت تقييماته، وبريده في الطابور وإشعاراته. الطلبات تبقى بلا هوية.
- `record_user_consent(version, ip, ua, kind, min_age)`: عنوان الإنترنت يُشتقّ
  على الخادم، واتفاقيّة البوابة في `gateway_agreement_version` فلا تدهس الشروط.
- `LEGAL_VERSION` في `src/data/legalVersion.ts` = تاريخ «آخر تحديث» في الوثائق
  الستّ **وإصدار الموافقة** معاً. أي تعديل قانوني يُغيّره.
- السنّ الأدنى ١٨، يُسجَّل إقراراً لا تاريخ ميلاد.

## 💰 المال: رقمٌ واحد لا ثلاثة (v14.11)
إجمالي كل طلب يكتبه **الخادم** لحظة الحجز في `bookings.total_amount`
(بضاعة + إضافات + رسوم توصيل، عبر `taki_booking_amount()`)، ويُجمَّد فلا
يُغيّره تغيّرُ سعر العرض بعده. يقرؤه الدفعُ (`_booking_amount_due`، لـ
`service_role` وحده) والفاتورةُ المطبوعة وفاتورةُ البوتين. **دالة الدفع لا
تحسب مالاً**، ولا يُختم طلبٌ «مدفوع» بأقلّ من فاتورته.
🪤 عناوين الدفع تُبنى من `API_PUBLIC_URL` لا من `SUPABASE_URL` (الأخير داخل
الحاوية هو `http://kong:8000`).
🔴 مفتوح: `merchant_gateways.secret_key_id` لمتجر ناصر يشير إلى سرّ غير موجود
في الخزنة (لم ينجُ من انتقال ٥ أغسطس) ⇒ «ادفع الآن» مقفل بـ`GATEWAY_UNAVAILABLE`
حتى يحفظ ناصر مفاتيح بوابته مرّة من لوحة التاجر.

## ⏳ مهلة الحجز (v14.10 → v14.12)
`platform_settings.booking_holds` = استلام · توصيل · شبكة أمان. تُضبط من
**لوحة المدير ← الأدوات ← «⏳ مهلة الحجز»** بلا نشر. الصياغة النصّية في
`utils/bookingHold.ts` + `components/HoldHours.tsx` (الموقع والصفحات القانونية)
و`server/lib/holds.js` (البوتان، تخزين دقيقة). **لا يُكتب رقم ساعات نصّاً في أي
مكان** — أوّل ضبط يجعله كذباً.

## 🔔 إشعارات الجوّال (v14.13)
المفتاح العام في `platform_settings.vapid_public_key` (يقرؤه المتصفّح لحظياً،
بلا متغيّر بناء)، والخاص + `PUSH_SHARED_SECRET` في بيئة حاوية الدوال وحدها.
`handle_notification_push` يقرأ العنوان والسرّ من **الخزنة** لا من
`current_setting` (ذاك لا يسري على اتصالات PostgREST المجمّعة). اللغة تُحسم في
`send-push` من `users.preferred_lang`. **الإذن لا يُطلب عند الفتح**: زرّ صريح في
«حجوزاتي» ومفتاح دائم في «حسابي ← الإشعارات». آيفون: لا إشعار إلا للتطبيق المثبَّت.

## 🖼 الصور (v14.14)
كل ملف في مستودع `deals` بترويسة `max-age=31536000`، ولكل صورة معروضة توأم
مصغّر `_t.jpg` ورابطها موسوم `t=1`. `thumb.ts` يرتدّ إلى `render/image` لأي صورة
بلا مصغّرة مؤكَّدة (شبكة أمان دائمة). ⚠️ النمط يجب أن يقبل المجلّدات
(`deals/bot/…`). التحميل المسبق = الجاران فقط بعد خمول المتصفّح.
ℹ️ ١٤٤ ملفاً (١٠٢ ميجابايت) لا يشير إليها أي عرض — بقايا، لم تُحذف.

## 📊 الحالة (٢٢ أغسطس ٢٠٢٦)
- الهجرات على جدة: **١٦/١٦ ✅** (v13.67 · 71 · 75 · 76 · 80 · 81 · 82)
- اكتمال الخصائص: **٢١٦/٢١٦ دالة · ٢٨/٢٨ جدولاً · حارس البوت ✅**
- البيانات: ٤ حسابات · ٢ متجر · ٢١ عرضاً · ٨٩ حجزاً · ١١٢ رسالة · ١١٧٧ إشعاراً · ١٩٠ صورة
- الأمان: RLS على ٥٠/٥٠ جدولاً · مسار كل دالة مثبَّت · لا كتابة للزائر · بيانات التجار محميّة

## 🪤 فخاخ أُضيفت في v14.25–v14.29

- **ترويسة تخزين الصور تُقرأ من خاصّية ممتدّة على الملف** (`user.supabase.cache-control`)
  **لا من `storage.objects.metadata`.** بصمُ القاعدة وحده لا يغيّر ما يصل الجوال.
  والأداة `getfattr` غير مثبّتة على الخادم — تُقرأ من داخل حاوية التخزين بـ`fs-xattr`.
- **`CREATE OR REPLACE` مع معاملٍ جديد لا يستبدل**: يُنشئ نسخة ثانية فيصير النداء
  ملتبساً («function is not unique») والبوتان يسكتان. `DROP` القديمة أولاً.
- **`REVOKE … FROM PUBLIC` لا يُلغي منح `anon` المباشر** على الدوال الجديدة —
  ولا العكس. يلزم الاثنان معاً. (واستثناء `bot_*` قائم: الحارس هو السرّ.)
- **ترتيب الخطّافات**: أي `useState/useEffect` بعد `return` مبكّر يُسقط الشجرة
  عند أول تبديل بين الفرعين. حالةُ تحميلٍ جديدة قد **توقظ** خطّافاً قديماً نائماً.
- **`srcset` يتقدّم على `src`**: أي ارتداد `onError` يجب أن يمسح `srcset` أولاً.
- **جدول `bookings`**: `booked_at`/`expiry_time` **bigint بالمللي ثانية** لا timestamp،
  والكمّية عمودها `booked_quantity`. و`tr_guard_booking_status` يمنع إحياء حجزٍ ملغى.

## 🔒 ثغرتان أمنيّتان كُشفتا ١٤ سبتمبر ٢٠٢٦ — **سُدّتا كلتاهما**

**سُدّت (v14.35):** `bot_delivery_quote` و`bot_delivery_quote_at` كانتا ممنوحتين
لـ`anon` **وبلا نداء `_bot_gate_ok()`** — أي شخص على الإنترنت يستدعيها بمعرّف
تيليجرام مخمَّن فيستلم وسم عنوان ذلك المستخدم وتفاصيله.

**سُدّت (v14.38):** `is_admin()` تُرجع TRUE لأي أدمن حين لا يوجد إطار `admin_*`
على مكدّس النداء — أي عند أي قراءة أو كتابة **مباشرة على الجدول** من المتصفّح،
و٦٤ سياسة RLS كانت مبنيّة عليها. قِيس: أدمن فرعي بصلاحية «الرئيسية» وحدها قرأ
جوّالات وبُرُد كل العملاء وأوقف متجرين. الآن `taki_admin_perm()` تسأل صفّ
المستخدم مباشرةً، و٦٤ سياسة أُعيد بناؤها **من نصّها الحيّ** (تُستبدل `is_admin()`
وحدها ويبقى كل شرطٍ آخر). القياس بعدها: ١ صفّ بدل ٦، صفر جوّالات، صفر إيقافات.
🪤 و٦٣ من ٦٤ سياسة كانت `TO public`: إعادة بنائها بـ`authenticated` كانت ستحجب
البانرات والمسابقات عن الزوّار. **انسخ الأدوار حرفياً عند أي إعادة بناء لسياسة.**

**منح الصلاحيات الفرعية صار آمناً** — والكتالوج مصدرٌ واحد في
`src/data/adminPermissions.ts`، ولكل صلاحية حقل `enforcedAt` يقول أين تُفرض.

## 🧪 بيئة المعاينة (v14.58–v14.62)

قاعدةُ المعاينة = مشروع **طوكيو** `kbmqzxcjdankdgiovctm` (مختبر، بلا بيانات
عملاء إطلاقاً). المزامنة بعد **كل هجرة**، أمرٌ واحد على خادم جدة:

```
scp scripts/sync-preview-db.sh ubuntu@141.147.142.147:/tmp/s.sh
ssh ubuntu@141.147.142.147 "PREVIEW_DB_URL='<الرابط من ملف المفاتيح ق٥>' bash /tmp/s.sh"
```

- حارسان: يرفض أن يكون المصدر هو المختبر، **ويرفض هدفاً بلا وسم المختبر** —
  فلو أُعطي رابط الإنتاج لمسح تاكي كلها.
- ويتحقّق أن الجداول والسياسات والدوال تطابق جدة، وأن **مستخدمي المعاينة صفر**.
- ولا يُنسخ إليها بشرٌ أبداً: `users`/`bookings`/`messages`/`addresses`/الفواتير.
- ودوال مشغّلات التخزين تُنقل إلى `public` لأن `storage` يملكه
  `supabase_storage_admin` على Supabase المُدار — لذلك الدوال ٤٨٣ لا ٤٨٢.
- والمعاينة **تُعرّف نفسها**: `scripts/preview-env.js` يقرأ `VERCEL_ENV`
  (تحقنها Vercel تلقائياً) فيوجّه بناء المعاينة وحده. لا ضبط على Vercel.
- ومفتاح Turnstile في المعاينة هو مفتاح الاختبار الرسمي — مفتاح الإنتاج مسجَّل
  لنطاق الإنتاج وحده، فالتسجيل كان سيُرفض.
- و`taki-preview-keepalive.sh` (يومياً) يمنع نوم المشروع المجاني بعد ٧ أيام.

## 🪤 فخاخ أُضيفت في v14.50–v14.57 (أخطرها أوّلها)

- 🔴 **`؛` في نهاية أمر البناء يمسح كل فشلٍ قبله.** حالةُ خروج سكربت npm هي حالةُ
  **آخر** أمر، وآخرُه كان `|| true` — فبناء Vercel **لم يكن يستطيع الفشل قطّ**.
  كل خطوةٍ يُراد لها أن تُفشل البناء توضع في سلسلة `&&` واحدة، والاختيارية
  تُعلَن في سكربت لا تُهرَّب بفاصلة منقوطة.
- 🔴 **جدول «✅/❌» في نهاية الهجرة لا يُفشل `psql`** — صفٌّ يقول ❌ ليس خطأً في
  SQL فالخروج صفر. قِيس. كل تحقّقٍ يجب أن يكون `DO` يرفع استثناءً؛ والجدول للقراءة.
- 🔴 **جدولة GitHub Actions بذلُ أفضل لا ضمان**: `*/10` قِيست **٢٠٥ دقيقة وسيطاً**
  (أسوأها ٤١٤). لا يُبنى كشفٌ سريع عليها — مكانُه كرون الخادم.
- 🔴 **الفحص الأمني: «غير موجود» ليس نجاحاً.** `404/PGRST205` جدولٌ غائب،
  `PGRST202` دالة غائبة، `42P01` علاقة غائبة — كلها كانت تُحسب «محجوب ✅».
  السطح يُثبت وجودَه أوّلاً: موجود ومحميّ ⇒ `200 + []` أو `401 + 42501`.
- **`GREATEST('crit','warn')` = `'warn'`** — المقارنة أبجدية، فالتصعيد يحتاج رتبةً صريحة.
- **`curl … | grep -q` يموت بـSIGPIPE تحت `pipefail`** — نزّل إلى ملفٍ ثم افحص.
- **حالة الخروج عبر أنبوب هي حالة الأنبوب** — `node t.js | tail; echo $?` يكذب.
- **صفحة الموقع تحمّل أكثر من حزمة**، وعنوان القاعدة في واحدةٍ لا في الأولى.
- **ارتفاع الصفحة في PDF مسقوفٌ دائماً** بـ`Math.min` — فالتأكيد عليه لا يفشل.
  العقد الحقيقي `plan.height` قبل القصّ (`planFinal`).
- **قائمة «ما يغيّر الحزمة» تكون قائمة سماح لا منع** — المنع يفشل مفتوحاً.
- **`zsh` لا يُقسّم المتغيّرات بالمسافات** كما `bash` — `for c in $list` يدور
  مرّةً واحدة **بصمت**. أي حلقة تحقّق تُكتب `… | while read -r c` أو داخل `bash -c`.
- **الفحص يقرأ ما يقرؤه المُجمِّع**: Parcel يقرأ `.env` كما يقرأ البيئة، ففحصٌ
  ينظر في `process.env` وحدها يرفض بناءً صحيحاً.
- **`psql -c "SET …; SELECT …"` يطبع وسم `SET` سطراً أوّل** فيُفسد أي ملفٍّ
  مولَّد. يُمرَّر مسار البحث بـ`PGOPTIONS` لا بعبارة.
- **سياسة تخزينٍ بلا `owner` وبلا مسار ليست سياسة**: `name LIKE '%.jpg'` وحده
  يسمح لأي موثَّق بالكتابة في أي مسار وبأي عدد.

## 🪤 فخاخ أُضيفت في v14.44–v14.48

- **`vault.update_secret` على مؤشّرٍ ميت لا يُخطئ** — `UPDATE … WHERE id=` بصفر
  صفوف، بلا استثناء. كل كتابةٍ في الخزنة تُثبَت بقراءةٍ بعدها وإلا فهي وهم.
  ومؤشّر uuid إلى مخطّطٍ آخر **مفتاحٌ أجنبيّ لا تفرضه القاعدة**، فيتعفّن بصمت.
- **اسم الخزنة فريد** (`vault.secrets.name` UNIQUE where not null) — فالإنشاء
  الأعمى باسمٍ موجود يفشل؛ والشفاء الصحيح تبنّي السرّ بالاسم القانوني.
- **مزوّد `sim` يختم الطلب «مدفوعاً» بلا مال.** لا يُفعَّل على متجرٍ له عروض حيّة.
- **Turnstile هنا غير مرئي**: صفر iframe ورمزٌ في الحقل المخفي = سليم. للتفرقة
  بين «غير مرئي» و«مفتاح معطوب» قارِن بمفتاح الاختبار `1x00000000000000000000AA`.
  ⚠️ **متصفّح الجلسة المدمج لا يصل `challenges.cloudflare.com`** (504) فيبدو
  معطوباً دائماً — استعمل Claude in Chrome لهذا الفحص وحده.
- **حماية Vercel (SSO) تسبق `vercel.json`** — قاعدة تحويلٍ على مضيفٍ محميّ لا تسري.
- **لا يُقرأ ملف المفاتيح بأمرٍ يطبع مخرجاته إطلاقاً.** مرشّح التمويه
  `sed 's/[=:]…/<REDACTED>/'` **فشل** لأن كل سرٍّ في الملف على سطرٍ مستقلّ بلا
  `=`. يُستخرج بـ`grep -o` داخل متغيّر ويُستعمل `$VAR`؛ وللتأكيد اطبع **الطول**
  لا الرأس والذيل. (وتنظيف السجلّات يرفضه حارس الأمان — السكربت جاهز في
  `~/Desktop/TAKI-تنظيف-السجلات.py` ويُشغّله ناصر.)
- **الاختبار الذي لا يختبر شيئاً أسوأ من لا اختبار**: فحصتُ تدوير سرّ البوت على
  `bot_payments_enabled` فمرّ الجميع بـ٢٠٠ — لأنها ليست من الدوال المحروسة.
  اختر دالةً يظهر فيها `_bot_gate_ok` في `pg_get_functiondef` (٢٣ دالة).

## 🪤 فخاخ أُضيفت في v14.63

- 🔴 **Parcel يُخزّن `index.html` مؤقّتاً فيبني نسخةً قديمة بنجاح.** حُذف وسمُ
  تنسيقٍ خارجي من المصدر، وبقي في `dist/index.html` بعد بناءٍ أخضر — أي أن
  «البناء نجح» كان سيُصدّق كذباً. `rm -rf .parcel-cache` قبل أي بناءٍ يُراد
  التحقّق من مخرجه (Vercel تبني نظيفاً فالإنتاج سليم، لكن **تحقّقك المحلّي يكذب**).
- 🔴 **حزمُ `leaflet.css` محلياً يقتل الدبّوس.** ليفلت يستنتج مسار صوره من وسم
  `<link href$="leaflet.css">` أو من قاعدة `leaflet-default-icon-path`؛ وبعد
  الحزم يصير اسم الصورة مبصوماً فيفشل النمط، والوسم غير موجود ⇒ المسار فارغ
  فتُطلب الصورة من مسار الصفحة وتعود ٤٠٤. تُستورد الصور الثلاث صراحةً
  (`src/utils/leafletSetup.ts` هو المكان الوحيد لذلك).
- **`style={{ outline: 'none' }}` المضمّن يهزم أي قاعدة CSS عادية** مهما كان
  تحديدها — و٤٦ عنصراً في هذه الواجهة يكتبه. حلقةُ التركيز تحتاج `!important`
  لزوماً لا تجميلاً (المهمّ في ورقة الأنماط يعلو على المضمّن غير المهمّ).
- **استبدالٌ آليّ لأحجام الخطوط يُقاس بالقيمة لا بالنمط**: نمط
  `0\.(6[0-9]|7[0-4])rem` يبدو شاملاً وهو يُغفل `0.7rem` (رقمٌ واحد بعد
  الفاصلة) — ٧٢ موضعاً مرّت. العدّ الصحيح: حوّل كل قيمة إلى بكسل وقارن.
- 🔴 **الاستبدال النصّي الشامل داخل ملفٍ يُصيب ما لم تقصده**: `await load()` ⇐
  `await reload()` جعلت `reload` تنادي **نفسها**، واستبدالُ فتحةِ زرٍّ أسقط
  `onClick` منه فصار الإشعار لا يُفتح. كلاهما مرّ typecheck. بعد كل استبدالٍ
  شامل: اقرأ المواضع المُصابة، لا العدد فقط.
- **`useEffect` يضبط الاتجاه بعد الرسم** — فيومض إطارٌ بالاتجاه القديم عند كل
  تبديل لغة. ما يمسّ DOM قبل الرسم يُكتب في `useLayoutEffect`.
- **لوحة المتصفّح المدمجة تُبطئ إعادة حساب الأنماط حين تكون مخفيّة**، فقياسُ
  `getComputedStyle` يعطي قيماً قديمة. لا يُبنى حكمٌ على قياسٍ منها وهي مخفيّة.
- **معاينة Vercel محميّة بـSSO**، فلا تُفحص بـ`curl` — يفتحها ناصر بمتصفّحه
  بعد دخوله لحسابه، أو يُتحقَّق من `dist` محلياً بخادمٍ ثابت.

## 🎯 الناقص (بالأولوية)
1. ✅ ~~النشر التلقائي للبوت~~ — **يعمل** (٣ سبتمبر) عبر `.github/workflows/deploy-bot.yml` + سرّ `RENDER_DEPLOY_HOOK`. قِيس: البوت أعاد نشر نفسه وصار على `13.95.0`. **ملاحظة:** ربط Render↔GitHub ما زال ساقطاً، لكن لا أثر له — سير العمل ينادي رابط نشر Render مباشرة. سابقاً — قِيس ٢ سبتمبر: النشر اليدوي من لوحة Render **ينجح** لأن المستودع عام فالاستنساخ المجهول يعمل رغم تحذير «we don't have access to your repo»، لكن الدفع إلى `main` **لا يُطلق نشراً** (webhook من GitHub ساقط). الحل الدائم جاهز في `.github/workflows/deploy-bot.yml` وينتظر سرّاً واحداً: `RENDER_DEPLOY_HOOK` (من Render ← Settings ← Deploy ← Deploy Hook، يُضاف في GitHub ← Settings ← Secrets ← Actions). حتى ذلك الحين: كل تعديل على البوت يحتاج **Manual Deploy** من لوحة Render.
1.5 ✅ ~~الصور اليتيمة~~ — حُذفت (v14.16)، والنسخة اليومية صارت تشمل الصور.
1.7 ✅ ~~«المفضلة» خاصّية ميتة~~ — بُنيت (v14.63): زرّ حفظٍ على كل بطاقة وفي
   صفحة العرض، وشاشة `/favorites`، ومفضلةٌ للزائر تُدمج في حسابه عند الدخول.
   الجدول والسياسات كانت جاهزة في جدة منذ زمن — الناقص كان الواجهة وحدها.
   ⚠️ صفحة «حولي» تعرض بطاقات خاصّة بها (لا `DealCard`) فلا زرّ حفظ فيها بعد.
2. ✅ ~~مفاتيح بوابة الدفع~~ — **حُلّ (v14.44)**. المفتاح لم يضع: كان في الخزنة
   باسمه `mg_<التاجر>_sk` والمؤشّر وحده ضلّ في انتقال ٥ أغسطس. و**أربع طبقات
   كانت تسأل المؤشّر لا الخزنة**، وأخطرها `vault.update_secret` على مؤشّرٍ ميت:
   `UPDATE … WHERE id=` بصفر صفوف **بلا خطأ** — أي أن حفظ المفاتيح كان سيقول
   «✅ حُفظت» ولا يحفظ شيئاً. الآن `_gateway_secret_live()` قاعدةً واحدة تسألها
   الطبقات الأربع، والكتابة تُثبَت بقراءة (`SECRET_WRITE_FAILED`).
   🔴 **و`sim` محاكاة تختم الطلب «مدفوعاً» بلا مال** — فشفاء المؤشّر يفكّ ختم
   «مختبَرة» ويُطفئ التفعيل عمداً. أي تفعيلٍ لـ`sim` على متجرٍ حيّ = دفعٌ مجّاني
   لأي زائر. (المتبقّي بيد التاجر وحده: يحفظ مفاتيح مزوّده الحقيقي من لوحته.)
3. 🔴 **نسخة احتياطية خارج الخادم** — النسخ اليومية على نفس الجهاز؛ فقدُه يفقدها.
3. ✅ ~~اسم المتجر القديم عند الحجز~~ — حُلّ (v14.36): `deals.shop_name` نسخة ثانية مجمّدة.
4. ✅ ~~وسم الأدمن في صفحة المتجر~~ — **أغلقه ناصر ١٥ سبتمبر: «لم يعد يظهر»**.
5. ✅ ~~العناوين القديمة~~ — الثلاثة وُجدت كلها في نطاقات مشروع Vercel:
   `taki-test-eight` (يحوّل بـ308) و`taki-test-nasser-projects1` و
   `taki-test-git-main-nasser-projects1`. والأخيران **محميّان بـSSO** أصلاً
   (`ssoProtection: all_except_custom_domains`) فلا يصل إليهما زائر.
   🪤 وقاعدة التحويل المضافة لهما في `vercel.json` **لا تسري اليوم** — حماية
   Vercel تعمل قبل قواعد التحويل. أُبقيت عمداً كشبكة أمان تسري لحظة رفع الحماية.
6. ✅ ~~Turnstile~~ — **يعمل** (قِيس ١٥ سبتمبر بمتصفّح حقيقي على الإنتاج: يُصدر
   رمزاً فعلاً). وهو مضبوط على النمط **غير المرئي** فصندوقه فارغٌ في الحالة
   السليمة — ولذلك كان فشله بلا أي أثر على الشاشة. v14.46 يجعله يقول اسمه
   ومعه زرّ إعادة محاولة.
7. 🟠 **١٨٩ دالة موجودة على الخادم فقط** — النسخة اليومية تغطّيها، لكن المستودع وحده لا يُعيد البناء (والمستودع عام فلا يصلح لحمل المخطط بلا فحص أسرار).
8. ✅ ~~CAPTCHA + منع كلمات المرور المسرَّبة~~ — **مفعّلان** (٣ سبتمبر). 🪤 درس: كابتشا GoTrue تحرس **`/signup` و`/token` و`/recover` و`/resend`** معاً. تفعيلها بفحص التسجيل وحده **أقفل تسجيل الدخول للجميع** (v13.96)، ثم كاد زر «إعادة إرسال الكود» يكسر بنفس السبب (v13.98). **أي إعداد يحرس عدّة مسارات: اجرد كلها قبل التفعيل.** الودجت الآن في ثلاث شاشات (تسجيل · دخول · تحقّق)، و`resetSignal` يجدّده بعد كل فشل لأن **الرمز يُستهلك مرة واحدة**.
9. ✅ ~~`SUPABASE_URL` و`APP_URL` في Render~~ — وُحِّدا على النطاق الرسمي
   (١٥–١٦ سبتمبر). 🪤 **ولا تُعِد فتح هذا الملف: البوت لم يكن يُظهر الاسم القديم
   أصلاً** — حارس `LEGACY_HOSTS` في `server/bot.js` يترجمه منذ v14.08.
   والبحث في تيليجرام نفسه انتهى (١٦ سبتمبر، بتحقّق ناصر على BotFather + قياس
   برمجي): **لا نطاق في `Domain`** («Web login is currently unavailable») ·
   **لا Mini App** (`has_main_web_app=false` و`/myapps` ⇒ «there is no web app») ·
   زرّ القائمة أوامرٌ لا رابط · وصف البوت فارغ. أي أن **صفر موضع على خوادم
   تيليجرام يحمل عنواناً** — كل الروابط تُبنى في الكود لحظياً.
   ⚠️ ولا يُسجَّل أيٌّ منهما لاحقاً بلا داعٍ: البوت يعمل بلا كليهما، وتسجيلهما
   يخلق مكاناً ثانياً يجب تذكّر تحديثه.

**أُنجز:** ✅ **بريد الحساب بلغة المستخدم** (v13.92 — تأكيد التسجيل واستعادة كلمة المرور وتغيير البريد؛ قالب واحد يفرّع على `.Data.lang`، والعناوين كذلك؛ الافتراضي عربي) · ✅ النطاق كاملاً (`www.takisa.net` رئيسياً · الجذر والعنوان القديم يحوّلان بـ308 · `api.takisa.net` للقاعدة) · ✅ **البريد يعمل فعلياً** (Resend موثَّق على أيرلندا · DKIM+SPF+MX+DMARC · `no-reply@takisa.net` · **اختُبر بتسجيل حقيقي فوصل**) · ✅ عناوين الصور نُقلت (٢٣ صفاً) · ✅ مراقبة كل ١٠ دقائق + فحص أمني يومي على خوادم GitHub · ✅ الفحص الأمني الحيّ ٣٦/٣٦ · ✅ سطح انتحال البوت = صفر · ✅ لا تسمية أدمن في أي نصّ ظاهر.

---

## 💬 نمط العمل المتوقع
1. أصف المشكلة بلهجتي (قد تكون فيها أخطاء إملائية).
2. أنت تفهم القصد، تستكشف الكود، **تقيس قبل أن تحكم**، تعدّل، ترفع، تنشر.
3. تخبرني بالعربي ماذا فعلت وكيف أتأكد.
4. **إن أخطأت، قل ذلك صراحةً وصحّح** — لا تُجمّل. خطآن من ٢٢ أغسطس وفّرا وقتاً لأنهما قيلا: «الهجرة مطبَّقة» (كانت على المختبر)، و«١٣٦/١٣٦» (كان الفحص ناقصاً ٨٠ دالة).
