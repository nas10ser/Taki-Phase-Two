# TAKI — سجل الإصدارات 📊

> منصة حجز التخفيضات في السعودية. React 18 + Parcel + Supabase + Vercel.
> المالك: ناصر (غير مبرمج — كل التنفيذ يتم من جهة Claude).

## 📌 قواعد ثابتة (لا تتغير)

- **CACHE_NAME في sw.js يجب رفعه كل deploy** (`taki-cache-vXX.YY`) — وإلا iOS Safari يبقى على نسخة قديمة.
- **dealRepository.updateQuantity** للتحديثات الجزئية على deals (الـtrigger `tr_guard_deal_publish` يرفض UPDATE OF status بصمت).
- **notification routing** يقرأ `meta_data.audience` (DB trigger يكتبه)، لا `user.userType`.
- **admin RPCs** بـ`RETURNS TABLE` تحتاج تأهيل (qualify) للأعمدة المتداخلة مع DB columns.
- **Vercel deploy**: `cd ~/Desktop/TAKI && npx vercel deploy --prod --archive=tgz` — `--archive=tgz` إلزامي.
- **Worktree flow**: تعديل في worktree → commit → `git push origin HEAD:main` → `git -C ~/Desktop/TAKI pull origin main` → vercel deploy.

---

## 🗂 backlog (مهام مؤجلة)

- **آلية اشتراك "باقة أعلى" للسماح بأكثر من 3 لوكيشنات** (تم تحديد القاعدة في v10.42): الباقة الأساسية = 3 مواقع فريدة كحد أقصى للمنتجات النشطة. للمزيد لازم باقة مدفوعة. **ناصر سيحدد لاحقاً**: السعر، الفئات (5/10/∞)، نموذج الدفع، وكيف يُعرض الـupsell. الكود الحالي يرفض الإضافة في موقع 4+ مع رسالة "الترقية قريباً"، وزر "إدارة الاشتراك" يفتح `/subscription`.

---

## 🗓 سجل الإصدارات (v10.x → v11.x)

| إصدار | الموضوع |
|--------|---------|
| **v11.34** | **زر تعديل للبانر + خيار «نجمة بالزاوية» للإطار الذهبي**. طلبان من ناصر. **(١) تعديل البانر**: `BannerModal` صار يقبل `initial` ويملأ الحقول مسبقاً (incl. اسم المتجر المختار من `deals`)، و`handleSave` يعمل `update().eq('id')` عند التعديل بدل `insert`، والعنوان/الزر يتبدّلان («✏️ تعديل البانر» / «💾 حفظ التعديلات»). أُضيف زر ✏️ على كل بطاقة بانر في الأدوات (+ `bannerEdit` state). **(٢) خيار رابع للإطار الذهبي = «✨ نجمة بالزاوية»**: نجمة ⭐ صغيرة في الزاوية العلوية (الجهة المقابلة للقلب) **بدون شريط نص** — عكس «إعلان»/«راعٍ رسمي». التغييرات: `SponsorLabel` += `'star'`؛ `sponsorLabelText('star')=''` (لا شريط)؛ `DealCard` يرسم شارة نجمة دائرية ذهبية بالزاوية + إزاحة شارة الموقع تحتها؛ خيار في `AdminSellers` (grid-cols-2) + شرح؛ `sponsorRepository` type؛ **migration**: `admin_set_sponsor` يقبل `'star'` في قائمة التحقّق (تحقُّق بعدي ✅). typecheck ✅، CACHE_NAME → v11.34. |
| **v11.33** | **كاروسيل البانر: حركة انسيابية + حلقة لا نهائية + سحب من الجهتين**. اشتكى ناصر أن الحركة غير سلسة وأن السحب يعمل من جهة واحدة فقط. السبب: المنزلق كان محدوداً (clamp + rubber-band عند الأطراف) فيُمنع السحب في اتجاه عند الطرف. **الحل**: أُعيد بناء `BannerSlider` بنظام **infinite loop عبر استنساخ شريحتين** (`[cloneLast, ...reals, cloneFirst]`) — يمشي مؤشّر `pos` على الشريط الممتد، وعند الوصول لاستنساخ ينطّ بلا أنيميشن للشريحة الحقيقية المطابقة (`onTransitionEnd` + double-rAF لإعادة تفعيل الانتقال). **السحب من الجهتين** يعمل دائماً (لا حواجز). **الانسيابية**: تتبّع الإصبع 1:1 أثناء السحب، ومنحنى `easeOutQuint` عند الإفلات، + **كشف سرعة الإصبع (flick)** بـ px/ms فيتقدّم حتى بسحبة قصيرة سريعة، ومدّة انتقال أقصر للفليك (0.34s) وأطول للسحب البطيء (0.46s). يحترم RTL/LTR في اتجاه التقدّم. typecheck ✅، CACHE_NAME → v11.33. |
| **v11.32** | **تصحيح: البانر بطاقة بهوامش جانبية (ليس بعرض الشاشة)**. وضّح ناصر أن قصده عكس v11.31: يريد **قصّ الحواف من الجهتين** (هوامش) لا full-bleed. أُرجع قسم البانر في Home لهامش `margin:16px 16px 8px` (يطابق إزاحة بقية الصفحة)، و`BannerSlider` عاد `borderRadius:20` + `boxShadow: var(--shadow-lg)` → بطاقة مُنسّقة محصورة بحوافها مقصوصة من الجانبين. typecheck ✅، CACHE_NAME → v11.32. |
| **v11.31** | **بانر بعرض الشاشة + ربط بالاسم (متجر/عرض) بدل المعرّفات**. طلب ناصر نقطتين بعد v11.30. **(١) البانر في الرئيسية بعرض كامل حافة-لحافة** كما أشّر بالأحمر: غُلّف قسم البانر في Home بـ`width:100vw; marginLeft:calc(50% - 50vw)` (full-bleed يكسر حدود `.page-content`)، و`BannerSlider` صار `borderRadius:0` بلا ظل → حواف رأسية مستقيمة ملاصقة لحافتي الشاشة. **(٢) الربط بالاسم بدل ID/رابط** (ناصر لا يستطيع إدخال معرّفات أو روابط من التطبيق): في نافذة البانر، قسم «عند الضغط على البانر» أُعيد تصميمه — **بحث عن المتجر بالاسم** (قائمة منسدلة من المتاجر المشتقّة من `deals` في الذاكرة: storeId→shopName، لا استعلامات جديدة)، وعند اختيار متجر تظهر **قائمة عروضه النشطة** لاختيار عرض محدد (اختياري) أو ترك «صفحة المتجر». الأولوية في `BannerSlider`: deal_id ← store_id ← target_url. الرابط الخارجي بقي كخيار متقدّم مطوي. typecheck ✅، CACHE_NAME → v11.31. |
| **v11.30** | **بانر احترافي: أداة قص WYSIWYG + كاروسيل سحب مثل نون + إصلاح فشل النشر**. أبلغ ناصر بأربع نقاط على نافذة «بانر إعلاني جديد». **(١) فشل النشر** — السبب الجذري: النموذج كان يُرسل `deal_id:''` و`store_id:''` (نص فارغ) وكلاهما مفتاح خارجي (`deals.id` / `users.id`)، والنص الفارغ يُعامَل كقيمة حقيقية فيخرق `banners_deal_id_fkey`. **الإصلاح**: بناء صفّ نظيف يحوّل كل حقل اختياري فارغ إلى `null`، + ترجمة خطأ FK الخام إلى رسالة عربية واضحة («رقم العرض غير موجود...»). **(٢) أداة قص WYSIWYG** — مكوّن جديد `BannerImageEditor`: إطار 2:1 ثابت، سحب الصورة لأعلى/أسفل + شريط تكبير + خطوط دليلية علوية/سفلية، يُصدِّر 1200×600 JPEG عبر canvas (data/blob URL → لا تلوّث canvas). الرفع يتم بعد القص. زر «✂️ ضبط الجزء الظاهر» يعيد فتح الأداة على صورة موجودة (fetch→blob). **(٣) كاروسيل نون** — أعيدت كتابة `BannerSlider`: تتبّع الإصبع أثناء السحب + snap + مقاومة عند الأطراف، مع الاحتفاظ بالتقديم التلقائي كل 5 ث والنقاط، والإطار صار `aspect-ratio: 2/1` ثابت ليطابق القص على كل الأجهزة. **(٤) شرح ID** — قسم «عند الضغط على البانر» يوضح أن الحقول اختيارية، رابط الوجهة هو الأسهل، ومعرّفا العرض/المتجر داخل قسم متقدّم مطوي مع شرح وتحذير. + معاينة 2:1 داخل النافذة + ذكر المقاس المثالي 1200×600. CACHE_NAME → v11.30، typecheck ✅. | **إصلاح تداخل البانر العلوي في لوحة الأدمن (تصميم أنظف)**. أبلغ ناصر أن شريط التبويبات العلوي يتداخل مع المحتوى أسفله. السبب: الشريط اللاصق كان بخلفية زجاجية شفافة ٨٢٪ (`bg-card-glass` + backdrop-blur)، وعلى iOS أحياناً لا يُرسَم الـblur فيظهر بانر الترحيب من خلف الشريط. **الإصلاح**: الشريط صار بخلفية **معتمة بالكامل** (`var(--body-bg)`) + ظل رفع (`box-shadow`) + رفع z-index لـ٣٠ فيمرّ المحتوى تحته بنظافة دون أي تداخل، ومسافة المحتوى زِيدت (`mt-5`). نتيجة احترافية متّسقة في الوضعين الفاتح والغامق. CACHE_NAME → v11.29، typecheck ✅. |
| **v11.28** | **خانة تاريخ ابتداء مستقلة للراعي + إصلاح فشل حفظ خفيّ**. طلب ناصر خانة تاريخ ابتداء منفصلة عن الانتهاء. **اكتُشف خلال التنفيذ عطل صامت**: ترحيل v11.25 الذي أضاف `label_type` + بارامتر `p_label_type` لم يُطبَّق فعلياً على القاعدة الحيّة (العمود غير موجود، والدالة بقيت 11 بارامتر)، فكان العميل يُمرّر `p_label_type` لدالة لا تقبله → **كل حفظ راعٍ يفشل بصمت منذ v11.25**. **الإصلاح**: (١) أُضيف عمود `label_type` فعلياً (تحقُّق بعدي). (٢) أُعيد بناء `admin_set_sponsor` بـ١٣ بارامتر تشمل `p_label_type` + `p_starts_at` الجديد، مع إسقاط النسخ القديمة لمنع تعدد التحميل، وتحقُّق `start < expiry` داخل الدالة. (٣) `isSponsorActive` صار يحترم `startsAt` (راعٍ بتاريخ بدء مستقبلي لا تظهر إعلاناته قبل موعده). (٤) واجهة الأدمن: خانتان منفصلتان «يبدأ في» و«ينتهي في» + تحقُّق أن الانتهاء بعد الابتداء. اختبار end-to-end كسوبر-أدمن: حُفظ label_type='sponsor' + starts_at + expires_at بنجاح. CACHE_NAME → v11.28، typecheck ✅. |
| **v11.27** | **إصلاح قصّ شارة «إعلان/راعٍ رسمي»**. أبلغ ناصر أن الكلمة تظهر مقصوصة وغير واضحة (الصورة: شريط ذهبي نصفه مقطوع). السبب: الشارة كانت بـ`top:-12` (خارج أعلى البطاقة) بينما `.deal-card` فيه `overflow:hidden` فتُقَصّ. **الإصلاح**: حُوِّلت الشارة إلى **شريط ذهبي بعرض كامل** مثبّت داخل أعلى الصورة (`top:0`, `left/right:0`, نص في الوسط) — يستحيل قصّه، حادّ وواضح في الوضعين. ولمنع التداخل: زرّ المفضّلة (❤️) وشارة الموقع يُزاحان لأسفل (`top:42`) عند وجود الشريط، ورُفع z-index القلب فوق الشريط ليبقى قابلاً للضغط. CACHE_NAME → v11.27، typecheck ✅. |
| **v11.26** | **تدرّج صارم بين الرعاة والمعلنين: كل الرعاة الرسميين أولاً ثم المعلن**. أبلغ ناصر: لو فيه عدة رعاة، يجب أن يظهر الثلاثة (الرعاة) أولاً ثم المعلن. كان `interleaveSponsored` في v11.25 يلفّ على كل المتاجر بالتناوب في دورة واحدة (S1→S2→S3→**معلن**→S1...) فيتسلّل المعلن قبل استنفاد الرعاة. **الإصلاح**: أُعيد بناء اختيار الإعلانات على شكل طابور مُسبَّق التدرّج — يُجمَّع المتاجر حسب الرتبة (sponsor=٢، ad=١)، ويُستنفَد كامل تدرّج الرعاة **بالمداورة بينهم** (S1,S2,S3,S1,S2,S3...) قبل الانتقال لتدرّج المعلنين. النتيجة المؤكَّدة باختبار مستقل (٣ رعاة عرضين لكل + معلن): `S1→S2→S3→S1→S2→S3 → A1→A1`. تبقى البطاقة الإعلانية الأولى متصدّرة الصفحة ثم واحدة بعد كل ٥ عروض، في كل الأقسام. CACHE_NAME → v11.26، typecheck ✅. |
| **v11.25** | **تحسين الإعلانات: نص أوضح + تحكّم بالكتابة (إعلان/راعٍ/بدون) + ظهور في كل الأقسام + أولوية الراعي + الإعلان أولاً**. طلب ناصر: (١) كلمة «إعلان» غير واضحة — كُبّر الخط (0.8rem) وأُضيف إطار أبيض + ظل نص فصار حاداً فوق أي صورة. (٢) **يظهر أولاً**: `interleaveSponsored` صار يضع أول بطاقة إعلانية **في رأس القائمة** ثم واحدة بعد كل ٥ عروض (مع عروض قليلة تبقى الإعلانية أولاً). (٣) **في كل الأقسام**: طُبّق على «الأكثر تداولاً» و«أقوى الخصومات» و«كل العروض» وقائمة العروض — لا «كل العروض» فقط. (٤) **تحكّم بالكتابة**: عمود `label_type` جديد ('ad'=إعلان / 'sponsor'=راعٍ رسمي / 'none'=إطار فقط بلا كلمة) يختاره الأدمن من أزرار في صندوق الراعي. (٥) **الأولوية**: «راعٍ رسمي» يظهر **دائماً قبل** «إعلان» في كل الصفحات (labelRank في الفرز)، ثم حسب رقم الأولوية. RPC `admin_set_sponsor` + `admin_list_sponsors` يحملان label_type؛ اختبار مستقل أكّد: الراعي الرسمي يتصدّر حتى لو أولويته الرقمية أقل، والإعلان يظهر بعد ٥ عروض. CACHE_NAME → v11.25، typecheck ✅. |
| **v11.24** | **إصلاح عدم ظهور الرعاة + إشعار الراعي + إتاحة الصلاحية للمسؤول الموظف**. أبلغ ناصر أن تفعيل الراعي (تمتومي) لم يُظهر إطاراً ذهبياً ولا إشعاراً. التشخيص بالقاعدة الحيّة كشف ٣ جذور: (١) جدول `sponsors` كان فارغاً — الحفظ من الواجهة لم يصل (كاش قديم أو حساب موظف «Nass» بلا صلاحية `action_manage_sponsors`؛ المالك سوبر-أدمن يتجاوز). مُنحت الصلاحيات الجديدة لـNass وأُدرج صف راعٍ فعليّ لتمتومي (بلا استهداف = كل منتجاته النشطة ذهبية، ٦٠ يوماً). (٢) `sponsorRepository.getActive` كان يستخدم `.or(expires_at...)` مع ISO timestamp فيه `:`/`.` تكسر صيغة PostgREST فتُرجع لا شيء → خريطة رعاة فارغة → لا إطارات؛ أُزيل والتصفية بالـJS. (٣) لا إشعار — أُعيد بناء `admin_set_sponsor` ليُرسل «🌟 أصبحت راعياً رسمياً» عند أول تفعيل. اختبار مستقل لـ`interleaveSponsored` على البيانات الحقيقية: إعلانان ذهبيان ✅. RLS مؤكَّد للمشتري المجهول. CACHE_NAME → v11.24. |
| **v11.23** | **نظام الرعاة الرسميين (إعلان ذهبي) + تقوية أزرار لوحة الأدمن + زر تحديث إجباري**. طلب ناصر: (أ) أزرار اللوحة (رفع صورة البانر/نشر البانر/عروض الموسم) لا تعمل؛ (ب) ميزة راعٍ رسمي: منتجات الراعي تظهر كإعلان بإطار ذهبي بعد كل ٥ عروض، بالمداورة بين حتى ٢٠ راعياً ثم العودة للبداية، مع استهداف (تصنيف/مدينة/منطقة/نطاق كم/الكل) وتاريخ انتهاء وأولوية، يُمنح من لوحة الأدمن. **التنفيذ**: **(١) DB**: جدول `sponsors` (استهداف + انتهاء + أولوية، RLS: قراءة للجميع/كتابة عبر `is_admin()`) + RPCs `admin_set_sponsor`/`admin_remove_sponsor`/`admin_list_sponsors` (SECURITY DEFINER، أعمدة مؤهّلة). المنح يُحدّث أيضاً `store_profiles.is_pinned` مرآةً للمسار القديم. **(٢) منطق العرض** (`helpers.ts` — `interleaveSponsored`، نقي ومُختبَر): يسحب عروض الرعاة المطابقة للاستهداف من التدفق ويُعيد حقنها كإعلانات ذهبية بعد كل ٥ عروض، بمداورة round-robin حسب الأولوية ثم تكرار؛ خارج الاستهداف تظهر منتجات الراعي عادية بلا إطار. **(٣) AppContext**: تحميل الرعاة النشطين + realtime على جدول `sponsors` فيظهر المنح/الإلغاء فوراً. **(٤) الصفحات**: الرئيسية + قائمة العروض تستخدمان المداورة (عروض الموسم تبقى بلا إعلانات). **(٥) DealCard**: إطار ذهبي مزدوج متدرّج + توهّج + شارة «إعلان» (كانت «برعاية»). **(٦) إصلاح اللوحة**: زر «رفع صورة البانر» ونشره — أُضيف `try/finally` + مهلة ١٢s فلا يعلّق؛ زر أخضر بارز «🔄 تحديث التطبيق للأحدث» أعلى أدوات الإدارة يمسح الكاش ويعيد التحميل لتجاوز نسخة iOS المخزّنة (السبب الجذري لظهور الأزرار «معطّلة»). **توضيح**: «عروض الموسم» صفحة «قريباً» ثابتة بلا محرّر — أُجِّل بناء محرّرها بطلب ناصر. CACHE_NAME → v11.23، typecheck ✅، اختبار المداورة ✅. |
| **v11.23** | **نظام الرعاة الرسميين (إعلانات ذهبية) + صفحة مراقبة الرسائل اللحظية + صلاحيتان جديدتان + زر تحديث إجباري + تقوية أزرار الأدوات**. (١) **الرعاة الرسميون (راعٍ رسمي)**: جدول `sponsors` + RPCs (`admin_set_sponsor`/`admin_remove_sponsor`/`admin_list_sponsors`) — الأدمن يمنح متجراً صفة راعٍ بتاريخ انتهاء + استهداف (تصنيف/منطقة/مدينة/نطاق كم/الكل) + أولوية. منتجات الراعي النشطة (حتى القديمة) تظهر كإعلان بإطار ذهبي احترافي وشارة «إعلان»، تُحقن **بعد كل ٥ عروض** بالمداورة بين كل الرعاة (round-robin بالأولوية) فلا يطغى راعٍ على آخر، وإذا انتهت الدورة تبدأ من جديد. الاستهداف محترَم: الراعي لا يظهر كإعلان ذهبي إلا داخل استهدافه. helper نقيّ `interleaveSponsored` في `helpers.ts`، مطبَّق على الرئيسية + قائمة العروض. UI المنح داخل `SubscriptionModal` (مَجزوء على `action_manage_sponsors`). (٢) **مراقبة الرسائل**: تبويب `AdminMessages` جديد يعرض كل محادثات الموقع لحظة بلحظة (realtime على `booking_messages` + poll 8s)، مع بروفايل الطرفين + الباركود، وحذف أي رسالة + إنذار أي طرف. RPCs `admin_list_message_threads`/`admin_get_thread_messages`/`admin_delete_message`/`admin_warn_user` (SECURITY DEFINER، admin-gated، تتجاوز RLS الطرفي). (٣) **صلاحيتان جديدتان**: `tab_messages` (رؤية تبويب الرسائل) + `action_manage_sponsors` + `action_moderate_messages` — أُضيفت لقائمتَي الصلاحيات (الترقية + تعديل المسؤولين الحاليين) ولـ`AdminPermission`. (٤) **زر «تحديث التطبيق للأحدث»** أعلى الأدوات — ينفّذ `applySwUpdate` (مسح الكاش + إعادة تحميل) لتجاوز عَلق iOS على نسخة قديمة. (٥) **تقوية**: `handleSave` للبانر بـtry/finally + مهلة 12s فلا يعلّق زر «نشر البانر». CACHE_NAME → v11.23، typecheck ✅. **ملاحظة**: صفحة «عروض الموسم» شاشة ثابتة بلا محرر — أُجّل محررها بطلب ناصر. |
| **v11.22.1** | **تقوية إصلاح زر «حفظ التعديلات» في الحملات (لا يعلّق أبداً)**. بعد v11.22 أبلغ ناصر أن زر حفظ تعديل الحملة ما زال يعلّق على «جاري الحفظ». الفحص أثبت أن النسخة المنشورة حيّاً تحوي الإصلاح فعلاً (مهلة + try/finally) — أي أن جهازه كان على نسخة مخزّنة قديمة. ولزيادة المتانة بصرف النظر عن الكاش: **(١)** نُقل بناء كائن الصف (`row`) وتحويل التواريخ بـ`.toISOString()` إلى **داخل** كتلة `try` (كان خارجها في v11.22 فأي تاريخ غير صالح كان يرمي استثناءً قبل `setSaving(false)` ويُجمّد الزر). **(٢)** تحقّق فوريّ من صلاحية التواريخ **قبل** ظهور أي سبينر (تاريخ غير صالح/نهاية قبل بداية → رسالة فورية بلا تعليق). **(٣)** خُفّضت مهلة الأمان لـ١٢ ثانية وأُضيفت مهلة منفصلة لتوزيع الإشعارات (`broadcastNow`) فلا تعلّق الواجهة على fan-out بطيء. CACHE_NAME → v11.22.1، typecheck ✅. **ملاحظة لناصر**: لو ظهر العطل مجدداً فهو كاش الجهاز — أغلق التطبيق تماماً وأعد فتحه (التحديث التلقائي v10.25 سيجلب النسخة الجديدة عند العودة للواجهة). |
| **v11.22** | **إصلاح ٣ أعطال في لوحة الأدمن: تفعيل الاشتراك + وصول الإشعارات + تعليق الأزرار**. أبلغ ناصر: تفعيل اشتراك متجر لشهر لا يُفعّل (يبقى يطلب اشتراك)، إشعارات الأدمن للمشترين/البائعين لا تصل، وزر الحفظ يعلّق على «جاري الحفظ» عند تاريخ معيّن. **التشخيص بـSQL على القاعدة الحيّة كشف ٣ جذور منفصلة**: **(١) الاشتراك**: `admin_apply_subscription` يكتب الباقة + الانتهاء في `store_profiles`، لكن بوّابة فتح القفل في `SellerDashboard` كانت تقرأ **فقط** `merchant_subscriptions` (سجل تجربة منتهٍ) — فالمتجر «تمتومي» كان عنده `store_profiles.premium` صالح حتى ٢٩ يونيو لكن `merchant_subscriptions.trial` منتهٍ في ٢٧ مايو → يبقى مقفلاً. الإصلاح (كان جاهزاً غير مُرحَّل): البوّابة تفتح إذا **أيّ** مصدر إيجابيّ (التجربة/الاشتراك الفعّال **أو** نسخة `store_profiles`)، + إعادة فحص عند العودة للتطبيق (focus/visibility) + realtime على الجدولين فيُفتح القفل لحظة التفعيل بلا إعادة تثبيت. **(٢) الإشعارات (الجذر الأساسي)**: زرّا «نشر فوراً» و«حملة جديدة» في AdminTools كانا **يُدرجان صفّ حملة فقط** ولا يستدعيان `broadcast_campaign()` الذي يوزّع الحملة على صناديق إشعارات المستخدمين — فالحملات تبقى عند `current_impressions=0` ولا تصل أحداً (أثبتته القاعدة: حملتان أُنشئتا اليوم بصفر وصول). الإصلاح: بعد إدراج الحملة نستدعي `promoRepository.broadcastNow(id)` فوراً ونُظهر للأدمن «وصل الإشعار إلى N مستخدم». **(٣) تعليق الأزرار**: تاريخ غير صالح كان يجعل `.toISOString()` يرمي استثناءً **قبل** `setSaving(false)` فيتجمّد الزر؛ إضافة تحقّق من التواريخ + `try/finally` على كل أزرار الأدمن الحسّاسة (تطبيق اشتراك فرديّ/جماعيّ، حفظ مشترٍ، حفظ المبلغ، الأوضاع الثلاثة «مجاني/مدفوع/تجربة ثم مدفوع»، حفظ الحملة) فلا يعلّق أيّ زر مهما فشلت الشبكة. **تأكيد**: المشتري والأدمن فقط يحجزون؛ التاجر لا يحجز (كما في v11.21). CACHE_NAME → v11.22، typecheck ✅، نُشِر وتُحقّق منه حيّاً. |
| **v11.21** | **العروض القادمة بلا حدّ زمنيّ أعلى + منع تعارض موعد البدء مع تاريخ الانتهاء + إصلاح دوران أزرار الحفظ**. طلب ناصر ٣ نقاط: (١) «لا يهمّ ٣٠ يوم أو أكثر للعروض القادمة، احذفها»؛ (٢) منع التعارض لو وضع تاريخ انتهاء بالأعلى وجدول بدءاً أبعد منه؛ (٣) «الأزرار تبقى تدور حتى مع نقص البيانات، أصلحها كلها». **التنفيذ في `SellerDashboard.tsx`**: **(١) حذف سقف الـ٣٠ يوماً نهائياً** — أُزيل `maxMs` وفحصه من `submitAction`، وأُزيلت خاصية `max=` من `<input type="datetime-local">`، وحُذف نصّ «(لا يتعدى ٣٠ يوماً)» من الليبل و«Up to 30 days out» من الوصف؛ التاجر يجدول لأيّ تاريخ مستقبليّ (بقي فقط الحدّ الأدنى: ١٠ دقائق من الآن). **(٢) حارس تعارض جديد**: متى كان الانتهاء `expiryType==='date'` ومعه `expiryGregorian`، يُشترط `startMs < expiryMs` وإلا يُرفض الحفظ برسالة «موعد بدء العرض يجب أن يكون قبل تاريخ انتهاء العرض». الأنواع الأخرى (ساعات/أيام/كمية) لا تتعارض لأن عمرها يبدأ من `startsAt`. وفي `calcExpiryMinutes` صار انتهاء النوع `date` يُحسب من `computedStartsAt` لا من لحظة الحفظ، فينتهي العرض في تاريخه المختار بالضبط. **(٣) سبب دوران الأزرار**: السبينر كان مربوطاً بـ`submitMode` (تُضبط فور النقر) لا بـ`isSaving` (تُضبط بعد نجاح التحقق)، فعند نقص البيانات يرجع `submitAction` مبكراً ويبقى `submitMode` مضبوطاً → دوران أبديّ. الإصلاح: الأزرار الثلاثة «حفظ وإضافة العرض / حفظ التعديل فقط / إضافة وتكرار» تعرض السبينر بشرط `isSaving && submitMode===X`، فلا يظهر إلا أثناء حفظ فعليّ ويختفي فور رسالة النقص. **تأكيد (بدون تغيير) لِـ`DealDetails.tsx`**: التاجر `userType==='seller'` لا يُمكنه الحجز إطلاقاً (يرى «أنت تتصفح كبائع، لا يمكنك الحجز»، والمودال محميّ بـ`!isSeller`)؛ القدرة على الحجز عند ناصر سببها أنه أدمن — الأدمن والمشتري فقط يحجزون. CACHE_NAME → v11.21، typecheck ✅، نُشِر على Vercel وتُحقّق منه حيّاً. |
| **v11.20** | **Coming Soon — التاجر يَقدِر يَجدول العَرض بعد فترة قصوى ٣٠ يوم؛ يَظهر للمشتري في «العروض القادمة» قبل أسبوع من البدء بقفل وعدّاد تنازليّ، يَفتَح الحجز تلقائياً عند انتهاء العدّاد**. طَلب ناصر طَويل (٤ صَفحات): «اضافة العروض القادمة او coming soon بالانجلش ... ويبدأ العد التنازلي وتضيف في الصفحة الرئيسية ... مع اضافة قفل عالصورة ويكون شكل الصوره غامق زي العروض المنتهيه ... ويستطيع الدخول للبرفايل ولكن لا يستطيع حجز المنتج الا عند انتهاء العد التنازلي ... عند قرب انتهاء العد التنازلي ب٤ ساعات يظهر باللون الاحمر ... جهز العرض من قبل شهر على ان يكون بعد شهر ممتاز يستطيع ذلك لكن العرض لا ينزل الا اذا بقي اسبوع يبدا يظهر في العروض القادمة coming soon في الصفحة الرئيسيه». **التَنفيذ في ٧ طَبَقات**: **(١) DB migration `v11_20_coming_soon`**: عَمود جَديد `deals.starts_at BIGINT` (epoch ms، nullable — `NULL` يَعني عَرض عاديّ يبدأ فَوراً، مَوجود = جَدولة)؛ partial index `idx_deals_starts_at_future` على `WHERE starts_at IS NOT NULL` لِتَسريع فِلتر Home «العروض القادمة». **بِدون** مَسّ لـ`tr_guard_deal_publish` (الجَدولة تَستهلك مَوقعاً من باقة التاجر فَوراً، كَأيّ نَشر عاديّ — قَرار مَقصود لِمَنع double-booking الـslots المُجدوَلة). **(٢) Rebuild `taki_dispatch_smart_alert`** مَع وَعي Coming-Soon: متى ما الـtrigger يَرى `starts_at` في المُستَقبل، يَستَخرِج عَدد الأيام (`خلال يوم/بعد يومين/خلال N أيام`)، يُضيف chip `⏳ يبدأ ...` لِكل match، ويَغلِب على كل العَناوين الأُخرى بِعَنوان `⏳ عَرض قادم — [اسم التاجر] يبدأ ...`. الـ`meta_data.coming_soon=true` + `meta_data.starts_at` يُكتَبان فَيَستَطيع الـclient routing لِصَفحة التَّفاصيل المُقفَلة. النَّتيجة: مُشتَرٍ بتنبيه ذكيّ «عيادة الرياض» يَصِله إشعار **يَوم تَجدول العَرض** (لا يَنتَظِر أُسبوع الظُّهور) فيَقدِر يُجَهِّز نَفسه — كَما طَلَب ناصر صَراحَةً («عرض قادم اي كان تصنيف العرض ... سيبدأ بعد يوم يظهر هذا التنبيه للمشتري وبامكانه ان يجهز العرض من بدري»). **(٣) `helpers.ts` — مَركَز المَنطِق**: ٣ ثَوابِت (`COMING_SOON_WINDOW_MS=7d`, `COMING_SOON_URGENT_MS=4h`, `COMING_SOON_MAX_LEAD_MS=30d`) + helpers `isDealComingSoon`, `isDealVisibleComingSoon`, `isDealScheduledHidden`, `isComingSoonUrgent`, `formatComingSoonRemaining`, `dealLifespanStart`, `dealExpiryTs`. **`dealLifespanStart` نُقطة حَرِجة**: العَرض المُجَدوَل بـ«ساعتَين صَلاحية» وَجَدول بَعد أُسبوع لا يَجوز أن «يَنتَهي» قَبل ما يَبدَأ. كل حِسابات الـexpiry الآن تَأخذ `max(startsAt, createdAt)` كَنُقطة بداية الـlifespan. **(٤) `Deal` interface في `mock.ts`**: حَقل اختياريّ `startsAt?: number`. **`dealRepository.ts`**: 3 تَعديلات — `save()` يُمَرّر `starts_at`، `mapRowToDeal` يَقرَأ `starts_at` (يَدعم BIGINT و ISO fallback)، `saveDeals()` (bulk) كَذلك. **forward-compat**: لو الـmigration ما طُبِّقت بَعد على DB أُخرى، الـrepo يَلتَقِط `/starts_at/i` في الـerror و يُعيد الـupsert بِدون الحَقل — التاجِر لا يَنحَجِب عَن النَّشر. **(٥) `DealCard.tsx`**: الـcard كاملاً مُدرِك بِالـcoming-soon — صورة بـ`filter: brightness(0.6) saturate(0.85)` لِلتَّعتيم، overlay داكِن (linear-gradient + chip أبيَض «🔒 قَريباً»)، countdown badge يَتَحَوَّل لِـ`linear-gradient(#6366f1→#4f46e5)` (نيليّ مُمَيِّز عَن البَرتُقاليّ العاديّ) ويَصير أحمر صُلب `linear-gradient(#dc2626→#b91c1c)` في آخر ٤ ساعات. الـformatRemaining للعَرض المُجَدوَل (بَعد ما يبدَأ) يَستَخدِم `dealLifespanStart` بَدلاً مِن createdAt المُجَرَّد. **(٦) `Home.tsx`**: قِسم جَديد `العروض القادمة ⏳` يُعرَض **فَقَط** لَو عِنده deals مَطابِقة (`isDealVisibleComingSoon`) — قِسم فارِغ يَكون ضوضاء. مَوضوع **فَوق** «الأكثر تداولاً» لِلتَّوكيد البَصَريّ. الـtrending/discount/all استُبعِدوا الـcoming-soon (helper `isLive`) لِأنّ buyer عاديّ في «كل العروض» لا يُريد رَؤية عَروض مُقفَلة. الفَرز الافتراضيّ في قِسم Coming Soon = soonest-to-launch first (أَكثَر فِعلاً لِلمُشتَرِي). **`DealsList.tsx`**: type جَديد `coming_soon` مَع title `العروض القادمة ⏳`، الـfiltering يُحَمِّل فَقَط الـcoming-soon في هذا الـtype ويَستَبعدها مِن البَقيّة. **`Nearby.tsx`**: استَبعَد الـcoming-soon مِن الخَريطة (المُستَخدِم في «حوالي» يُريد ما يَلتَقِط الآن في حُدود X كم). **`StoreDetails.tsx`**: الـcoming-soon يَبقى في تَبويب «العَروض النَّشِطة» (المُشتَرِي يُريد يَشوف ما هو قادم مِن هذا التاجِر) لكن `isTimedOut` صار يَفهَم startsAt فَلا يُخفيها بِالخَطَأ. **(٧) `DealDetails.tsx`**: `canBook = !isSoldOut && !isComingSoon`. زر «احجز الآن» يُستَبدَل بِصَندوق كَبير ملوّن (نِيليّ → أحمَر في آخِر ٤ ساعات) فيه عدّاد كبير `1.9rem` + رَسالة «يَفتح الحجز خلال ...» + زرّ مُقفَل («🔒 احجز الآن» disabled). صورة الـhero فيها overlay داكِن + chip «🔒 عَرض قادم — مُغلَق حالياً ... تابع العَدّ التَّنازليّ لِلحَجز». الـcountdown يَتَحَدَّث كل ثانية عبر `nowTick` الموجود مُسبَقاً. عند انتهاء العدّاد → re-render تلقائيّ → `isComingSoon=false` → الزرّ الحقيقيّ يَظهَر بِدون refresh. حِماية إضافيّة: `handleBooking` يَتَحَقَّق مِن `isComingSoon` ثانية (defense in depth لو buyer فَتَح الـmodal قبل الإطلاق). **(٨) `SellerDashboard.tsx`**: قِسم جَديد «جَدولة العَرض (اختياري)» في الفورم بَعد قِسم «نِظام انتهاء العَرض». toggle UI أَنيق (٢٦×٤٤ px مَع slider)، عند التَّفعيل يَظهَر `<input type="datetime-local">` مَع `min=now+10min` و `max=now+30days` كـattributes (الـbrowser يَحرس + validation داخل submitAction كَ defense in depth). صَندوق معلوماتيّ ديناميكيّ يُخبِر التاجِر: إذا الجَدول داخل ٧ أيام → «سَيَظهَر في «العروض القادمة» ويبدَأ خلال Xي Yس» — إذا أَبعَد → «العَرض مَحفوظ ومُجَدوَل. سَيَظهَر لِلمُشتَرين قبل أسبوع مِن البَدء (يَبقى N يوماً لِلظُّهور)» — تَأكيد بَصَريّ مُهِمّ لِأن العَرض المُجَدوَل بعد ٢٠ يوم سَيَختَفِي مِن «مَنتَجاتي النَّشِطة» إن لم يَفهَم التاجِر إنّه فِعلاً سُجِّل. `submitAction` يُضيف `startsAt: computedStartsAt` فَقَط لو الـtoggle مُفعَّل والتاريخ صالِح (`>= now+10min && <= now+30d`)، وإلا `undefined` (لا يُمكن لِلتاجِر يُعيد قَفل عَرض حَيّ بِالخَطَأ). `resetForm` و `handleEdit` يُديران الـstate الجَديد. `isTimedOut` (الفِلتَر بَين «نَشِطة» و«مُنتَهية» في لَوحة التاجِر) صار يَفهَم startsAt فَيَعرِض العَرض المُجَدوَل في «نَشِطة» (التاجِر يُريد يَراقِبه ويُعَدِّله، لا يُخفى عَنه). CACHE_NAME → v11.20، typecheck ✅. |
| **v11.19** | **٣ ميزات كبيرة في إصدار واحد** بَعد طَلَب ناصر لِمَرونة عالية في إدارة المنصة. **(١) نظام صلاحيات مسؤولين granular**: كان يُوجَد admin واحد فَقَط (ناصر) بِكل الصلاحيات. الآن ناصر = `super_admin` يُعَيِّن «admins أ» يَختار لِكل واحد التَّبويبات والإجراءات بِالتَّفصيل. **DB migration `v11_19_admin_permissions_system`**: عَمودَين جديدَين على `users` (`is_super_admin BOOLEAN`، `admin_permissions TEXT[]`) + ٧ functions (`is_super_admin`، `has_admin_permission`، `admin_list_staff`، `admin_promote_user`، `admin_set_permissions`، `admin_demote_user`، `admin_my_permissions`) كلها SECURITY DEFINER. أيّ super_admin تَجاوز فَوريّ، أيّ staff admin يَحتاج permission مَوجود في الـarray. جَدول audit `admin_permission_log` للتَّتبُّع. **١٤ permission key**: `tab_overview/buyers/sellers/reports/analytics/tools/admins` + `action_impersonate/view_finance/manage_users/delete_deals/manage_seasonal/manage_campaigns/manage_banners`. **Frontend**: نَوع `AdminPermission` في `authService.ts`، حُقول جَديدة في `UserProfile` (`isSuperAdmin`, `adminPermissions`)، `userRepository.findById` يَجلِبها. **AppContext** يُعَرِّض `isSuperAdmin: boolean` + `adminPermissions: string[]` + `hasPermission(perm)` callback. **`AdminDashboard.tsx`**: TABS صار فيه حَقل `permission`، tab جَديد `admins` (👑 المسؤولون)، `visibleTabs` يَفلتر حَسب `hasPermission`، deep-link محصور بِالـvisibleTabs، fallback لأول tab مَسموح إذا الـcurrent مَش مَسموح. لو الـstaff admin بِدون أي tab يَرى رسالة «لا توجد صلاحيات مفعّلة لحسابك». **صَفحة جَديدة `AdminAdmins.tsx` (~280 سَطر)**: تَعرِض كل الـstaff (RPC `admin_list_staff`)، لِكل واحد grid checkboxes لِلتَّبويبات + grid آخر لِلإجراءات، أزرار «💾 حفظ التغييرات» و«إزالة المسؤول» (RPC `admin_demote_user`). super admin row يَظهَر مُحمَّى («لا يُمكن تَعديل صلاحياته من هُنا»). **`AdminBuyers.tsx` + `AdminSellers.tsx`**: زرّ «🔓 دخول كحساب» صار مَفلتر بِ`hasPermission('action_impersonate')`، زرّ جَديد «👑 ترقية لمسؤول» (super only، target ≠ admin)، يَفتح dialog (`PromoteToAdminDialog.tsx`، promise-based لِسُهولة الاستِعمال) يَختار الصلاحيات أوّلاً ثُمّ يَستَدعي `admin_promote_user`. **Edge function `admin-impersonate` v2**: يَجلب `is_super_admin, admin_permissions` ضِمن callerProfile، يَرفض الـimpersonation لِأيّ admin بِدون `action_impersonate` (super بِيتجاوز). **حماية**: super admin's row مَحميّ في كل RPC (لا يَستَطيع staff admin تَعديل permissions الـsuper، ولا حتى super يُعَدِّل نَفسه عَبر هذه الـRPCs)، الـtarget لا يُمكن أن يَكون نَفس الـcaller أو admin آخر، promote يَحفَظ `is_super_admin = FALSE` دائماً (إلا عَبر SQL يَدويّ). **(٢) Profile self-edit (تَعديل بيانات الحساب)**: مكوّن جَديد `AccountSettingsCard.tsx` (~290 سَطر) يُضاف في tab «الإعدادات» في `Profile.tsx`. ٤ rows قَابِلة لِلطّيّ (collapsible): الاسم + المتجر، رقم الجوال، البريد، كلمة السر. **الاسم/الجوال**: عَبر `updateProfile` (partial-aware saveProfile v10.25). الجوال يَتحقَّق `/^05\d{8}$/`. **البريد**: `supabase.auth.updateUser({email})` يُرسل تأكيد لِلإيميلَين (القديم + الجديد)، التَّغيير يَستَقِرّ فَقَط بَعد الضَّغط على الرابطَين. **كلمة السر**: الـUI يَطلُب «كلمة السر الحالية» للـreauth ثُمّ يَستَدعي `supabase.auth.signInWithPassword` لِلتَّحقُّق قَبل `updateUser({password})` — هذا يَمنع session-hijack من تَحويله إلى account-takeover دائم. أقَلّ 8 أحرف، تَأكيد مُطابق. كل العمليات مَع customAlert لِلنّجاح/الفَشل. **(٣) اتصال بِالمشتري بدون كَشف الرقم + auto-hide 2hr**: قَبل v11.19 كان `tel:${phone}` صريح يَعرِض الرَّقم في الـUI. الآن: زرّ «📞 اتصال» (بدون رَقم) — ضَغطة تَفتَح `customConfirm` («هل تريد الاتصال بـ «اسم المشتري»؟»)، عِند الموافقة يُفتَح `tel:` link (الرَّقم يَظهر في الـOS dialer لكن لا يَظهر داخل التَّطبيق). **DB schema**: `Booking` interface في `bookingRepository.ts` فيه حَقل جَديد `completedAt?: number` يُجلب من `b.completed_at` (الـDB trigger يَكتُبه عِند status='completed'). **منطق الـauto-hide**: `Date.now() - completedAt > 2 * 60 * 60 * 1000` → الزرّ يَختَفي بِالكامل (الرَّقم يَبقى في DB لكن غير قابل لِلوصول من الواجهة). **النَّتيجة**: الرَّقم لا يَظهر في الـUI أبَداً، ١٠٠٪ من المشاهَدات تَتطلَّب confirm، وبَعد ٢ ساعة من الإكمال يَختَفي زرّ الاتصال كاملاً. CACHE_NAME → v11.19. typecheck ✅. edge function admin-impersonate v2 deployed. |
| **v11.18** | **مرونة عالية للرد على التقييمات في كل مكان**. ناصر أرسل screenshot لتبويب «آراء المشترين» في صَفحة المتجر `StoreDetails` ولاحظ أنّ التاجِر لا يَستَطيع الردّ مِن هُناك (الردّ كان مُتاحاً فَقَط مِن tab «التقييمات» في SellerDashboard ومِن DealDetails). طَلَب: «اريد التاجر ايضا يَستَطيع الردّ مِن هُنا وليس فَقَط مِن التقييمات اريد مرونه عاليه جدا». **التَنفيذ في ٣ مَلَفّات**: **(١) `StoreDetails.tsx`**: أُضيف `addReply` لاستيرادات `useApp` (سَطر 15)، state جَديد `activeReplyId` + `replyDrafts` بَعد `viewTab`. بلوك التقييمات (سَطر ~580) أُعيد بِناؤه كاملاً: يَستَنتج `canManage = user?.id === store?.id && !!r.id` (التاجِر يَرى صَفحة مَتجَره فَقَط)، ولِكل تَقييم يَعرِض: الردّ الحاليّ مَع زرَّي «✏️ تَعديل» (primary color) و «✕ حذف» (gray) — أو زرّ «💬 الردّ على هذا التَّعليق» إذا لا يُوجَد ردّ بَعد — أو textarea + Send/Cancel أثناء الكِتابة. عَمَل cancel لا يَمَسّ البَيانات. الإحضار يَستَخدِم نَفس `addReply` مِن AppContext الذي يَستَدعي `ratingRepository.setReply` → RPC `set_rating_reply` (SECURITY DEFINER، فَقَط مالك المَحلّ يَكتُب). **(٢) ميزة جَديدة في الثَّلاثة: «✏️ تَعديل الردّ» بَدلاً مِن «حذف ثُمّ إعادة كِتابة»**: قَبل v11.18 كان التَّاجِر مُضطَرّاً لِحذف الردّ (يَختَفي) ثُمّ كِتابته مِن الصِّفر إذا أَراد تَعديل كَلِمة فَقَط. الآن زرّ «✏️ تَعديل» يُعَبِّئ الـtextarea بِالردّ الحاليّ ويَفتَح وضع الـcompose مَع زرّ «💾 حفظ التَعديل» — Cancel لا يَمَسّ شَيء، Send يَكتُب فَوق الردّ السَّابِق عَبر نَفس `addReply` (الـRPC `set_rating_reply` يَدعَم overwrite بِشَرط الـownership). التَّعديل المُكَرَّر في الـ٣ مَلَفّات يَحتَفِظ بِتَزامُن التَّجرِبة. **(٣) `SellerDashboard.tsx` (سَطر 3174~3280)**: نَفس التَّحويل لِبلوك تَقييمات tab «التقييمات» — wrap في IIFE لِالتقاط `isEditing = activeReplyId === r.id`، الردّ المَعروض مُخفِيّ أثناء التَّعديل (تَجَنُّب double-display)، زرّ تَعديل بِجانِب زرّ Remove، Send button يَعرِض «💾 حفظ التَعديل» أو «💬 إرسال الردّ» حَسَب وُجود ردّ سابِق. **(٤) `DealDetails.tsx` (سَطر 1102~)**: نَفس التَّحويل لِبلوك التَّعليقات في صَفحة العَرض. النَّمط `canReply = isOwner && !!r.id` يَبقى كَما هو، يُضاف `isEditing` المُشتَقّ، الرَّدّ يُخفى أثناء التَّعديل، أزرار Edit/Remove جَنباً لِجَنب، Send button عُنوانه ديناميّ. الـtypecheck نَجَح بِدون أيّ خَطأ. CACHE_NAME → v11.18. النَّتيجة: التَّاجِر الآن يَستَطيع الردّ والتَّعديل والحَذف لِأيّ تَقييم مِن ٣ أَماكِن مُختَلِفة (صَفحة مَتجَره، صَفحة عَرضه، tab التَّقييمات في لَوحَته) — مَرونة عالية كَما طَلَب ناصِر. |
| **v11.17** | **Hotfix لـv11.16**: ناصر اشتَكى مِن مُشكِلتَين بَعد تَجربة الجَلسة الكاملة. **(١) زرّ «دخول كَهذا المُستَخدِم» لا يَستَجيب في النَّقرة الأولى** — السبب: `startImpersonating` يُنَفِّذ سِلسِلة async (`getSession` → edge function invoke → `verifyOtp` → `setItem` → `location.assign`) تَأخذ 2-3 ثَواني، وبِدون أيّ مُؤشِّر بَصَريّ فالمُستَخدِم يَظُنّ أنّ النَّقرة لم تُسَجَّل ويَنقُر مَرَّة أُخرى. **الإصلاح**: في AdminBuyers/AdminSellers أُضيف `useState` مَحلّيّ `opening` + handler يَلُفّ الاستِدعاء مَع `setOpening(true)` قَبل و `setOpening(false)` في finally، والزرّ يُصبِح `disabled` مَع spinner دائريّ ونَص «جاري فَتح الجَلسة...» بَدلاً مِن «دخول كَهذا...». الحارس `if (opening) return;` يَمنع double-call. **(٢) زرّ «🔙 رجوع للمدير» لا يَعود** — السبب الجَذريّ: `stopImpersonating` كان `await` على إدخال audit log في `admin_impersonation_log` وهذا الإدخال يَفشل بِـRLS (الجَلسة الحاليّة = target فَـ`is_admin()` تُعيد false)، لكن في الشَّبكات البَطيئة الاستِدعاء يَتَعَلَّق قَبل أن يَرجع الـerror فَيَحبس الـchain. **الإصلاح ثُلاثيّ**: (أ) تَحويل audit log إلى **fire-and-forget** (`.then(() => {}, () => {})` بِدون await) لِأنّ السِّجلّ المُهمّ أمنياً هو سِجلّ «start» الذي يَكتُبه edge function بِـservice_role قَبلاً. (ب) `supabase.auth.setSession` مُغَلَّف في **Promise.race مَع timeout 8 ثَواني** — لو الشَّبكة هَنَجت أو الـrefresh_token rotated بِشَكل غَير مَحسوب فالـrace يَحُلّ `{ error: 'timeout' }` وتَستَكمل السِّلسِلة بَدلاً مِن الجُمود. (ج) الدالّة **دائماً** تَصل إلى navigation step (`location.assign('/admin')` عِند النَّجاح، `location.assign('/register')` بَعد signOut عِند الفَشل) فَالنَّقرة لا تَشعُر «مَيتة» أبداً. **مُؤشِّر بَصَريّ في الـbanner**: state جَديد `stoppingImp` (+ ref مَنع double-tap) يُحوِّل زرّ الـbanner إلى spinner أبيض دائريّ مَع نَصّ «جاري الرُّجوع...» — `@keyframes taki-spin` مُعرَّف داخل الـbanner JSX لِضَمان تَوفُّره أثناء عَرض الـbanner (الكَيفريم الأصليّ في App.tsx لا يَكون mounted في كل صَفحة). CACHE_NAME → v11.17، typecheck ✅. |
| **v11.16** | **ترقية «تَصفّح كَهذا المُستخدم» من v11.15 إلى جَلسة كاملة فِعليّة (act-as)**. ناصر طَلب رفع المستوى من تَصفّح فَقَط إلى تَحكم كامل («اجعل اعمق اسطتيع الحذف وعمل جميع ما يعمله من ارسال منشورات وحذف ومراسلات ... كاني سويت تسجيل دخول بحسابه»). الميكانيكية القَديمة (v11.15) كانت swap لـ`user` state فَقَط مَع بَقاء Supabase session = admin (RLS تَقرأ كل شَيء لكن أيّ كتابة تَتم بِـauth.uid=admin مَع تَوقُّعات RLS مكسورة). **التَّنفيذ الجديد**: real session swap عَبر Supabase magiclink. **(١) Database migration v11_16_admin_impersonation_log**: جَدول `admin_impersonation_log(admin_id, target_id, target_email, action='start'\|'stop', ip_address, user_agent, created_at)` مَع RLS عبر `is_admin()` (لِتَجنُّب self-reference، CLAUDE.md feedback_rls_no_self_reference). **(٢) Edge Function `admin-impersonate` (verify_jwt=true)**: يَتَحقَّق من JWT الـcaller → يَقرأ `public.users.user_type` عبر service_role (لا يَثق بـuser_metadata لِأنّه قابل لِلتَّلاعب)، يَرفُض إذا لم يَكن admin أو إذا الـtarget admin أو الـcaller نَفسه أو الـtarget بِدون email. يَستَدعي `admin.auth.admin.generateLink({type:'magiclink', email:targetEmail})` — هذا لا يُرسل إيميل حين الاستِدعاء بـservice_role، فَقَط يولِّد `hashed_token` يُمكن استبداله بِجَلسة كاملة. يَكتب audit log + يُعيد `{hashed_token, target:{name,userType,shop,phone}, admin:{name}}`. CORS مَفتوح. **(٣) Client-side `startImpersonating` في `AppContext.tsx`**: (أ) يَلتَقِط tokens المدير الحاليّة من `supabase.auth.getSession()` قَبل أيّ شَيء (هذه احتياطي العَودة). (ب) يَستَدعي edge function عَبر `supabase.functions.invoke('admin-impersonate', {body:{targetUserId}})`. (ج) يَستَدعي `supabase.auth.verifyOtp({type:'magiclink', token_hash})` الذي يَستَهلِك الـhashed_token ويُبَدِّل الجَلسة فِعلياً — بَعد هذا كل استِدعاء Supabase من المتصفِّح authorized as target. (د) إذا فَشل الـverify → يُحاول `setSession(adminTokens)` لِلعَودة + alert. (هـ) لو نَجَح → يَكتب في localStorage `TAKI_IMPERSONATION_SESSION` يَحتوي `{adminAccessToken, adminRefreshToken, adminId, adminName, targetId, targetName, targetUserType, startedAt}` ثم `window.location.assign('/seller'\|'/')` (reload كامل لِتَجديد كل state). الكتابة بعد verifyOtp فَقَط لِتَجنُّب stale state على فَشل. **(٤) `stopImpersonating`**: (أ) يَقرأ localStorage. (ب) يَكتب audit log `action='stop'` (best-effort، الجَلسة لا تَزال target's فَلَن تَفشل RLS). (ج) يَستَدعي `supabase.auth.setSession({access_token:adminAccessToken, refresh_token:adminRefreshToken})` لاستِعادة جَلسة المدير. (د) لو فَشل (الـrefresh_token rotated بِسبب تَبويب آخر) → `signOut()` + `customAlertRef.current('انتَهت صَلاحية ... سَجِّل دخول مَرَّة أُخرى')` + redirect /register. (هـ) clear localStorage + `window.location.assign('/admin')`. **(٥) Init في AppContext**: يَقرأ `TAKI_IMPERSONATION_SESSION` لِتَهيِئة state، ويُنَظِّف v11.15 keys (`TAKI_IMPERSONATE_TARGET` / `TAKI_IMPERSONATE_ADMIN`) لِأنّها deprecated. في initData يَتم sanity-check: إذا `parsed.targetId !== currentUser.id` → الجَلسة drifted (admin استَعاد session يَدوياً في تَبويب آخر) → clear localStorage. **(٦) حُذف v11.15 mechanism بِالكامل**: (أ) currentUser swap في initData (مُستَبدَل بِsanity-check فَقَط). (ب) auth listener short-circuit للأدمن أثناء impersonation (لم يَعُد لازم لِأنّ session = target فِعلاً). (ج) `impersonationTargetIdRef`. (د) `TAKI_IMPERSONATE_TARGET/_ADMIN` localStorage keys (يَتم نَظافتها على init). **(٧) Banner أحمر مُحدَّث**: «🔓 جَلسة كاملة كَـ [اسم] (تاجر/مُشتري) — كل تَعديل يَتم بِاسمه» (بَدلاً من «تَصفّح كَـ»). gradient أَدكَن `#ef4444 → #b91c1c` لِيَنبِّه أنّه act-as وليس مُجرد view. **(٨) أزرار AdminBuyers/AdminSellers مُحدَّثة**: «🔓 دخول كَهذا المُشتري (جَلسة كاملة)» / «🔓 دخول كَهذا التاجِر (جَلسة كاملة)» مَع نَصّ مُساعد جديد: «كأنّك سَجَّلت دخول بِحسابه — تَحجز/تَنشر، تَحذف، تُراسِل، تُعدِّل. كل إجراء مُسجَّل في سِجل التَّدقيق». **(٩) AppContextType**: نَوع `impersonating` تَوسَّع لِيَشمل `targetId` و `startedAt`، و `stopImpersonating` صار `Promise<void>` (يَنتَظر setSession). **محدودية**: المُستَخدِم بِدون email (مُسجَّل بِجوّال OTP فَقَط) لا يُمكن impersonation عَلَيه حالياً — Supabase generateLink يَتَطلَّب email؛ الـedge function تُعيد رسالة واضحة. CACHE_NAME → v11.16، typecheck ✅. |
| **v11.15** | **«تَصفّح كَهذا المُستخدم» من لوحة المدير**. ناصر طَلب لمّا يَضغط على بائع/مشتري في لوحته يَدخل على بُروفايلهم كأنّه هم بالضبط. اختار «تَصفّح فقط» (آمن وسَريع) دون إجراءات كِتابة بِاسم المُستخدم. **التَنفيذ**: مِيكانيكية impersonation تَعتمد على swap لـ`user` state دون لمس Supabase session (الـadmin يَبقى مُصرَّحاً عبر RLS). **(١) في `AppContext.tsx`**: state جَديد `impersonating: { adminId, adminName } | null` مَع mirror ref `impersonationTargetIdRef` (الـauth listener يَستهلكه دون closures stale). يُحفَظ في localStorage `TAKI_IMPERSONATE_TARGET` + `TAKI_IMPERSONATE_ADMIN` ليُلتَقَط من initData بعد الـreload. **(٢) في `initData()`**: لو الـsession admin والـlocalStorage فيه target — جَلب الـtarget عَبر `userRepository.findById(targetId)` ثم reassign `currentUser = target`، وكل hydration الـsnapshots (`fav_<uid>`, `notif_<uid>`, `bk_<uid>`) يَستَخدم target.id تلقائياً. لو الـtarget stale أو userType='admin' → cleanup للـlocalStorage. **(٣) في الـauth listener**: short-circuit عند INITIAL_SESSION/TOKEN_REFRESHED للأدمن المُتَصفِّح — يَرجع مُبكِّراً قبل `setUser(optimisticProfile)` حتى لا يَنقلب الـuser للأدمن أثناء الـimpersonation. **(٤) `startImpersonating(targetUserId)`**: validation كامل (admin only، not self، not another admin، target exists)، يَحفظ في localStorage ثم `window.location.assign('/seller' | '/')` لإعادة بِناء كامل شَجرة الـstate بـtarget id. **(٥) `stopImpersonating()`**: clear localStorage + `window.location.assign('/admin')` لإستعادة هَوية الأدمن عبر مَسار الـinit الطَبيعي. **(٦) `logout()`**: يَمسح impersonation localStorage + state قبل clearing user (يَمنع زامبي impersonation بعد تَسجيل الخروج). **(٧) Banner أحمر ثابت في الأَعلى**: full-width fixed top مع `env(safe-area-inset-top)` لـiOS notch، z-index=99999، يُظهِر «تَصفّح كَـ X (تاجر/مشتري)» + زرّ أبيض «🔙 رجوع للمدير». **(٨) `AuthRedirector` في App.tsx**: حُرس إضافي — لو `impersonating` non-null، تَخطّي redirect إلى /complete-profile وreplace لـ/admin\|/seller\|/. السبب: لو الـtarget عنده profile incomplete، لا يَنبغي تَوريط الأَدمن في صفحة إكمال profile حساب آخر. **(٩) زرّ «👁️ تَصفّح التطبيق كَهذا المُشتري» في `AdminBuyers.tsx`** و«👁️ تَصفّح التطبيق كَهذا التاجر» في `AdminSellers.tsx` — في رأس الـmodal فوق إحصائيات الـuser/الباقة، gradient `amber→orange→red` بارز، تَحته نَصّ مُساعد بـ10px يُوضِّح القُدرة («ستَشاهد كل ما يَراه — حُجوزاته، إشعاراته، مُفضّلته. ارجع للمدير في أيّ وقت»). **(١٠) `AppContextType` interface**: 3 حُقول جَديدة (`impersonating`, `startImpersonating`, `stopImpersonating`) لتَجنُّب TS2339. CACHE_NAME → v11.15، typecheck ✅. |
| **v11.14** | **5 طلبات من ناصر** بعد اختباره لـFAQ ولوحة التاجر: 4 منها تَتطلَّب تَعديل كود فعليّ، و1 تَعديل نَصّي. **(١) رقم الجوال للتاجر**: ناصر اشتكى أنّ التاجر لا يَرى رقم جوّال المشتري في الواقع، رغم أنّ الجواب في FAQ يَقول العكس. **السبب الجذريّ**: في `AppContext.tsx:1472` عند إنشاء الحجز كان السطر `userPhone: user?.phone || ''` — فإذا لم يَكن للمشتري `phone` مَملوء (مثلاً سَجّل بإيميل بدلاً من OTP)، يُحفَظ الـbooking بدون جوّال، والـUI في `SellerDashboard.tsx:2952` `{(order as any).userPhone && (...)}` فلا يَظهر شيء. **الإصلاح**: `userPhone: user?.phone || user?.contactPhone || ''` — fallback لِجوال التَواصل الذي عادةً مَوجود (signUpWithEmail يَحفظ `contact_phone: userData.contact_phone || userData.phone`). **(٢) «تَنتظر مراجعة فريق الإدارة لتفعيل الحساب»**: ناصر يَقول الحساب يَتفعَّل فورياً. **التَنفيذ**: حُذف الانتظار من الجواب في FAQ. الصياغة الجديدة: «يُفعَّل الحساب فوراً بعد الإكمال، وتَبدأ باستخدام لوحة التاجر مباشرةً. وللإدارة الحقّ في طَلب أيّ وثائق نظامية لاحقاً عند الحاجة». **(٣) لا تَسجيل مجاني — تَجربة 14 يوم**: الجواب القديم «التسجيل والاستخدام الأساسي مجاني» مُضلِّل لأنّ نَموذج TAKI الفعليّ = تَجربة 14 يوم ثم اشتراك. **التَنفيذ**: استُبدِل بـ«يَحصل كلّ تاجر جديد على **تجربة مجانية لمدّة 14 يوماً** يَستخدم خلالها كلّ ميزات لوحة التاجر دون أيّ دفع. بعد انتهاء التَجربة، يَلزم تَفعيل اشتراك مدفوع لمتابعة النشر واستقبال الحجوزات». **(٤) حذف ذِكر الإيميل**: «إشعارات فورية في التطبيق وعبر الإيميل عند كلّ حجز جديد» — لا تَصل رسائل بالإيميل فعلياً. **التَنفيذ**: حُذف ذِكر الإيميل من الجواب. الصياغة الجديدة: «تَصِلك إشعارات فورية داخل التطبيق عند كلّ حجز جديد ... مع بيانات المشتري الأساسية (الاسم ورقم الجوّال) ووقت الحجز». المسار صَحيح للتاجر «الطلبات» (بدل «الحجوزات»). **(٥) الردّ على التَقييمات — كان موجوداً في DealDetails فقط، أُضيف tab جديد كامل في SellerDashboard**: ناصر اشتكى أنّ التاجر لا يَستطيع الردّ على التَقييمات، طَلب «نَفس فيسبوك». **السبب**: ميزة الردّ كانت موجودة في `DealDetails.tsx:1121+` (مع `canReply = isOwner && !!r.id`)، لكنّها مَخفية داخل صفحة كل عرض على حِدَة، فالتاجر لا يَجدها بسهولة من لوحته. **التَنفيذ**: **(أ)** أُضيف tab سَادس «⭐ التقييمات» في `SellerDashboard.tsx` — type definition (110) + URL params allow-list (195) + tabs render array (1741) + icon ⭐ + label (التقييمات/Reviews). **(ب)** أُضيف state جديد في الـcomponent: `activeReplyId` + `replyDrafts` (نفس النَمط في DealDetails). **(ج)** أُضيف conditional block جديد لـview === 'reviews' (~145 سطر) قَبل view === 'form': يَجمع كلّ التَقييمات عَبر `myDeals.flatMap(d => d.ratings || [])` ويُرتّبها زمنياً (الأَحدث أوّلاً). كل تَقييم في بطاقة منفصلة فيها: اسم المُقيِّم + النجوم + اسم العَرض المُقيَّم + النصّ + التاريخ. **(د) UI الردّ نَفس فيسبوك**: إذا فيه ردّ مَوجود → يُعرَض في صندوق منفصل مع زرّ «✕ حذف الردّ» (يَستدعي customConfirm ثم `addReply(dealId, ratingId, '')` لِحذفه). إذا لا يُوجد ردّ → زرّ «💬 الردّ على هذا التعليق» يَفتح textarea inline تحت التَعليق + زرّ «إرسال الردّ» (يَستدعي `addReply` المُتاح من useApp، الذي يَستخدم `ratingRepository.setReply` → RPC `set_rating_reply` SECURITY DEFINER). **(هـ)** عند Empty state: «لا توجد تقييمات على عروضك بعد» مع icon ⭐ كبير. **(و)** الـtypecheck أخطأ بسبب `Rating` type في mock.ts (يَستخدم `date`) ≠ `Rating` في ratingRepository.ts (يَستخدم `createdAt`). الإصلاح: `(r.createdAt || r.date)` كـfallback، مع cast `as any` لِلـsort. **(ز)** **FAQ تَعديل المسار**: تَحديث الجواب لِيَشير لـ«⭐ التقييمات» tab الجديد + ذِكر «نَفس فيسبوك» صراحةً. النسخة الإنجليزية مُحدَّثة بنفس التَغييرات. CACHE_NAME → v11.14، typecheck ✅. |
| **v11.13** | **طلب ناصر**: «لا تَذكر المدّة القصوى للمستخدمين، اكتفِ بـ15 يوم». في Refund.tsx Section 6 — البند الذي عَدَّلناه في v11.12 لِيُصبح «15 يوماً متواصلة»، كانت الجملة بين القوسين تَكشف القانون: «(المدّة القصوى المنصوص عليها في نظام التجارة الإلكترونية السعودي للتأخير في تَنفيذ الخدمة)». **التَنفيذ**: حُذِفت الجملة بين القوسين في النَسختَين العربية والإنجليزية. البند صار بسيطاً: «**عطل في خدمة TAKI لأكثر من 15 يوماً متواصلة**: يَحقّ للتاجر استرداد قيمة الأيام المُتعطّلة تناسبياً». السبب: نفس فلسفة v11.3 (سرّية الإجراءات الداخلية كـ«أسرار تَشغيلية») — لا نَكشف للمستخدمين أن الـ15 يوم هي الحدّ النظاميّ الأقصى لأنّ ذلك قد يُؤطّر النزاعات اللاحقة («أنتم تَعترفون أنّ هذا الحدّ القانونيّ»). البند يَبقى مَعقولاً تجارياً وقانونياً، ولكن دون كَشف الأساس القانونيّ صراحةً. CACHE_NAME → v11.13، typecheck ✅. |
| **v11.12** | **4 أسئلة قانونية + UI من ناصر** بعد مراجعته لـRefund.tsx. **البحث القانوني الذي تَمّ**: راجعتُ نظام التجارة الإلكترونية السعودي (مرسوم ملكي م/126-1440هـ ولائحته التنفيذية، وزارة التجارة، Lexology، Saudipedia) ولوائح البنك المركزي السعودي (SAMA). **النتائج**: (أ) **حقّ العدول في التجارة الإلكترونية = 7 أيام** من تاريخ تَسلّم المنتج/التعاقد على الخدمة، لو لم يَستفد المستهلك. (ب) **التَأخير في تَسليم الخدمة = حدّ أقصى 15 يوماً** قبل أن يَحقّ للمستهلك الفسخ. (ج) **استرداد بطاقات SAMA = 3 أيام عمل** من جانب البنك (للـacquirer والـissuer). **التَنفيذ على Refund.tsx**: **(١) Section 5 — البند «بعد مرور 14 يوماً ... تَسقط الفترة النظامية للمطالبة»**: غُيِّر 14 → **7 أيام** مع إضافة «وفقاً لحقّ العدول المنصوص عليه في نظام التجارة الإلكترونية السعودي». السبب: 14 كانت أعلى من القانون، تَخفيضها لـ7 يَحفظ TAKI من المطالبات الأطول من الحقّ النظامي. **(٢) Section 6 — البند «عطل في خدمة TAKI لأكثر من 7 أيام متواصلة»**: غُيِّر 7 → **15 يوماً** (الحدّ الأقصى المنصوص عليه في القانون السعودي للتأخير في تَنفيذ الخدمة قبل حقّ الفسخ). مع إضافة شرح بين قوسين. السبب: «أعلى رقم نظامياً» كما طَلب ناصر — يَمنح TAKI حصانة لمدّة أطول قبل أن يَقع تحت طائلة الاسترداد الإلزاميّ. **(٣) Section 7 — مدّة معالجة طلبات الاسترداد**: استَجبتُ لِطلب ناصر مع تَأكيد قانونيّ. غُيِّرت من «3–7 أيام مراجعة + 7–14 يوم تَحويل» إلى **«14 يوم عمل مراجعة + 14 يوم عمل تَحويل»** — مَسموح قانونياً لأنّه: (أ) القانون لا يُحدّد سَقفاً لمراجعة طلبات الاسترداد من جانب التاجر؛ (ب) SAMA تَفرض على البنوك معالجة الاسترداد خلال 3 أيام عمل، لكنّ هذا يَخصّ الجانب البنكي بعد التَحويل من جانبنا. أُضيف بند ثالث جديد يُوضِّح: «قد تُضاف حتى **3 أيام عمل إضافية** لإتمام الإيداع من جانب البنك أو شبكة البطاقة (مدى / Visa / Mastercard) وفقاً للوائح البنك المركزي السعودي (SAMA)» — هذا يَحمي TAKI من مطالبات التَأخير البنكي بَعد تَحويلها. **(٤) إصلاح dark mode للصناديق الثلاث**: في Refund.tsx الصناديق الثلاث (المبدأ الجوهري emerald-50/900، تَنبيه مهم amber-50/900، كيف يَعمل الاشتراك emerald-50/900) كان النصّ غير واضح في dark mode بسبب الـcontrast السيّئ (نص غامق `text-emerald-900` على خلفية شبه فاتحة في الـdark mode). الـtailwind config في TAKI لا يَدعم `dark:` modifier (يَستخدم `.dark-mode` class على html بدلاً). الحلّ: أُضيفت 8 rules في src/styles.css بعد قواعد admin pills الموجودة: `.dark-mode .text-emerald-900 { color: #ffffff; }` و amber-900، red-900، blue-900، مع الـ-800 المُكافِئة بألوان فاتحة (`#d1fae5`, `#fef3c7`, ...). النتيجة: في dark mode الصناديق تَبقى ملوّنة لكن النصّ يَصير أبيضاً واضحاً (نفس النَمط المُتَّبع للـtext-emerald-600/700 الموجود سابقاً للـadmin pills). CACHE_NAME → v11.12، typecheck ✅. |
| **v11.11** | **طلب ناصر**: «خلّ قسم مشاركة البيانات في Privacy.tsx بصياغة أوسع ضمن القانون مثل نون وحراج. وتَأكّد لي من القانون السعودي بخصوص نقل البيانات (Section 8) وأَعطني أمثلة لشركات كبرى تَستخدمه». **البحث الذي تَمّ**: (أ) **حراج** (haraj.com.sa/privacy): «لن نقوم ببيع، متاجرة، تَأجير، أو إفشاء أي معلومات لمصلحة طرف ثالث خارج هذه المنصّة، إلا بيانات إحصائية مُجمَّعة بدون تَعريف». لا يَذكر نَقل البيانات. (ب) **نون**: WebFetch فَشل بـtimeout، لكن نون من المُلتزِمين بـPDPL. (ج) **STC** (stc.com.sa): «لا تُؤجّر أو تَبيع أو تَنقل البيانات لأطراف ثالثة خارج STC وشركائها». (د) **Tamara** (tamara.co): «قد تُشارَك مع كيانات خارج المملكة بمستوى حماية كافٍ، مع تَدابير أمنية وحدّ أدنى من البيانات». (هـ) **Jarir** (jarir.com): «حسب الحاجة لتقديم الخدمات أو الامتثال للقانون أو حماية الحقوق، مع ضمانات مناسبة». **PDPL Article 29 + لائحة SDAIA**: نَقل البيانات خارج المملكة **مَسموح قانونياً** بشروط: الحدّ الأدنى من البيانات + لا ضرر بالأمن الوطني أو المصالح الحيوية + جهة مُتلقّية تَوفّر مستوى حماية كافٍ أو ضمانات مناسبة (Standard Contractual Clauses SCC أو Binding Common Rules BCR). صياغة TAKI الحالية في Section 8 (نقل البيانات) **مُطابقة قانونياً** ومُتطابقة مع Tamara و STC و Jarir. **التَنفيذ**: في Privacy.tsx Section 5 «مع من نُشارك البيانات»: تَقليص 7 بنود تَفصيلية إلى **4 بنود عامّة** (مثل Jarir + Tamara) بدلاً من السرد التَفصيلي للمزوّدين والمستشارين والمُتنازَل لهم: (أ) «الطرف الآخر في المعاملة» — التاجر/المشتري. (ب) «الشركاء التشغيلون والتقنيون والمالِيون» — يَشمل الاستضافة والمعالجة والمدفوعات والاتصالات والتحليلات في بند واحد بدلاً من 3 بنود مُنفصلة. (ج) «الجهات النظامية والأمنية والقضائية» — مَدمَج مع «حماية المنصّة من ضرر محتمل» في بند واحد. (د) «الأغراض المشروعة الأخرى» — كالتدقيق، حلّ النزاعات، إعادة الهيكلة، التَنازل — في بند عامّ بدل 3 بنود مُنفصلة. النَتيجة: أبسط، أَعمّ، يَفتح مجالاً قانونياً أوسع للمنصّة دون كَشف تَفاصيل تشغيلية، مَدموجاً في إطار قانوني سعوديّ مع تَأكيد «لا تُستخدَم بياناتك لأغراض خارجة عن الغرض الذي جُمعت من أجله» (سَطر أمان). **Section 8 (نقل البيانات)**: تُرك كما هو لأنّه مُطابق لـPDPL ولأمثلة Tamara و STC. CACHE_NAME → v11.11، typecheck ✅. |
| **v11.10** | **5 طلبات من ناصر** بعد مراجعته للنسخة v11.9: **(١) حذف بانر «المسوّدة»** من جميع الصفحات القانونية: في LegalLayout.tsx غُيِّر default `draftNotice` من true إلى false، فلا يَظهر بانر التحذير الأصفر «⚠️ مسوّدة...» تلقائياً في Terms / Privacy / Refund (كانت تَترك الخيار الافتراضي true). الـprop باقٍ كاحتياط مستقبليّ لكن لا يُستخدم. **(٢) تَخفيف شروط التاجر بنموذج حراج**: في Terms Section 3 (الأهلية والتاجر)، أُزيلت الصياغة الإلزامية «حِيازة سجلّ تجاريّ ساري» و«تَقديم وثائق التحقّق التي تَطلبها الإدارة قبل التَفعيل» (كانتا تُلزِمان المنصّة بالتحقّق المسبق وكأنّها بوّابة قانونية). استُبدلتا ببندَين جديدَين بأسلوب حراج: (أ) «**التاجر وحده مسؤول** عن حِيازة جميع الوثائق والتَراخيص النظامية اللازمة لمزاولة نشاطه ... والمنصّة **لا تَلتزم بالتحقّق المسبق** من هذه الوثائق قبل تَفعيل الحساب أو نَشر العروض» — يَفتح الباب لقبول التجار فوراً مع تَحويل العبء القانوني لهم. (ب) «للإدارة الحقّ في طَلب أيّ من هذه الوثائق متى رأت ذلك ضرورياً ... وأيّ مخالفة لمتطلّبات التَرخيص أو نَشر نشاط دون التَراخيص اللازمة تَقع مسؤوليّتها الكاملة — جنائياً ومدنياً — على التاجر وحده، دون أيّ مسؤولية على المنصّة» — يَحفظ حقّ الإدارة في الطلب اللاحق ويُؤكّد عدم مسؤولية المنصّة على المخالفات. النصّ الإنجليزي مُترجَم بنفس الدقّة. **(٣) دَمج Section 9 (بنود الحماية الخاصّة) في Section 7 (المحتوى المحظور قطعياً)**: كان لدينا قسم مُنفصل (Section 9) فيه 4 بنود تَفصيلية عن الاتجار بالبشر / حماية الأطفال / النساء / المُضايقة. حُذف القسم بكامله وأُضيفت 4 بنود مَدمجة في نهاية قائمة المحظورات Section 7 بصياغة أكثر اقتضاباً: «الاتجار بالبشر واستغلالهم» + «أيّ محتوى يَتضمّن تَحرّشاً بالقاصرين أو استغلالاً لهم أو تَنمّراً» + «التَحرّش بالنساء والفتيات أو ابتزازهنّ أو التَنمّر عليهنّ» + «التَمييز أو التَهديد أو الإساءة أو التَنمّر بسبب الدين أو العرق أو الجنس أو الجنسية». الروابط لـhrc.gov.sa محفوظة. **إعادة ترقيم الأقسام**: 10→9، 11→10، 12→11، ...، 23→22. صار العَدد الإجمالي 22 قسماً بدل 23. **(٤) حذف «الرياض» من الاختصاص القَضائيّ**: في Terms Section 21 (القانون الحاكم): تَعديل «الاختصاص حصرياً للمحاكم السعودية المختصّة بمدينة الرياض» → «الاختصاص حصرياً للمحاكم السعودية المختصّة داخل المملكة العربية السعودية». وفي النصّ الإنجليزي: «Saudi courts seated in Riyadh» → «competent courts within the Kingdom of Saudi Arabia». في Refund.tsx Section 8 (الجهات الرقابية): «المحاكم التجارية السعودية المختصّة بمدينة الرياض» → «المحاكم التجارية السعودية المختصّة داخل المملكة العربية السعودية» (نَسختا العربي والإنجليزي). السبب: ناصر يَتعامل من مَواقع متعدّدة، فحَصر القَضاء بالرياض قد يَخلق مشاكل اختصاص للقضايا التي يُمكن النظر فيها في أيّ محكمة سعودية مُختصّة. **(٥) إصلاح زرّ الرجوع في الصفحات القانونية**: المشكلة كانت أنّ ناصر يَفتح Terms من «Profile ← الإعدادات»، وعند الضغط على زرّ الرجوع (←) في LegalLayout يَستخدم `history.goBack()`، فيُعيده لـProfile لكن مع `activeTab='notifications'` (الافتراضي) بدلاً من `activeTab='settings'` (آخر tab كان فيه). السبب: Profile كان يَستخدم `useState` العادي لِـactiveTab، فيُعاد للقيمة الافتراضية عند كل re-mount. **الحلّ**: في Profile.tsx، (أ) استَبدلتُ initial state من `useState<...>('notifications')` بِـlazy initializer يَقرأ من `sessionStorage.getItem('taki:profile:activeTab')` ويَقبل القيم 'followed' أو 'settings' فقط، وإلّا فالافتراضي 'notifications'. (ب) أَضفتُ `useEffect` يَكتب `activeTab` في sessionStorage عند كل تَغيير. النتيجة: الآن عند الرجوع من أيّ صفحة قانونية، Profile يَتذكّر الـtab الذي كان فيه ناصر تماماً (الإعدادات/المتابعة/التنبيهات الذكية). استخدمتُ try/catch حول `sessionStorage` لِأنّ بعض المتصفّحات (private mode في Safari مثلاً) قد تَرمي SecurityError. CACHE_NAME → v11.10، typecheck ✅. |
| **v11.9** | **طلب ناصر**: «دَعم اللغة الإنجليزية الكامل للصفحات القانونية — كلّ صفحة لازم تَظهر بالإنجليزية عند تَبديل اللغة من «حسابي» ← «اللغة والتفضيلات» ← English. ترجمة قانونية احترافية تَحفظ نفس المعنى والقوّة الحماية التي في النَصّ العربي، تَحافِظ على دور الوسيط، تَحافِظ على مَراجع الأنظمة السعودية بأسمائها الإنجليزية الرسمية، وتَستخدم الإنجليزية البريطانية (organisation, behaviour) لتَوافقها مع الأنظمة الرسمية». **التنفيذ**: **(١) LegalLayout.tsx**: استَخرج `language` من `useApp()`، dir صار dynamic (rtl/ltr)، draftNotice مُترجَم، "آخر تحديث:" ← "Last updated:"، زرّ الرجوع يَنعكس (→ أو ←) وaria-label يَتغيّر. غيّرت `Bullets` من `pr-6` إلى `ps-6` (padding-inline-start) لِيَعكس الـbullets تلقائياً في LTR. **(٢) About.tsx**: 5 أقسام بـ`isRTL ?` ternary — رسالتنا/ما يميّزنا/كيف تعمل/التزامنا القانوني/تواصل معنا، بترجمة احترافية. **(٣) Contact.tsx**: 6 أقسام مُترجَمة (الشكاوى، الإبلاغ، التسجيل كتاجر، DPO، FAQ، الجهات الرقابية بأسمائها الرسمية: Ministry of Commerce / Maroof Platform / SAMA / SDAIA). **(٤) Refund.tsx**: 9 أقسام كاملة بترجمة قانونية — صندوق المبدأ الجوهريّ مع تأكيد دور الوسيط في الجملتين الثلاث، استرداد ثمن المشتريات (Section 2)، إلغاء الحجز، نموذج Netflix-style («Pay monthly, cancel anytime, use until end of paid month»)، 6 حالات عدم استرداد، 4 استثناءات للاسترداد الكامل، مدد المعالجة، الحقوق النظامية، إقرار وقبول. حدّ المسؤولية: "SAR 300". **(٥) Privacy.tsx**: 13 قسم بـ«general noon-style» محفوظ بالإنجليزية — بدون أسماء مزوّدين تَشغيلية، بدون مدد ملزِمة («within the periods prescribed by law»)، Personal Data Protection Law (PDPL)، حقوق المستخدم، السرّية، نقل البيانات، Cookies، الأطفال، التواصل. **(٦) FAQ.tsx**: 3 أقسام بـ8+14+14=36 سؤال — قوائم QA منفصلة (howItWorksAR/EN، buyerFAQ_AR/EN، merchantFAQ_AR/EN)، الـaccordion يَستخدم details/summary بدون JS إضافي، يَدعم Ctrl+F، rotation icon ▸ يَتدوّر تلقائياً عند الفَتح. **(٧) Terms.tsx (الأكبر)**: 23 قسم كاملاً بالإنجليزية — التعريفات (10 تعاريف)، قبول الشروط مع التوقيع الإلكتروني، الأهلية (شروط عامة + إضافية للتاجر 7 بنود)، **Section 4** «TAKI is a technical intermediary platform» (إقرار جوهريّ في صندوق emerald) مع 8 إقرارات صريحة، آلية الحجز، الأسعار، **Section 7** المحتوى المحظور 25 بنداً بأسماء سعودية رسمية (Anti-Cyber Crime Law، Capital Market Authority، Saudi Food &amp; Drug Authority، Saudi Council of Engineers، National Centre for Wildlife، Palermo Protocol)، **Section 8** السلوكيّات المحظورة 16 بنداً، **Section 9** بنود الحماية الخاصّة (الاتجار بالبشر / الأطفال / النساء / المضايقة) مع رابط hrc.gov.sa، تَحمّل المسؤولية الكاملة، الحماية من الاحتيال (مسؤولية مشتركة)، نظام التقييمات، إخلاء المسؤولية مع الحدّ المُزدوج (SAR 300 / 12 months)، Indemnity إلى TAKI، الملكية الفكرية، صلاحيّات الإدارة (8 صلاحيات)، نظام البلاغات مع سرّية الإجراءات الداخلية كـ«operational secrets»، القوّة القاهرة، علاقة الأطراف (no partnership/agency/employment/joint venture)، مُجمل الاتفاق وعدم التنازل، تَعديل الشروط، **Section 22** القانون الحاكم: «governed exclusively by the laws of the Kingdom of Saudi Arabia» + «competent Saudi courts seated in Riyadh» + waiver of foreign jurisdiction + regulatory authorities path، التواصل. صندوق emerald النهائي بـ«binding electronic signature pursuant to the Saudi Electronic Transactions Law». **(٨) NotFound.tsx**: ترجمة كاملة — title، body، زرّ العودة للرئيسية، زرّ الرجوع، الروابط السفلية (About/Contact/Terms/Privacy). dir يَتبع اللغة. **معايير الترجمة المتّبعة**: (أ) الإنجليزية البريطانية: «organisation» / «behaviour» / «recognised» / «authorised». (ب) الأنظمة السعودية بأسمائها الإنجليزية الرسمية المعتمَدة: Personal Data Protection Law (PDPL)، Anti-Cyber Crime Law، Civil Transactions Law، E-Commerce Law، Capital Market Authority، Saudi Central Bank (SAMA)، Saudi Data &amp; AI Authority (SDAIA)، Ministry of Commerce (mc.gov.sa)، Maroof Platform، Human Rights Commission (hrc.gov.sa)، Saudi Food &amp; Drug Authority، Saudi Council of Engineers، Ministry of Human Resources &amp; Social Development. (ج) دور الوسيط مُؤكَّد 4 مرّات: Section 4 الجوهري + Section 6 السلوك + Section 10 المسؤولية + Refund banner. (د) حدّ المسؤولية SAR 300 / zero for free users. (هـ) Netflix-style: «Pay monthly, cancel anytime, use until end of paid month». (و) بدون اختراع بنود — ترجمة دقيقة فقط. CACHE_NAME → v11.9، typecheck ✅. |
| **v11.8** | **طلب ناصر**: «استخدم سياسة حراج كمرجع، خذ ما يَلزم وحوِّر الصياغة، أَدمج مركز الأمان داخل الأقسام الخمس الموجودة (لا تَضع تَبويباً مُنفصلاً)، احذف ما لا يَنطبق على TAKI (الوصفات الطبية المرخّصة وكلّ ما لا نَستخدمه)، أَضف زَخماً يَحفظ حقوقي، عَزِّز دور الوسيط مع التأكيد على أنّ البيع والشراء وسياسة الاسترجاع والاستبدال مَحصورة بين الطرفين وعَلى سياسة المحل، وأنّ الموقع لا يَتدخّل في طريقة الشراء، وعند ورود شكوى يَحقّ للموقع اتّخاذ ما يَراه مناسباً دون تَحديد مدّة». **التنفيذ**: **(١) Terms.tsx مُعاد كَتابته بالكامل** من 20 إلى **23 قسم**، مع إضافات مَدمجة بأسلوب جديد دون نسخ من حراج: (أ) **القسم 1 - التعريفات**: إضافة «المتجر الإلكتروني» و«موفّر الخدمة» كأسماء بديلة، توضيح «الطرف الأوّل / الطرف الثاني». (ب) **القسم 2 - قبول الشروط**: إضافة «تَعتبر تَعاميم وقرارات الإدارة وتَوجيهاتها مُلزِمة فور إيصالها عبر أيّ من قنوات المنصّة». (ج) **القسم 3 - الأهلية**: قسم فرعيّ مَوسَّع لشروط المتجر (7 بنود) — اسم لائق، منع تعدُّد الحسابات، منع البيع/التنازل، الالتزام بتحديث الجوّال، علامات تجارية مَملوكة، السجلّ التجاريّ والتراخيص النظامية. (د) **القسم 4 - الطبيعة القانونية**: تَعزيز صريح أنّ «سياسة الاسترجاع والاستبدال مَحصورة بين التاجر والمشتري، تَختلف من تاجر لآخر، TAKI ليست طرفاً، ولا تَضمنها، ولا تُنفِّذها». (هـ) **القسم 5 - آلية الحجز**: إضافة «التعامل في أماكن عامّة وآمنة، التحقّق من الهوية قبل أيّ إجراء، فحص البضاعة قبل استلامها، استخدام وسائل الدفع المعتمَدة». (و) **القسم 6 - الأسعار**: إضافة منع «الطُّعم والتَّبديل» (bait-and-switch) صراحةً. (ز) **القسم 7 - المحتوى المحظور**: قائمة موسَّعة 24 بنداً مَدمجة من حراج المُكيَّفة لـTAKI: التَّقسيط والمنتجات البنكية، التَسويق الشبكيّ، الأسلحة بأنواعها، أجهزة التَجسّس/التَنصّت/التَشويش، الأوراق المالية (أسهم/سندات/صكوك/عملات رقمية)، السلع المُقلَّدة، التَبغ والخمر والمخدّرات، الأدوية الموصوفة، المنتجات الجنسية، الكائنات الحية، المواد الكيميائية، التَسوّل، غسل الأموال، السلع التي تَتطلّب تَرخيصاً نظامياً دون حِيازته، أنشطة الحياة الفطرية والمياه دون تَراخيص. **حُذفت من حراج**: السيارات (لا تَنطبق على TAKI)، العقارات (لا تَنطبق)، الوصفات الطبية المرخّصة (لا تَستخدمها TAKI). (ح) **القسم 8 - السلوكيّات المحظورة**: 16 بنداً مَدمجاً (anti-circumvention، التَلاعب بالأسعار، خفض تَقييم المنصّة، انتحال شخصية TAKI، نَسخ الإعلانات لمواقع أخرى، الـbots دون إذن باستثناء عناكب البحث المعروفة، السبام). (ط) **القسم 9 الجديد - بنود الحماية الخاصّة (Safety Center مُدمج)**: 4 بنود في قسم واحد بَدلاً من 4 صفحات مُنفصلة: **الاتجار بالبشر** (مع رابط hrc.gov.sa وذِكر بروتوكول باليرمو)، **حماية الأطفال** (مع روابط hrsd.gov.sa و hrc.gov.sa)، **حماية النساء والفتيات** (مع أدوات الحظر والإبلاغ)، **الحماية من المضايقة والعنصرية**. (ي) **القسم 10 - تَحمّل المسؤولية**: 6 بنود، مع تَفويض صريح لـTAKI بحفظ البيانات والاطّلاع عليها عند اللزوم. (ك) **القسم 11 الجديد - الحماية من الاحتيال (Fraud Protection مُدمج)**: 8 إرشادات وقاية + 4 خطوات عند الاشتباه، مع تَأكيد «TAKI ليست طرفاً، لكنّها تَتعاون مع الجهات الأمنية وتُقدّم البيانات عند ورود طلب رسميّ، وتُوقف الحسابات المُتورّطة». (ل) **القسم 12 الجديد - نظام التقييمات**: 5 بنود من حراج المُكيَّفة («التَقييمات تَعبير عن آراء مُرسليها، TAKI لا تَتحمّل مسؤوليتها، ولا تَلتزم بحذف أو تَعديل أيّ تَقييم»). (م) **القسم 13 - إخلاء المسؤولية**: مع إبقاء حدّ المسؤولية المُزدوج (12 شهر أو 300 ر.س). (ن) **القسم 16 - صلاحيّات الإدارة**: مع إضافة «إدراج رقم جوّال المُخالف في قائمة الأرقام المحظورة دون أدنى مسؤولية» و«مراقبة الرسائل الخاصة عند الحاجة». (س) **القسم 17 - البلاغات**: تَأكيد صَريح «دون التزام بمدّة زمنية محدَّدة، دون شرح أسباب». (ع) **القسم 22 - الاختصاص**: إضافة هيئة حقوق الإنسان للجهات الرقابية. **(٢) FAQ.tsx مُحدَّث**: (أ) سؤال «هل بياناتي محميّة» — إزالة ذكر TLS صراحةً → «إجراءات وقائية فنّية وإدارية معقولة تجارياً». (ب) سؤال جديد عن آلية البلاغات يُؤكّد سرّية الآليّات الداخلية. (ج) **3 أسئلة جديدة للمشتري حول الحماية**: «كيف أَحمي نفسي من الاحتيال» (6 إرشادات)، «تَعرّضت لاحتيال» (4 خطوات + بيان موقف TAKI)، «تَعرّضت لمُضايقة» (4 خطوات مع رابط hrc.gov.sa). (د) **2 أسئلة جديدة للتاجر**: «تَعرّضت لمُشتري احتياليّ» (4 خطوات)، «هل التاجر مسؤول عن سياسة الاسترجاع» (تَأكيد كامل أنّها بينه وبين المشتري وأنّ TAKI ليست طرفاً). (هـ) سؤال المنتجات المحظورة موسَّع ليَشمل التَسويق الشبكيّ والأوراق المالية وأجهزة التَجسّس. **(٣) Refund.tsx مُحدَّث**: صندوق المبدأ الجوهريّ يَحوي 3 جُمَل بَدلاً من 2: «TAKI لا تَتدخّل في طريقة الشراء ولا في آلية البيع» + «سياسة الاسترجاع والاستبدال مَحصورة بينهما وفق سياسة المتجر المُعلَنة» + «عند ورود شكوى يَحقّ للموقع اتّخاذ ما يَراه مناسباً دون التزام بمدّة محدَّدة ودون شرح أسباب». **ملاحظة عن دعم اللغة الإنجليزية**: ناصر طَلب أن تكون الصفحات بالإنجليزية عند تَبديل اللغة. هذا حجم ضخم (~5 صفحات قانونية كاملة + FAQ) ويَحتاج إصداراً مستقلاً للترجمة بدلاً من تَأخير الـdeploy الحاليّ. سيُتابَع في إصدار قادم. CACHE_NAME → v11.8، typecheck ✅. |
| **v11.7** | **طلب ناصر**: «بسّط سياسة الخصوصية مثل نون، احذف كل ما لا يحتاج توضيحه للعميل، خاصّةً أسماء المزوّدين التقنيين (Supabase / Vercel) والتفاصيل التقنية. لا تَلتزم بمدد محدّدة (30 يوم، 72 ساعة، 5 سنوات...) — أنا موظف واحد وآلاف المستخدمين، لو كل واحد طَلب بياناته يَصعب تَلبية كل طلب بسرعة. اجعل كل شيء عاماً. استخدم نموذج نون». **التنفيذ في Privacy.tsx (إعادة كتابة كاملة بأسلوب عام «نون-style»)**: **(١) حذفت أسماء المزوّدين**: لا Supabase ولا Vercel ولا أي اسم ملموس → استُبدلت بـ«مزوّدو الخدمات التشغيلية والتقنية» و«شركاء يُقدّمون خدمات استضافة، معالجة بيانات، تواصل، إشعارات، تحليلات، أو معالجة مدفوعات». **(٢) حذفت كل المدد الملزِمة**: «الرد خلال 30 يوم» → «ضمن المدد المنصوص عليها نظاماً»؛ «إخطار خرق خلال 72 ساعة» → حُذف الالتزام؛ «بيانات الحساب 3 سنوات / المعاملات 5 سنوات / IP 90 يوم» → استُبدلت بمعايير عامّة (الغرض/الحساسية/متطلبات الأنظمة). **(٣) حذفت التفاصيل التقنية الكاشفة**: لا TLS 1.2+، لا Row-Level Security، لا SECURITY DEFINER، لا CSP، لا «تشفير قاعدة البيانات في حالة التخزين» → استُبدلت بـ«تَشفير في النقل والتخزين، وضوابط وصول صارمة لا تُتيح البيانات إلا لمن يَلزمهم الاطّلاع عليها». **(٤) حذفت SCC وذكر الاتحاد الأوروبي / الولايات المتحدة** من قسم نقل البيانات → استُبدل بصياغة عامّة «قد يَستلزم تقديم الخدمات معالجة بعض البيانات لدى شركاء قد تَكون مَواقعهم خارج المملكة، وفقاً لأحكام PDPL». **(٥) حذفت الأساس النظامي بفقراته الأربع المفصّلة** (تنفيذ العقد / الالتزام النظامي / المصلحة المشروعة / الموافقة) → استُبدل بدمج عام في الأغراض. **(٦) حقوق PDPL عُمِّمت**: حذف «الإفصاح خلال 30 يوم» و«30 يوم إضافي عند الاستثناء»؛ أُضيفت صياغة حماية «قد يَتعذّر الاستجابة في حالات يُحدّدها النظام كالطلبات المُتكرّرة أو غير المعقولة أو المُتعارضة مع التزامات قانونية أخرى». **(٧) حذف Cookies الاستخدامات التفصيلية** (لا «اللغة، الوضع الليلي» صراحةً) → عُمِّمت إلى «تشغيل الجلسة وحفظ تَفضيلاتك وحماية المنصّة». **(٨) حذف القرارات الآلية كقسم مستقل** — التفصيل غير مطلوب نظاماً ويُقيّد المنصّة. **(٩) حذف قسم DPO المستقل** — دُمج في «التواصل» العامّ. **(١٠) تَسلسل الأقسام صار 13 بدلاً من 14**: نطاق → البيانات التي نَجمعها → كيف نَجمع → أغراض → مع من نُشارك → الحقوق → مدّة الاحتفاظ → نقل البيانات → Cookies → أمن البيانات → الأطفال → تعديل السياسة → التواصل. الصياغة قانونية فصيحة لكن بلا التزامات قابلة للقياس قانونياً تَكشف TAKI لمخالفات SLA. CACHE_NAME → v11.7، typecheck ✅. |
| **v11.6** | **بلاغان من ناصر مع لقطتي شاشة**. **(١) المستخدم يفتح TAKI أحياناً فيرى صفحة NotFound («الصفحة غير موجودة /index.html»)**: السبب — `manifest.webmanifest` كان فيه `"start_url": "./index.html"`، و`vercel.json` rewrites قاعدتها `"/((?!.*\\.[a-zA-Z0-9]+$).*)"` تَستثني أي مسار ينتهي بـextension. وعليه كل ما يفتح المستخدم الـPWA من شاشته الرئيسية على iPhone/Android يطلب `/index.html`، Vercel يسلّم الملف الفعلي بدون إعادة كتابة، React Router يَرى المسار `/index.html` كـURL لا يُطابق أيّ Route فيرسم NotFound. **الإصلاح**: (أ) `manifest.webmanifest` ← `"start_url": "./?source=pwa"` (الـ`?source=pwa` يتطابق مع `id` الموجود ويُستخدم في التحليلات للتمييز بين تشغيل من PWA المثبّت vs المتصفّح)، (ب) `App.tsx` يضيف `<Route path="/index.html"><Redirect to="/" /></Route>` قبل NotFound كحماية للمستخدمين الذين سبق وأضافوا الـPWA لشاشتهم الرئيسية بالـmanifest القديم — متصفّحاتهم تحتفظ بالقيمة المخزّنة حتى التحديث، فهذا الـRedirect يَلتقطهم. **(٢) طلب ناصر إعادة هيكلة الـSidebar (☰)**: «أزل الأسئلة الشائعة + مربع الصفحات القانونية من البار، انقلهما لصفحة «حسابي» (Profile)، ضع المربع تحت اللغة والتفضيلات وقبل إدارة الحساب». **التنفيذ**: (أ) Sidebar.tsx — حذف عنصر `{ id: 'faq' }` من menuItems وحذف مربع «الصفحات القانونية» بأكمله من قسم الإعدادات → الـSidebar رجع لشكله الأساسي قبل v11.4 (نظيف وبسيط: رئيسية / مفضلة / حجوزاتي / حولي / شكاوى). (ب) Profile.tsx — إدراج بطاقة جديدة «المساعدة والصفحات القانونية» / «Help & Legal» بين «اللغة والتفضيلات» و«إدارة الحساب» تماماً (الموقع اللي حدّده ناصر بالدائرة الحمراء)، نفس styling البطاقات الأخرى (`var(--card-bg)` + `1px solid var(--border-color)` + `padding 20` + `borderRadius 20`)، h3 بنفس وزن العنوان (1rem, fontWeight 900, marginBottom 15)، 6 أزرار rows (`padding 14×14`، `borderRadius 12`، `1.5px border`، `fontSize 0.92rem`، `fontWeight 800`، gap 8 بين الأزرار): ❓ الأسئلة الشائعة، ℹ️ من نحن، 📄 شروط الاستخدام، 🔒 سياسة الخصوصية، 💳 سياسة الاسترداد، ✉️ اتصل بنا. كل زر مع شيفرون `‹/›` على الطرف المقابل (RTL-aware) وhover state ينير الخلفية بـvar(--gray-50). التنقل عبر `history.push(item.path)` — `useHistory` كان مستورد أصلاً في Profile. CACHE_NAME → v11.6، typecheck ✅. |
| **v11.5** | **طلب ناصر**: «أبقي الأسئلة الشائعة فقط في القائمة الرئيسية، ضع بقية الروابط (من نحن / شروط / خصوصية / استرداد / اتصل بنا) في الإعدادات، تحديداً مربع تحت اللغة والوضع الليلي». **التنفيذ**: **(١) menuItems الرئيسية**: أُضيف عنصر `{ id: 'faq', icon: '❓', ar: 'الأسئلة الشائعة', en: 'FAQ', path: '/faq' }` بعد «حولي» مباشرةً — يظهر للجميع (مُسجَّل/زائر، بائع/مشتري/أدمن). **(٢) حذف قسم «حول TAKI»** بأكمله من تحت الـnav الرئيسية (كان فيه 6 روابط بعد borderTop). **(٣) مربع «الصفحات القانونية» جديد**: بطاقة compact داخل قسم الإعدادات بعد زر Dark Mode مباشرةً وقبل admin preview، بحدود `1.5px solid border-color`، خلفية `card-bg`، padding 8، borderRadius 12. عنوان البطاقة `'الصفحات القانونية' / 'LEGAL PAGES'` بـ uppercase letter-spacing 0.5. داخلها 5 أزرار بـ rows compact (padding 9×8، fontSize 0.82rem، أيقونات 0.95rem في عرض ثابت 22): ℹ️ من نحن، 📄 شروط الاستخدام، 🔒 سياسة الخصوصية، 💳 سياسة الاسترداد، ✉️ اتصل بنا. كل زر يُغلق الـSidebar وينتقل عبر `handleNav`. **المنطق**: الأسئلة الشائعة تبقى في الـnav لأنّها أكثر زيارة يومياً، والوثائق التعاقدية تنزل لمربع الإعدادات لأنّها تُفتح نادراً لكن يجب أن تبقى متاحة بنقرة لأغراض الامتثال. CACHE_NAME → v11.5، typecheck ✅. |
| **v11.4** | **طلب ناصر شامل عن الصفحات القانونية**: أعطاني نصّ شامل من موقع نون رسمي وطلب: «استخدم ما ينفع منه لموقعي بدون نسخ ولصق، كمحامي سعودي إلكتروني ذكي، بمراجعة القانون السعودي، أبقي الخمس صفحات (لا أنشئ صفحات جديدة)، التأكيد أن الموقع مجرّد وسيط، حماية كاملة للموقع من المشاكل الحالية والمستقبلية، وضع FAQ في القائمة المنسدلة (الثلاث شرطات) عن الخدمة وعمليات الحجز للمشتري والتاجر». **التنفيذ الكامل بأسلوب محامي سعودي**: **(١) Terms.tsx معاد كتابته بالكامل** من 15 قسم إلى **20 قسم**: تعريفات موسّعة (11 تعريفاً)، قبول شروط بصياغة المادتين 7-8 من نظام التعاملات الإلكترونية، أهلية مفصّلة (أ. عامة، ب. تاجر مع شروط السجل التجاري والتفويض الرسمي)، **قسم جوهري جديد «الطبيعة القانونية للمنصّة — TAKI وسيط رقميّ فقط»** ضمن صندوق emerald مع 8 إقرارات صريحة (لا تبيع، لا تشتري، لا تخزّن، لا تشحن، لا تضمن، لا تستلم ثمن، الحجز التزام مبدئيّ لا دفع، التواصل تيسير تقنيّ فقط، لا شراكة/وكالة)، آلية الحجز والاستلام، الأسعار والفواتير الضريبية، 17 محظوراً مكتمَلاً (الشريعة، الذوق العام، الأمن الوطني، المقامرة، الأوراق المالية، الأسلحة، المخدرات، السلع المقلَّدة، إلخ.)، 14 سلوكاً محظوراً موسّعاً (anti-circumvention، التلاعب بالأسعار، خفض تقييم المنصّة، إلخ.)، تحمّل المسؤولية الكاملة 5 بنود، إخلاء مسؤولية شامل بـ11 فئة + **حدّ مسؤولية مزدوج**: «أقلّ قيمة من إجمالي 12 شهر مدفوع أو 300 ر.س» + صفر للمستخدمين المجانيين، **قسم تعويض TAKI Indemnity** مستقل 5 بنود، حقوق الملكية الفكرية موسّعة، صلاحيّات الإدارة، نظام البلاغات مع سرّية الإجراءات الداخلية، **القوّة القاهرة** (قسم جديد كامل)، **علاقة الأطراف** (إقرار صريح بعدم وجود شراكة/وكالة/مشروع مشترك)، **مجمل الاتفاق وعدم التنازل** (5 بنود)، تعديل الشروط، **قانون حاكم سعودي حصري** + اختصاص الرياض + تنازل عن المحاكم الأجنبية + حقّ اللجوء للجهات الرقابية. **مراجع نظامية صريحة**: التجارة الإلكترونية م/126-1440هـ، مكافحة جرائم المعلوماتية م/17-1428هـ، PDPL م/19-1443هـ، المعاملات المدنية م/191-1444هـ، مكافحة غسل الأموال م/20-1439هـ، التعاملات الإلكترونية م/18-1428هـ. **(٢) Privacy.tsx معاد كتابته** من 11 قسم إلى **14 قسم** مع تعمّق PDPL: نطاق صريح، تصنيف بيانات بثلاث طبقات (مباشرة/تلقائية/طرف ثالث)، ما لا نجمعه أبداً (PCI + بيومتري + صحّي + جنسية + دين)، **الأساس النظامي للمعالجة** (تنفيذ العقد / الالتزام النظامي / المصلحة المشروعة / الموافقة) كقسم مستقل، أغراض الجمع 8 أغراض، مع من نشارك 7 جهات (التاجر يَرى الاسم+الجوّال+عدد الحجز فقط — لا البريد ولا البيانات المالية)، حقوق PDPL الـ8 (علم، وصول، تصحيح، إتلاف، سحب موافقة، نقل، اعتراض، شكوى لـSDAIA) مع 30 يوم استجابة، مدد احتفاظ مفصّلة لكلّ نوع، نقل بيانات خارج المملكة بـSCC، Cookies ضرورية فقط بدون تتبّع إعلاني، تدابير أمنية تفصيلية مع التزام إخطار 72 ساعة، حماية الأطفال، قرارات آلية لا ملزمة قانونياً، تعديل السياسة، DPO. **(٣) Refund.tsx معاد كتابته** بتقسيم واضح: **مبدأ جوهريّ في صندوق emerald** أن TAKI لا تستلم ثمن مشتريات وأنّ الاسترداد بين المشتري والتاجر مباشرة، **قسم كامل «استرداد ثمن المشتريات — سياسة التاجر هي الحاكمة»** (كما طلب ناصر: «كل محل له سياسة استرجاع مختلفة») يوضّح أنّ الاسترداد ليس على عاتق TAKI، الفئات غير القابلة للاسترداد طبيعياً (غذاء طازج، عناية شخصية، خدمات مستهلكة)، آلية حلّ الخلاف عبر «📣 الشكاوى» ثم الجهات الرقابية، إلغاء الحجز للمشتري بدون رسوم، نموذج Netflix-style للتاجر، 6 حالات عدم استرداد، 4 حالات استثنائية للاسترداد الكامل (خطأ تقني، سحب بدون موافقة، عطل 7 أيام، إنهاء بلا مخالفة)، مدد المعالجة، 4 جهات رقابية بروابطها. **(٤) About.tsx مُحسَّن** برسالة أكثر مهنية + أقسام «كيف تعمل المنصّة باختصار» و«التزامنا القانوني والأمني». **(٥) Contact.tsx مُحسَّن** بإحالة لـFAQ + ربط مسؤول حماية البيانات (DPO) مع 30 يوم استجابة. **(٦) FAQ.tsx صفحة جديدة كاملة** بنموذج accordion (details/summary بدون JS إضافي، يدعم Ctrl+F للبحث، RTL، يدعم قارئات الشاشة): **3 أقسام رئيسية** — 🔄 «كيف تتم الخدمة» (7 أسئلة: ما هي TAKI، هل تبيع بنفسها، الفرق عن المتاجر التقليدية، كيف تكسب، التغطية الجغرافية، حماية البيانات، آلية حلّ الخلاف)، 🛒 «للمشتري» (12 سؤالاً: مجانية، آلية الحجز خطوة بخطوة، لا سحب من البطاقة، إلغاء الحجز، عدم الحضور، رؤية التاجر للجوّال، الاسترداد عبر التاجر، التقييمات، البلاغات، التنبيهات الذكية، حذف الحساب)، 🏪 «للتاجر» (12 سؤالاً: التسجيل خطوة بخطوة، الاشتراك، إضافة عرض، استقبال الحجوزات، تنفيذ الحجز عند الحضور، تخلّف المشتري، الفروع المتعدّدة، الردّ على التقييمات، المنتجات المحظورة، عدم استلام TAKI للأموال، المسؤوليات النظامية، إغلاق الحساب). كل سؤال جوابه مفصّل بـ2-5 جمل مع روابط داخلية للوثائق القانونية والأنظمة الرسمية. صندوق emerald في الأسفل لمن لم يجد جواباً. **(٧) Sidebar.tsx (القائمة المنسدلة / الثلاث شرطات)**: قسم جديد «حول TAKI» تحت الـnav الأساسية بفاصل border-top، يحتوي 6 روابط بأيقونات: ❓ الأسئلة الشائعة / ℹ️ من نحن / 📄 شروط الاستخدام / 🔒 سياسة الخصوصية / 💳 سياسة الاسترداد / ✉️ اتصل بنا. **(٨) App.tsx**: `lazy import` للـFAQ + Route `/faq`. **(٩) sitemap.xml**: أُضيف `/faq` بـpriority 0.5 و changefreq monthly. **التركيز التشريعي**: كلّ بند تحت مرجع نظامي صريح (المرسوم الملكي ورقمه)، لا هلوسة، لا اقتباس من نون، صياغة عربية فصيحة قانونية، تشديد مكرّر على «TAKI وسيط فقط» في 4 أماكن منفصلة (Terms قسم 4 + Refund المبدأ الجوهري + About + FAQ) لضمان عدم التباس. typecheck ✅. |
| **v11.3** | **ملاحظة دقيقة من ناصر**: «ليس شرّاً أن يعرف الناس طريقة حلّي للمواضيع» — قسم البلاغات في Terms كان يكشف **آليات الكشف الداخلية**: «تُراجَع البلاغات خلال 48 ساعة» (SLA معلَن) و«تجميع 3 بلاغات من مُبلِّغين مختلفين خلال 14 يوماً يضع الحساب تحت المراجعة تلقائياً» (خوارزمية الرصد). هذا يعطي المسيئين خارطة طريق للإفلات: «أنا وأصدقائي نحطّ بلاغين فقط، لن يفعل النظام شيئاً». أو «لو حذفوا حسابي بعد 50 ساعة سأرفع دعوى تأخّر». **الإصلاح**: استبدال الصياغة في **Terms قسم 10** بـ: «تُراجَع البلاغات في أقرب وقت ممكن وفقاً لتقدير الإدارة، للإدارة وحدها سلطة تقديرية مطلقة لاتخاذ أيّ إجراء (تحذير/تعليق/حذف/تجميد/تحت المراجعة) دون إشعار مسبق أو شرح، والآليات الفنية للكشف والعتبات والخوارزميات تُعتبر **أسراراً تشغيلية** لا يحقّ لأيّ مستخدم الاطّلاع عليها». **Contact.tsx**: حذف «نلتزم بالرد خلال 48 ساعة عمل» (SLA معلَن قد لا نلتزم به) → «نسعى للردّ في أقرب وقت ممكن وفقاً لطبيعة الطلب». **ما تُرك بقصد**: الـ14 يوم في Refund.tsx (فترة المطالبة بالاسترداد + معالجتها) — هذه **حقوق المستخدم** القانونية، إخفاؤها يضرّ المستخدم لا TAKI. النتيجة: الـbusiness rules صارت سرّية، حقوق المستخدم بقيت شفّافة. typecheck ✅. |
| **v11.2** | **طلب ناصر: «أضف الشروط في التسجيل + علامة صح يقرأ ويوافق + إخلاء مسؤولية كامل من كل شي + شروط الاسترداد + بأقوى صيغة قانونية ممكنة»**. **سياسة الاسترداد المختارة من ناصر**: «بعد الدفع لا استرداد، يلغي الاشتراك متى يبي، يبقى الاشتراك مفعّل شهر من تاريخه». **التنفيذ الكامل**: **(١) Migration `v11_2_user_consent_tracking`**: أعمدة جديدة على `users` لتتبّع الموافقة (`consent_terms_at`، `consent_terms_version`، `consent_privacy_at`، `consent_refund_at`، `consent_ip`، `consent_user_agent`) مع COMMENTs توضّح أنّ كلّ عمود دليل قانوني للحوار في النزاعات. **`record_user_consent(p_terms_version, p_ip, p_user_agent)` RPC**: SECURITY DEFINER، يكتب الـtimestamp فقط لو الإصدار جديد (لا يدوس على الموافقة الأصلية)، يقصّ user-agent لـ250 حرف، GRANTed لـauthenticated. **(٢) Terms.tsx مُعاد كتابتها بأقوى صياغة قانونية**: 15 قسم، **قسم 6** «المحتوى المحظور» يفصّل 14 فئة جرائم (إساءة دين، إباحية، عنف وكراهية، مخدرات، أسلحة، احتيال، **غسل أموال** مع ربط بالمركز الوطني للأمن السيبراني، انتحال شخصية، سلع مقلّدة، أدوية بوصفة، استغلال قاصرين، سبام، اختراق المنصّة). **قسم 7** «تحمّل المستخدم للمسؤولية الكاملة» يصرّح: «المستخدم وحده مسؤول جنائياً ومدنياً عن كل محتوى وأيّ معاملة، يُعفي TAKI ومالكها وموظفيها من أيّ مطالبة من طرف ثالث، يتنازل عن حقّ رفع دعوى ضد المنصّة بسبب فعل مستخدم آخر، يدفع كل تكاليف TAKI القانونية لو تكبّدتها بسببه». **قسم 8** «إخلاء المسؤولية الكامل» يستثني TAKI من 9 فئات (المعاملات بين الأطراف، محتوى المستخدمين، الأخطاء التقنية، الأضرار غير المباشرة، أفعال غير قانونية، محتوى محظور، البلاغات، الاختراق، محتوى الطرف الثالث) + **حدّ المسؤولية = ما دفعه المستخدم لـTAKI خلال 12 شهر = صفر للمستخدمين المجانيين**. **قسم 9** «حقّ TAKI في الإجراءات الفورية» (حذف فوري، تعليق، تجميد رصيد، تسليم بيانات للجهات الأمنية بدون إذن، تعاون كامل في تحقيقات نظام مكافحة جرائم المعلوماتية). القانون الحاكم: السعودي، اختصاص محاكم الرياض. **(٣) Refund.tsx محدّث بنموذج «Netflix-style»**: لا استرداد بعد الدفع → إلغاء في أي وقت → الاشتراك يبقى نافذاً لنهاية الشهر المدفوع → لا تجديد تلقائي. صندوق تحذير بارز في الأعلى، 8 أقسام، استثناءات الاسترداد (خطأ تقني، سحب بدون موافقة، عطل 7 أيام، إلغاء حساب التاجر بدون مخالفة)، إجراء المطالبة 14 يوم، روابط جهات الرقابة (mc.gov.sa + maroof.sa + sama.gov.sa). **(٤) Register.tsx محدّث**: state `acceptedLegal` + `TERMS_VERSION='2026-05-20'`، صندوق checkbox أنيق قبل زر الإرسال (RTL، gradient أخضر عند التفعيل) يحتوي 3 روابط للصفحات القانونية تفتح في tab جديد، نصّ «أُقرّ بأنني قرأت ووافقت على شروط الاستخدام وسياسة الخصوصية وسياسة الاسترداد، وأتعهّد بتحمّل المسؤولية الكاملة عن أي محتوى أنشره»، ملاحظة قانونية «موافقتك توقيع إلكتروني ملزم وفقاً لنظام التعاملات الإلكترونية السعودي المادة 7». **`isSubmitDisabled` يفعّل عند `!acceptedLegal`** = الزر معطّل حتى الموافقة. **بعد verifyOtp ناجح**، استدعاء `record_user_consent` تلقائياً يسجّل التاريخ + user-agent (fire-and-forget، لا يحجب وصول المستخدم للوحة). أصول: 1 migration + 1 RPC + 2 صفحة قانونية مُعاد كتابتها + Register.tsx معدّل. typecheck ✅. |
| **v11.1** | **سؤال ناصر: «هل الاختبار فحص جميع الأزرار + ماذا ينقص للإطلاق الرسمي»**. التقييم الصادق: Health Check في v11.0 فحص infrastructure فقط (APIs/RPCs/PWA)، ليس UI buttons أو user flows. أُضيف **كل ما ينقص للإطلاق الرسمي حقيقةً**: **(١) 5 صفحات قانونية كاملة** تحت `/src/pages/legal/`: `Terms.tsx` (12 قسم: تعريفات + أهلية + آلية حجز + أسعار + محتوى + بلاغات + إلغاء + إنهاء + مسؤولية + تعديل + قانون حاكم سعودي + تواصل)، `Privacy.tsx` (11 قسم متوافق مع PDPL وSDAIA: بيانات + غرض + مشاركة + حقوق المستخدم تحت PDPL + احتفاظ + أمان + cookies + نقل دولي + أطفال + DPO)، `Refund.tsx` (7 أقسام: نوع المعاملة + إلغاء + استرداد + مدة + نزاع غير محلول)، `About.tsx` (من نحن)، `Contact.tsx` (قنوات التواصل مع روابط mc.gov.sa + maroof.sa + sdaia.gov.sa). كلها بـ`LegalLayout` مشترك مع draft-notice بنفسي «هذه مسوّدة، تحتاج مراجعة محامي». **(٢) صفحة 404 مخصصة** `NotFound.tsx` بدل الـredirect الصامت السابق — مهم للـSEO (Google كان يرى كل URL = 200 OK وهذا يربكه). **(٣) robots.txt** يحظر `/admin`, `/seller`, `/profile`, `/bookings`, `/notifications` (لا تظهر في Google) ويسمح بـ`/sw.js` و`manifest.webmanifest` (يحسّن PWA ranking). **(٤) sitemap.xml** فيه 10 routes مع `<changefreq>` و`<priority>` لكل واحد. **(٥) Open Graph + Twitter Card كاملة** في index.html — لما يُشارَك الرابط في واتساب/تويتر تظهر بطاقة جميلة بدل URL خام. **(٦) `keywords` + `canonical` + `author` meta** للـSEO. **(٧) Routes في App.tsx**: `/terms`, `/privacy`, `/refund`, `/about`, `/contact` كلها مرتبطة، وحذف `<Redirect from="*">` الصامت واستبداله بـ`<Route component={NotFound}>`. **(٨) `SmokeTestRunner`** قسم جديد في تاب الإطلاق: نقرة واحدة تفحص كل الـ14 route (HTML routes + sw.js + manifest + robots.txt + sitemap.xml) عبر fetch، تتحقق من HTTP status + content + زمن استجابة بالـms. **(٩) `LaunchChecklist` موسّع** من 18 إلى **38 بند** بأربع فئات: أمان (8 بنود)، قانوني (8)، تقني (15)، أعمال (7). الإحصاء: 18 جاهز ✓، 2 يحتاج عمل (دفع/Edge Function)، 18 يدوي (بنود تحتاج قرار من ناصر مثل: tester اختراق، تسجيل معروف، اختبار أجهزة حقيقية، خطة تسعير، إلخ). **(١٠) Build script** محدّث ينسخ `robots.txt + sitemap.xml` تلقائياً لـ`dist/` بعد Parcel build → الملفات تُخدم من الـroot على Vercel. أصول: 5 ملفات قانونية جديدة + 404 + 2 ملف SEO + 1 component جديد (SmokeTestRunner) + 6 ملفات معدّلة. typecheck ✅. |
| **v11.0** | **طلب ناصر شامل: «اضف كل غير الموجود + فعل خيار الدفع + تجربة لملايين المستخدمين بنقرة»**. **توقّفت بصراحة** قبل التنفيذ لأن النقطتين الأخيرتين فيهما خطر — بوابة الدفع حالياً [paymentService.ts](src/services/paymentService.ts) **محاكاة فقط** (لا تكامل فعلي مع Moyasar/PayTabs)، وأي toggle بدون تكامل = حجب التجار بدون قدرة دفع. اختار ناصر «اختر بوابة جاهزة (بدون تكامل فعلي)» + «Load Test حقيقي لمليون طلب». تم بناء **تاب «🚀 الإطلاق» كامل**. **Migration `v11_0_prelaunch_suite`**: (a) `admin_health_check()` — diagnostics شامل (عدد rows لكل الجداول، حالة الـtriggers الحرجة، حالة الـindexes، RLS، DB size)، (b) `admin_list_platform_settings()` — كل platform_settings للقراءة، (c) جدول `payment_attempts` جديد مع RLS صارمة (التاجر يشوف محاولاته فقط، الأدمن يشوف الكل، secret key لا يدخل DB أبداً)، (d) `admin_list_payment_attempts()` — سجل كل checkout مع status + gateway_reference + error_message، (e) `admin_payment_gateway_status()` — هل البوابة مهيأة؟ (يقرأ enabled + provider + has_publishable_key + webhook_url وأرقام نجح/فشل). Seeded 3 platform_settings: `payment_gateway_provider/publishable_key/webhook_url`. **مكوّن `PreLaunchSuite.tsx` (~870 سطر)** فيه 5 أقسام: **(1) 🩺 HealthCheckRunner** — نقرة واحدة → 22 فحص متوازي عبر Promise.all بكل الطبقات (Browser/PWA/DB/RPCs/Storage/Realtime) مع زمن كل فحص بالـms + تقرير ملوّن (✓ أخضر / ! أصفر / ✕ أحمر)، يكشف تلقائياً CACHE_NAME المتوقّع من sw.js. **(2) 💳 PaymentGatewaySetup** — اختيار البوابة من 4 خيارات (Moyasar موصى به للسعودية)، حقل password-style لـpublishable key مع **رفض تلقائي لأي مفتاح يبدأ بـ`sk_`** (تنبيه «لا تلصق المفتاح السرّي هنا أبداً»)، حفظ في `platform_settings`، عرض hint للمفتاح المحفوظ + webhook URL مع copy، **زر تفعيل البوابة محمي بـconfirm صارم** (يرفض التفعيل بدون provider+key+تنبيهات). **(3) 💸 PaymentAttemptsTable** — سجل آخر 30 يوم من محاولات الدفع. **(4) 📋 LaunchChecklist** — 18 بند ملوّن (جاهز/يحتاج عمل/يدوي) موزّعة 4 فئات (أمان/قانون/تقنية/أعمال) مع تقييم صادق لكل بند: RLS ✓، PWA ✓، Backup ✓، بوابة دفع ✕، Edge Function ✕، Terms ✕، Sentry ✕، Lighthouse ✕. **(5) ⚙️ TogglesAudit** — كل platform_settings للقراءة. **(6) 🚀 LoadTestGuide** — دليل 7 خطوات لـk6 مع سكربت كامل للـcopy جاهز + تحذير صريح «لا تشغّل على الإنتاج». **تكامل** : تاب جديد 🚀 الإطلاق في AdminDashboard، entry في CommandPalette، deep-link `/admin?tab=launch`. typecheck ✅. **ما لا يزال يحتاج قرار من ناصر**: مفاتيح API حقيقية + Edge Function للـwebhook + تسجيل تجاري لبوابة الدفع. |
| **v10.99** | **طلب ناصر: «المشاهدات اليومية بتاريخ يضعه أو شهري أو سنوي + جذب مستثمرين خارجيين بأعلى معايير الجودة العالمية»**. تم بناء **«ملف المستثمر» (Investor Pack) كامل** على معايير VC عالمية. **(١) 7 RPCs جديدة (migration `v10_99_investor_grade_analytics`)**: `admin_lookup_by_date(p_date)` — كل الأرقام لتاريخ يحدّده بدقّة + GMV ووفر العملاء، `admin_lookup_by_month(p_year, p_month)` — تفصيل يومي JSONB داخل الشهر، `admin_lookup_by_year(p_year)` — تفصيل شهري JSONB داخل السنة، `admin_investor_kpis(p_days)` — **22 مقياس استثماري في صف واحد**: GMV / GMV مكتمل / وفر العملاء / AOV / DAU/WAU/MAU / Stickiness % / Conversion % / Repeat Customer Rate % / MoM Growth (GMV/Bookings/New Users) / مشترون-جدد / تجار-جدد / تجار نشطون فعلياً، `admin_geographic_breakdown` — توزيع GMV على مدينة×منطقة (top 20)، `admin_retention_curve` — D1/D7/D30/D60 retention بالـcohort، `admin_gmv_monthly` — GMV شهري لـ12 شهر مع savings_delivered. **GMV** محسوب من `SUM(booked_quantity × deals.discounted_price)` على status IN ('completed','acknowledged') مع LEFT JOIN يعالج العروض المحذوفة. **(٢) مكوّن `InvestorPack.tsx` (~870 سطر)** يجمع كل شيء: **InvestorKpiHero** بـgradient غامق احترافي يعرض GMV/MAU/AOV كأهم 3 بطاقات + grid لـ8 مقاييس فرعية + MoM Growth chips ملوّنة (↑/↓/→)، **DateLookupCard** مع 3 tabs (📆 يوم / 🗓️ شهر / 📅 سنة) — اختيار تاريخ معين عبر `<input type=date>` أو dropdown للشهر/السنة، يعرض النتائج فوراً مع mini-charts (bars للحجوزات اليومية، GMV شهري داخل السنة)، **GmvMonthlyChart** لـ12 شهر مع gradient أخضر + overlay المكتمل/الإجمالي وtooltips، **RetentionCurveTable** بألوان متدرّجة (D1≥50% أخضر، 25-50 أخضر فاتح، 10-25 أصفر، <10 رمادي)، **GeographicTable** بشريط تقدّم يعرض حصّة GMV لكل مدينة من الإجمالي + تصدير CSV. **(٣) زر «📥 تنزيل ملف المستثمر»** ينزّل كل المقاييس الـ22 في CSV واحد جاهز للإرسال للمستثمرين باسم `taki-investor-pack-kpis-YYYY-MM-DD.csv`. **(٤) ترتيب AdminAnalytics**: ملف المستثمر في الأعلى (لجذب الاستثمار) → فاصل متقطّع → التحليلات التشغيلية الموسّعة v10.98 (للتشغيل اليومي) → المؤشرات اللحظية. كل الـcharts بـSVG inline بلا أي مكتبة → الحجم ثابت. typecheck ✅. |
| **v10.98** | **بلاغان من ناصر مع لقطة شاشة**. **(١) شريط التابات في AdminDashboard يصطدم بـnotch/الساعة/الشاحن في الـiPhone**: السبب — TabNav كان `sticky top-0 pt-2` بدون احتساب `env(safe-area-inset-top)`، فعلى iPhone الـDynamic Island يغطّي زر الرجوع والـ⌘K. **الإصلاح**: TabNav الآن `style={{ top: 'env(safe-area-inset-top, 0px)', paddingTop: 'calc(env(safe-area-inset-top, 0px) + 12px)' }}` — يلتصق أسفل الـnotch بالضبط مع padding مريح بصرياً، ولا يؤثر على شكله على ديسكتوب. **(٢) طلب «أقوى نظام تحليلات عالمي»**: تم بناء **12 RPC جديد** في DB (migration `v10_98_admin_world_class_analytics`) وكل واحد محمي بـ`is_admin()` SECURITY DEFINER: (a) `admin_mrr_monthly` — إيرادات شهرية مع zero-fill لـ12 شهر، (b) `admin_subscription_lifecycle` — توزيع الاشتراكات حسب status، (c) `admin_subscription_growth` — جدد vs ملغين شهرياً، (d) `admin_activity_heatmap` — شبكة 7×24 بتوقيت الرياض، (e) `admin_booking_funnel` — قمع view→book مع conversion %، (f) `admin_browse_no_book` — قائمة من شاهدوا ولم يحجزوا (للاستهداف)، (g) `admin_revenue_forecast` — متوقع شهري + ARPU + breakdown، (h) `admin_churned_subscribers` — قائمة استرجاع، (i) `admin_subscription_timeline` — جدول التواريخ والمبالغ، (j) `admin_user_cohorts` — استبقاء mom بالـcohort، (k) `admin_daily_metrics` — multi-series يومي، (l) `admin_category_funnel` — أداء التصنيفات. **مكوّن `AdvancedAnalytics.tsx` ضخم (~900 سطر)** يجمع كل التحليلات في تاب «التحليلات» مع: hero card للإيراد المتوقع، funnel بصري لقمع التحويل، multi-line chart يومي (events/bookings/new_users)، **خريطة حرارية 7×24 بألوان متدرّجة** تشير لأنشط أوقات اليوم، MRR bar chart 12 شهر مع ↑/↓ % vs شهر سابق، subscription growth chart مزدوج (جديد أخضر / ملغي أحمر)، **donut chart** للـlifecycle مع SVG arcs محسوبة بـcum-percent، **cohort retention table** مع شريط تقدّم ملوّن، subscription timeline مقسّم (7d/30d/منتهي)، churned list للاستهداف، browse-no-book مع نسخ سريع، category conversion مع شريط مزدوج. **PeriodSelector** (7/30/90 يوم) يحدّث كل التحليلات معاً عبر `Promise.all` (12 طلب موازي). كل الـcharts بـSVG inline بدون مكتبات → bundle خفيف. CSV export متاح على timeline + churned + browse-no-book. أصول: 1 migration + 12 RPC + 1 component جديد (900+ سطر) + 12 type-safe wrappers في adminService. typecheck ✅. |
| **v10.97** | **المرحلة 4 (الأخيرة) من إعادة تصميم لوحة الأدمن — "الصقل النهائي"**. (1) **drag-and-drop لإعادة ترتيب البانرات** في AdminTools باستخدام HTML5 native dnd (بدون مكتبات): مقبض السحب ⋮⋮ في الزاوية اليسرى العلوية لكل بانر مع `cursor: grab/grabbing`، شارة ترتيب `#1, #2, #3...` تظهر فوق كل بانر، عند الإفلات الترتيب يحفظ في `display_order` عبر `Promise.allSettled` (يدعم الفشل الجزئي). للمسات/الموبايل: أزرار ↑/↓ احتياطية بجانب كل بانر تنقله صف للأعلى/الأسفل. (2) **KPI Trend Snapshot system**: `useKpiSnapshot` hook يخزّن آخر snapshot يومي في `localStorage` (`taki:admin:kpi:YYYY-MM-DD`)، يقرأ snapshot الأمس للمقارنة، يحسب delta + percentage. **`TrendChip` component**: شارة صغيرة داخل كرت الـKPI تعرض `↑ 12% vs أمس` (أخضر للأعلى الجيد، أحمر للأسفل، رمادي للثابت). تظهر «—» إلى أن تكتمل أول 24 ساعة من البيانات. مطبّق على 3 KPIs في AdminOverview: حجوزات اليوم، مستخدمين جدد اليوم، MRR. التنفيذ client-only — لا تعديلات DB ولا RPCs جديدة، البيانات تتراكم تلقائياً بعد كل زيارة. (3) tooltip توضيحي على كل TrendChip يعرض الرقم الفعلي «اليوم: X · أمس: Y». **القرار التصميمي**: skip-ed Breadcrumbs (TabNav الموجود + ⌘K + Tooltips من المرحلة 1 يغطّون «أين أنا؟» بالفعل، الإضافة كانت ستكرّر معلومة)، و skip-ed Campaign Calendar (مؤجَّل للـbacklog — الحملات بقائمة مرتّبة بالأحدث تخدم المالك الحالي بشكل كافٍ، التقويم يستحق ميزة منفصلة). أصول: 2 ملف جديد (`useKpiSnapshot.ts` + `TrendChip.tsx`) + 2 ملف معدّل (AdminTools + AdminOverview)، 0 deps، 0 RPC. **مجموع المراحل 1-4**: 9 مكوّنات جديدة + 4 hooks + 4 utilities + 7 ملفات admin معدّلة + 4 إصدارات نُشرت v10.94→v10.97. typecheck ✅. |
| **v10.96** | **المرحلة 3 من إعادة تصميم لوحة الأدمن — "التحليلات والتقارير"**. (1) **utility `csvExport.ts`**: builder + downloader عام مع UTF-8 BOM إلزامي عشان Excel على ويندوز يفهم العربي بدون mojibake، يدعم `escapeCell` صحيح للقيم اللي فيها فاصلة/سطر/quote، يضيف stamp التاريخ تلقائياً للاسم. (2) **`ExportButton` component**: زر "📥 تصدير CSV {N}" مع 3 ألوان (emerald/blue/purple) متناغمة مع كل تاب، disabled تلقائياً عند 0 صفوف، يعرض ✓ مؤقت بعد التنزيل، tooltip يوضح أن التنزيل للقائمة المعروضة فقط (بعد البحث والفلاتر). (3) **تصدير من AdminBuyers**: 10 أعمدة (الاسم، الجوال، الإيميل، العنوان، عدد الحجوزات، إجمالي الصرف، معلّق، آخر نشاط، تاريخ التسجيل، المعرّف). (4) **تصدير من AdminSellers**: 12 عمود (المتجر، المالك، الباقة، تنتهي في، المبلغ الشهري، الخصم %، إلخ). (5) **تصدير أعلى البائعين/المشترين من AdminAnalytics**. (6) **سجل تعديلات الأدمن (Audit Log)**: toggle جديد «👑 تعديلات الأدمن فقط» داخل قسم النشاط في AdminAnalytics — يفلتر `getRecentActivity` على الأفعال اللي تبدأ بـ`admin_*`. خرائط labels + icons محدّثة لتغطية `admin_apply_subscription` + `admin_update_user` + `admin_soft_delete_user` + `admin_set_report_status` + `admin_set_complaint_status` + `admin_set_platform_setting` + `admin_bulk_subscription`. يدعم تصدير CSV للسجل (`taki-admin-audit-YYYY-MM-DD.csv`) — مفيد للأرشيف. (7) عرض `entity_type · entity_id` تحت كل سطر audit ليُسهّل التتبّع. أصول: 2 ملفات جديدة + 3 ملفات معدّلة، 0 RPC جديد. typecheck ✅. |
| **v10.95** | **المرحلة 2 من إعادة تصميم لوحة الأدمن — "القوة اليومية"**. الميزات اللي تحوّل اللوحة من أداة "أبحث وأعدّل" إلى أداة "أنجز شغلي اليومي بنقرة": **(1) `useAdminRecents`**: hook يخزّن آخر 8 مستخدمين فتحهم الأدمن في `localStorage` (مع cross-tab sync عبر `storage` event)، يُحدَّث تلقائياً كل ما فتح UserEditModal (buyers) أو SubscriptionModal (sellers). يظهر في الـCommandPalette تحت قسم "آخر ما فتحت 🕒" عندما يكون المدخل فارغاً → فتح ⌘K + Enter بدون كتابة شيء = الرجوع لآخر تاجر/مشتري عملت عليه. **(2) `useLocalStringList`**: hook عام لقوائم الـIDs المثبّتة في localStorage، يخدم نظام "المفضّلة" — تحدّ 50 مشتري و100 تاجر. **(3) `PinButton` (★)**: زر نجمة بجانب كل صف؛ التجار والمشترون المفضّلون يظهرون في قسم منفصل "★ المفضّلة" أعلى القائمة قبل بقية النتائج. **(4) فلاتر ذكية في AdminBuyers**: المفضّلة، جدد هذا الأسبوع، الأكثر صرفاً (ترتيب)، بدون حجوزات، معلّق. **(5) فلاتر ذكية في AdminSellers** (تتراكب مع فلتر الباقة الموجود): المفضّلة، **ينتهي خلال 7 أيام**، **ينتهي خلال 30 يوم** (الأكثر فائدة — يكشف التجار اللي على وشك تنتهي اشتراكاتهم)، بدون اشتراك، خصم ≥ 30%. **(6) Bulk operations في AdminBuyers**: زر «☑ تحديد متعدد» يفعّل وضع التحديد بصناديق ☑ لكل صف، شريط أزرق sticky يعرض العدد + «تحديد الكل ({n})» + «🚫 تعليق جماعي» + «✅ استرجاع جماعي»، تأكيد قبل التنفيذ + Promise.allSettled لمعالجة الفشل الجزئي مع تقرير نجح/فشل، الحذف الجماعي غير مُتاح عمداً (footgun لمالك غير تقني). أصول: 2 hook جديد + 1 component جديد + 4 ملفات معدّلة. typecheck ✅. |
| **v10.94** | **طلب ناصر شامل: «راجع كل لوحة الأدمن، اجعلها كأفضل لوحات الإدارة في العالم، مع مرونة في التنقل لأني جديد ولا أفقه شيئاً»**. المرحلة الأولى من إعادة التصميم — **التنقل والوضوح**. تمت إضافة 4 مكوّنات مشتركة جديدة في `src/components/admin/` + `src/hooks/`: **(1) `CommandPalette` (⌘K/Ctrl+K)**: spotlight احترافي يفتح من أي مكان داخل /admin، يبحث في الصفحات + الإجراءات السريعة + المستخدمين (بائعين+مشترين) في آن واحد عبر `adminService.searchUsers` بـdebounce 250ms، تنقّل بـ↑↓ وفتح بـEnter. الإجراءات السريعة («إضافة بانر»، «إنشاء حملة») تستخدم جسر `sessionStorage` (`taki:admin:quick_action`) ينقل النيّة للتاب الهدف ويُمسح فوراً بعد التنفيذ. **(2) `Tooltip`**: hover/focus/long-press مع تأخير 350ms، RTL-aware، يختفي عند Esc. مطبّق على شريط التبويبات وأزرار الحالة في AdminReports. **(3) `Breadcrumbs`**: مكوّن مسار "أنت الآن في..." جاهز للاستخدام (سيُربط في المرحلة 2). **(4) `CopyButton`**: نسخ بنقرة مع fallback لـSafari القديم وعرض ✓ مؤقت — مدمج في صفوف المشترين والبائعين وفي بطاقات البلاغات. **(5) `useEscClose` hook**: مستخدم في UserEditModal (مع تحذير «تغييرات غير محفوظة» إذا كان النموذج dirty)، SubscriptionModal، BannerModal، CampaignModal. **AdminDashboard v10.94**: زر رجوع كبير مع تسمية «رجوع» (بدلاً من `›` غامض)، زر بحث مع shortcut chip `⌘K`، badge أحمر على تاب البلاغات يعرض عدد المفتوحة (poll كل 60ث)، Tooltips على كل تاب توضح ما يحتويه. **AdminReports**: إعادة كتابة كاملة من inline styles القديمة إلى Tailwind — بطاقات ملخّص في الأعلى (مفتوح/قيد المراجعة/تم الحل/الإجمالي) مع pulse أحمر، فلاتر pill chips بدلاً من selects، أزرار «المُبلِّغ» و«المُبلَّغ ضده» قابلة للنقر تنتقل لصفحة المتجر أو لتاب المشترين فلتر-pre-applied، نسخ سريع لرقم الجوال، شارات حالة ملوّنة (🟠 مفتوح / 🔴 تحت المراجعة / ✅ تم الحل / ⛔ مرفوض)، empty states أنيقة. **AdminBuyers**: deep-link query عبر `?q=name` يربط CommandPalette بالبحث الفوري، تحذير «تغييرات غير محفوظة» عند الإغلاق إذا كان النموذج معدّلاً، زر «حفظ» يصبح disabled إذا لا تغييرات. **AdminSellers + AdminTools**: تكامل useEscClose + CopyButton + Tooltips. أصول: 5 ملفات جديدة + 6 صفحات معدّلة، 0 deps خارجية مضافة، 0 تغيير على RPC. typecheck ✅. |
| **v10.93** | **بلاغان من ناصر**. **(١) حقل إدخال الرمز اليدوي في سكانر الحجز غير واضح بالوضع الفاتح (التاجر)**: لا `background` صريح على `<input>` في `BarcodeScanner`، فآيفون **بنظام داكن** يرسم الحقل أسود عبر التحكم الأصلي بينما لون النص يتبع ثيم التطبيق الفاتح (كحلي غامق) → الكود يكتب غير مرئي. **الإصلاح**: خلفية داكنة صريحة `#0f172a` + نص أبيض + `WebkitTextFillColor:#fff` + `colorScheme:'dark'` + `caretColor:#fff` — واضح في الوضعين بمعزل عن مظهر النظام. **(٢) زر «🚩 إبلاغ» للمشتري على الحجز (مثل التاجر تماماً، مربوط بقاعدة البيانات)**: أُضيف زر إبلاغ على بطاقات الحجز في صفحة «حجوزاتي» (القسمين: نشطة + السجل) يفتح **نفس `ReportDialog`** الذي يستخدمه التاجر — نفس أنواع/أسباب البلاغ، يحفظ عبر `reportRepository.create` (المشتري→المتجر، `reportedRole='seller'`)، والـtrigger `tr_guard_report_insert` (v10.79) يختم الأدوار ويثبّت أنه بلاغ على هذا المتجر. `ReportDialog` يستخدم `createPortal` لـbody (لا يقع في فخّ transform الخاص بـPullToRefresh). typecheck ✅. |
| **v10.92** | **بلاغ ناصر (بسيط): زر «✅ تم الحجز — انتقل لحجوزاتي» ينقل لصفحة حجوزاتي بنجاح لكن يقف في نصّ الصفحة لا في أعلاها حيث الحجز الأحدث**. السبب: `DealDetails` يستدعي `history.push('/bookings')` (السطر 584 بعد الحجز + السطر 1207 من زر CTA) **بدون `?barcode=`**، وSPA يحتفظ بموضع تمرير الصفحة السابقة (DealDetails كانت مُمرَّرة للأسفل) فتفتح حجوزاتي في المنتصف. **الإصلاح**: `useEffect` عند تركيب صفحة `Bookings` يستدعي `window.scrollTo(0,0)` — إلا إذا كان الرابط يحمل `?barcode=` (عندها متتبّع التنبيه الموجود يتكفّل بالتمرير لذلك الحجز وتوسيطه، فلا نتعارض معه). الترتيب الافتراضي `newest` فالحجز الأحدث يظهر فوراً في الأعلى دون تمرير. تغيير موضعي صغير بلا أثر على بقية الصفحات. typecheck ✅. |
| **v10.91** | **«لا زال نفس الخلل» — السبب الجذري الفعلي للكاميرا (تكميل v10.90)**. v10.90 أصلح مسار الاستوديو فقط؛ لكن مسار **الكاميرا** كان فيه حلقة تغذية راجعة: `SellerDashboard` يمرّر `maxShots={4 − images.length − cropQueue.length}` لـ`CameraCapture`، و`CameraCapture` يحسب `cap = Math.max(1, maxShots)` **في كل render من القيمة الحيّة**. كل لقطة تُلتقط تستدعي `onCapture → uploadCroppedFile → setImages([...])` فيكبر `images.length` فيُعاد تمرير `maxShots` **أصغر**، فتصبح `count >= 4 − count` ⇒ **الكاميرا تتجمّد دائماً عند صورتين** (شرح الصورة: «تمت إضافة ٢ صور — اكتمل الحد الأقصى» بلا زر «المزيد»). **الإصلاح**: تجميد سعة الجلسة لحظة فتح الكاميرا — `const [cap] = useState(() => Math.max(1, maxShots))` (لقطة واحدة للمتبقّي عند الفتح، تتجاهل تقلّص الـprop أثناء الجلسة). جلسة فارغة الآن = ٤ لقطات كاملة؛ ولو في صور سابقة = المتبقّي الصحيح حتى مجموع ٤. `CameraCapture` يُركّب من جديد كل فتح (render شرطي `{showCamera && …}`) فاللقطة الأولية صحيحة دائماً. typecheck ✅. |
| **v10.90** | **بلاغ ناصر (محبط): إضافة صور المنتج — «الاستوديو يختار صورة واحدة فقط، وكان ٤ سابقاً، أبي ٤ مع بعض مثل واتساب»**. السبب الجذري (انحدار من v10.75): زر «الاستوديو» داخل `CameraCapture` يستدعي `onPickStudio` في `SellerDashboard` الذي كان يفتح معرض النظام عبر **`setTimeout(() => fileInputRef.current?.click(), 60)`** — تأخير ٦٠ms **يكسر user-activation في iOS Safari** فينهار اختيار الصور المتعدد إلى **صورة واحدة فقط** (الحقل أصلاً `multiple accept="image/*"` بلا `capture`، فلا قيد إلا كسر اللمسة). **الإصلاح**: استدعاء `fileInputRef.current?.click()` **متزامناً داخل نفس اللمسة** ثم `setShowCamera(false)` بعده (الحقل المخفي داخل الفورم وما زال مركّباً تحت طبقة الكاميرا، فالـref صالح؛ والكاميرا أوقفت بثّها قبل النداء). يستعيد سلوك v10.75: اختيار حتى ٤ صور دفعة واحدة من المعرض مثل واتساب، ثم تمرّ بطابور القص الموجود. (سعة الكاميرا اللحظية = `4 − الموجود` صحيحة لحد الـ٤ الإجمالي المتفق عليه؛ التذبذب ٢/١/٣ كان من صور سابقة في الفورم تستهلك الخانات، وليس خللاً في الحد.) typecheck ✅. |
| **v10.89** | **بلاغان من ناصر على شريط حالة الطلب (الموقع احترافي عالمي)**. **(١) «شريط أخضر زائد — المفروض عند الاستلام يتوقف»**: في `DealDetails.StatusTracker` كان شريط التقدّم `position:absolute` بعرض `${(currentIndex/(steps-1))*100}%` داخل صفّ فيه `padding:'0 10px'` ومثبّت بـ`left/right:30px` — فعند `completed` يصبح العرض 100% من عرض الحاوية كاملةً (وليس من مسار العُقد) فيتجاوز آخر عقدة ويبرز شريط أخضر زائد من الطرف (الذي حوّطه ناصر). **الإصلاح (حسابي دقيق)**: مراكز العُقد عند `(2i+1)/(2N)` من الصف؛ حُذف `padding:'0 10px'` وصار المسار والتعبئة بنِسَب مئوية مرتكزة على مركز أول/آخر عقدة (`edge = 100/(2N)%`، `span = 100−2·edge`، `fill = currentIndex/(N−1)·span`) — فالتعبئة تنتهي **بالضبط** على العقدة الحالية ولا تبرز أبداً، صحيحة في RTL وLTR. **(٢) «عند الإلغاء يتحوّل لأحمر مع X عند تم الاستلام — أفضل من علامة صح خضراء على مؤكد»**: حالة `cancelled` كانت تُعامَل كـindex=0 فتظهر ✓ خضراء على «مؤكد» (مضلِّل). **الإصلاح**: الإلغاء حالة منتهية → المسار يمتلئ كاملاً بلون **أحمر** وكل العُقد حمراء بعلامة **✕** (بدل ✓ الخضراء) في `StatusTracker`؛ ونفس المعالجة طُبِّقت على متتبّع صفحة **حجوزاتي ← سجل الطلبات السابقة** (`Bookings.tsx`، الذي يعرض الحجوزات الملغاة فعلياً بعد v10.88) — أحمر + ✕ على «مؤكد/استلمه التاجر/تم الاستلام» بدل ✓ خضراء على مؤكد فقط. مظهر احترافي ومتسق في المكانين. typecheck ✅. |
| **v10.88** | **٣ بلاغات من ناصر**. **(١) القائمة الجانبية ما زالت تتحرّك (v10.87 لم تكفِ) — «استخدم نفس قاعدة الإشعارات»**. السبب الجذري الحقيقي (ليس مستمعات اللمس): `Navbar` هو الذي يركّب `<Sidebar>`، وفي **الرئيسية وحجوزاتي** يُعرض `Navbar` **داخل `<PullToRefresh>`** الذي يلفّ أبناءه في `<div style="transform: translateY()">`. **أي `transform` غير `none` على عنصر أب يعيد ربط `position:fixed` بذلك الأب بدل نافذة العرض** — فالدِّرج «المثبّت» صار يتمرّر/يهتزّ مع الصفحة هناك، بينما **الإشعارات (بلا PullToRefresh) ثابتة تماماً**. **الإصلاح الجذري النهائي**: `Sidebar` صار يُعرض عبر **`createPortal` إلى `document.body`** — فيخرج من أي أب مُحوَّل ويصبح مثبّتاً على نافذة العرض في **كل الصفحات** تماماً كالإشعارات. **(٢) رسالة تجديد وهمية في المدخل الثاني**: التجديد له مكانان — «عروضي» (لوحة التاجر، صحيح منذ v10.78: يفحص نجاح `updateDeal` ويفتح نموذج التعديل ببانر إعادة اختيار الموقع عند الفشل) و**«صفحتي ← العروض السابقة» (`StoreDetails`)** الذي كان يعرض «✅ تم تجديد العرض بنجاح!» **بلا فحص** فيبقى العرض منتهياً فعلياً (نفس باق v10.78 لكن في المسار الثاني). **الإصلاح**: `StoreDetails.reActivateDeal` صار يفحص قيمة `updateDeal` المنطقية؛ عند الفشل **لا يدّعي النجاح** بل ينبّه «لم يعد موقعه ضمن مواقعك / وصلت حد المواقع — جدّده من لوحة التاجر» ويحوّله لـ`/seller` حيث يكتمل التجديد فعلياً. **(٣) حجز ملغى يظهر «حجز مؤكد» ويمنع إعادة الحجز**: تأكّدت من قاعدة البيانات — كل حجوزات المشتري على «عيادة اسنان» الستة **منتهية (ملغاة/مكتملة)** ولا يوجد أي حجز نشط؛ ومع ذلك صفحة العرض تعرض `EETTT2JT` (وهو **`cancelled`**) كـ«حجز مؤكد» بمؤقّت وهمي. السبب: `DealDetails.activeBooking` كان `bookings.find(...)` **بلا أي فلترة حالة/انتهاء** فيلتقط صفّاً قديماً ملغى (والـ`cancelled` يسقط في فرع «حجز مؤكد»). **الإصلاح**: `activeBooking` صار يستخدم **نفس قاعدة `useBooking`** — يستبعد `cancelled` ويستبعد pending/acknowledged المنتهي (سقف الساعتين الفعّال)، ويبقي `completed` فقط لإظهار الإيصال/التقييم، ويختار **الأحدث** عند تعدّد الصفوف. زر «احجز الآن» يعتمد أصلاً على `isBooked` (قاعدة صحيحة) فإعادة الحجز تعمل بعد انتهاء/إلغاء الحجز. (فلترة طلبات التاجر النشطة `activeOrders` صحيحة أصلاً 1680-1681؛ أي بقايا عند التاجر = حالة محلية قديمة عالجتها مزامنة v10.86 عند العودة للمقدمة.) typecheck ✅. |
| **v10.87** | **بلاغ ناصر: القائمة الجانبية (٣ شرطات) «تتحرك نزولاً وطلوعاً» في الرئيسية وحجوزاتي، بينما ثابتة في الإشعارات — أريد كل الصفحات مثل الإشعارات**. **السبب الجذري** (لا تخمين — تتبّع الفرق بين الصفحات): `PullToRefresh` (السحب للتحديث) يُركّب مستمعات لمس **على `document` عالمياً** (`touchstart/touchmove/touchend`)، وهي تبقى فعّالة **حتى لو كانت القائمة الجانبية مفتوحة فوق الصفحة**. مكوّن `PullToRefresh` يُستخدم في **الرئيسية + حجوزاتي فقط** (وليس الإشعارات — لذلك كانت ثابتة هناك بالضبط). فعند فتح الدِّرج وسحب الإصبع عليه: الصفحة في الأعلى (`atTop()` = true) فيبدأ التتبّع ويُطبَّق `transform: translateY()` على غلاف محتوى الصفحة **خلف** الدِّرج + يظهر مؤشّر السحب → تتحرّك الخلفية صعوداً/نزولاً ويهتزّ المنظر. **الإصلاح (جذري، بلا دين برمجي)**: `PullToRefresh` صار **خاملاً تماماً متى وُجد دِرج/مودال مفتوح** — دالة `blockedByModal()` تتحقّق من قفل تمرير الصفحة (`document.body.style.overflow === 'hidden'` الذي يضبطه `Sidebar` عند الفتح) أو وجود عنصر `[aria-modal="true"]` غير مخفي؛ تُفحص في `onTouchStart` (فلا يبدأ التتبّع أصلاً) ودفاعياً في `onTouchMove` (إن فُتح المودال أثناء لمسة). النتيجة: عند فتح ٣ الشرطات لا تتحرّك الخلفية ولا الدِّرج في أي صفحة — تماماً مثل صفحة الإشعارات كما طلب ناصر. السحب للتحديث يعمل طبيعياً عند إغلاق الدِّرج. typecheck ✅. |
| **v10.86** | **بلاغان من ناصر: تعليق ~٦ث عند إرسال رسالة المحادثة + توضيح/إتاحة إلغاء الطلب**. **(١) «الضغط على إرسال يعلّق ٦ ثوانٍ ثم يُرسل، وقبلها كان فورياً»** — تشخيص جذري عبر سجلات Supabase + الكود (لا تخمين): RPC `send_booking_message` يرجع 200 بسرعة خادمياً (لا مشكلة DB — pg_net غير مثبّت لكن `taki.push_url` فارغ فالـ`tr_notification_push` يخرج مبكراً؛ الـtriggers سليمة). الخلل في العميل: على iOS الـPWA المثبَّت، **لمس زر «إرسال» يُغلق لوحة المفاتيح فيُطلق حدث `focus` على النافذة**، و`realtimeService.handleFocus` كان يعتبر أي `focus` بعد ثانية «رجوعاً من الخلفية» فيستدعي `onRefreshAll()` (٥ استعلامات ثقيلة + ٥ `setState` ضخمة = إعادة بناء كامل بنفس حِمل ٦-٧ث الموصوف في v10.71) فيتجمّد الخيط الرئيسي حتى تظهر الرسالة المُرسَلة فعلاً. **الإصلاح (جذري، بلا دين برمجي، وبلا تراجع عن مزامنة iOS المثبّتة)**: تتبّع `lastHiddenAt` (يُضبط حصراً عند `visibilitychange→hidden` أو `pagehide`) + `cameBackFromHidden()`؛ `handleFocus`/`handleVisibilityChange` لا يطلقان المزامنة الثقيلة إلا إذا اختفت الصفحة فعلياً (رجوع حقيقي من الخلفية) — وإغلاق لوحة المفاتيح لا يُخفي الصفحة فلا تحدث العاصفة. نفس الحارس طُبِّق على `BookingThread` (كان يعيد جلب الرسائل + mark-read عند كل إرسال). الرجوع الحقيقي من الخلفية ما زال يزامن عبر visibilitychange/pageshow/pagehide. **(٢) «زر إلغاء مضلِّل + لا يمكن إلغاء الطلب عند كتابة الكود»** — (أ) في «سكانر الحجز» كان الزر «❌ إلغاء» فظنّه ناصر إلغاءً للسكانر بينما هو **إلغاء الطلب كاملاً** → صار «❌ إلغاء الطلب» مع **نافذة تأكيد** قبل التنفيذ («سيُلغى الطلب نهائياً وتُعاد الكمية للمخزون ويُبلَّغ المشتري»). (ب) كان الإلغاء متاحاً عبر السكانر فقط؛ أُضيف زر **«❌ إلغاء الطلب»** أسفل بطاقة الطلب في لوحة التاجر (لحالتَي pending/acknowledged) بنفس التأكيد — فيقدر التاجر يلغي الطلب حتى لو أدخل الكود يدوياً دون فتح السكانر. typecheck ✅. |
| **v10.85** | **القائمة الجانبية: تحويل التثبيت إلى `position: sticky` (أمتن نمط على iOS Safari)**. بعد تكرار بلاغ ناصر، حُسم الأمر بأكثر نمط مضمون: بدل تثبيت الهيدر عبر flex داخل `position:fixed` (قد يتعثّر في حساب الارتفاع على iOS)، صار الـ`<aside>` `overflowY:auto` (يتمرّر طبيعياً) و**الهيدر `position:sticky; top:0` بخلفية صلبة + zIndex** فيبقى ملتصقاً أعلى القائمة والعناصر تتمرّر **تحته** بنظافة (paddingBottom بدل marginBottom حتى لا يظهر شيء من خلف الفراغ). أُزيل غلاف التمرير الداخلي ورجعت `<nav>`/الفوتر عناصر مباشرة. قائمة قصيرة = تدفّق طبيعي بلا فجوة؛ قائمة طويلة = تمرّر والهيدر ثابت. typecheck ✅. **تذكير حاسم لناصر**: هذا مع v10.84 (الفحص عند كل عودة للمقدمة). إن بقي الشكل القديم فجهازك ما زال على بناء مخزَّن قديم — الحل النهائي **مرة واحدة**: احذف أيقونة التطبيق من الشاشة، افتح Safari على الرابط، ثم «إضافة إلى الشاشة الرئيسية» — بعدها كل تحديث يصل تلقائياً للأبد. |
| **v10.84** | **السبب الجذري الحقيقي لـ«أعيد الفتح ويبقى الشكل القديم» (٣ نشرات والمستخدم يرى نفس البناء بالضبط)**. تشخيص: كود القائمة صحيح ١٠٠٪ (v10.83: هيدر مثبّت خارج منطقة التمرير = النمط القياسي) — لكن **تعديلاتي لا تصل جهاز ناصر إطلاقاً**. الفحص: `autoCheckServerVersion()` في `sw-cleanup.ts` (يجلب sw.js الحيّ متجاوزاً الكاش ويقارن CACHE_NAME ويطبّق `applySwUpdate` عند الاختلاف) كان يعمل **فقط عند تحميل الصفحة (cold start)**؛ وعلى iOS الـPWA يُستأنف من الذاكرة/bfcache **بدون إعادة تنفيذ الموديول ولا navigation** — فلا يُفحص الإصدار أبداً مهما «أعاد الفتح»، ويبقى مجمّداً على بناء قديم للأبد. **الإصلاح**: إضافة `_lastVersionCheck` (خنق ١٥ث) + استدعاء `autoCheckServerVersion()` على **`visibilitychange` + `pageshow` + `focus`** — أي عند كل عودة للمقدمة يُفحص إصدار الخادم وإن تقدّم يُمسح الكاش ويُعاد التحميل تلقائياً، بلا حاجة لقتل التطبيق. هذا يجعل كل النشرات المستقبلية تصل iOS تلقائياً. typecheck ✅. **ملاحظة لناصر**: للخروج من البناء القديم العالق **مرة واحدة فقط**: أغلق التطبيق **كلياً** (اسحبه للأعلى وأزله من مبدّل التطبيقات) ثم افتحه — بعدها كل تحديث يصلك تلقائياً. |
| **v10.83** | **تثبيت القائمة الجانبية بشكل احترافي (بلاغ ناصر بصورتين: لا تزال تنزلق والهيدر يختفي)** — v10.82 ثبّت الهيدر والفوتر منفصلين مع `nav flex:1` فأنتج فجوة فارغة كبيرة في المنتصف مع قلة العناصر (شكل «مبتدئين»). **الإصلاح النهائي (نمط الـdrawer القياسي في التطبيقات الاحترافية)**: الـ`<aside>` `overflow:hidden`؛ **الهيدر مثبّت تماماً** (`flexShrink:0`، خارج منطقة التمرير، تحت شريط الحالة عبر `safe-area`) فلا ينزلق أبداً مهما طالت القائمة؛ و**كل ما تبقّى (القائمة + الإعدادات) داخل منطقة تمرير واحدة** (`flex:1; minHeight:0; overflowY:auto`) تنساب طبيعياً تحت الهيدر — قائمة قصيرة = تكدّس طبيعي بلا فجوة وسطية قبيحة، قائمة طويلة = تتمرّر بنعومة والهيدر ثابت. typecheck ✅. **ملاحظة لناصر**: إن استمر الشكل القديم فهو كاش iOS — لازم ضغط شريط التحديث الأخضر أو إغلاق التطبيق وفتحه ليصل v10.83. |
| **v10.82** | **إصلاح خلل القائمة الجانبية (الثلاث شرطات)** — بلاغ ناصر بصورة: الهيدر وعناصر القائمة تختفي والمحتوى يتداخل مع شريط الحالة (الساعة/البطارية). **السبب الجذري**: الـ`<aside>` كاملةً كانت `overflowY:auto` بلا هيدر مثبّت؛ بعد نمو القائمة (إضافة «📣 الشكاوى» v10.81 + بلوك معاينة الأدمن) صار المحتوى أطول من الشاشة، فأي تمرير ينزلق الهيدر/القائمة **تحت شريط الحالة** (لأن لا يوجد منطقة مثبّتة) فتبدو القائمة مكسورة (يظهر الفوتر فقط). **الإصلاح (نمط الـdrawer الحديث، بلا دين برمجي)**: الـ`<aside>` صارت `overflow:hidden` (لا تتمرّر بنفسها)؛ **الهيدر** `flexShrink:0` (مثبّت أعلى تحت شريط الحالة عبر `safe-area` الموجود)؛ **`<nav>`** صارت `flex:1; minHeight:0; overflowY:auto` (تتمرّر داخلياً وحدها إذا كثُرت العناصر)؛ **الفوتر** `flexShrink:0` (مثبّت أسفل). النتيجة: الهيدر والفوتر ظاهران دائماً، والقائمة فقط تتمرّر في الوسط — يستحيل أن ينزلق المحتوى تحت شريط الحالة مهما طالت القائمة. typecheck ✅. |
| **v10.81** | **مركز بلاغات وشكاوى الأدمن (#1) + مدخل الشكاوى للمستخدم (#3)** — طلب ناصر: الشكاوى تظهر في مركز التحكم **مع المبلِّغين** (لا إيميل فقط). **DB (`v10_81_complaints_and_admin_report_rpcs`)**: جدول `public.complaints` (RLS: إدراج كنفس + قراءة صاحبها + أدمن via `is_admin()`) + ٤ دوال **SECURITY DEFINER** بنفس نمط `admin_search_users` (بوابة أدمن داخلية + أعمدة مؤهَّلة): `admin_list_reports` (يرجع البلاغ + اسم/جوال/دور المبلِّغ والمُبلَّغ عنه + **كم بلاغ أعطى المبلِّغ** + **كم استلم المُبلَّغ عنه** + **عدد المبلِّغين المختلفين/١٤ي** + `under_review`، مع فلاتر بحث/نوع/حالة/أيام/دور)، `admin_list_complaints`، `admin_set_report_status`، `admin_set_complaint_status`. **العميل**: `adminService` (أنواع `AdminReportRow`/`AdminComplaintRow` + ٤ دوال)، `complaintRepository` + `ComplaintDialog` (نموذج شكوى للمستخدم)، صفحة **`admin/AdminReports`** (تبويبان البلاغات/الشكاوى، فلاتر، بطاقات تعرض «المبلِّغ ← ضدّ» بالأسماء والجوال وفتح حساب التاجر `/store/:id`/المشتري عبر تبويب المشترين، عدّادات الكشف عن المبلِّغ الكيدي، أزرار حالة: تحت المراجعة/تم الحل/رفض كيدي — كله **يدوي** كما اخترت). **الربط**: تبويب «🚩 البلاغات والشكاوى» في `AdminDashboard` + قراءة `?tab=` من الرابط؛ إشعار البلاغ التجميعي يفتح `/admin?tab=reports`؛ مدخل «📣 الشكاوى / تواصل الإدارة» في القائمة الجانبية لأي مستخدم مسجَّل (#3). أُضيف `'report'` لنوع `Notification`. typecheck ✅. **متبقٍّ #1: الإيميل التلقائي (مفتاح Resend من ناصر) + إجراءات الإشراف (كتم/إيقاف حجز/إخفاء عروض/تعليق) + الحظر النهائي بالإيميل/الجوال.** |
| **v10.80** | **بلاغان من ناصر (صورة صفحة المتجر): مسافة علوية كبيرة + زر تواصل**. **(١)** هيدر `StoreDetails` كان يأخذ الصفحة كاملة بمسافات كبيرة — رُفع **قليلاً** مع **إبقاء `env(safe-area-inset-top)`** فلا يصعد لشريط الساعة/البطارية: حشوة الهيدر العلوية `+14px → +6px` والسفلية `24 → 16`، صف الرجوع/التعديل `marginBottom 16 → 10`, كتلة البروفايل `marginBottom 24 → 14`, هامش الأفاتار `16 → 10px` (تضييق محسوب «قليلاً جداً» كما طلب، بدون قص محتوى). **(٢)** كتلة التواصل كانت زرّ اتصال عريض + مربّع واتساب صغير — حُوّلت إلى **زرّين متساويين (50/50)** تماماً كصورة ناصر المرجعية: «📞 اتصال» (خلفية فاتحة) + «WhatsApp 💬» (أخضر)، كلاهما `flex:1` بترتيب يجعل واتساب يساراً والاتصال يميناً في RTL. typecheck ✅. |
| **v10.79** | **بلاغات ناصر من الاختبار: تعليقات صفر + إشعار متابعة مكرّر + بناء أساس نظام البلاغات (#1)**. **(أ) إصلاح «التعليقات والآراء (0)» في صفحة العرض** رغم وجود تقييمات للمتجر — السبب: `DealDetails` كان يعرض `deal.ratings` (تقييمات هذا العرض فقط)؛ عرض آخر لنفس المتجر = مصفوفة فارغة → «لا توجد تقييمات». **الإصلاح**: تجميع على مستوى المتجر (نفس منطق `StoreDetails.allStoreReviews`) — `storeReviews` يجمع تقييمات كل عروض المتجر مع `dealId`+`itemName` لكل تقييم؛ العدّاد/المتوسط والقائمة صارت من تجميع المتجر، وكل إجراء (إعجاب/حذف/رد) يوجَّه لعرضه الصحيح عبر `r.dealId`، وأُضيف سطر «🏷️ اسم المنتج» لكل تقييم. الآن تعليق المشتري يظهر في كل عروض نفس المتجر. **(ب) إشعار «متابع جديد» مكرّر** — السبب الجذري عبر Supabase MCP: `tr_follow_notification` هو AFTER UPDATE على `users` (أي عمود)، فالكتابة المتفائلة + كتابة ملف ثانية (أو سباق نقرتين) كلٌّ يقدّم التاجر كـ«مُضاف حديثاً» مقابل OLD → إشعار مكرّر. **الإصلاح (ترحيل `v10_79_dedupe_follow_notification`)**: حارس منع تكرار داخل الدالة (تخطّي إن وُجد إشعار follow لنفس (تاجر، متابِع) خلال ساعة — نفس أسلوب منع تكرار التنبيه الذكي) + تنظيف المكرّر الموجود + إعادة تأكيد `REVOKE EXECUTE`. **(ج) أساس نظام البلاغات #1 (ترحيل `v10_79_reports_foundation`)**: جدول `public.reports` + عمودا `users.under_review/under_review_at`؛ `tr_guard_report_insert` (BEFORE، SECURITY DEFINER) يفرض قواعد ناصر: **مشتري↔تاجر فقط** (يمنع تاجر↔تاجر/مشتري↔مشتري/النفس/طرف admin)، ويختم الأدوار من `users.user_type` الحقيقي (لا انتحال)؛ `tr_report_threshold` (AFTER): **٣ مبلِّغين مختلفين** (ليس ٣ من نفس الشخص) خلال ١٤ يوم → يعلّم الحساب `under_review` + إشعار أدمن **تجميعي واحد** عند التجاوز فقط (لا سبام، بلا تقييد تلقائي)؛ RLS (إدراج كنفس + أدمن عبر `is_admin()`)، وEXECUTE مسحوب على الدالتين. **الواجهة**: `reportRepository` + مكوّن `ReportDialog` (أنواع بلاغ + سبب، رسائل خطأ عربية تترجم رفض الـtrigger)؛ زر **🚩** في صفحة المتجر (مشتري→تاجر، يظهر للمشتري فقط) وفي كرتَي الطلب الحالي/السجل بلوحة التاجر (تاجر→مشتري). typecheck ✅. **متبقٍّ #1: مركز بلاغات الأدمن (فلاتر/أسماء/دخول الحسابين/عدّاد المستلَم+المُعطى/مراجعة الرسائل) + الإيميل (Resend) + مكان الشكاوى #3 + إجراءات الإشراف والحظر النهائي**. |
| **v10.78** | **٣ من ٥ نقاط لناصر: إلغاء سبام تقييم الأدمن (#5) + إصلاح باق تجديد العرض (#4) + حظر المشتري للتاجر (#2)**. **(#5)** بعد v10.77 طلب ناصر عدم إشعار الأدمن بكل تقييم (سبام مع ملايين المتاجر). ترحيل `v10_78_rating_notif_drop_admin_fanout`: `handle_rating_notification` صار يُشعر **التاجر فقط** (حُذف admin fan-out)، أعيد تأكيد `REVOKE EXECUTE`، ونُظّفت أي إشعارات تقييم admin. الأدمن سيراجع التقييمات من صفحة تقارير الأدمن (ضمن #1 لاحقاً). **(#4)** بلاغ ناصر + صورة: «تجديد العرض» يظهر «✅ تم التجديد بنجاح» لكن العرض يبقى في السابقة — السبب: `reActivateDeal` كان يعرض النجاح **بلا شرط** و`updateDeal` يبتلع فشل الحفظ (الـtrigger الخادمي يرفض إعادة التفعيل غالباً لأن لوكيشن العرض حُذف/تغيّر فيتجاوز حدّ المواقع). **الإصلاح**: `updateDeal` صار يرجّع `Promise<boolean>` (نجاح فعلي/فشل)؛ و`reActivateDeal`: إن فشل الحفظ **لا يدّعي النجاح** بل يفتح العرض في نموذج التعديل حيث **البانر الموجود** «⚠️ تم حذف لوكيشن العرض السابق — اختر أحد مواقعك الحالية لتجديد هذا العرض» + شرائح المواقع الحالية تتيح إعادة الإسناد بنقرة ثم الحفظ يجدّد فعلياً (مطابق تماماً لطلبه). الكمية/المدة كما اتُّفق (createdAt جديد = نفس المدة من لحظة النقر). **(#2)** **حظر المشتري للتاجر** — ترحيل `v10_78_buyer_block_merchant`: عمود `users.blocked_merchants TEXT[]`، وفلترة **خادمية** في `handle_deal_smart_notifications` (مسار العرض الجديد الحيّ) و`handle_smart_alerts_backfill` (مسار تعديل التنبيهات) بإضافة شرط استبعاد المتاجر المحظورة — فلا يصل المشتري أي تنبيه ذكي من متجر حظره **حتى لو طابق تنبيهه أو يتابعه** (تعديل سطر واحد لكل دالة، البقية مطابقة للتعريف الحيّ، SECURITY DEFINER محفوظ). **العميل**: انعكاس كامل لنمط `followed_merchants` → `blockedMerchants`: نوع `UserProfile`، `userRepository` (saveProfile partial-aware + تعيين الصفوف + fallback)، `AppContext` (واجهة + حالة + تهيئة + `toggleBlockMerchant` [حظر متجر متابَع يلغي المتابعة تلقائياً] + مزامنة realtime + تصدير)، زر **🚫 حظر/إلغاء حظر** بجانب زر المتابعة في `StoreDetails` مع تأكيد ورسالة واضحة، وفلترة `blockedMerchants` في `Home` (trending/bestDiscounts/filteredDeals) و`Nearby` — المتجر المحظور يختفي فوراً من كل مكان. typecheck ✅. **يتبقّى #1 (نظام الإبلاغ/الشكاوى/الإشراف/الإيميل/الحظر النهائي) + #3 (مكان الشكاوى) — epic كبير يحتاج قرار آلية الإيميل، سيُخطَّط ويُنفَّذ على مراحل.** |
| **v10.77** | **إصلاح: التاجر لا يصله إشعار عند تقييم المشتري له (ولا أي تقييم سابقاً)** — بلاغ ناصر. **تشخيص جذري عبر Supabase MCP (لا تخمين)**: سياسة RLS على `notifications` اسمها `notifs_insert_self` شرطها `auth.uid()::text = user_id` — أي **المشتري لا يستطيع إدخال صفّ إشعار باسم التاجر** (مُعرّفه مختلف) فيُرفض الإدخال **بصمت** (`notificationRepository.save` يبتلع الخطأ بـ`console.warn`). إشعارات الحجز تشتغل لأنها تُنشأ عبر **DB trigger `handle_booking_notification` (SECURITY DEFINER يتجاوز RLS)**؛ أما `addRating` في `AppContext` فكان يحاول الإدخال من جهة العميل مباشرة → RLS يرفضه. **الدليل القاطع**: الجدول فيه **تقييمان فعليان** («ممتاز التعامل» ٥★ لعيادة أسنان، «المحل رائع» ٥★ لكوورة) مقابل **صفر إشعارات تقييم** (كل الـ١٧٧ إشعار نوعها `booking` من الـtrigger). **الإصلاحات: (١) ترحيل DB `v10_77_rating_merchant_notification`**: دالة `handle_rating_notification()` **SECURITY DEFINER** + `SET search_path=public` + trigger `tr_rating_notification` **AFTER INSERT ON ratings** — تنشئ إشعار التاجر خادمياً (يتجاوز RLS) بـ`type='rating'` و`meta_data.audience='seller'`، مع fan-out للأدمن (`audience='admin'`) تماماً كنمط الحجز المُثبَّت. **(٢) تعبئة رجعية idempotent** (حارس `NOT EXISTS` على `meta_data->>'ratingId'`) — أنشأت إشعارَي التقييمين القديمين للتاجر `N15` فصار يراهما الآن. **(٣) تقوية أمنية `v10_77_rating_notif_revoke_execute`**: `REVOKE EXECUTE` على الدالة من `PUBLIC/anon/authenticated` (أغلق تحذير linter «SECURITY DEFINER قابلة للاستدعاء كـRPC»؛ الـtrigger لا يحتاج هذا الإذن فلا يتأثر — `proacl` صار `postgres/service_role` فقط). **(٤) `AppContext.addRating`**: حُذف نداء `addNotification(storeId,…)` الميت (محظور بـRLS أصلاً، وصار تكراراً بعد الـtrigger) + أُزيل `addNotification` من مصفوفة اعتماديات الـcallback. التحديث المتفائل المحلي لقائمة تقييمات العرض باقٍ. **ملاحظة**: إصلاح DB فعّال **فوراً** دون نشر؛ نشر العميل هو تنظيف فقط. typecheck ✅. **(بقي تحذير قديم مماثل على `handle_booking_notification` — لم أمسّه احترازاً لأنه أعلى مسار حركة (١٧٧ إشعار) وخارج نطاق البلاغ؛ يُنصح بتطبيق نفس `REVOKE EXECUTE` عليه لاحقاً.)** |
| **v10.76** | **تعديل تدفّق الكاميرا حسب طلب ناصر: مربع واحد + قص فوري لكل لقطة + حلقة «المزيد/تم»**. بعد v10.75 طلب ناصر: يرجع **مربع واحد فقط** (لا مربعين منفصلين كاميرا/استوديو)، وبعد تصوير الصورة يطلع **القص فوراً لنفس الصورة**، ثم زر **«المزيد»** لصورة إضافية (تتكرر نفس طريقة القص) أو **«تم»** للإنهاء. **التغييرات: (١) `SellerDashboard`**: أُلغيت كتلة المربعين، ورجع **مربع واحد** بنفس شكل القديم (📸 «إضافة صور» + سحب/لصق) — لمسه يفتح الكاميرا الحيّة مباشرة. الحقل المخفي للاستوديو بقي في DOM (يُفتح من زر «الاستوديو» داخل الكاميرا + هدف السحب). حُذفت `ingestCameraFiles` (لم تعد لازمة). **(٢) `CameraCapture` أُعيد بناؤه كآلة حالات** (`camera` → `crop` → `choice`): الكاميرا الحيّة (`getUserMedia`) → زر التقاط → **يظهر `ImageCropEditor` فوراً لنفس اللقطة** (نفس محرّر قص الاستوديو بالضبط) → بعد «✓ تطبيق» (أو «تخطّي القص») تُسلَّم الصورة للأب (`onCapture` = `uploadCroppedFile` → ضغط + رفع + إضافة) → شاشة **«📷 المزيد» / «✓ تم»**: «المزيد» يرجّع للكاميرا للقطة التالية بنفس الحلقة، «تم» يُغلق. البثّ يبقى حيّاً أثناء القص/الاختيار (إيقاف المسارات فقط عند الإنهاء/الخروج — ضوء الكاميرا ينطفئ) فالعودة عبر «المزيد» فورية. العرض **حصري لكل وضع** (كاميرا zIndex 100000 / محرّر القص 99998 / شاشة الاختيار) فلا تداخل طبقات. زر «🖼️ الاستوديو» داخل الكاميرا يُغلقها ويفتح معرض iOS (الحقل `multiple` — اختيار حتى ٤ صور تمرّ بطابور القص الموجود). احترام حدّ الـ٤ صور. الضغط قبل الرفع (v10.75) باقٍ كما هو. typecheck ✅. |
| **v10.75** | **كاميرا داخلية متعددة اللقطات (طريقة واتساب) + استديو متعدّد + إصلاح بطء الرفع (١٠ث/صورة)** — بلاغ ناصر بصورتين: كاميرا النظام تأخذ صورة واحدة ثم تُغلق فيُعاد فتحها لكل صورة، والرفع يصل ١٠ث للصورة الواحدة. **التشخيص الجذري**: `<input accept="image/*">` عبر مسار «التقاط صورة» في iOS يفتح **كاميرا النظام** (لقطة واحدة ثم إغلاق — حدّ تقني لا يُتجاوز عبر input إطلاقاً)؛ والرفع بطيء لأن `storageService.uploadImage` كان يرفع **الملف الخام كامل الدقة** (٤-٨ ميجا) بلا أي ضغط (الضغط كان يحدث فقط إذا مرّ المستخدم بمحرّر القص يدوياً). **الإصلاحات: (١) `src/components/CameraCapture.tsx`** جديد — كاميرا حيّة داخل التطبيق عبر `getUserMedia` (الكاميرا الخلفية، `playsInline`+`muted`، إيقاف المسارات عند الإغلاق فلا يبقى ضوء الكاميرا): زر التقاط دائري يصوّر **عدّة لقطات متتالية بدون إغلاق**، شريط مصغّرات سفلي (حذف أي لقطة قبل الإرسال)، عدّاد `n/الحد`، زر تبديل أمامية/خلفية، «تم» يرجّع كل الصور دفعة واحدة، رسائل خطأ عربية واضحة عند رفض إذن الكاميرا مع توجيه لزر «الاستديو». مصغّرات الشريط بصيغة `data:` (دون تعديل CSP). صور الكاميرا **تتخطّى محرّر القص** (مؤطّرة في الكاميرا الحيّة أصلاً — القص لكل لقطة يعيد نفس الاحتكاك المطلوب إزالته). **(٢) `src/utils/imageCompression.ts`** جديد — يفكّ الصورة (`createImageBitmap` يحترم EXIF، مع fallback لـ`<img>`)، يصغّرها لـ≤١٦٠٠px ويعيد ترميزها JPEG q=0.82، يتخطّى JPEG الصغير أصلاً، **fail-open** (يرجّع الملف الأصلي عند أي فشل — لا يحجب البائع أبداً). **(٣)** رُبط الضغط داخل `storageService.uploadImage` كـ**نقطة اختناق وحيدة** — كل مسارات الرفع (كاميرا/استديو/قص مُطبَّق/قص متخطّى/فشل فكّ) تمرّ به، فلا تصل صورة خام للشبكة: ٤ميجا → ~٢٥٠كب ⇒ الرفع من ~١٠ث إلى ~١ث. **(٤) `SellerDashboard`**: استُبدلت خانة «إضافة صورة» الواحدة بخانتين — 📷 **الكاميرا** (تفتح الكاميرا الحيّة) و🖼️ **الاستديو** (الحقل يحتفظ بـ`multiple` فيُختار حتى ٤ صور دفعة واحدة من معرض iOS)، مع إبقاء السحب/اللصق على الاستديو. `vercel.json` يسمح بـ`camera=(self)` مسبقاً. typecheck ✅. |
| **v10.74** | **٤ متطلبات بدقة عالية من ناصر: تتبّع خريطة حيّ + رئيسية حسب المدينة + إشعار الساعتين + صندوق تقييم بعد الشراء**. **(١) الخريطة تمشي معي بالثانية (حولي)** — السبب: `MapUpdater` كان `map.flyTo(center,12)` (يصفّر الزووم ويعمل fly جديد كل تحديث)، و watcher يتجاهل أي حركة < **٣٠م**، فبالسيارة الخريطة تبدو متجمّدة. **الإصلاح**: `FollowController` جديد — زووم شارع (١٥) مرة واحدة ثم `panTo` بنعومة مع الحفاظ على زووم المستخدم؛ عتبة الحركة صارت **٦م** و`maximumAge:1000` (تحديث ثانية-بثانية)؛ سحب الخريطة يوقف التتبّع (استكشاف حر) وزر 📍 عائم يعيد تفعيله؛ اختيار منطقة/مدينة يدوياً يوقف التتبّع، وزر «موقعي» يعيده. **(٢) الرئيسية حسب مدينة العميل (تتوسّع للأقرب)** — السبب: `trending/bestDiscounts/filteredDeals` كانت **تقصّ بحدّ صارم** على `topLocation` (تتوقف عند المدينة). **الإصلاح**: نافذة `LocationGate` تُطلب **مرة واحدة** أول فتح (GPS تلقائي → `findNearestCity`، أو اختيار المدينة يدوياً عند الرفض) وتُحفظ كـ`homeCity` في `localStorage`. أُضيف `dealProximityTier` (نفس المدينة=٠، نفس المنطقة=١ [المخواة→بلجرشي/قلوة/الباحة]، <١٥٠كم=٢، <٤٠٠كم=٣، أبعد=٤). الأقسام الثلاثة تُرتّب بالـtier **ثم** تصنيف القسم (موثوقية/خصم) **بدون قصّ** — تبدأ بمدينته ثم تتوسّع للأقرب فالأقرب حتى نهاية العروض، وليس بترتيب مسافة صارم (هذا عمل صفحة حولي). اختيار المنطقة/المدينة يدوياً من فلاتر الرئيسية يبقى قصّاً صريحاً (يحترم اختيار المستخدم). **(٣) إشعار الساعتين** — في شاشة «إتمام الحجز» (`DealDetails`) فوق زر التأكيد، بلوك احترافي ثنائي اللغة: «مدة الحجز ساعتان فقط — استلم طلبك خلال ساعتين من التأكيد، وبعد انتهاء المهلة يُلغى الحجز تلقائياً ويعود المنتج للبيع دون أي التزام عليك» (مطابق لمدة الانتهاء الفعلية `TWO_HOURS_MS`). **(٤) صندوق تقييم المتجر بعد الشراء** — عند تحوّل حجز المشتري إلى `completed` (التاجر مسح QR) عبر `onBookingChange`، يفتح **صندوق** تلقائياً (مرّة واحدة، ولا يظهر إن سبق وقيّم): «نرجو تقييم المتجر — تقييمك يهمّنا» مع **٥ نجوم + مربع تعليق** يستدعي `addRating` (يُشعر التاجر تلقائياً). هذا الصندوق مقصود (طلب ناصر) ومستقل عن صندوق الإشعارات المُلغى في v10.73. typecheck ✅. |
| **v10.73** | **مهمتان بدقة عالية من ناصر: ٤ باقات لوكيشن يتحكم بها الأدمن + إلغاء صندوق الإشعار المُعيق**. **(١) نظام الباقات (١/٣/٦/١٠ لوكيشن)** — **تشخيص جذري عبر Supabase MCP**: الحدّ كان مثبّتاً `3` في ٤ أماكن (عميل ×٢ + رسائل خطأ + **trigger `enforce_seller_location_cap` في DB ثابت `v_max:=3`**) — فأي تغيير في العميل وحده ما كان بيشتغل لأن DB يرفض الموقع الـ٤ مهما صار. كذلك `admin_apply_subscription` ما كان يضبط `max_branches` إطلاقاً. **الإصلاحات**: (أ) **ترحيل DB `v10_73_location_packages`**: `enforce_seller_location_cap` صار يقرأ `COALESCE(store_profiles.max_branches,3)` لكل تاجر؛ و`admin_apply_subscription` أضيف له `p_max_branches` (يتحقّق ∈ {1,3,6,10}) يكتبه في الإدراج وفي ON CONFLICT (NULL = إبقاء كما هو). (ب) **`src/data/packages.ts`** جديد: الباقات الأربع (الأولى=موقع واحد، الثانية=حتى ٣، الثالثة=حتى ٦، الرابعة=حتى ١٠) + `maxForPackage/packageForMax/packageLabel`. (ج) **لوحة الأدمن (`AdminSellers`)**: مُحدِّد باقة بأربعة أزرار داخل نافذة الاشتراك، يجلب الباقة الحالية عبر `subscriptionRepository.getStoreSubscription` ويمرّر `maxBranches` في `adminService.applySubscription` → الـRPC. (د) **لوحة التاجر**: `maxLocations` يُجلب من `store_profiles.max_branches` (افتراضي ٣)، استبدل `MAX_LOCATIONS=3` الثابت في الموضعين بالقيمة الديناميكية؛ البانر يعرض اسم الباقة + `العدد/الحد`؛ **chips ملوّنة حسب الحالة**: 🔒 **أخضر = نشط** (مرتبط بعروض، يُحتسب، **لا يُحذف** حتى تنتهي/تُحذف عروضه) ، 🟠 **كهرماني = شاغر** (لا يُحتسب، **يمكن حذفه ✕** لتفريغ خانة) ، **أزرق = المختار حالياً**؛ + سطر «مفتاح ألوان» يشرح ذلك؛ زر الحذف ✕ يظهر **فقط** للشاغر، وللنشط شارة 🔒 بدل الحذف. كل رسائل تجاوز الحد صارت ديناميكية/عامة (لا «٣» ثابتة) وتذكر التواصل مع الإدارة للترقية. **(٢) إلغاء صندوق الإشعار المُعيق** — **السبب الجذري**: `SellerDashboard` فيه `useEffect` يطلق `customAlert` (نافذة «موافق» وسط الشاشة تحجب العمل) لـ**كل** إشعار حجز غير مقروء — تاجر بمئات الطلبات ما يقدر يخلّص. والمشتري ما كان يشوف شيء أصلاً (ما عنده صندوق، والإشعار العلوي مرهون بإذن المتصفح). **الإصلاحات**: حُذف الـuseEffect + state `processedNotifIds` نهائياً (انتهى الصندوق للتاجر). أُضيف **بانر علوي غير مُعيق** (`InAppBanner.tsx` + `inAppBanner` في AppContext، يُضبط في `onNotificationInsert` لصاحب الإشعار) — يظهر **للطرفين** (تاجر ومشتري) أعلى الشاشة، يختفي تلقائياً بعد ٥ ثوانٍ، **طبقته الخارجية `pointer-events:none`** فلا يحجب أي ضغط على الصفحة (التاجر يكمل عمله)، الضغط عليه ينقل لمكان الحجز/المحادثة. يبقى أيضاً الإشعار العلوي للنظام (`showRealTimeAlert`) + قائمة الإشعارات السفلية كما هي. هذا أيضاً يصلح «المشتري ما يوصله إشعار لما التاجر يرسل» لأن البانر داخل الموقع لا يعتمد على إذن إشعارات المتصفح. typecheck ✅. |
| **v10.72** | **٣ إصلاحات من بلاغ ناصر (٥ صور): قائمة التاجر + بحث عالمي موحّد + سكانر QR حيّ**. **(١) "حجوزاتي" تظهر لحساب التاجر** — `Sidebar.tsx` كان يضيف عنصر "📅 حجوزاتي" دائماً في القائمة الجانبية بلا استثناء؛ التاجر يضغطه فيفتح صفحة حجوزات فاضية أبداً (التاجر يدير الحجوزات من لوحة التاجر عبر السكانر، ما عنده "حجوزاته" كمشتري). **الإصلاح**: بناء `menuItems` شرطياً — يُحذف "حجوزاتي" متى ما كان `effectiveUserType === 'seller'` (يغطي التاجر الحقيقي + المدير وهو يعاين كتاجر). **(٢) البحث لا يجد المتجر + ناصر طلب "أقوى بحث عالمي في كل الصفحات"** — **تشخيص فعلي عبر Supabase MCP**: قاعدة البيانات فيها **متجر واحد فقط** (`shop=N15`, `name=تمتومي`) — **لا يوجد متجر اسمه "تاكي" إطلاقاً**، وRLS `users_select_all` يسمح لأي مستخدم (تاجر أو غيره) برؤية كل المتاجر فالمشكلة ليست صلاحيات. فبحث "تاكي" منطقياً لا يعطي متجر "تاكي" (غير موجود)، وكان مرادف ملوّث `'تاكي':['taki','site','موقع','تطبيق'...]` في `dealService` (اسم المنصة نفسها كمرادف منتج!) يسبب نتائج مضللة. **الإصلاحات (محرّك واحد مشترك لكل الصفحات، بلا دين برمجي): (أ)** `dealService.normalizeText` موحّد: يطوي ألف/ياء/تاء-مربوطة (أإآا→ا، يىئ→ي، ةه→ه، ؤ→و)، يزيل التشكيل والتطويل، **يحوّل الأرقام العربية-الهندية والفارسية إلى ASCII** (`ن١٥` ↔ `n15`)، يحوّل علامات الترقيم لمسافات ويطوي الفراغات. **(ب)** `dealService.searchScore` + `matchStores`: **ترتيب بالأهمية** (تطابق تام > يبدأ بـ > كلمة كاملة > يحتوي > ضبابي/مرادف) فأقرب نتيجة تطفو للأعلى بدل تطابق ضبابي ضعيف؛ حقول اسم المتجر/المنتج موزونة أعلى بكثير من الوصف. **(ج)** حذف مرادف `'تاكي'` المضلِّل. **(د)** `Home` + `DealsList` + `Nearby` الآن **كلها** تستخدم نفس المحرّك وتعرض شريط "المتاجر 🏪" المرتّب — البحث عن متجر بالاسم صار متسقاً وقوياً على كل صفحات الاكتشاف، والعروض تُرتَّب بالأهمية أثناء البحث. **(٣) كاميرا السكانر لا تظهر (الخلفية تبقى كما هي)** — **السبب الجذري**: عنصر `<video>` في `BarcodeScanner.tsx` كان يُركَّب فقط بعد `cameraActive=true`، لكن `startCamera()` يسند `videoRef.current.srcObject` **قبل** `setCameraActive` فيقع على ref = null والبث لا يُربط أبداً → مربع أسود. **زيادة**: لم تكن هناك أي مكتبة قراءة QR إطلاقاً (إدخال يدوي فقط) رغم أن رمز الحجز يُعرض للمشتري كـQR. **الإصلاح**: `<video>` صار **مركَّباً دائماً** (زر فتح الكاميرا فوقه كطبقة)، والبث يُربط في `useEffect` بعد التركيب (مضمون)؛ + **قراءة QR حيّة فعلية عبر `jsqr`** (حلقة رسم إطار على canvas بخنق ~١٨٠ms، مصغّرة لـ٦٤٠px للأداء، متوافقة مع iOS Safari) — رموز QR للحجز تُمسح فعلياً الآن لا إدخال يدوي فقط، مع إيقاف نظيف للكاميرا عند الإيجاد/الإغلاق. أُضيفت `jsqr` للحزم. typecheck ✅. |
| **v10.71** | **تحميل فوري للبيانات (٦-٧ ثوانٍ → عرض لحظي) — stale-while-revalidate**. بعد v10.70 الموقع تحسّن لكن ناصر بلّغ أن **تحميل البيانات يأخذ ٦-٧ ثوانٍ** رغم قلة المشتركين، ويريده ثانية-ثانيتين ويتحمّل ملايين المستخدمين. **القياس الفعلي (أرقام لا تخمين)**: الحزمة ليست السبب (JS ١٥٠KB مضغوط brotli عبر Vercel CDN، x-vercel-cache HIT — ممتاز). **السبب الجذري**: مشروع Supabase في **`ap-northeast-1` (طوكيو)** والمستخدمون في **السعودية** → كل طلب REST/Auth ~٣٠٠-٥٠٠ms ذهاباً وإياباً من جوال سعودي، ومسار التحميل البارد يصنع عدة طلبات متسلسلة فتتراكم لـ٦-٧ ثوانٍ. **الحلول المطبَّقة (أحدث نمط — مثل SWR/TanStack Query persistence): (١) طبقة لقطة محلية `snapshotCache.ts`** — كاش عرض stale-while-revalidate في localStorage: schema-versioned + TTL ٧ أيام + مفاتيح لكل مستخدم (uid) + مغلّف بالكامل في try/catch (الوضع الخاص/الحصة لا يكسر شيء). **(٢) عرض لحظي**: `deals` و`storeProfiles` تُهيّأ **متزامنة** من آخر لقطة في `useState(() => readSnapshot(...))` — الشاشة الرئيسية ترسم محتوى حقيقياً في **أول إطار** قبل أن يبدأ طلب الشبكة لطوكيو أصلاً؛ و`loading` يبدأ false لو وُجدت لقطة (لا spinner يحجب). قوائم المستخدم (حجوزات/إشعارات/مفضلة) تُهيّأ من لقطة `uid`-scoped لحظة معرفة هويته. السيرفر **دائماً** يكتب فوق اللقطة عند وصوله — اللقطة كاش عرض فقط لا مصدر حقيقة. **(٣) كتابة اللقطة** بعد كل جلب ناجح (initData + auth-listener SIGNED_IN + onRefreshAll + refreshDeals + refreshBookings). **(٤) مسح اللقطة** عند logout / SIGNED_OUT / deleteAccount (جهاز مشترك لا يسرّب حساباً سابقاً). **(٥) إزالة جلب مكرر حرج**: `bookingRepository.getByUser` كان يستدعي `dealRepository.getAll()` داخلياً (= جلب كل العروض **+ كل التقييمات** = رحلتا طوكيو إضافيتان على المسار الحرج كل مرة تُحمَّل الحجوزات). الآن يقبل `knownDeals` (AppContext يمرّر `dealsRef.current`)؛ وإن غابت، يجلب فقط صفوف العروض المُشار إليها بـ`.in('id', …)` بلا استعلام التقييمات. **(٦) `preconnect`** في `index.html` لـSupabase(طوكيو)+Google Fonts → يفتح DNS+TCP+TLS أثناء تحليل الـHTML فيوفّر ~رحلة كاملة على أول طلب. **النتيجة**: للمستخدم العائد (غالبية الحمل عند الملايين) الشاشة الرئيسية + قوائمه تظهر **فوراً (<١٠٠ms)** بلا انتظار الشبكة، والتحديث يجري صامتاً بالخلفية؛ والتحميل البارد أقل رحلات. **بدون دين برمجي** — كاش غير موثوق يُعاد التحقق منه دائماً، نسخة + TTL + عزل لكل مستخدم. **⚠️ يبقى قرار بنيوي لناصر**: نقل مشروع Supabase من طوكيو لمنطقة قرب السعودية (مومباي `ap-south-1` ~٣٠-٦٠ms، أو فرانكفورت `eu-central-1`) = تسريع ٥-١٠× على **كل** طلب للأبد وهو الإصلاح الحقيقي لمقياس الملايين — لكنه ترحيل بنية تحتية (مشروع/منطقة جديدة + نقل بيانات + تغيير مفاتيح البيئة + توقّف وجيز) يحتاج قرار المالك. |
| **v10.70** | **علاج جذري لثقل الموقع + "لازم أحدّث ٣ مرات" + إصلاحان UI من صور ناصر**. ناصر بلّغ أن الموقع ثقيل جداً ولا تظهر الحجوزات/الإشعارات/تسجيل الدخول إلا بعد تحديث الصفحة ٢-٣ مرات. **التشخيص الفعلي** (ليس النصائح العامة — العميل أصلاً Singleton، والجداول كلها داخل نشر realtime، وأحجام الجداول صغيرة جداً ٣ مستخدمين/٦ عروض فالـDB ليست العنق): المشكلة ١٠٠٪ في الواجهة — **(أ) عاصفة إعادة بناء قنوات realtime**: الـuseEffect المركزي للـrealtime في `AppContext` كان يعتمد على `[user?.id, showRealTimeAlert, reconcileStatus, addNotification]`؛ و`addNotification`/`showRealTimeAlert` يُعاد إنشاؤهما كلما تبدّل كائن `user` (٣-٤ مرات في كل دخول: null → getCurrentUser → JWT optimistic → صف DB → لغة). كل تبديل = `realtimeService.connect()` → هدم كل القنوات وإعادة اشتراكها أثناء الـhandshake → إشعارات/حجوزات INSERT تضيع حتى يحدّث المستخدم يدوياً. **(ب) جلب مزدوج/ثلاثي**: `initData` + `onAuthStateChange(INITIAL_SESSION)` + `onRefreshAll` (بعد ٥٠٠ms من connect) كلها تجلب نفس البيانات في كل فتح صفحة. **(ج) `onRefreshAll` يُطلق مرتين** عند العودة للتبويب (visibilitychange + focus، عتبة ١s لكليهما = ١٠ استعلامات). **(د)** `getFavorites/setFavorites/getFollowedMerchants` كانت تستدعي `supabase.auth.getUser()` (رحلة شبكة لخادم GoTrue) بدل `getSession()` المحلي. **الإصلاحات: C1** — الـeffect صار يعتمد على `[user?.id]` فقط؛ المعالجات تقرأ أحدث callbacks عبر refs (`showRealTimeAlertRef`/`addNotificationRef`/`reconcileStatusRef`) فالقنوات تُنشأ مرة واحدة لكل جلسة وتبقى مستقرة. **C2** — `onAuthStateChange` تُصفّى بالحدث: الجلب الثقيل يعمل فقط على `SIGNED_IN` صريح ومرة واحدة لكل id (`lastSignInHydratedIdRef`)؛ `INITIAL_SESSION` يملكه `initData` (الذي صار يملأ followed/keywords/smartAlerts/اللغة من نفس الملف بلا طلبات إضافية)؛ `TOKEN_REFRESHED`/`USER_UPDATED` لا تعيد الجلب إطلاقاً. **C3** — `realtimeService` فيه `requestRefresh()` يدمج أي دفعة triggers (visibility/focus/online/bfcache) في تحديث واحد بحد أدنى ٦s بين التحديثات؛ حُذف الـrefresh التلقائي ٥٠٠ms عند connect (يملكه initData)؛ pull-to-refresh الصريح يتجاوز المدمج. **C4** — الثلاث دوال تستخدم `getSession()` المحلي بدل `getUser()`. **النتيجة**: من ١٥-٢٥ طلب مكرر + إعادة اشتراك ٣-٤ مرات في كل تحميل → اشتراك واحد مستقر + تحميل واحد. الإشعارات/الحجوزات تصل فوراً بلا تحديث يدوي. **+ تحصين المقياس (طلب ناصر "ملايين في نفس اللحظة، بدون دين برمجي")**: مستشار أداء Supabase كان فيه **٢١١ تحذير**؛ كل سياسات RLS على الجداول الساخنة (deals/users/bookings/notifications/favorites/booking_messages/store_branches) كانت تعيد تقييم `auth.uid()`/`is_admin()` **لكل صف**. هجرة `v10_70_rls_initplan_hot_tables`: غلّفت كل النداءات بـ`(SELECT …)` (تقييم مرة واحدة لكل استعلام — مكافئ سلوكياً ١٠٠٪) + دمجت السياسات المكرّرة (users: سياستا SELECT/UPDATE → واحدة؛ store_branches: حذف `branches_admin_all` الـALL المتداخلة وطيّ admin عبر `is_admin()` لكل أمر). هجرة `v10_70_covering_fk_indexes`: ١٢ فهرس تغطية للمفاتيح الأجنبية. **النتيجة: ٢١١ → ١٥٦ تحذير، والجداول الساخنة السبعة = صفر تحذيرات** (المتبقي كله على جداول إدارية باردة). **UI-1 (صورة لوحة الطلبات)**: التاجر كان يرى UUID المشتري `1d38446e…` بدل الاسم — جدول `bookings` لا يخزّن اسم المشتري إطلاقاً، وRLS لا يسمح للتاجر بقراءة صف المشتري في `users`. الإصلاح: هجرة `v10_70_bookings_buyer_name` تضيف `user_name`+`user_phone` (denormalized، يُلتقط وقت الحجز) + backfill للحجوزات الموجودة (الطلبات الـ٣ القديمة عُبّئت)؛ `bookingRepository.save/getByUser/getByBarcode` + مُعالج realtime `onBookingChange` يكتبون/يقرؤون الاسم. **UI-2 (صورة الوضع الليلي)**: اسم المتجر واسم المنتج في `DealCard` كانا يستخدمان `var(--accent)`/`var(--dark)` وكلاهما داكن في الوضع الليلي (#1e293b/#334155) فيختفيان على الخلفية الداكنة → بُدّلا إلى `var(--text-primary)` (يتكيّف: داكن نهاراً، أبيض ليلاً) — بلا ألوان مثبّتة ولا دين برمجي. |
| **v10.69** | **إصلاحان حرجان من ناصر بعد اختبار v10.68: routing الإشعارات + الرسالة الثالثة لا تصل**. (١) **routing الإشعارات** — ناصر طلب أن أي إشعار حجز أو تأكيد استلام أو رسالة عند الضغط عليه يفتح **صفحة "حجوزاتي"** حيث تظهر المحادثة، لا صفحة المنتج. **التشخيص**: كل الإشعارات المتعلقة بالحجز (إنشاء، تأكيد استلام، رسائل، إكمال، إلغاء) تستخدم `type='booking'` ويتم تمييزها بـ`meta_data.audience` و`meta_data.isMessage`. قبل v10.69 كان buyer audience يُوجَّه إلى `/deal/{id}?barcode=...` — أي يفتح صفحة المنتج لا صفحة الحجز. **الإصلاح في `Notifications.tsx`**: غيّرت buyer destination إلى `/bookings?barcode={barcode}` + أضفت fallback لأي booking notification بدون audience محدد ليذهب لـ`/bookings?barcode=...`. (٢) **`Bookings.tsx` يحترم query parameter الـbarcode**: أضفت `useLocation` + useEffect يقرأ `?barcode=` من الـURL ويـauto-expand البطاقة المستهدفة + `scrollIntoView({ block: 'center' })` + شارة highlight بـ`var(--secondary)` (برتقالي) تختفي بعد 3.5 ثانية — نفس نمط `highlightedBarcode` في SellerDashboard. أضفت `id="booking-{barcode}"` على كل بطاقة (نشطة وسابقة) ليعمل الـscroll. النتيجة: المشتري يضغط إشعار "💬 رسالة جديدة من التاجر" → يفتح Bookings، البطاقة الصحيحة تتمدد تلقائياً، الصفحة تتمرر إليها، وbookingThread يظهر الرسالة فوراً. (٣) **الرسالة الثالثة لا تصل** — السبب الجذري: **realtime websocket على iOS Safari يموت بصمت** لما التطبيق يـbackgrounded (قفل الشاشة، تبديل تطبيقات، مكالمة)؛ أي `INSERT` على `booking_messages` يحدث أثناء الفجوة يضيع نهائياً — الـchannel يعيد الاشتراك لكن لا يـreplay الأحداث الفائتة. **اختبار حي** عبر Supabase MCP أكّد التشخيص: barcode `SEXKU2HX` يحتوي **٥ رسائل في الـDB** (٣ buyer + ٢ seller)، لكن جهاز التاجر كان يعرض ٤ فقط (counter: "أنت 2/3 — الطرف الآخر 2/3"). الرسالة المفقودة `3ac90efb...` "تفهم انت ولا لا" مخزّنة بصحة 100% في الـDB، RLS يسمح للتاجر بقراءتها، فقط الـrealtime ما وصّلها. **الإصلاح ١: في `BookingThread.tsx`** أضفت listeners لـ`visibilitychange` + `focus` + `pageshow(persisted)` على document/window — كلما الـthread يصبح visible (بعد background → foreground) يستدعي `fetchBookingMessages(barcode)` + `markBookingMessagesRead(barcode)` لسحب أي رسائل فاتت + تمييز رسائل الطرف الآخر كمقروءة. هذا يغلق الـgap بشكل deterministic بدون polling. **الإصلاح ٢: bug جانبي اكتُشف أثناء التشخيص** — `onRefreshAll` في `AppContext.tsx` (يُستدعى من realtimeService على focus/online/bfcache) كان يستبدل `bookings` بالكامل بـ`getByUser(user.id)` الذي لا يجلب `messages`. النتيجة: كل focus event يمسح كل الرسائل المخزّنة في الذاكرة، فالـthread يضطر للـrefetch من الصفر. **الإصلاح**: نسخت pattern الـ`refreshBookings` (يحفظ messages by barcode) إلى `onRefreshAll` فلا تضيع الرسائل عند الـrefresh العام. النتيجة الكلية: التاجر يفقد التركيز ويعود → الرسالة المفقودة تظهر تلقائياً خلال ميلي ثوانٍ، بدون pull-to-refresh يدوي. |
| **v10.68** | **سجل طلبات سابقة للتاجر + موقع زر الرجوع/اللغة في شاشات المصادقة**. (١) **سجل طلبات للتاجر**: قبل v10.68 كان تبويب "الطلبات" يعرض فقط `activeOrders` (status = pending / acknowledged) — أي طلب بعد ما يُكمل أو يُلغى يختفي نهائياً من شاشة التاجر. ناصر طلب أن تكون السجلات السابقة موجودة للرجوع لها لاحقاً. **التنفيذ**: أضفت state جديد `ordersFilter: 'active' \| 'history'` + sub-tabs أعلى تبويب الطلبات بشكل segmented control: **🔔 نشطة (N)** و **📜 السجل (N)**. الـcount badge على كل تبويب يعرض العدد فوراً. لما `ordersFilter === 'history'` يُعرض `pastOrders` (كان محسوباً أصلاً من قبل) مرتباً تنازلياً بـ`bookedAt`، كل طلب في **card قراءة فقط** (لا أزرار، لا scanner، لا chat) يعرض: اسم العرض، التاريخ والوقت (locale عربي/إنجليزي)، **شارة الحالة** (✅ مكتمل أخضر / ❌ ملغي أحمر مع خلفية شفافة)، اسم المشتري، الكمية، الرمز المرجعي (#barcode بـmonospace LTR)، وملاحظات المشتري/التاجر إن وُجدت. opacity 0.95 ليوحي بصرياً بأنها أرشيف. لو كان `order.deal` محذوفاً يعرض "عرض محذوف" بدل crash. **النتيجة**: التاجر يرجع لأي طلب سابق يتأكد من تفاصيله بدون مغادرة لوحة التحكم. (٢) **رفع زر الرجوع وزر اللغة عن الـnotch**: شاشات تسجيل الدخول وإنشاء الحساب في `Register.tsx` كانت `TopHeader` فيها بـ`padding: '16px 20px'` بدون احترام `safe-area-inset-top` — على iPhones مع dynamic island/notch (iPhone 14 Pro+، Pro Max) كانا "يلامسان الشاحن" حسب وصف ناصر. **الإصلاح**: غيّرت الـpadding إلى `calc(env(safe-area-inset-top, 12px) + 14px) 20px 16px` — يستخدم fallback 12px للأجهزة اللي ما تدعم safe-area + يضيف 14px فراغ بصري إضافي. هذا الـpattern موجود أصلاً في 13 مكان آخر في المشروع (DealDetails، Bookings، StoreDetails، Profile، إلخ) فيتسق مع البقية. الـTopHeader يُستعمل في ثلاثة `mode`s: landing / login / type / form — كل ما يظهر يستفيد من الإصلاح. |
| **v10.67** | **٤ إصلاحات UI بطلب ناصر** بناءً على ٤ لقطات. (١) **زر "احجز الآن" + الكمية كانوا fixed في أسفل DealDetails** — ناصر أراد القسم في نهاية الصفحة يـscroll مع المحتوى وليس مثبتاً. **الإصلاح**: أخرجت `<div className="book-cta">` من خارج `.page-content` ووضعته داخل الـscroll area كآخر عنصر، مع `marginBottom: calc(env(safe-area-inset-bottom) + 80px)` لإفساح مكان لـBottomNav الـfixed. شكّلته كـcard بـrounded border بدلاً من شريط ممتد على عرض الشاشة — يندمج بصرياً مع باقي الـcards. الـclassName أزلتها لتفادي قواعد `.book-cta { position:fixed }` الموجودة في styles.css (تركتها لئلا تكسر فئات أخرى مستقبلية). حذفت الـspacer الـ80px اللي كان فوق الـCTA. (٢) **زر تأكيد الحجز في modal "إتمام الحجز" كان مخفياً خلف BottomNav** — `z-index` الـmodal كان 1000، الـBottomNav 1100، فالـBottomNav كان يرسم فوق الزر. **الإصلاح**: رفعت z-index الـmodal إلى 1200، وأضفت `paddingBottom: calc(env(safe-area-inset-bottom) + 28px)` على المحتوى الداخلي + رفعت `maxHeight` من 90vh لـ92vh لإعطاء مساحة أكبر. (٣) **مربع التعليق أسود في الوضع الفاتح + الإشارة لا تظهر**: (٣أ) الـtextarea في `showReviewForm` كان بلا `background` ولا `color` صريحين، فعلى iOS Safari ورث خلفية النظام الافتراضية السوداء. أضفت `background: 'var(--card-bg)'` + `color: 'var(--text-primary)'`. (٣ب) `addRating` في AppContext كان `Promise<void>` يصمت على الفشل (RLS، شبكة، إلخ). `handleReview` ما يـawait. النتيجة: الفورم يُغلق، لا رسالة، التعليق ما يظهر — التاجر يحسبها فاشلة. **الإصلاح**: حولّت `addRating` إلى `Promise<boolean>` — يرجع false لو الـDB رفض، true لو نجح. `handleReview` صار `async` + ينتظر + يعرض "✅ تم إرسال تقييمك — شكراً لمشاركتك!" على النجاح، "❌ تعذّر إرسال التقييم..." على الفشل. زر "إرسال التقييم" صار "⏳ جاري الإرسال..." + disabled أثناء المعالجة. (٤) **القائمة الجانبية: "🏠 الرئيسية" مرتفعة جداً تلامس notch** — كان `paddingTop: env(safe-area-inset-top) + 16px`. **الإصلاح**: زدتها إلى `+ 36px` فيُعطي مسافة طبيعية مثل الـdrawer في تطبيقات iOS الأصلية، يجلس الـheader بمستوى أيقونة البطارية لا تحتها. |
| **v10.66** | **إصلاحان حرجان من ناصر بعد اختبار v10.64**. **(١) التنبيه ما اشتغل على حساب ثاني**: ناصر فعّل تنبيه إلكترونيات + رنج كم على حساب التاجر "تمتومي" لكن ما وصله إشعار. **التشخيص**: اختبرت في DB مباشرة (UPDATE smart_alerts للسيلر) — الـbackfill اشتغل صح وأنتج إشعار سليم بشكل احترافي ("📍 عرض بجانبك تماماً  •  📱 إلكترونيات"). يعني المشكلة في الـclient، لا في الـDB. **السبب الجذري**: `addSmartAlert`/`removeSmartAlert` في `AppContext.tsx` كانوا يستدعون `userRepository.saveProfile(...)` **بدون await** (fire-and-forget). على iOS Safari الـSDK من Supabase يعلق على inTabLock للـauth-refresh لمدة ١٥+ ثانية لما التطبيق يرجع من backgrounding، فـsaveProfile يفشل بـTimeoutError صامت. الـUI أظهر القاعدة (لأن setSmartAlerts ركض)، لكن الـDB ما حفظ شي، فالـtrigger ما عنده قاعدة يطابق ضدها → ولا إشعار. **الإصلاح**: حولّت addSmartAlert/removeSmartAlert إلى async مع `await` + try/catch — على فشل يـrevert الـlocal state ويرجع `false`، على نجاح يرجع `true`. **Profile.tsx handleAdd** صار async ينتظر النتيجة: على فشل يعرض "❌ تعذّر حفظ التنبيه — تحقق من الاتصال وحاول مجدداً"، على نجاح يعرض "✅ تم حفظ التنبيه. سيصلك إشعار فور نزول عرض مطابق (أو أي عرض نشط من آخر ٧ أيام)" — رسالة تشرح للمستخدم سلوك الـbackfill اللي أضفناه في v10.64. زر "إضافة قاعدة تنبيه" صار disabled مع "⏳ جاري الحفظ..." أثناء المعالجة. كذلك زر الـ✕ لحذف قاعدة صار يستخدم `handleRemove` async يعرض رسالة على فشل. **(٢) ضغط مكرر على الكروب → نفس الصورة ٤ مرات**: لما الـupload يبطئ، الـeditor يبقى ظاهراً، فالمستخدم يضغط "تطبيق ومتابعة" أو "تخطّي القص" عدة مرات → الـcallback يتنفّذ ٤ مرات → نفس الـFile يُرفع ٤ مرات. **الإصلاح**: (أ) في `ImageCropEditor.tsx` أضفت `dispatchingRef = useRef(false)` — متغير مرجع قابل للقراءة بشكل sync (بدون انتظار React re-render). أول ضغط يضع `dispatchingRef.current = true`، الضغطات اللاحقة ترجع فوراً. يُعاد ضبطه تلقائياً في `useEffect([file])` عند تقدم الـqueue للصورة التالية. غلّفت `onSkip` و`onCancel` بـ`handleSkip`/`handleCancel` يستخدمان نفس الحارس. (ب) في `SellerDashboard.tsx` غيّرت onApply/onSkip لتقدّم الـqueue **فوراً (sync)** ثم تـupload في الخلفية بدلاً من `await uploadCroppedFile(...)` ثم advance — هذا يخلي الـeditor يـunmount/يتبدّل للصورة التالية فوراً بمجرد الضغط، فما يتاح للمستخدم وقت لإعادة الضغط على نفس الزر. **النتيجة**: ضغطات سريعة متتالية = صورة واحدة فقط تُرفع، وحفظ التنبيه يفشل بصوت عالي وواضح بدلاً من الصمت. حذفت بيانات الاختبار من حساب التاجر (smart_alerts reset + notification deleted) فيـclean state لما يختبر ناصر بنفسه. |
| **v10.65** | **٦ تصنيفات جديدة** بطلب ناصر: 💈 صالون رجالي، 💇‍♀️ صالون نسائي، 🥩 ملحمة، 🧵 خياطة رجالية، 🪡 مشغل نسائي، 🩺 عيادات. **التنفيذ كان في ٤ أماكن لأن قائمة التصنيفات مكرّرة عن قصد** (للأنواع، للسيلر، للبوت، للـDB): (١) `src/data/mock.ts` — أضفت ٦ قيم لـ`Category` union وموقّعتها ثيمياً في `CATEGORIES[]` (الصالونات بعد Beauty، الملحمة بعد Supermarket، الخياطة بعد Laundry، العيادات بعد Pharmacy) عشان المستخدم يلقاها مع الفئات المشابهة. (٢) `src/data/botQuestions.ts` — نفس الـ٦ في قائمتي السيلر (`category`) والمشتري (`buyer_category`) بنفس الـemojis. (٣) `src/pages/DealsList.tsx` — حذف `slice(0, 12)` من شريط الفلاتر؛ كان يخفي أي تصنيف بعد الـ١٢ والآن كل التصنيفات تظهر مع تمرير أفقي (overflow-x: auto موجود أصلاً). (٤) `taki_category_label_ar/_en` في DB — أضفت WHEN clauses للستة الجدد عشان الإشعارات الذكية ترجم IDs لأسماء عربية مع emojis (المنطق في `taki_dispatch_smart_alert` يستدعي هاتين الدالتين مباشرة، فالـ٦ الجدد يشتغلون مع التنبيهات تلقائياً بدون أي تعديل في الترايقر). **النتيجة الفورية**: التاجر يقدر يضيف عرض في أي من الـ٦ تصنيفات، المشتري يقدر يحط أي منها في تنبيهاته الذكية، وإشعار التنبيه يستخدم الاسم العربي مع emoji ('🥩 ملحمة' بدل 'Butcher'). الـ٢٤ تصنيف القديمة تبقى بنفس الـIDs، فما في deals قديمة تأثرت. |
| **v10.64** | **التنبيهات الذكية: دمج كل التطابقات في إشعار واحد + backfill للقواعد الجديدة + تجهيز البوتات**. ناصر اشتكى إن تنبيه "إلكترونيات" ما اشتغل بعد ما فعّله. **التشخيص**: الترايقر `tr_deal_smart_notifications` يشتغل فقط `AFTER INSERT` على deals، فالعروض اللي أُنشئت قبل تفعيل التنبيه ما يُعاد تقييمها. ناصر أيضاً سأل: لو طابقت عدة تنبيهات نفس العرض (متابعة + ٥كم + إلكترونيات)، هل يجي ٣ إشعارات أو واحد؟ **رأيي**: واحد فقط بكل الأسباب مرتّبة بالأولوية — مثل Instagram/TikTok/Booking. **التنفيذ**: (١) **دالة `taki_dispatch_smart_alert(user, deal, geo, skip_existing)` مشتركة** تأخذ زوج واحد (مستخدم، عرض)، تقيّم القواعد (AND داخل القاعدة، OR عبر القواعد) + `followed_merchants`، وتنشئ إشعار واحد مدمج لو في تطابق. (٢) **ترتيب الأولوية في العنوان**: متابعة → نطاق → مول → مدينة → كلمة مفتاحية → تصنيف → منطقة. **chips** بنفس الترتيب في الـbody، محدودة بأعلى ٣ + "+N" لو أكثر. (٣) **تحويل ID التصنيف**: `taki_category_label_ar/_en` يحوّل 'Electronics' → '📱 إلكترونيات'، مع كل ٢٤ تصنيف من `data/mock.ts`. (٤) **شكل الإشعار الجديد (٣ أسطر مقتضبة)**: `{المنتج}  —  خصم X٪` / `🏪 {المتجر}` / `chip1  •  chip2  •  chip3`. مسافات مزدوجة قبل وبعد `•` للتنفس البصري. (٥) **مسافة طبيعية**: < 100 م → "بجانبك تماماً"، < 1 كم → "أقل من 1 كم منك"، ≥ 1 كم → "1.2 كم منك" (يقطع الـtrailing zeros). (٦) **Backfill جديد**: ترايقر `tr_smart_alerts_backfill` على `users AFTER UPDATE OF smart_alerts` يفحص كل قاعدة في `NEW` ضد قواعد `OLD` بـdeep-equality (jsonb_array_elements + NOT EXISTS)؛ لو فيه قاعدة جديدة أو معدّلة، يسحب آخر ٢٠٠ عرض active من آخر ٧ أيام ويعيد `taki_dispatch_smart_alert` مع `skip_existing=TRUE` (يـdedupe ضد الإشعارات القائمة عبر `meta_data.dealId`). الإزالات الصرفة لا تُشغّل replay. (٧) **خطأ مكتشف أثناء الاختبار**: `deals.created_at` نوعه BIGINT (epoch ms) لا timestamptz — الـbackfill كان يفشل بـ"operator does not exist: bigint > timestamp"، صحّحته بحساب `cutoff_ms := EXTRACT(EPOCH FROM NOW() - INTERVAL '7 days') * 1000`. (٨) **تجهيز البوتات (inert)**: أضفت أعمدة `telegram_chat_id`, `whatsapp_chat_id`, `notify_via_telegram`, `notify_via_whatsapp` (default FALSE) على users، + listener جديد في `server/bot.js` يستمع `notifications` INSERT ويرسل لو المستخدم فعّل الـopt-in. الترايقر يـpre-render النص في `meta_data.bot_message_ar/_en` فالبوت يبعثه كما هو بدون escape. **debounce** ٢٠ ثانية لكل user يمنع backfill من إطلاق burst إرسال. (٩) **اختبار حي**: حذف الإشعار القديم لـHbdd، toggle smart_alerts لـ`[]` ورجوع، الترايقر أنتج: title `🔥 تاكي نزّل عرضاً يطابق تنبيهك` + body `Hbdd — خصم 59٪ / 🏪 تاكي / 🔥 متجر تتابعه  •  📍 بجانبك تماماً  •  📱 إلكترونيات` — مطابق تماماً لما طلب ناصر "احترافي جداً جداً". |
| **v10.63** | **شاشة قص الصورة تفتح فوراً بالصورة جاهزة — بلا "صفحة ثانية"**. ناصر كان يشتكي من شاشة "تعذّر عرض الصورة" تطلع بعد ما يضغط Use Photo في كاميرا الآيفون، وبعدها أوضح: "اريد الكروب يظهر لحظة التقاط الصوره مباشره في نفس صفحة use photo وليس كفلتر ثاني وصفحة ثانيه". **السبب الجذري**: `ImageCropEditor` كان يفتح أولاً، وبعدها يطلب `URL.createObjectURL(file)` ويـmount الـ`<img>` وينتظر `onLoad` ليعرف `naturalWidth/Height`. على بعض ملفات iOS (HEIC variants أو race مع revoke الـobject-URL القديم لما القائمة تتقدّم) كان `onLoad` يطلق بـ`naturalWidth=0` → `setLoadFailed(true)` → شاشة الخطأ. وحتى لما تشتغل، حالة "⏳ جاري تحضير الصورة..." + fade-in opacity 0→1 كانت تخلّيها تحس صفحة منفصلة تأتي بعد. **الحل**: نقل decode الصور إلى مرحلة الـingestion في `SellerDashboard` نفسها — `preDecodeFile()` يقرأ الـFile كـdata URL بـFileReader ثم يفك تشفيرها بـ`new Image()` ويتأكد أن `naturalWidth > 0`. ينجح → يُدخل عنصر `{file, dataUrl, naturalW, naturalH}` في `cropQueue`. يفشل → يرفع الأصل مباشرة عبر `uploadCroppedFile(f)` بدلاً من عرض شاشة خطأ. **النتيجة**: `ImageCropEditor` صار يستقبل الـdata URL + الأبعاد كـprops، يرسم الصورة + إطار القص + المقابض على **أول paint** بدون أي state تحميل ولا fade-in ولا fallback خطأ. قلّصت الكود ~175 سطر (حذف load-state machine بالكامل). إضافة بسيطة: `useLayoutEffect` للقياس الأولي للـstage بدل `useEffect` يلغي flicker واحد frame كانت تنطّ فيه المقابض. الكود الآن من ناحية المستخدم: Use Photo → تظهر شاشة سوداء بالصورة الكاملة + إطار قص + 4 مقابض جاهزة للسحب فوراً، إحساس استمرار للـUse Photo لا صفحة ثانية. |
| **v10.62** | **إعادة كتابة كاملة لـCrop Editor** بناءً على تشخيص ناصر: (أ) الصورة ما تظهر بعد التقاطها من كاميرا الآيفون — الشاشة تطلع سوداء فيها إطار القص فقط بدون صورة. (ب) أزرار النسب مربكة و16:9 شكلها معكوس على شاشة بورتريه. (ج) ناصر طلب: احذف أزرار النسب وخلّ القص حرّ بالكامل. **السبب الجذري للمشكلة (أ)**: كان `<img>` في v10.60 يُعرَض بـ`width: auto` قبل ما `onLoad` يضبط `imgNatural` — يعني الصورة تطلع بحجمها الطبيعي (4032×3024 على iPhone) للحظة، تمتد آلاف البكسلات خارج المسرح، والـcrop frame مع الـbox-shadow أسود في كل مكان فيغطّيها. **التنفيذ الجديد**: (١) تغيير نموذج التفاعل من "إطار ثابت + pinch/pan للصورة" (انستغرام) إلى "صورة fit-to-stage + إطار قابل للسحب والتغيير" (مثل تطبيق الصور في الآيفون) — تجربة مألوفة وما عندها bug الـauto-size. (٢) **`fitScale`** يُحسب من `Math.min(stageW/visualNW, stageH/visualNH)` بحيث الصورة دائماً تنطبق داخل المسرح؛ width/height تستخدم `imgNatural.w * fitScale` بدل auto، فما فيه frame واحد قبل الـonLoad. (٣) إطار قص (`crop = {x, y, w, h}` بإحداثيات display-px) يبدأ بـ90% مركزي من الصورة المعروضة. (٤) تفاعلات: touch/mouse على جسم الإطار = move (مع clamp داخل الصورة)، touch/mouse على أي زاوية (radius 36px) = resize من تلك الزاوية بحرّية تامة (لا aspect lock، حد أدنى 60px). (٥) ٤ مقابض دائرية بيضاء على الزوايا — visual cue واضح. (٦) Rule-of-thirds guides داخل الإطار. (٧) **حُذفت جميع أزرار النسب** — رد على طلب ناصر "احذف المقاسات وخلّ الكروب حرّ فقط". (٨) **معالجة أخطاء**: حالة تحميل ("جاري تحضير الصورة..."), حالة فشل decode (يعرض رسالة + زر "رفع بدون قص" + إلغاء). (٩) زر التدوير ↻ ما يزال موجود (يدوّر الصورة بـ90° steps، displayBox يتحدث تلقائياً). (١٠) **Export** = نفس النموذج المُجرّب: temp canvas للـrotation ثم crop من نطاق المعروض بمقياس `fitScale`؛ output capped at 1600px JPEG q=0.9. |
| **v10.61** | **HOTFIX: لوحة التاجر كانت تتعطّل بـReferenceError on load** — ناصر شاف `ReferenceError: Cannot access 'tC' before initialization` (tC هو `locNameOf` بعد minification). السبب: في v10.59 أضفت `mergedLocationChips = useMemo(...)` يستدعي `locNameOf(d)` داخل factory الـuseMemo، لكن `locNameOf` كان مُعرّف **بعد** الـuseMemo في الـsource. الـuseMemo factory يُنفّذ فوراً عند أول render، فيقع في Temporal Dead Zone (TDZ) لأن `const locNameOf = ...` لم يُهيّأ بعد. أي تاجر يدخل لوحته كان يحصل على ErrorBoundary بدل الفورم. **الإصلاح**: نقلت تعريف `locNameOf` لقبل `mergedLocationChips`. تم التأكد بأن لا توجد TDZ ثانية في الـuseMemos الأخرى (`activeLocationsList`/`activeLocationKeys` تستدعي `locKeyOf` المُعرّف قبلها). |
| **v10.60** | **٣ إصلاحات** بناءً على بلاغ ناصر. (١) **تعذّر حفظ العرض: malformed array literal "تصنيفك"** — الـtrigger `handle_deal_smart_notifications` كان يفشل على أي عرض يطابق smart-alert فيه `categories` (أو region/city/mall/keyword/coord). السبب: `rule_parts := rule_parts || 'تصنيفك'` هو `text[] || unknown`، وPostgres عنده overloads متعددة (`anyarray || anyelement` و `anyarray || anyarray`) — مع right operand string literal بدون cast، الـplanner يقدر يختار overload الـarray ويحاول يـparse 'تصنيفك' كـtext[] literal (لازم يبدأ بـ`{`) → boom 22P02. **الإصلاح**: cast صريح `::TEXT` على كل الـ6 instances في الـfunction (regions/cities/malls/categories/keywords/coords) عبر migration `fix_handle_deal_smart_notifications_array_concat_cast`. تم التحقق بإعادة إنشاء deal Electronics في منطقة مطابقة — INSERT نجح بدلاً من الفشل. (٢) **swipe بين صور العرض داخل الـlightbox** — في `ImageZoomViewer` (DealDetails)، أضفت swipe-to-navigate بأسلوب Instagram: لما الصورة بـ scale 1، الـsingle-finger drag الأفقي يحرّك الصورة مع الإصبع ويـsnap للصورة التالية/السابقة عند الإفلات إذا تجاوز commit threshold (`max(60px, viewport*0.22)`) أو سرعة الإفلات > 0.6 px/ms. axis lock بعد 8 px منع التفعيل العفوي للـvertical scrolls. عكس الاتجاه في RTL يطابق ترتيب القراءة العربي. الـtransform بيتأخّر بـ`transition: none` أثناء الـswipe ويرجع `0.18s cubic` عند الإفلات للـspring-back. الـpinch-zoom + pan لما scale > 1 يلغي الـswipe (الـpriority للـzoom). (٣) **Crop editor جديد بعد التصوير/الاختيار** — مكوّن `ImageCropEditor.tsx` modal full-screen يفتح لكل صورة جاية من picker/camera/drag-drop/clipboard في `ingestFiles`. UX: قص بأربع نسب (1:1 افتراضي + 4:3 + 16:9 + حر)، تدوير ↻ بـ90°، pinch-zoom + pan داخل الـframe، rule-of-thirds guides داخل الـcrop window، dark overlay خارجها. الـExport عبر 2-step canvas: (أ) رسم rotated image على temp canvas upright، (ب) crop من الـtemp إلى output canvas. السقف 1600px على الجانب الأطول + JPEG quality 0.9 — يقلّص متوسط الصورة من ~4MB لـ~300KB بدون فقدان جودة ملحوظ. أزرار "تخطّي القص" (يرفع الأصل بدون تعديل) و"إلغاء" (يحذف الصورة من الـqueue). دعم batch — لو اختار 3 صور دفعة واحدة، يفتح الـeditor 3 مرات مع counter "1/3". الـ4-image cap محترم في الـqueue. حُلّ bug ثانوي: input file `e.target.value = ''` بعد كل اختيار حتى يقدر يختار نفس الصورة مرتين متتاليتين. |
| **v10.59** | **اللوكيشن السابق صار DB-backed مع حذف**. ناصر طلب: تأكد كل شي مربوط بالـDB، اللوكيشن السابق لو اضاف لوكيشنين تظهر، ويستطيع الحذف، والتثبيت/الحفظ/الحذف كلها في الـDB. **التنفيذ**: (١) **`branchRepository.ts` جديد** يلف `store_branches` table الموجودة أصلاً في الـDB (محمية بـRLS: select=public، insert/update/delete=own merchant_id فقط، admin bypass) — `listByMerchant`/`upsert`/`remove`. (٢) **AppContext** يحمّل branches للـsellers/admins على تسجيل الدخول (مع clear على SIGNED_OUT/account-switch)، يعرّض `branches` + `saveBranch` + `removeBranch` بـoptimistic updates ورجوع تلقائي على فشل الـDB delete. (٣) **`mergedLocationChips`** يدمج branches المحفوظة في الـDB مع لوكيشنات العروض النشطة (للـbackward-compat) + يـdedupe بنفس `locKeyOf`؛ branches تأخذ الأولوية فعندها `branchId` يفعّل زر ✕. (٤) **chip UI** صار بـ2 buttons داخل pill واحد — جسم الـchip يتبنّى اللوكيشن، و✕ منفصل بفاصل عمودي يحذف بعد customConfirm. (٥) **auto-save branch عند submit عرض**: بعد ما addDeal/updateDeal ينجح، يـupsert الـlocation كـbranch إن مو موجودة أصلاً (نفس الـlocKey dedupe) — fire-and-forget عشان فشل الـbranch upsert ما يفشل العرض. (٦) **`saveShopLocation`** صار يـmirror الموقع كـbranch بـ`is_primary=true`؛ إعادة الحفظ تحدّث نفس الصف (بدل ما تـcreate صف جديد كل مرة). **النتيجة**: التاجر يقدر الحين يحفظ عدة لوكيشنات (deals × N + primary shop)، يضغط chip ليتبنّى لوكيشن سابق فوراً، ويضغط ✕ ليحذف من الـDB — كله بـRLS سليم وحماية للمالك فقط. |
| **v10.58** | **إصلاحان** بناءً على لقطتي ناصر. (١) **زر الحجز كان مخفياً على صفحة المنتج**: المشتري كان يشوف صف "الكمية -/+" فقط فوق الـBottomNav بدون أي زر "🎟️ احجز الآن". السبب: قاعدة CSS قديمة `@supports (height: 100dvh) { .book-cta { bottom: 0 } }` في `styles.css:1581` (أُضيفت قبل v10.33 لأجل keyboard handling) كانت تُعيد `.book-cta` إلى bottom: 0 على كل المتصفحات الحديثة، فيغطّيه `.bottom-nav` (z-index 1100) كلياً — لكن لأن الكمية في **أعلى** الـCTA flex column والزر في **أسفله**، الكمية تطلع فوق الـBottomNav والزر يبقى مدفون تحته. الإصلاح: حذف الـ@supports block بالكامل — القاعدة الأساسية `bottom: calc(env(safe-area-inset-bottom, 0px) + 60px)` تتعامل مع الـdynamic viewport بدون أي override (الـbrowsers الحديثة تربط `bottom` بحافة الـvisual viewport السفلية تلقائياً عند ظهور الكيبورد). (٢) **زر "لوكيشن سابق" في إضافة عرض**: ناصر أراد فوق التلميح المُحاط بالأحمر في الصورة (تلميح "اضغط على الخريطة مباشرة") خانة تعرض لوكيشناته المُستخدَمة سابقاً كـchips، وعند الضغط على chip يملأ التطبيق منطقة + مدينة + نوع المكان + إحداثيات الدبوس تلقائياً ويحدّث الخريطة والفلاتر بضغطة واحدة. **التنفيذ**: (أ) قسم جديد قبل تلميح الخريطة في `SellerDashboard.tsx` يظهر فقط لو `activeLocationsList.length > 0` وغير مخفي بسبب banner تجديد اللوكيشن المحذوف (لتفادي التكرار). (ب) chips دائرية بـborder رمادي تتلوّن بـprimary لما تكون الـchip هي اللوكيشن الحالي المُختار في الفورم. (ج) **حسّنت `adoptLocationFromDeal`** ليضبط `locationType` أيضاً (يقرأ `LOCATIONS[locationId].type` للأماكن المعروفة، أو 'other' للوكيشن المخصص) — قبلها كان dropdown نوع المكان لا يتحدث مع الـchip فيضطر التاجر يغيّره يدوياً. الـchips تستخدم نفس مصدر بيانات الـ"الباقة تسمح بـ3 مواقع — المستخدم حالياً X/3" الموجود فوق الفورم، فما فيها duplication state. |
| **v10.57** | **إصلاح "الزر يلف للأبد"** عند حفظ موقع المتجر وعند إضافة العرض. السبب الحقيقي ما كان الـtimeout الخارجي (كان 30s/40s منذ v10.55 — كافٍ على شبكة سليمة)، بل **awaits داخلية بلا سقف** على `supabase.from(...).upsert(...)` نفسها. عندما تعلق الـSupabase JS SDK على inTabLock للـauth-refresh (يحصل خصوصاً على iOS Safari في الـbackground)، الـpromise الداخلية لا تنتهي أبداً، فالـ`withTimeout` الخارجي ينتظر زمنه الكامل بدون أي feedback خلال تلك المدة. **الإصلاحات**: (١) **`userRepository.saveProfile`** غلّفت الـupsert بـ`withTimeout(..., 15s)` داخلياً. (٢) **`dealRepository.save`** غلّفت كل من الـ3 upsert paths (الرئيسي + الـlegacy fallback + الـnoGeo fallback) بـ`withTimeout(..., 25s)` — الـDeal triggers (smart-notifications + location-cap + guard) قد تأخذ أطول قليلاً من user upsert. (٣) **حذف الـpre-sync** في `addDeal`: كان يستدعي `userRepository.saveProfile(user)` قبل insert الـdeal — مكرر بلا فائدة (handle_new_user trigger ينشئ الـuser row عند signup أصلاً)، ويأكل نصف الـtimeout budget على lock عالق. الـDB سيرمي FK error واضح في الحالة النادرة فعلاً اللي يفقد فيها الـrow. النتيجة: زر "حفظ كموقع دائم" وزر "حفظ وإضافة العرض" يفشلان بسرعة بدلاً من اللف للأبد، والمستخدم يحصل على رسالة قابلة للعمل بدلاً من spinner أبدي. |
| **v10.56** | **تنظيف البيانات الوهمية** بطلب ناصر. (١) **DB**: حُذفت كل العروض الـ5 التجريبية اللي كانت في `public.deals` (تيشرتات، الهلال، مغسلة سيارات، شاورما، منظف عفن) — كلها أنشئها الأدمن للاختبار + كانت كلها expired/paused قبل ما تُمسح. حُذفت أيضاً الجداول المرتبطة (`booking_messages`, `bookings`, `store_analytics_events`) لكل تلك العروض. الـDB الآن صفر deals — جاهزة للاستخدام الحقيقي. (٢) **Code**: حُذف `MOCK_DEALS` array من `src/data/mock.ts` (dead code من قبل v10.47 لما الـapp صارت DB-first). 0 مراجع له في باقي الكود فالحذف آمن. **النتيجة**: الصفحة الرئيسية الآن فاضية شرعياً، تنتظر عروض حقيقية من تجار حقيقيين. لما ناصر أو أي تاجر يضيف عرض من لوحة التاجر، يظهر فوراً. |
| **v10.55** | **٣ إصلاحات** بناءً على بلاغ ناصر. (١) **رابط التحقق كان يفتح localhost:3000**: `signUpWithEmail` ما يمرر `emailRedirectTo`، فالـemail link يستخدم Site URL الافتراضي = `localhost:3000`. الإصلاح: تمرير `emailRedirectTo: window.location.origin` في `signUp` options. الآن الرابط يفتح `https://taki-test-eight.vercel.app/...` تلقائياً. ⚠️ يحتاج إضافة الـURL إلى Supabase Redirect-URL allowlist (انظر تعليمات الـDashboard أدناه). (٢) **السعر العشري ممنوع** كان `.replace(/\D/g, '')` يفلتر النقطة فيمنع الفواصل (16.50 ر.س). الإصلاح: helper جديد `sanitizeDecimalInput` في `helpers.ts` يسمح بالأرقام + نقطة واحدة + سقف خانتين عشريتين (للهللات). type="text" inputMode="decimal" بدل type="tel". DB column كان `numeric` أصلاً (يدعم decimal بدون migration). (٣) **"تأخر الحفظ" تظهر فوراً**: الـtimeouts كانت 15s/20s — قصيرة جداً لحفظ deal يطلق `tr_deal_smart_notifications` + `tr_enforce_location_cap` مع RTT من AP-NE1 إلى السعودية. رفعتها: profile save 15s→30s، deal save 20s→40s. الـtimeout ما يزال موجود (لا يعلق spinner للأبد) لكن نادراً ما يطلق على network سليم. **Email-with-code vs link**: شاشة verify الموجودة في Register أصلاً تقبل كود 6 أرقام عبر `verifyOtp` RPC، لكن الـemail template الافتراضي يحتوي رابط فقط. تعديل التيمبليت ليضمن `{{ .Token }}` يحتاج Supabase Dashboard. |
| **v10.54** | **إصلاح OAuth UX** — في v10.53 الزرين كانا يظهران دائماً، ولما يضغطهم المستخدم بدون تكوين Google Cloud + Supabase Dashboard، الـbrowser كان ينتقل لصفحة `supabase.co` بـJSON: `Unsupported provider: provider is not enabled` ويترك التطبيق! الإصلاح: (١) **`skipBrowserRedirect: true`** في `signInWithGoogle/Apple` — يخلي الـcaller يمسك الـerror قبل ما يحدث redirect. (٢) **`handleOAuthClick(provider)` wrapper في Register**: يستدعي الـauth method ويتحقق من الـerror؛ لو `not enabled` يعرض customAlert عربي ودود: "⚠️ تسجيل الدخول عبر Google غير مفعّل حالياً. استخدم البريد وكلمة المرور." بدون أن يفقد المستخدم سياقه. (٣) **Feature flags في `platform_settings`** — صفّان جديدان `oauth_google_enabled` و `oauth_apple_enabled` (افتراضياً false). AppContext يقرأهما + يستمع للـrealtime تحديثات. (٤) **رندر شرطي**: الزرين والفاصل "أو" يظهرون فقط لو الـflag مفعّل. الآن الصفحة تبدأ بـForm نظيف بدون أزرار معطّلة. لتشغيل Google/Apple فعلياً: ناصر يخبر Claude "فعّل Google" بعد ضبط Google Cloud + Supabase Dashboard، Claude يرفع الـflag بـUPDATE واحد، الزر يظهر فوراً عبر realtime في كل الـclients. |
| **v10.53** | **تسجيل دخول Google + شاشة "أكمل بياناتك" + إصلاح الفضاء الأبيض** على صفحة Register. (١) **زر Google**: `authService.signInWithGoogle()` يستدعي Supabase OAuth بـprovider `google` مع `access_type=offline` للـrefresh token. زر أبيض بـlogo Google الرسمي (SVG inline) فوق زر Apple. (٢) **شاشة CompleteProfile جديدة** `src/pages/CompleteProfile.tsx` تظهر للمستخدمين القادمين من OAuth (Google/Apple) لأنهم لا يعطون phone/user_type/shop. تطلب: الاسم (prefilled من Google)، نوع الحساب (مشتري/تاجر بـcards مرئية)، رقم الجوال (مع validation + uniqueness check)، اسم المحل (إذا تاجر). تحفظ via `updateProfile` (partial-aware) + `supabase.auth.updateUser` لمزامنة metadata. (٣) **AuthRedirector الجديد** يفحص بعد OAuth callback إذا profile incomplete (phone فاضي أو seller بدون shop) ويوجّه لـ`/complete-profile` بدلاً من landing page. الـemail/password signup لا يتأثر لأنه يجمع كل البيانات في Register أصلاً. (٤) **إصلاح الفضاء الأبيض**: iOS Safari rubber-band overscroll كان يكشف `--body-bg` الفاتح (#f1f5f9) خلف الـgradient navy. الإصلاح: useEffect في Register + CompleteProfile يضع `documentElement.style.background = '#050a18'` ويسترجع عند unmount. **⚠️ لتفعيل Google OAuth فعلياً**: ناصر يحتاج (أ) إنشاء OAuth Client ID في Google Cloud Console، (ب) تفعيل Google provider في Supabase Dashboard → Auth → Providers مع الـclient ID/secret. الكود جاهز لكن الزر سيرجع "provider not enabled" حتى يتم التكوين. |
| **v10.52** | **تحصين أمني — جولة ثانية** فحص شامل للـclient + البوتات + headers. **Client side**: تأكيد 0 XSS vectors (لا `dangerouslySetInnerHTML`, لا `eval`, لا `document.write`)، 0 secrets في localStorage (فقط UI cache + Supabase auth tokens المعيارية)، كل CORS proxies في الـCSP مستخدمة فعلاً (fallback chain لـmaps.app.goo.gl). **bot.js — 3 ثغرات حرجة اتصلحت**: (١) WhatsApp HMAC كان يـsignature على `JSON.stringify(req.body)` بعد ما Express parsed الـbody — أي تغيير في whitespace/key-order يفشل المطابقة. الإصلاح: `express.raw({ type: 'application/json' })` على `/webhook/whatsapp` فقط، والـHMAC يُحسب على الـraw bytes الفعلية. (٢) لو `WHATSAPP_APP_SECRET` فاضي، الـsignature check كان **يُتجاوز كلياً** — أي attacker يقدر يرسل POST مزيف. الآن hard-reject بـ503. نفس الإصلاح طُبّق على `/webhook/telegram` مع `TELEGRAM_WEBHOOK_SECRET`. (٣) أُضيف **global rate-limit middleware** (300 req/5min/IP) فوق الـper-chat limits الموجودة. **Headers/CSP**: تأكيد HSTS 2 سنة + preload، X-Frame-Options: DENY، COOP/CORP same-origin، Permissions-Policy محدّد، CSP صارم بـscript-src 'self' وframe-ancestors 'none'. **HIBP**: تبيّن أن الـorganization على **free plan** و"Leaked Password Protection" حصرية لـPro ($25/شهر) — لكن validation الـclient (8 chars + uppercase + lowercase + digit + symbol) أقوى من متطلبات HIBP الافتراضية، فالـwarning compensating-control موجود. |
| **v10.51** | **تحصين أمني شامل** بعد تنبيه Supabase advisor (143 ثغرة). أهم الإصلاحات: (١) **ERROR حرج**: `sa_cities_geo` كان RLS مغلق — أي anon يقدر يحذف 73 صف مدن السعودية ويعطّل `find_nearest_sa_city`. تم enable RLS + قراءة عامة + كتابة admin-only. (٢) **booking_messages_update_recipient** كانت `WITH CHECK = true` — الطرف المستلم يقدر يعيد كتابة sender_role/content/barcode لأي رسالة! حُذفت كلياً (الـRPC `mark_booking_messages_read` SECURITY DEFINER هو الكاتب الوحيد). (٣) **store_analytics_events.an_insert_anyone** سمحت لـanon بحقن أحداث analytics — حُذفت (كل الكتابات تمر عبر RPCs SECURITY DEFINER). (٤) **storage 'deals'** كان أي anon يقدر يرفع أي ملف بأي حجم — قيّدتها لـauthenticated + صور فقط (JPEG/PNG/WebP/GIF) + سقف 10MB + منع الـlisting، مع الحفاظ على CDN reads. (٥) **16 function بـsearch_path قابل للتغيير** (vector لـsearch_path injection) — pin `SET search_path = 'public'`. (٦) **6 functions cron-only بدون admin check** (`purge_expired_accounts`, `expire_trials`, `cleanup_old_activity`, `prune_analytics_events`, `send_trial_ending_notifications`, `send_trial_warnings`) — أي مستخدم authenticated كان يقدر يستدعيها ويحذف الحسابات أو يفرض انتهاء الفترات التجريبية! `REVOKE EXECUTE FROM anon, authenticated, PUBLIC`. الـ7 admin_* functions تركت لأنها فعلاً تحقق `user_type='admin'` داخلياً. **النتيجة:** 143 → 111 (-32)، 1 ERROR → **0**، 16 search_path → 0، 2 always-true RLS → 0، 1 bucket listing → 0. المتبقي 110 SECURITY DEFINER warnings مقصودة (RPCs للـclient كلها فيها admin checks داخلية) + 1 تحتاج Dashboard (تفعيل Leaked Password Protection). ملف الـmigration: `supabase/migration_v10_51_security_hardening.sql`. |
| **v10.50** | إصلاح ٣ مشاكل في صفحة إنشاء الحساب الجديد. (١) **Database error saving new user** عند كل محاولة تسجيل: السبب trigger `handle_new_seller_trial` كان يحاول `UPDATE users SET trial_starts_at, trial_ends_at` لكن الأعمدة **غير موجودة** في `public.users` — كل صف auth.users جديد كان يفشل بـ `SQLSTATE 42703`. الإصلاح: حذف الـUPDATE المُكرّر (التعليق نفسه يقول "redundant trial tracking"؛ `store_profiles.subscription_started_at/expires_at` هي المصدر الموثوق). (٢) **ألوان غامقة على خلفية غامقة** في checklist قوة كلمة المرور — `var(--accent)` يحُل لـ`#1e293b` (slate-800 شبه أسود) على gradient navy فأصبح غير مقروء. الإصلاح: استبدال صريح بألوان فاتحة (`#10b981` للمحقّق، `#94a3b8` لغير المحقّق) في الـchecklist وفي شاشة verify أيضاً (الإيميل + رسالة "افتح بريدك"). (٣) **حذف حقل "موقع المتجر (المدينة/الحي)"** من نموذج تسجيل التاجر — موقع المتجر الفعلي يُحدد لاحقاً من داخل التطبيق عبر اللوكيشن (نظام v10.42-v10.46 للوكيشنات الثلاث). أُستبدِل بسطر توضيحي صغير "📍 سيتم تحديد موقع المتجر لاحقاً من داخل التطبيق". |
| **v10.49** | "معاينة كمشتري" في حساب الأدمن صارت فعلياً تدخّله كمشتري. كان الأدمن لمّا يضغط 🛒 مشتري من الشريط الجانبي، يروح للصفحة الرئيسية بس الـBottomNav يظل بشريط التاجر (لوحتي/صفحتي) وصفحة "حسابي" تعرض شارة "مدير النظام 👑" وقسم بيانات المتجر. السبب: `BottomNav` كان يقرأ `user.userType === 'admin'` مباشرة (يتجاهل `effectiveUserType`)، وصفحة Profile كذلك. الإصلاح: (١) `BottomNav.tsx` صار يعتمد فقط على `effectiveUserType` — `showSellerNav = (effectiveUserType === 'seller' \|\| 'admin')`. (٢) `Profile.tsx` صار يقرأ `effectiveUserType` لكل فروع العرض (اسم/شارة الدور، الإحصائيات بائع/مشتري، قسم "معلومات التواصل للمتجر"). نتيجة: الأدمن في وضع معاينة كمشتري يشوف شريط المشتري كاملاً (الرئيسية/الإشعارات/حولي/حجوزاتي/حسابي) + شارة "مشتري ⭐" + إحصائيات التوفير، تماماً مثل ما يصير لمّا يضغط معاينة كتاجر. |
| **v10.48** | ثلاث مشاكل في نموذج إضافة العرض على الجوال: (١) **الخريطة لا تتحرك** بعد لصق رابط قوقل ماب رغم استجابة فلتر المنطقة/المدينة. السبب: `MapCenterUpdater` كان يستدعي `setView` بـ`animate:true, duration:0.4` داخل `setTimeout(0)` — على iOS Safari عند فتح modal نجاح فوق الخريطة، `invalidateSize()` يقيس tile-grid بصفر ارتفاع فينقطع الـpan. الإصلاح: pan فوري بـ`animate:false` ثم إعادة `invalidateSize+setView` بعد 300ms كـsafety net، كله داخل try/catch. (٢) **زر "حفظ كموقع دائم للمتجر" يعلّق spinner** للأبد. السبب: `await updateProfile()` بدون أي سقف زمني — لو الـSupabase auth-refresh عالق في الـinTabLock، الـawait ينتظر للأبد. الإصلاح: `withTimeout(updateProfile(...), 15000)` + رسالة "⏱️ تأخر الحفظ، تأكد من اتصال الإنترنت" عند تجاوز السقف. (٣) **نفس المشكلة في "حفظ وإضافة العرض" و"إضافة وتكرار"** — `withTimeout(addDeal/updateDeal, 20000)` + رسالة timeout واضحة. الـhelper `withTimeout` + `TimeoutError` مضافين في `utils/helpers.ts` لإعادة الاستخدام. |
| **v10.47** | **DB-first فعلياً** — إزالة أي تخزين تطبيقي محلي. (١) `addDeal` و `updateDeal` ينتظران الـDB قبل أي تحديث على الـUI (لا optimistic update). إذا الـDB رفض → رسالة فشل واضحة + لا يظهر شيء في الواجهة. (٢) إزالة رسالة "تم الحفظ محلياً لكن المزامنة فشلت" نهائياً — رسالة كاذبة لأنه لا يُحفظ شيء فعلياً عند فشل الـDB. (٣) إزالة `TAKI_LAST_PROMO_*` من localStorage — الـDB column `users.last_promo_check_at` هو المصدر الوحيد للـthrottle. (٤) إزالة `TAKI_LAST_PATH` من localStorage في App.tsx — المسار بعد تسجيل الدخول يُحدد من `user.userType` فقط. (٥) إزالة `TAKI_SEASONAL_NOTIFY` — جدول `push_subscriptions` هو الـopt-in الحقيقي. **يبقى ضرورة تقنية:** Supabase auth tokens (لا يمكن إزالتها) و SW versioning. |
| **v10.46** | فرض حد 3 لوكيشنات على مستوى **قاعدة البيانات** (كان client-only). trigger جديد `tr_enforce_location_cap` على deals يطلق BEFORE INSERT/UPDATE OF (status, location_id, map_lat, map_lng) — يحسب الـlocKey بنفس طريقة الـclient (loc:<id> أو geo:<lat>,<lng> مدور 3 منازل) ويرفض بـ `LOCATION_LIMIT_EXCEEDED` إذا تجاوز. admin يتجاوز. status≠active لا يخضع. `addDeal` و `updateDeal` في AppContext يكشفان الـerror code ويعرضان رسالة عربية محددة (بدل "تم الحفظ محلياً لكن المزامنة فشلت"). ملف migration: `supabase/migration_v10_46_enforce_seller_location_cap.sql`. |
| **v10.45** | إضافة عبارة "تم تغيير موقعك السابق" في banner تجديد العرض المنتهي (v10.44). النص الكامل صار: "انتهت كل عروض موقعه السابق فحُذفت الخانة. تم تغيير موقعك السابق — اختر أحد مواقعك الحالية الـ3 لتجديد هذا العرض:". لا تغيير في الـlogic. |
| **v10.44** | حالة خاصة على قاعدة v10.43 — تجديد عرض منتهي بلوكيشن "محذوف" (بمعنى: انتهت كل عروض الموقع وتحررت الخانة قبل ما يحاول التاجر التجديد). الكشف: editing deal بـstatus غير active، locKey الأصلي ليس في activeKeys، و activeKeys.size = 3. النتيجة: banner برتقالي يظهر فوق قسم الموقع برسالة "تم حذف لوكيشن العرض السابق" + chips قابلة للضغط للمواقع الـ3 الحالية. ضغطة على chip → adoptLocationFromDeal يضبط locationId/region/city/mapPos تلقائياً وينظف googleMapsLink. |
| **v10.43** | تعديل قاعدة v10.42 — التعديل يخضع للحد أيضاً. الـ3 لوكيشنات قيد ثابت على كل العمليات (إضافة + تعديل). نقل منتج موجود لـلوكيشن 4 جديد **ممنوع** الآن — التاجر يجب يحذف كل منتجات أحد مواقعه أولاً لتفريغ خانة. counter مرئي حتى في الـedit mode. زر "حفظ التعديل فقط" يصير معطّل عند wouldExceedLimit. admin بدون قيود (كما كان). |
| **v10.42** | حد أقصى 3 لوكيشنات فريدة لمنتجات التاجر النشطة في الباقة الأساسية. الحساب: locationId مفهرس (مول/سوق) أو إحداثيات مدوّرة لـ3 منازل (~110م) للدبابيس المخصصة. **التعديل** على منتج موجود حر بالكامل (يقدر التاجر ينقل أي منتج لأي مكان حتى لو أنتج 4 لوكيشنات). **الإضافة** الجديدة في لوكيشن 4+ مرفوضة مع رسالة وزر "إدارة الاشتراك". counter مرئي في النموذج "X / 3 مواقع" مع تلوين أصفر/أحمر. admin يتجاوز القاعدة. آلية الـupgrade لباقة أعلى موجودة في backlog. |
| **v10.41** | ثلاث مشاكل مترابطة: (١) **DB**: فشل مزامنة المنتج مع السيرفر `new row violates RLS for table notifications` — الـtrigger `handle_deal_smart_notifications` كان يحاول إدراج إشعارات للمتابعين بصلاحيات الـseller بدل صلاحيات النظام. الإصلاح: `SECURITY DEFINER` على الـfunction. ملف migration: `supabase/migration_v10_41_smart_notifications_security_definer.sql`. (٢) **علم أوكرانيا**: Leaflet 1.9+ يحقن العلم في الـattribution badge؛ أُضيف `attributionControl={false}` على كل MapContainer (SellerDashboard + Nearby). (٣) **الخريطة لا تتمركز** بعد حلّ الرابط/الفلتر: `MapCenterUpdater` يستدعي `flyTo` على tile-grid قديم. أُضيف `invalidateSize()` قبل `setView` ليُعيد Leaflet حساب الـviewport. |
| **v10.40** | لوحة البائع: تبويب "إضافة +" كان يفتح صفحة "السكانر" بدل نموذج الإضافة. السبب: فحص الاشتراك كان يقرأ من `storeProfiles.subscription_expires_at` (حقل قديم لا يُحدّث للتجار في فترة التجربة ولا للـadmin) → `isSubscriptionValid=false` → الـform يسقط للـscanner fallback. الإصلاح: (١) admin يتجاوز فحص الاشتراك دائماً. (٢) المصدر الحقيقي للاشتراك صار `merchant_subscriptions` (status + trial_ends_at + current_period_end). (٣) لو فعلاً لا اشتراك للتاجر، تظهر لوحة "الاشتراك مطلوب" مع زر إدارة الاشتراك بدل صفحة السكانر. |
| **v10.39** | **DB fix حرج** — سياستا RLS على جدول users (`users_select_all` و `users_update_admin`) كانتا تستخدمان `EXISTS (SELECT FROM users ...)` داخل تعريفهما → infinite recursion عند أي UPDATE/UPSERT على users. النتيجة: `saveProfile` يفشل بصمت → "حفظ كموقع دائم" + "إضافة وتكرار" + addDeal trigger smart_notifications كلها معطلة. الإصلاح: استبدال الـsubquery بـ `is_admin()` SECURITY DEFINER. ملف الـmigration: `supabase/migration_v10_38_fix_users_rls_infinite_recursion.sql`. |
| **v10.38** | نموذج إضافة العرض — تعليق spinner عند فشل رابط Google Maps: (١) `handleMapLinkUpdate` يسجّل المحاولة في `lastResolvedLink` حتى عند الفشل، فالضغطات التالية على Save/حفظ كموقع دائم لا تكرر الـresolution. (٢) إخفاء تنبيه الفشل عن الـauto-resolver (يبقى فقط لو ضغط المستخدم "تحديد" يدوياً). (٣) re-entrance guard (Ref) يمنع تشغيل resolution موازية. (٤) spinner منفصل لكل زر (`submitMode` state) — لا يدور الزرّان معاً. (٥) زر "حفظ كموقع دائم" له spinner مستقل ولا ينتظر أكثر من 2s. |
| **v10.37** | نموذج إضافة العرض: (١) تنبيه inline أحمر فوري + حدود حمراء + تعطيل أزرار الحفظ لو `discountedPrice >= originalPrice`. (٢) زر "حفظ كموقع دائم" يحل رابط Google Maps أولاً (cap 3s) قبل الحفظ، فلا يحفظ الإحداثي الافتراضي. (٣) submitAction: نقل الـvalidation السريع قبل بدء الـspinner + تقليل link-resolution timeout من 5s إلى 3s + spinner داخل "إضافة وتكرار" أيضاً — لا تعليق ولا انتظار 5 ثوان للـvalidation. |
| **v10.36** | StoreDetails: العرض الزمني (بدون stock cap) كان يظهر في تبويبَي "نشطة" و"سابقة" معاً لأن `quantity<=0` كان يُعتبر sold-out حتى بدون initialQuantity. الفلترة الآن متطابقة مع SellerDashboard: العرض في "سابقة" فقط إذا status=expired/paused أو (active && sold-out-with-cap) أو (active && timed-out). |
| **v10.35** | BottomNav ثابت فعلياً في كل الصفحات — رفع z-index من 50 إلى 1100 (فوق Leaflet) + `isolation: isolate` على `.leaflet-container` + نقل BottomNav خارج PullToRefresh في Home و Bookings (translateY كان يكسر `position: fixed`) |
| **v10.34** | auto-version-check على كل page load: يقارن CACHE_NAME الـserver مع الـcached محلياً ويـapply تلقائياً إذا اختلف |
| **v10.33** | BottomNav على DealDetails + رفع زر الحجز فوقه + خريطة حولي بـ120 px clearance + Sidebar بدون flex:1 |
| **v10.32** | تثبيت BottomNav (إزالة auto-hide) + فراغ تحت خريطة "حولي" في وضع map-only |
| **v10.31** | إزالة Pull-to-Refresh من حولي + BottomNav يكشف على أي scroll-up (مثل X) |
| **v10.30** | Pull-to-Refresh spinner ≤ 700ms cap + fire-and-forget refresh per page |
| **v10.29** | PTR indicator centered + BottomNav auto-hide بنمط X (لاحقاً أُزيل في v10.32) |
| **v10.28** | Pull-to-Refresh component + applySwUpdate نووي (cache nuke + cache-busted reload) |
| **v10.27** | زر "📍 موقعي" في حولي يمسح فلاتر region/city تلقائياً |
| **v10.26** | Profile header غير sticky (override .premium-bar للـscroll الطبيعي) |
| **v10.25** | partial-aware saveProfile (يمنع wipe المتابعة) + StoreDetails notch + UpdateBanner |
| **v10.24** | عرض المنطقة + المدينة على بطاقة الموقع في DealDetails |
| **v10.23** | فلتر sort في صفحة حجوزاتي (newest-first default) |
| **v10.22** | realtime resync threshold 1s + pageshow bfcache handler + live GPS + Bookings header notch |
| **v10.21** | محادثة المشتري ↔ التاجر (٣+٣ رسائل) — جدول DB + RPCs + realtime + UI |
| **v10.20** | atomic booking RPCs (complete/acknowledge/cancel) + await/rollback — إصلاح "الحجز يرجع" |
| **v10.19** | فلتر region/city: deals.region + deals.city columns + DB trigger update + backfill 6 rows |
| **v10.18** | رسالة التاجر تظهر دائماً في حجوزاتي (status-driven fallback) |
| **v10.17** | إزالة inner scroll من صندوق التصنيفات في Smart Alerts (Profile) |
| **v10.16** | Permissions-Policy: camera=(self) — فتح الكاميرا في الباركود + KM label overflow fix |
| **v10.15** | SW cache bump إجباري كل deploy + وقت الوصول مشي/سيارة في حولي |
| **v10.14** | dark mode --primary → emerald `#10b981` (يصلح 33 موقع نص خفي دفعة واحدة) |
| **v10.13** | زر "✅ تم الحجز" → ينقل لـ/bookings + شارة الإشعارات في الدارك مود + distance badge |
| **v10.12** | Vercel SPA rewrites — يصلح صفحة 404 على أي رابط مباشر |
| **v10.0–v10.11** | (سابق للجلسة) — v10.0 SW cache strategy، v10.1 auto-recovery، v10.4–10.11 إصلاحات الإدارة + Vercel deploy |

---

## 🧠 ذاكرة دائمة (gotchas)

- **Parcel preview لا يشتغل في sandbox (EPERM)** — أعتمد على typecheck + Vercel deploy.
- **Custom location_id (`custom_<ts>`)** لا يطابق LOCATIONS array — حُلت في v10.19 عبر deals.region/city columns + find_nearest_sa_city RPC.
- **iOS Safari pauses realtime websocket في الـbackground** — الـpageshow + 1s threshold يحل ذلك (v10.22, v10.28).
- **controllerchange على iOS قد يأخر ثواني** — applySwUpdate الآن لا ينتظره، يمسح الكاش ويعمل `location.replace` فوراً (v10.28).
