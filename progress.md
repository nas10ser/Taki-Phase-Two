# TAKI — سجل الإصدارات 📊

> منصة حجز التخفيضات في السعودية. React 18 + Parcel + Supabase + Vercel.
> المالك: ناصر (غير مبرمج — كل التنفيذ يتم من جهة Claude).

## 📌 قواعد ثابتة (لا تتغير)

- **CACHE_NAME في sw.js يجب رفعه كل deploy** (`taki-cache-vXX.YY`) — وإلا iOS Safari يبقى على نسخة قديمة.
- **dealRepository.updateQuantity** للتحديثات الجزئية على deals (الـtrigger `tr_guard_deal_publish` يرفض UPDATE OF status بصمت).
- **notification routing** يقرأ `meta_data.audience` (DB trigger يكتبه)، لا `user.userType`.
- **admin RPCs** بـ`RETURNS TABLE` تحتاج تأهيل (qualify) للأعمدة المتداخلة مع DB columns.
- **Vercel deploy**: `cd ~/Desktop/TAKI && npx vercel deploy --prod --archive=tgz` — `--archive=tgz` إلزامي.
- **Worktree flow**: تعديل في worktree → commit → `git push origin HEAD:main` → `git -C ~/Desktop/TAKI pull origin main` → vercel deploy.

---

## 🗂 backlog (مهام مؤجلة)

- **آلية اشتراك "باقة أعلى" للسماح بأكثر من 3 لوكيشنات** (تم تحديد القاعدة في v10.42): الباقة الأساسية = 3 مواقع فريدة كحد أقصى للمنتجات النشطة. للمزيد لازم باقة مدفوعة. **ناصر سيحدد لاحقاً**: السعر، الفئات (5/10/∞)، نموذج الدفع، وكيف يُعرض الـupsell. الكود الحالي يرفض الإضافة في موقع 4+ مع رسالة "الترقية قريباً"، وزر "إدارة الاشتراك" يفتح `/subscription`.

---

## 🗓 سجل v10.x

| إصدار | الموضوع |
|--------|---------|
| **v10.93** | **بلاغان من ناصر**. **(١) حقل إدخال الرمز اليدوي في سكانر الحجز غير واضح بالوضع الفاتح (التاجر)**: لا `background` صريح على `<input>` في `BarcodeScanner`، فآيفون **بنظام داكن** يرسم الحقل أسود عبر التحكم الأصلي بينما لون النص يتبع ثيم التطبيق الفاتح (كحلي غامق) → الكود يكتب غير مرئي. **الإصلاح**: خلفية داكنة صريحة `#0f172a` + نص أبيض + `WebkitTextFillColor:#fff` + `colorScheme:'dark'` + `caretColor:#fff` — واضح في الوضعين بمعزل عن مظهر النظام. **(٢) زر «🚩 إبلاغ» للمشتري على الحجز (مثل التاجر تماماً، مربوط بقاعدة البيانات)**: أُضيف زر إبلاغ على بطاقات الحجز في صفحة «حجوزاتي» (القسمين: نشطة + السجل) يفتح **نفس `ReportDialog`** الذي يستخدمه التاجر — نفس أنواع/أسباب البلاغ، يحفظ عبر `reportRepository.create` (المشتري→المتجر، `reportedRole='seller'`)، والـtrigger `tr_guard_report_insert` (v10.79) يختم الأدوار ويثبّت أنه بلاغ على هذا المتجر. `ReportDialog` يستخدم `createPortal` لـbody (لا يقع في فخّ transform الخاص بـPullToRefresh). typecheck ✅. |
| **v10.92** | **بلاغ ناصر (بسيط): زر «✅ تم الحجز — انتقل لحجوزاتي» ينقل لصفحة حجوزاتي بنجاح لكن يقف في نصّ الصفحة لا في أعلاها حيث الحجز الأحدث**. السبب: `DealDetails` يستدعي `history.push('/bookings')` (السطر 584 بعد الحجز + السطر 1207 من زر CTA) **بدون `?barcode=`**، وSPA يحتفظ بموضع تمرير الصفحة السابقة (DealDetails كانت مُمرَّرة للأسفل) فتفتح حجوزاتي في المنتصف. **الإصلاح**: `useEffect` عند تركيب صفحة `Bookings` يستدعي `window.scrollTo(0,0)` — إلا إذا كان الرابط يحمل `?barcode=` (عندها متتبّع التنبيه الموجود يتكفّل بالتمرير لذلك الحجز وتوسيطه، فلا نتعارض معه). الترتيب الافتراضي `newest` فالحجز الأحدث يظهر فوراً في الأعلى دون تمرير. تغيير موضعي صغير بلا أثر على بقية الصفحات. typecheck ✅. |
| **v10.91** | **«لا زال نفس الخلل» — السبب الجذري الفعلي للكاميرا (تكميل v10.90)**. v10.90 أصلح مسار الاستوديو فقط؛ لكن مسار **الكاميرا** كان فيه حلقة تغذية راجعة: `SellerDashboard` يمرّر `maxShots={4 − images.length − cropQueue.length}` لـ`CameraCapture`، و`CameraCapture` يحسب `cap = Math.max(1, maxShots)` **في كل render من القيمة الحيّة**. كل لقطة تُلتقط تستدعي `onCapture → uploadCroppedFile → setImages([...])` فيكبر `images.length` فيُعاد تمرير `maxShots` **أصغر**، فتصبح `count >= 4 − count` ⇒ **الكاميرا تتجمّد دائماً عند صورتين** (شرح الصورة: «تمت إضافة ٢ صور — اكتمل الحد الأقصى» بلا زر «المزيد»). **الإصلاح**: تجميد سعة الجلسة لحظة فتح الكاميرا — `const [cap] = useState(() => Math.max(1, maxShots))` (لقطة واحدة للمتبقّي عند الفتح، تتجاهل تقلّص الـprop أثناء الجلسة). جلسة فارغة الآن = ٤ لقطات كاملة؛ ولو في صور سابقة = المتبقّي الصحيح حتى مجموع ٤. `CameraCapture` يُركّب من جديد كل فتح (render شرطي `{showCamera && …}`) فاللقطة الأولية صحيحة دائماً. typecheck ✅. |
| **v10.90** | **بلاغ ناصر (محبط): إضافة صور المنتج — «الاستوديو يختار صورة واحدة فقط، وكان ٤ سابقاً، أبي ٤ مع بعض مثل واتساب»**. السبب الجذري (انحدار من v10.75): زر «الاستوديو» داخل `CameraCapture` يستدعي `onPickStudio` في `SellerDashboard` الذي كان يفتح معرض النظام عبر **`setTimeout(() => fileInputRef.current?.click(), 60)`** — تأخير ٦٠ms **يكسر user-activation في iOS Safari** فينهار اختيار الصور المتعدد إلى **صورة واحدة فقط** (الحقل أصلاً `multiple accept="image/*"` بلا `capture`، فلا قيد إلا كسر اللمسة). **الإصلاح**: استدعاء `fileInputRef.current?.click()` **متزامناً داخل نفس اللمسة** ثم `setShowCamera(false)` بعده (الحقل المخفي داخل الفورم وما زال مركّباً تحت طبقة الكاميرا، فالـref صالح؛ والكاميرا أوقفت بثّها قبل النداء). يستعيد سلوك v10.75: اختيار حتى ٤ صور دفعة واحدة من المعرض مثل واتساب، ثم تمرّ بطابور القص الموجود. (سعة الكاميرا اللحظية = `4 − الموجود` صحيحة لحد الـ٤ الإجمالي المتفق عليه؛ التذبذب ٢/١/٣ كان من صور سابقة في الفورم تستهلك الخانات، وليس خللاً في الحد.) typecheck ✅. |
| **v10.89** | **بلاغان من ناصر على شريط حالة الطلب (الموقع احترافي عالمي)**. **(١) «شريط أخضر زائد — المفروض عند الاستلام يتوقف»**: في `DealDetails.StatusTracker` كان شريط التقدّم `position:absolute` بعرض `${(currentIndex/(steps-1))*100}%` داخل صفّ فيه `padding:'0 10px'` ومثبّت بـ`left/right:30px` — فعند `completed` يصبح العرض 100% من عرض الحاوية كاملةً (وليس من مسار العُقد) فيتجاوز آخر عقدة ويبرز شريط أخضر زائد من الطرف (الذي حوّطه ناصر). **الإصلاح (حسابي دقيق)**: مراكز العُقد عند `(2i+1)/(2N)` من الصف؛ حُذف `padding:'0 10px'` وصار المسار والتعبئة بنِسَب مئوية مرتكزة على مركز أول/آخر عقدة (`edge = 100/(2N)%`، `span = 100−2·edge`، `fill = currentIndex/(N−1)·span`) — فالتعبئة تنتهي **بالضبط** على العقدة الحالية ولا تبرز أبداً، صحيحة في RTL وLTR. **(٢) «عند الإلغاء يتحوّل لأحمر مع X عند تم الاستلام — أفضل من علامة صح خضراء على مؤكد»**: حالة `cancelled` كانت تُعامَل كـindex=0 فتظهر ✓ خضراء على «مؤكد» (مضلِّل). **الإصلاح**: الإلغاء حالة منتهية → المسار يمتلئ كاملاً بلون **أحمر** وكل العُقد حمراء بعلامة **✕** (بدل ✓ الخضراء) في `StatusTracker`؛ ونفس المعالجة طُبِّقت على متتبّع صفحة **حجوزاتي ← سجل الطلبات السابقة** (`Bookings.tsx`، الذي يعرض الحجوزات الملغاة فعلياً بعد v10.88) — أحمر + ✕ على «مؤكد/استلمه التاجر/تم الاستلام» بدل ✓ خضراء على مؤكد فقط. مظهر احترافي ومتسق في المكانين. typecheck ✅. |
| **v10.88** | **٣ بلاغات من ناصر**. **(١) القائمة الجانبية ما زالت تتحرّك (v10.87 لم تكفِ) — «استخدم نفس قاعدة الإشعارات»**. السبب الجذري الحقيقي (ليس مستمعات اللمس): `Navbar` هو الذي يركّب `<Sidebar>`، وفي **الرئيسية وحجوزاتي** يُعرض `Navbar` **داخل `<PullToRefresh>`** الذي يلفّ أبناءه في `<div style="transform: translateY()">`. **أي `transform` غير `none` على عنصر أب يعيد ربط `position:fixed` بذلك الأب بدل نافذة العرض** — فالدِّرج «المثبّت» صار يتمرّر/يهتزّ مع الصفحة هناك، بينما **الإشعارات (بلا PullToRefresh) ثابتة تماماً**. **الإصلاح الجذري النهائي**: `Sidebar` صار يُعرض عبر **`createPortal` إلى `document.body`** — فيخرج من أي أب مُحوَّل ويصبح مثبّتاً على نافذة العرض في **كل الصفحات** تماماً كالإشعارات. **(٢) رسالة تجديد وهمية في المدخل الثاني**: التجديد له مكانان — «عروضي» (لوحة التاجر، صحيح منذ v10.78: يفحص نجاح `updateDeal` ويفتح نموذج التعديل ببانر إعادة اختيار الموقع عند الفشل) و**«صفحتي ← العروض السابقة» (`StoreDetails`)** الذي كان يعرض «✅ تم تجديد العرض بنجاح!» **بلا فحص** فيبقى العرض منتهياً فعلياً (نفس باق v10.78 لكن في المسار الثاني). **الإصلاح**: `StoreDetails.reActivateDeal` صار يفحص قيمة `updateDeal` المنطقية؛ عند الفشل **لا يدّعي النجاح** بل ينبّه «لم يعد موقعه ضمن مواقعك / وصلت حد المواقع — جدّده من لوحة التاجر» ويحوّله لـ`/seller` حيث يكتمل التجديد فعلياً. **(٣) حجز ملغى يظهر «حجز مؤكد» ويمنع إعادة الحجز**: تأكّدت من قاعدة البيانات — كل حجوزات المشتري على «عيادة اسنان» الستة **منتهية (ملغاة/مكتملة)** ولا يوجد أي حجز نشط؛ ومع ذلك صفحة العرض تعرض `EETTT2JT` (وهو **`cancelled`**) كـ«حجز مؤكد» بمؤقّت وهمي. السبب: `DealDetails.activeBooking` كان `bookings.find(...)` **بلا أي فلترة حالة/انتهاء** فيلتقط صفّاً قديماً ملغى (والـ`cancelled` يسقط في فرع «حجز مؤكد»). **الإصلاح**: `activeBooking` صار يستخدم **نفس قاعدة `useBooking`** — يستبعد `cancelled` ويستبعد pending/acknowledged المنتهي (سقف الساعتين الفعّال)، ويبقي `completed` فقط لإظهار الإيصال/التقييم، ويختار **الأحدث** عند تعدّد الصفوف. زر «احجز الآن» يعتمد أصلاً على `isBooked` (قاعدة صحيحة) فإعادة الحجز تعمل بعد انتهاء/إلغاء الحجز. (فلترة طلبات التاجر النشطة `activeOrders` صحيحة أصلاً 1680-1681؛ أي بقايا عند التاجر = حالة محلية قديمة عالجتها مزامنة v10.86 عند العودة للمقدمة.) typecheck ✅. |
| **v10.87** | **بلاغ ناصر: القائمة الجانبية (٣ شرطات) «تتحرك نزولاً وطلوعاً» في الرئيسية وحجوزاتي، بينما ثابتة في الإشعارات — أريد كل الصفحات مثل الإشعارات**. **السبب الجذري** (لا تخمين — تتبّع الفرق بين الصفحات): `PullToRefresh` (السحب للتحديث) يُركّب مستمعات لمس **على `document` عالمياً** (`touchstart/touchmove/touchend`)، وهي تبقى فعّالة **حتى لو كانت القائمة الجانبية مفتوحة فوق الصفحة**. مكوّن `PullToRefresh` يُستخدم في **الرئيسية + حجوزاتي فقط** (وليس الإشعارات — لذلك كانت ثابتة هناك بالضبط). فعند فتح الدِّرج وسحب الإصبع عليه: الصفحة في الأعلى (`atTop()` = true) فيبدأ التتبّع ويُطبَّق `transform: translateY()` على غلاف محتوى الصفحة **خلف** الدِّرج + يظهر مؤشّر السحب → تتحرّك الخلفية صعوداً/نزولاً ويهتزّ المنظر. **الإصلاح (جذري، بلا دين برمجي)**: `PullToRefresh` صار **خاملاً تماماً متى وُجد دِرج/مودال مفتوح** — دالة `blockedByModal()` تتحقّق من قفل تمرير الصفحة (`document.body.style.overflow === 'hidden'` الذي يضبطه `Sidebar` عند الفتح) أو وجود عنصر `[aria-modal="true"]` غير مخفي؛ تُفحص في `onTouchStart` (فلا يبدأ التتبّع أصلاً) ودفاعياً في `onTouchMove` (إن فُتح المودال أثناء لمسة). النتيجة: عند فتح ٣ الشرطات لا تتحرّك الخلفية ولا الدِّرج في أي صفحة — تماماً مثل صفحة الإشعارات كما طلب ناصر. السحب للتحديث يعمل طبيعياً عند إغلاق الدِّرج. typecheck ✅. |
| **v10.86** | **بلاغان من ناصر: تعليق ~٦ث عند إرسال رسالة المحادثة + توضيح/إتاحة إلغاء الطلب**. **(١) «الضغط على إرسال يعلّق ٦ ثوانٍ ثم يُرسل، وقبلها كان فورياً»** — تشخيص جذري عبر سجلات Supabase + الكود (لا تخمين): RPC `send_booking_message` يرجع 200 بسرعة خادمياً (لا مشكلة DB — pg_net غير مثبّت لكن `taki.push_url` فارغ فالـ`tr_notification_push` يخرج مبكراً؛ الـtriggers سليمة). الخلل في العميل: على iOS الـPWA المثبَّت، **لمس زر «إرسال» يُغلق لوحة المفاتيح فيُطلق حدث `focus` على النافذة**، و`realtimeService.handleFocus` كان يعتبر أي `focus` بعد ثانية «رجوعاً من الخلفية» فيستدعي `onRefreshAll()` (٥ استعلامات ثقيلة + ٥ `setState` ضخمة = إعادة بناء كامل بنفس حِمل ٦-٧ث الموصوف في v10.71) فيتجمّد الخيط الرئيسي حتى تظهر الرسالة المُرسَلة فعلاً. **الإصلاح (جذري، بلا دين برمجي، وبلا تراجع عن مزامنة iOS المثبّتة)**: تتبّع `lastHiddenAt` (يُضبط حصراً عند `visibilitychange→hidden` أو `pagehide`) + `cameBackFromHidden()`؛ `handleFocus`/`handleVisibilityChange` لا يطلقان المزامنة الثقيلة إلا إذا اختفت الصفحة فعلياً (رجوع حقيقي من الخلفية) — وإغلاق لوحة المفاتيح لا يُخفي الصفحة فلا تحدث العاصفة. نفس الحارس طُبِّق على `BookingThread` (كان يعيد جلب الرسائل + mark-read عند كل إرسال). الرجوع الحقيقي من الخلفية ما زال يزامن عبر visibilitychange/pageshow/pagehide. **(٢) «زر إلغاء مضلِّل + لا يمكن إلغاء الطلب عند كتابة الكود»** — (أ) في «سكانر الحجز» كان الزر «❌ إلغاء» فظنّه ناصر إلغاءً للسكانر بينما هو **إلغاء الطلب كاملاً** → صار «❌ إلغاء الطلب» مع **نافذة تأكيد** قبل التنفيذ («سيُلغى الطلب نهائياً وتُعاد الكمية للمخزون ويُبلَّغ المشتري»). (ب) كان الإلغاء متاحاً عبر السكانر فقط؛ أُضيف زر **«❌ إلغاء الطلب»** أسفل بطاقة الطلب في لوحة التاجر (لحالتَي pending/acknowledged) بنفس التأكيد — فيقدر التاجر يلغي الطلب حتى لو أدخل الكود يدوياً دون فتح السكانر. typecheck ✅. |
| **v10.85** | **القائمة الجانبية: تحويل التثبيت إلى `position: sticky` (أمتن نمط على iOS Safari)**. بعد تكرار بلاغ ناصر، حُسم الأمر بأكثر نمط مضمون: بدل تثبيت الهيدر عبر flex داخل `position:fixed` (قد يتعثّر في حساب الارتفاع على iOS)، صار الـ`<aside>` `overflowY:auto` (يتمرّر طبيعياً) و**الهيدر `position:sticky; top:0` بخلفية صلبة + zIndex** فيبقى ملتصقاً أعلى القائمة والعناصر تتمرّر **تحته** بنظافة (paddingBottom بدل marginBottom حتى لا يظهر شيء من خلف الفراغ). أُزيل غلاف التمرير الداخلي ورجعت `<nav>`/الفوتر عناصر مباشرة. قائمة قصيرة = تدفّق طبيعي بلا فجوة؛ قائمة طويلة = تمرّر والهيدر ثابت. typecheck ✅. **تذكير حاسم لناصر**: هذا مع v10.84 (الفحص عند كل عودة للمقدمة). إن بقي الشكل القديم فجهازك ما زال على بناء مخزَّن قديم — الحل النهائي **مرة واحدة**: احذف أيقونة التطبيق من الشاشة، افتح Safari على الرابط، ثم «إضافة إلى الشاشة الرئيسية» — بعدها كل تحديث يصل تلقائياً للأبد. |
| **v10.84** | **السبب الجذري الحقيقي لـ«أعيد الفتح ويبقى الشكل القديم» (٣ نشرات والمستخدم يرى نفس البناء بالضبط)**. تشخيص: كود القائمة صحيح ١٠٠٪ (v10.83: هيدر مثبّت خارج منطقة التمرير = النمط القياسي) — لكن **تعديلاتي لا تصل جهاز ناصر إطلاقاً**. الفحص: `autoCheckServerVersion()` في `sw-cleanup.ts` (يجلب sw.js الحيّ متجاوزاً الكاش ويقارن CACHE_NAME ويطبّق `applySwUpdate` عند الاختلاف) كان يعمل **فقط عند تحميل الصفحة (cold start)**؛ وعلى iOS الـPWA يُستأنف من الذاكرة/bfcache **بدون إعادة تنفيذ الموديول ولا navigation** — فلا يُفحص الإصدار أبداً مهما «أعاد الفتح»، ويبقى مجمّداً على بناء قديم للأبد. **الإصلاح**: إضافة `_lastVersionCheck` (خنق ١٥ث) + استدعاء `autoCheckServerVersion()` على **`visibilitychange` + `pageshow` + `focus`** — أي عند كل عودة للمقدمة يُفحص إصدار الخادم وإن تقدّم يُمسح الكاش ويُعاد التحميل تلقائياً، بلا حاجة لقتل التطبيق. هذا يجعل كل النشرات المستقبلية تصل iOS تلقائياً. typecheck ✅. **ملاحظة لناصر**: للخروج من البناء القديم العالق **مرة واحدة فقط**: أغلق التطبيق **كلياً** (اسحبه للأعلى وأزله من مبدّل التطبيقات) ثم افتحه — بعدها كل تحديث يصلك تلقائياً. |
| **v10.83** | **تثبيت القائمة الجانبية بشكل احترافي (بلاغ ناصر بصورتين: لا تزال تنزلق والهيدر يختفي)** — v10.82 ثبّت الهيدر والفوتر منفصلين مع `nav flex:1` فأنتج فجوة فارغة كبيرة في المنتصف مع قلة العناصر (شكل «مبتدئين»). **الإصلاح النهائي (نمط الـdrawer القياسي في التطبيقات الاحترافية)**: الـ`<aside>` `overflow:hidden`؛ **الهيدر مثبّت تماماً** (`flexShrink:0`، خارج منطقة التمرير، تحت شريط الحالة عبر `safe-area`) فلا ينزلق أبداً مهما طالت القائمة؛ و**كل ما تبقّى (القائمة + الإعدادات) داخل منطقة تمرير واحدة** (`flex:1; minHeight:0; overflowY:auto`) تنساب طبيعياً تحت الهيدر — قائمة قصيرة = تكدّس طبيعي بلا فجوة وسطية قبيحة، قائمة طويلة = تتمرّر بنعومة والهيدر ثابت. typecheck ✅. **ملاحظة لناصر**: إن استمر الشكل القديم فهو كاش iOS — لازم ضغط شريط التحديث الأخضر أو إغلاق التطبيق وفتحه ليصل v10.83. |
| **v10.82** | **إصلاح خلل القائمة الجانبية (الثلاث شرطات)** — بلاغ ناصر بصورة: الهيدر وعناصر القائمة تختفي والمحتوى يتداخل مع شريط الحالة (الساعة/البطارية). **السبب الجذري**: الـ`<aside>` كاملةً كانت `overflowY:auto` بلا هيدر مثبّت؛ بعد نمو القائمة (إضافة «📣 الشكاوى» v10.81 + بلوك معاينة الأدمن) صار المحتوى أطول من الشاشة، فأي تمرير ينزلق الهيدر/القائمة **تحت شريط الحالة** (لأن لا يوجد منطقة مثبّتة) فتبدو القائمة مكسورة (يظهر الفوتر فقط). **الإصلاح (نمط الـdrawer الحديث، بلا دين برمجي)**: الـ`<aside>` صارت `overflow:hidden` (لا تتمرّر بنفسها)؛ **الهيدر** `flexShrink:0` (مثبّت أعلى تحت شريط الحالة عبر `safe-area` الموجود)؛ **`<nav>`** صارت `flex:1; minHeight:0; overflowY:auto` (تتمرّر داخلياً وحدها إذا كثُرت العناصر)؛ **الفوتر** `flexShrink:0` (مثبّت أسفل). النتيجة: الهيدر والفوتر ظاهران دائماً، والقائمة فقط تتمرّر في الوسط — يستحيل أن ينزلق المحتوى تحت شريط الحالة مهما طالت القائمة. typecheck ✅. |
| **v10.81** | **مركز بلاغات وشكاوى الأدمن (#1) + مدخل الشكاوى للمستخدم (#3)** — طلب ناصر: الشكاوى تظهر في مركز التحكم **مع المبلِّغين** (لا إيميل فقط). **DB (`v10_81_complaints_and_admin_report_rpcs`)**: جدول `public.complaints` (RLS: إدراج كنفس + قراءة صاحبها + أدمن via `is_admin()`) + ٤ دوال **SECURITY DEFINER** بنفس نمط `admin_search_users` (بوابة أدمن داخلية + أعمدة مؤهَّلة): `admin_list_reports` (يرجع البلاغ + اسم/جوال/دور المبلِّغ والمُبلَّغ عنه + **كم بلاغ أعطى المبلِّغ** + **كم استلم المُبلَّغ عنه** + **عدد المبلِّغين المختلفين/١٤ي** + `under_review`، مع فلاتر بحث/نوع/حالة/أيام/دور)، `admin_list_complaints`، `admin_set_report_status`، `admin_set_complaint_status`. **العميل**: `adminService` (أنواع `AdminReportRow`/`AdminComplaintRow` + ٤ دوال)، `complaintRepository` + `ComplaintDialog` (نموذج شكوى للمستخدم)، صفحة **`admin/AdminReports`** (تبويبان البلاغات/الشكاوى، فلاتر، بطاقات تعرض «المبلِّغ ← ضدّ» بالأسماء والجوال وفتح حساب التاجر `/store/:id`/المشتري عبر تبويب المشترين، عدّادات الكشف عن المبلِّغ الكيدي، أزرار حالة: تحت المراجعة/تم الحل/رفض كيدي — كله **يدوي** كما اخترت). **الربط**: تبويب «🚩 البلاغات والشكاوى» في `AdminDashboard` + قراءة `?tab=` من الرابط؛ إشعار البلاغ التجميعي يفتح `/admin?tab=reports`؛ مدخل «📣 الشكاوى / تواصل الإدارة» في القائمة الجانبية لأي مستخدم مسجَّل (#3). أُضيف `'report'` لنوع `Notification`. typecheck ✅. **متبقٍّ #1: الإيميل التلقائي (مفتاح Resend من ناصر) + إجراءات الإشراف (كتم/إيقاف حجز/إخفاء عروض/تعليق) + الحظر النهائي بالإيميل/الجوال.** |
| **v10.80** | **بلاغان من ناصر (صورة صفحة المتجر): مسافة علوية كبيرة + زر تواصل**. **(١)** هيدر `StoreDetails` كان يأخذ الصفحة كاملة بمسافات كبيرة — رُفع **قليلاً** مع **إبقاء `env(safe-area-inset-top)`** فلا يصعد لشريط الساعة/البطارية: حشوة الهيدر العلوية `+14px → +6px` والسفلية `24 → 16`، صف الرجوع/التعديل `marginBottom 16 → 10`, كتلة البروفايل `marginBottom 24 → 14`, هامش الأفاتار `16 → 10px` (تضييق محسوب «قليلاً جداً» كما طلب، بدون قص محتوى). **(٢)** كتلة التواصل كانت زرّ اتصال عريض + مربّع واتساب صغير — حُوّلت إلى **زرّين متساويين (50/50)** تماماً كصورة ناصر المرجعية: «📞 اتصال» (خلفية فاتحة) + «WhatsApp 💬» (أخضر)، كلاهما `flex:1` بترتيب يجعل واتساب يساراً والاتصال يميناً في RTL. typecheck ✅. |
| **v10.79** | **بلاغات ناصر من الاختبار: تعليقات صفر + إشعار متابعة مكرّر + بناء أساس نظام البلاغات (#1)**. **(أ) إصلاح «التعليقات والآراء (0)» في صفحة العرض** رغم وجود تقييمات للمتجر — السبب: `DealDetails` كان يعرض `deal.ratings` (تقييمات هذا العرض فقط)؛ عرض آخر لنفس المتجر = مصفوفة فارغة → «لا توجد تقييمات». **الإصلاح**: تجميع على مستوى المتجر (نفس منطق `StoreDetails.allStoreReviews`) — `storeReviews` يجمع تقييمات كل عروض المتجر مع `dealId`+`itemName` لكل تقييم؛ العدّاد/المتوسط والقائمة صارت من تجميع المتجر، وكل إجراء (إعجاب/حذف/رد) يوجَّه لعرضه الصحيح عبر `r.dealId`، وأُضيف سطر «🏷️ اسم المنتج» لكل تقييم. الآن تعليق المشتري يظهر في كل عروض نفس المتجر. **(ب) إشعار «متابع جديد» مكرّر** — السبب الجذري عبر Supabase MCP: `tr_follow_notification` هو AFTER UPDATE على `users` (أي عمود)، فالكتابة المتفائلة + كتابة ملف ثانية (أو سباق نقرتين) كلٌّ يقدّم التاجر كـ«مُضاف حديثاً» مقابل OLD → إشعار مكرّر. **الإصلاح (ترحيل `v10_79_dedupe_follow_notification`)**: حارس منع تكرار داخل الدالة (تخطّي إن وُجد إشعار follow لنفس (تاجر، متابِع) خلال ساعة — نفس أسلوب منع تكرار التنبيه الذكي) + تنظيف المكرّر الموجود + إعادة تأكيد `REVOKE EXECUTE`. **(ج) أساس نظام البلاغات #1 (ترحيل `v10_79_reports_foundation`)**: جدول `public.reports` + عمودا `users.under_review/under_review_at`؛ `tr_guard_report_insert` (BEFORE، SECURITY DEFINER) يفرض قواعد ناصر: **مشتري↔تاجر فقط** (يمنع تاجر↔تاجر/مشتري↔مشتري/النفس/طرف admin)، ويختم الأدوار من `users.user_type` الحقيقي (لا انتحال)؛ `tr_report_threshold` (AFTER): **٣ مبلِّغين مختلفين** (ليس ٣ من نفس الشخص) خلال ١٤ يوم → يعلّم الحساب `under_review` + إشعار أدمن **تجميعي واحد** عند التجاوز فقط (لا سبام، بلا تقييد تلقائي)؛ RLS (إدراج كنفس + أدمن عبر `is_admin()`)، وEXECUTE مسحوب على الدالتين. **الواجهة**: `reportRepository` + مكوّن `ReportDialog` (أنواع بلاغ + سبب، رسائل خطأ عربية تترجم رفض الـtrigger)؛ زر **🚩** في صفحة المتجر (مشتري→تاجر، يظهر للمشتري فقط) وفي كرتَي الطلب الحالي/السجل بلوحة التاجر (تاجر→مشتري). typecheck ✅. **متبقٍّ #1: مركز بلاغات الأدمن (فلاتر/أسماء/دخول الحسابين/عدّاد المستلَم+المُعطى/مراجعة الرسائل) + الإيميل (Resend) + مكان الشكاوى #3 + إجراءات الإشراف والحظر النهائي**. |
| **v10.78** | **٣ من ٥ نقاط لناصر: إلغاء سبام تقييم الأدمن (#5) + إصلاح باق تجديد العرض (#4) + حظر المشتري للتاجر (#2)**. **(#5)** بعد v10.77 طلب ناصر عدم إشعار الأدمن بكل تقييم (سبام مع ملايين المتاجر). ترحيل `v10_78_rating_notif_drop_admin_fanout`: `handle_rating_notification` صار يُشعر **التاجر فقط** (حُذف admin fan-out)، أعيد تأكيد `REVOKE EXECUTE`، ونُظّفت أي إشعارات تقييم admin. الأدمن سيراجع التقييمات من صفحة تقارير الأدمن (ضمن #1 لاحقاً). **(#4)** بلاغ ناصر + صورة: «تجديد العرض» يظهر «✅ تم التجديد بنجاح» لكن العرض يبقى في السابقة — السبب: `reActivateDeal` كان يعرض النجاح **بلا شرط** و`updateDeal` يبتلع فشل الحفظ (الـtrigger الخادمي يرفض إعادة التفعيل غالباً لأن لوكيشن العرض حُذف/تغيّر فيتجاوز حدّ المواقع). **الإصلاح**: `updateDeal` صار يرجّع `Promise<boolean>` (نجاح فعلي/فشل)؛ و`reActivateDeal`: إن فشل الحفظ **لا يدّعي النجاح** بل يفتح العرض في نموذج التعديل حيث **البانر الموجود** «⚠️ تم حذف لوكيشن العرض السابق — اختر أحد مواقعك الحالية لتجديد هذا العرض» + شرائح المواقع الحالية تتيح إعادة الإسناد بنقرة ثم الحفظ يجدّد فعلياً (مطابق تماماً لطلبه). الكمية/المدة كما اتُّفق (createdAt جديد = نفس المدة من لحظة النقر). **(#2)** **حظر المشتري للتاجر** — ترحيل `v10_78_buyer_block_merchant`: عمود `users.blocked_merchants TEXT[]`، وفلترة **خادمية** في `handle_deal_smart_notifications` (مسار العرض الجديد الحيّ) و`handle_smart_alerts_backfill` (مسار تعديل التنبيهات) بإضافة شرط استبعاد المتاجر المحظورة — فلا يصل المشتري أي تنبيه ذكي من متجر حظره **حتى لو طابق تنبيهه أو يتابعه** (تعديل سطر واحد لكل دالة، البقية مطابقة للتعريف الحيّ، SECURITY DEFINER محفوظ). **العميل**: انعكاس كامل لنمط `followed_merchants` → `blockedMerchants`: نوع `UserProfile`، `userRepository` (saveProfile partial-aware + تعيين الصفوف + fallback)، `AppContext` (واجهة + حالة + تهيئة + `toggleBlockMerchant` [حظر متجر متابَع يلغي المتابعة تلقائياً] + مزامنة realtime + تصدير)، زر **🚫 حظر/إلغاء حظر** بجانب زر المتابعة في `StoreDetails` مع تأكيد ورسالة واضحة، وفلترة `blockedMerchants` في `Home` (trending/bestDiscounts/filteredDeals) و`Nearby` — المتجر المحظور يختفي فوراً من كل مكان. typecheck ✅. **يتبقّى #1 (نظام الإبلاغ/الشكاوى/الإشراف/الإيميل/الحظر النهائي) + #3 (مكان الشكاوى) — epic كبير يحتاج قرار آلية الإيميل، سيُخطَّط ويُنفَّذ على مراحل.** |
| **v10.77** | **إصلاح: التاجر لا يصله إشعار عند تقييم المشتري له (ولا أي تقييم سابقاً)** — بلاغ ناصر. **تشخيص جذري عبر Supabase MCP (لا تخمين)**: سياسة RLS على `notifications` اسمها `notifs_insert_self` شرطها `auth.uid()::text = user_id` — أي **المشتري لا يستطيع إدخال صفّ إشعار باسم التاجر** (مُعرّفه مختلف) فيُرفض الإدخال **بصمت** (`notificationRepository.save` يبتلع الخطأ بـ`console.warn`). إشعارات الحجز تشتغل لأنها تُنشأ عبر **DB trigger `handle_booking_notification` (SECURITY DEFINER يتجاوز RLS)**؛ أما `addRating` في `AppContext` فكان يحاول الإدخال من جهة العميل مباشرة → RLS يرفضه. **الدليل القاطع**: الجدول فيه **تقييمان فعليان** («ممتاز التعامل» ٥★ لعيادة أسنان، «المحل رائع» ٥★ لكوورة) مقابل **صفر إشعارات تقييم** (كل الـ١٧٧ إشعار نوعها `booking` من الـtrigger). **الإصلاحات: (١) ترحيل DB `v10_77_rating_merchant_notification`**: دالة `handle_rating_notification()` **SECURITY DEFINER** + `SET search_path=public` + trigger `tr_rating_notification` **AFTER INSERT ON ratings** — تنشئ إشعار التاجر خادمياً (يتجاوز RLS) بـ`type='rating'` و`meta_data.audience='seller'`، مع fan-out للأدمن (`audience='admin'`) تماماً كنمط الحجز المُثبَّت. **(٢) تعبئة رجعية idempotent** (حارس `NOT EXISTS` على `meta_data->>'ratingId'`) — أنشأت إشعارَي التقييمين القديمين للتاجر `N15` فصار يراهما الآن. **(٣) تقوية أمنية `v10_77_rating_notif_revoke_execute`**: `REVOKE EXECUTE` على الدالة من `PUBLIC/anon/authenticated` (أغلق تحذير linter «SECURITY DEFINER قابلة للاستدعاء كـRPC»؛ الـtrigger لا يحتاج هذا الإذن فلا يتأثر — `proacl` صار `postgres/service_role` فقط). **(٤) `AppContext.addRating`**: حُذف نداء `addNotification(storeId,…)` الميت (محظور بـRLS أصلاً، وصار تكراراً بعد الـtrigger) + أُزيل `addNotification` من مصفوفة اعتماديات الـcallback. التحديث المتفائل المحلي لقائمة تقييمات العرض باقٍ. **ملاحظة**: إصلاح DB فعّال **فوراً** دون نشر؛ نشر العميل هو تنظيف فقط. typecheck ✅. **(بقي تحذير قديم مماثل على `handle_booking_notification` — لم أمسّه احترازاً لأنه أعلى مسار حركة (١٧٧ إشعار) وخارج نطاق البلاغ؛ يُنصح بتطبيق نفس `REVOKE EXECUTE` عليه لاحقاً.)** |
| **v10.76** | **تعديل تدفّق الكاميرا حسب طلب ناصر: مربع واحد + قص فوري لكل لقطة + حلقة «المزيد/تم»**. بعد v10.75 طلب ناصر: يرجع **مربع واحد فقط** (لا مربعين منفصلين كاميرا/استوديو)، وبعد تصوير الصورة يطلع **القص فوراً لنفس الصورة**، ثم زر **«المزيد»** لصورة إضافية (تتكرر نفس طريقة القص) أو **«تم»** للإنهاء. **التغييرات: (١) `SellerDashboard`**: أُلغيت كتلة المربعين، ورجع **مربع واحد** بنفس شكل القديم (📸 «إضافة صور» + سحب/لصق) — لمسه يفتح الكاميرا الحيّة مباشرة. الحقل المخفي للاستوديو بقي في DOM (يُفتح من زر «الاستوديو» داخل الكاميرا + هدف السحب). حُذفت `ingestCameraFiles` (لم تعد لازمة). **(٢) `CameraCapture` أُعيد بناؤه كآلة حالات** (`camera` → `crop` → `choice`): الكاميرا الحيّة (`getUserMedia`) → زر التقاط → **يظهر `ImageCropEditor` فوراً لنفس اللقطة** (نفس محرّر قص الاستوديو بالضبط) → بعد «✓ تطبيق» (أو «تخطّي القص») تُسلَّم الصورة للأب (`onCapture` = `uploadCroppedFile` → ضغط + رفع + إضافة) → شاشة **«📷 المزيد» / «✓ تم»**: «المزيد» يرجّع للكاميرا للقطة التالية بنفس الحلقة، «تم» يُغلق. البثّ يبقى حيّاً أثناء القص/الاختيار (إيقاف المسارات فقط عند الإنهاء/الخروج — ضوء الكاميرا ينطفئ) فالعودة عبر «المزيد» فورية. العرض **حصري لكل وضع** (كاميرا zIndex 100000 / محرّر القص 99998 / شاشة الاختيار) فلا تداخل طبقات. زر «🖼️ الاستوديو» داخل الكاميرا يُغلقها ويفتح معرض iOS (الحقل `multiple` — اختيار حتى ٤ صور تمرّ بطابور القص الموجود). احترام حدّ الـ٤ صور. الضغط قبل الرفع (v10.75) باقٍ كما هو. typecheck ✅. |
| **v10.75** | **كاميرا داخلية متعددة اللقطات (طريقة واتساب) + استديو متعدّد + إصلاح بطء الرفع (١٠ث/صورة)** — بلاغ ناصر بصورتين: كاميرا النظام تأخذ صورة واحدة ثم تُغلق فيُعاد فتحها لكل صورة، والرفع يصل ١٠ث للصورة الواحدة. **التشخيص الجذري**: `<input accept="image/*">` عبر مسار «التقاط صورة» في iOS يفتح **كاميرا النظام** (لقطة واحدة ثم إغلاق — حدّ تقني لا يُتجاوز عبر input إطلاقاً)؛ والرفع بطيء لأن `storageService.uploadImage` كان يرفع **الملف الخام كامل الدقة** (٤-٨ ميجا) بلا أي ضغط (الضغط كان يحدث فقط إذا مرّ المستخدم بمحرّر القص يدوياً). **الإصلاحات: (١) `src/components/CameraCapture.tsx`** جديد — كاميرا حيّة داخل التطبيق عبر `getUserMedia` (الكاميرا الخلفية، `playsInline`+`muted`، إيقاف المسارات عند الإغلاق فلا يبقى ضوء الكاميرا): زر التقاط دائري يصوّر **عدّة لقطات متتالية بدون إغلاق**، شريط مصغّرات سفلي (حذف أي لقطة قبل الإرسال)، عدّاد `n/الحد`، زر تبديل أمامية/خلفية، «تم» يرجّع كل الصور دفعة واحدة، رسائل خطأ عربية واضحة عند رفض إذن الكاميرا مع توجيه لزر «الاستديو». مصغّرات الشريط بصيغة `data:` (دون تعديل CSP). صور الكاميرا **تتخطّى محرّر القص** (مؤطّرة في الكاميرا الحيّة أصلاً — القص لكل لقطة يعيد نفس الاحتكاك المطلوب إزالته). **(٢) `src/utils/imageCompression.ts`** جديد — يفكّ الصورة (`createImageBitmap` يحترم EXIF، مع fallback لـ`<img>`)، يصغّرها لـ≤١٦٠٠px ويعيد ترميزها JPEG q=0.82، يتخطّى JPEG الصغير أصلاً، **fail-open** (يرجّع الملف الأصلي عند أي فشل — لا يحجب البائع أبداً). **(٣)** رُبط الضغط داخل `storageService.uploadImage` كـ**نقطة اختناق وحيدة** — كل مسارات الرفع (كاميرا/استديو/قص مُطبَّق/قص متخطّى/فشل فكّ) تمرّ به، فلا تصل صورة خام للشبكة: ٤ميجا → ~٢٥٠كب ⇒ الرفع من ~١٠ث إلى ~١ث. **(٤) `SellerDashboard`**: استُبدلت خانة «إضافة صورة» الواحدة بخانتين — 📷 **الكاميرا** (تفتح الكاميرا الحيّة) و🖼️ **الاستديو** (الحقل يحتفظ بـ`multiple` فيُختار حتى ٤ صور دفعة واحدة من معرض iOS)، مع إبقاء السحب/اللصق على الاستديو. `vercel.json` يسمح بـ`camera=(self)` مسبقاً. typecheck ✅. |
| **v10.74** | **٤ متطلبات بدقة عالية من ناصر: تتبّع خريطة حيّ + رئيسية حسب المدينة + إشعار الساعتين + صندوق تقييم بعد الشراء**. **(١) الخريطة تمشي معي بالثانية (حولي)** — السبب: `MapUpdater` كان `map.flyTo(center,12)` (يصفّر الزووم ويعمل fly جديد كل تحديث)، و watcher يتجاهل أي حركة < **٣٠م**، فبالسيارة الخريطة تبدو متجمّدة. **الإصلاح**: `FollowController` جديد — زووم شارع (١٥) مرة واحدة ثم `panTo` بنعومة مع الحفاظ على زووم المستخدم؛ عتبة الحركة صارت **٦م** و`maximumAge:1000` (تحديث ثانية-بثانية)؛ سحب الخريطة يوقف التتبّع (استكشاف حر) وزر 📍 عائم يعيد تفعيله؛ اختيار منطقة/مدينة يدوياً يوقف التتبّع، وزر «موقعي» يعيده. **(٢) الرئيسية حسب مدينة العميل (تتوسّع للأقرب)** — السبب: `trending/bestDiscounts/filteredDeals` كانت **تقصّ بحدّ صارم** على `topLocation` (تتوقف عند المدينة). **الإصلاح**: نافذة `LocationGate` تُطلب **مرة واحدة** أول فتح (GPS تلقائي → `findNearestCity`، أو اختيار المدينة يدوياً عند الرفض) وتُحفظ كـ`homeCity` في `localStorage`. أُضيف `dealProximityTier` (نفس المدينة=٠، نفس المنطقة=١ [المخواة→بلجرشي/قلوة/الباحة]، <١٥٠كم=٢، <٤٠٠كم=٣، أبعد=٤). الأقسام الثلاثة تُرتّب بالـtier **ثم** تصنيف القسم (موثوقية/خصم) **بدون قصّ** — تبدأ بمدينته ثم تتوسّع للأقرب فالأقرب حتى نهاية العروض، وليس بترتيب مسافة صارم (هذا عمل صفحة حولي). اختيار المنطقة/المدينة يدوياً من فلاتر الرئيسية يبقى قصّاً صريحاً (يحترم اختيار المستخدم). **(٣) إشعار الساعتين** — في شاشة «إتمام الحجز» (`DealDetails`) فوق زر التأكيد، بلوك احترافي ثنائي اللغة: «مدة الحجز ساعتان فقط — استلم طلبك خلال ساعتين من التأكيد، وبعد انتهاء المهلة يُلغى الحجز تلقائياً ويعود المنتج للبيع دون أي التزام عليك» (مطابق لمدة الانتهاء الفعلية `TWO_HOURS_MS`). **(٤) صندوق تقييم المتجر بعد الشراء** — عند تحوّل حجز المشتري إلى `completed` (التاجر مسح QR) عبر `onBookingChange`، يفتح **صندوق** تلقائياً (مرّة واحدة، ولا يظهر إن سبق وقيّم): «نرجو تقييم المتجر — تقييمك يهمّنا» مع **٥ نجوم + مربع تعليق** يستدعي `addRating` (يُشعر التاجر تلقائياً). هذا الصندوق مقصود (طلب ناصر) ومستقل عن صندوق الإشعارات المُلغى في v10.73. typecheck ✅. |
| **v10.73** | **مهمتان بدقة عالية من ناصر: ٤ باقات لوكيشن يتحكم بها الأدمن + إلغاء صندوق الإشعار المُعيق**. **(١) نظام الباقات (١/٣/٦/١٠ لوكيشن)** — **تشخيص جذري عبر Supabase MCP**: الحدّ كان مثبّتاً `3` في ٤ أماكن (عميل ×٢ + رسائل خطأ + **trigger `enforce_seller_location_cap` في DB ثابت `v_max:=3`**) — فأي تغيير في العميل وحده ما كان بيشتغل لأن DB يرفض الموقع الـ٤ مهما صار. كذلك `admin_apply_subscription` ما كان يضبط `max_branches` إطلاقاً. **الإصلاحات**: (أ) **ترحيل DB `v10_73_location_packages`**: `enforce_seller_location_cap` صار يقرأ `COALESCE(store_profiles.max_branches,3)` لكل تاجر؛ و`admin_apply_subscription` أضيف له `p_max_branches` (يتحقّق ∈ {1,3,6,10}) يكتبه في الإدراج وفي ON CONFLICT (NULL = إبقاء كما هو). (ب) **`src/data/packages.ts`** جديد: الباقات الأربع (الأولى=موقع واحد، الثانية=حتى ٣، الثالثة=حتى ٦، الرابعة=حتى ١٠) + `maxForPackage/packageForMax/packageLabel`. (ج) **لوحة الأدمن (`AdminSellers`)**: مُحدِّد باقة بأربعة أزرار داخل نافذة الاشتراك، يجلب الباقة الحالية عبر `subscriptionRepository.getStoreSubscription` ويمرّر `maxBranches` في `adminService.applySubscription` → الـRPC. (د) **لوحة التاجر**: `maxLocations` يُجلب من `store_profiles.max_branches` (افتراضي ٣)، استبدل `MAX_LOCATIONS=3` الثابت في الموضعين بالقيمة الديناميكية؛ البانر يعرض اسم الباقة + `العدد/الحد`؛ **chips ملوّنة حسب الحالة**: 🔒 **أخضر = نشط** (مرتبط بعروض، يُحتسب، **لا يُحذف** حتى تنتهي/تُحذف عروضه) ، 🟠 **كهرماني = شاغر** (لا يُحتسب، **يمكن حذفه ✕** لتفريغ خانة) ، **أزرق = المختار حالياً**؛ + سطر «مفتاح ألوان» يشرح ذلك؛ زر الحذف ✕ يظهر **فقط** للشاغر، وللنشط شارة 🔒 بدل الحذف. كل رسائل تجاوز الحد صارت ديناميكية/عامة (لا «٣» ثابتة) وتذكر التواصل مع الإدارة للترقية. **(٢) إلغاء صندوق الإشعار المُعيق** — **السبب الجذري**: `SellerDashboard` فيه `useEffect` يطلق `customAlert` (نافذة «موافق» وسط الشاشة تحجب العمل) لـ**كل** إشعار حجز غير مقروء — تاجر بمئات الطلبات ما يقدر يخلّص. والمشتري ما كان يشوف شيء أصلاً (ما عنده صندوق، والإشعار العلوي مرهون بإذن المتصفح). **الإصلاحات**: حُذف الـuseEffect + state `processedNotifIds` نهائياً (انتهى الصندوق للتاجر). أُضيف **بانر علوي غير مُعيق** (`InAppBanner.tsx` + `inAppBanner` في AppContext، يُضبط في `onNotificationInsert` لصاحب الإشعار) — يظهر **للطرفين** (تاجر ومشتري) أعلى الشاشة، يختفي تلقائياً بعد ٥ ثوانٍ، **طبقته الخارجية `pointer-events:none`** فلا يحجب أي ضغط على الصفحة (التاجر يكمل عمله)، الضغط عليه ينقل لمكان الحجز/المحادثة. يبقى أيضاً الإشعار العلوي للنظام (`showRealTimeAlert`) + قائمة الإشعارات السفلية كما هي. هذا أيضاً يصلح «المشتري ما يوصله إشعار لما التاجر يرسل» لأن البانر داخل الموقع لا يعتمد على إذن إشعارات المتصفح. typecheck ✅. |
| **v10.72** | **٣ إصلاحات من بلاغ ناصر (٥ صور): قائمة التاجر + بحث عالمي موحّد + سكانر QR حيّ**. **(١) "حجوزاتي" تظهر لحساب التاجر** — `Sidebar.tsx` كان يضيف عنصر "📅 حجوزاتي" دائماً في القائمة الجانبية بلا استثناء؛ التاجر يضغطه فيفتح صفحة حجوزات فاضية أبداً (التاجر يدير الحجوزات من لوحة التاجر عبر السكانر، ما عنده "حجوزاته" كمشتري). **الإصلاح**: بناء `menuItems` شرطياً — يُحذف "حجوزاتي" متى ما كان `effectiveUserType === 'seller'` (يغطي التاجر الحقيقي + المدير وهو يعاين كتاجر). **(٢) البحث لا يجد المتجر + ناصر طلب "أقوى بحث عالمي في كل الصفحات"** — **تشخيص فعلي عبر Supabase MCP**: قاعدة البيانات فيها **متجر واحد فقط** (`shop=N15`, `name=تمتومي`) — **لا يوجد متجر اسمه "تاكي" إطلاقاً**، وRLS `users_select_all` يسمح لأي مستخدم (تاجر أو غيره) برؤية كل المتاجر فالمشكلة ليست صلاحيات. فبحث "تاكي" منطقياً لا يعطي متجر "تاكي" (غير موجود)، وكان مرادف ملوّث `'تاكي':['taki','site','موقع','تطبيق'...]` في `dealService` (اسم المنصة نفسها كمرادف منتج!) يسبب نتائج مضللة. **الإصلاحات (محرّك واحد مشترك لكل الصفحات، بلا دين برمجي): (أ)** `dealService.normalizeText` موحّد: يطوي ألف/ياء/تاء-مربوطة (أإآا→ا، يىئ→ي، ةه→ه، ؤ→و)، يزيل التشكيل والتطويل، **يحوّل الأرقام العربية-الهندية والفارسية إلى ASCII** (`ن١٥` ↔ `n15`)، يحوّل علامات الترقيم لمسافات ويطوي الفراغات. **(ب)** `dealService.searchScore` + `matchStores`: **ترتيب بالأهمية** (تطابق تام > يبدأ بـ > كلمة كاملة > يحتوي > ضبابي/مرادف) فأقرب نتيجة تطفو للأعلى بدل تطابق ضبابي ضعيف؛ حقول اسم المتجر/المنتج موزونة أعلى بكثير من الوصف. **(ج)** حذف مرادف `'تاكي'` المضلِّل. **(د)** `Home` + `DealsList` + `Nearby` الآن **كلها** تستخدم نفس المحرّك وتعرض شريط "المتاجر 🏪" المرتّب — البحث عن متجر بالاسم صار متسقاً وقوياً على كل صفحات الاكتشاف، والعروض تُرتَّب بالأهمية أثناء البحث. **(٣) كاميرا السكانر لا تظهر (الخلفية تبقى كما هي)** — **السبب الجذري**: عنصر `<video>` في `BarcodeScanner.tsx` كان يُركَّب فقط بعد `cameraActive=true`، لكن `startCamera()` يسند `videoRef.current.srcObject` **قبل** `setCameraActive` فيقع على ref = null والبث لا يُربط أبداً → مربع أسود. **زيادة**: لم تكن هناك أي مكتبة قراءة QR إطلاقاً (إدخال يدوي فقط) رغم أن رمز الحجز يُعرض للمشتري كـQR. **الإصلاح**: `<video>` صار **مركَّباً دائماً** (زر فتح الكاميرا فوقه كطبقة)، والبث يُربط في `useEffect` بعد التركيب (مضمون)؛ + **قراءة QR حيّة فعلية عبر `jsqr`** (حلقة رسم إطار على canvas بخنق ~١٨٠ms، مصغّرة لـ٦٤٠px للأداء، متوافقة مع iOS Safari) — رموز QR للحجز تُمسح فعلياً الآن لا إدخال يدوي فقط، مع إيقاف نظيف للكاميرا عند الإيجاد/الإغلاق. أُضيفت `jsqr` للحزم. typecheck ✅. |
| **v10.71** | **تحميل فوري للبيانات (٦-٧ ثوانٍ → عرض لحظي) — stale-while-revalidate**. بعد v10.70 الموقع تحسّن لكن ناصر بلّغ أن **تحميل البيانات يأخذ ٦-٧ ثوانٍ** رغم قلة المشتركين، ويريده ثانية-ثانيتين ويتحمّل ملايين المستخدمين. **القياس الفعلي (أرقام لا تخمين)**: الحزمة ليست السبب (JS ١٥٠KB مضغوط brotli عبر Vercel CDN، x-vercel-cache HIT — ممتاز). **السبب الجذري**: مشروع Supabase في **`ap-northeast-1` (طوكيو)** والمستخدمون في **السعودية** → كل طلب REST/Auth ~٣٠٠-٥٠٠ms ذهاباً وإياباً من جوال سعودي، ومسار التحميل البارد يصنع عدة طلبات متسلسلة فتتراكم لـ٦-٧ ثوانٍ. **الحلول المطبَّقة (أحدث نمط — مثل SWR/TanStack Query persistence): (١) طبقة لقطة محلية `snapshotCache.ts`** — كاش عرض stale-while-revalidate في localStorage: schema-versioned + TTL ٧ أيام + مفاتيح لكل مستخدم (uid) + مغلّف بالكامل في try/catch (الوضع الخاص/الحصة لا يكسر شيء). **(٢) عرض لحظي**: `deals` و`storeProfiles` تُهيّأ **متزامنة** من آخر لقطة في `useState(() => readSnapshot(...))` — الشاشة الرئيسية ترسم محتوى حقيقياً في **أول إطار** قبل أن يبدأ طلب الشبكة لطوكيو أصلاً؛ و`loading` يبدأ false لو وُجدت لقطة (لا spinner يحجب). قوائم المستخدم (حجوزات/إشعارات/مفضلة) تُهيّأ من لقطة `uid`-scoped لحظة معرفة هويته. السيرفر **دائماً** يكتب فوق اللقطة عند وصوله — اللقطة كاش عرض فقط لا مصدر حقيقة. **(٣) كتابة اللقطة** بعد كل جلب ناجح (initData + auth-listener SIGNED_IN + onRefreshAll + refreshDeals + refreshBookings). **(٤) مسح اللقطة** عند logout / SIGNED_OUT / deleteAccount (جهاز مشترك لا يسرّب حساباً سابقاً). **(٥) إزالة جلب مكرر حرج**: `bookingRepository.getByUser` كان يستدعي `dealRepository.getAll()` داخلياً (= جلب كل العروض **+ كل التقييمات** = رحلتا طوكيو إضافيتان على المسار الحرج كل مرة تُحمَّل الحجوزات). الآن يقبل `knownDeals` (AppContext يمرّر `dealsRef.current`)؛ وإن غابت، يجلب فقط صفوف العروض المُشار إليها بـ`.in('id', …)` بلا استعلام التقييمات. **(٦) `preconnect`** في `index.html` لـSupabase(طوكيو)+Google Fonts → يفتح DNS+TCP+TLS أثناء تحليل الـHTML فيوفّر ~رحلة كاملة على أول طلب. **النتيجة**: للمستخدم العائد (غالبية الحمل عند الملايين) الشاشة الرئيسية + قوائمه تظهر **فوراً (<١٠٠ms)** بلا انتظار الشبكة، والتحديث يجري صامتاً بالخلفية؛ والتحميل البارد أقل رحلات. **بدون دين برمجي** — كاش غير موثوق يُعاد التحقق منه دائماً، نسخة + TTL + عزل لكل مستخدم. **⚠️ يبقى قرار بنيوي لناصر**: نقل مشروع Supabase من طوكيو لمنطقة قرب السعودية (مومباي `ap-south-1` ~٣٠-٦٠ms، أو فرانكفورت `eu-central-1`) = تسريع ٥-١٠× على **كل** طلب للأبد وهو الإصلاح الحقيقي لمقياس الملايين — لكنه ترحيل بنية تحتية (مشروع/منطقة جديدة + نقل بيانات + تغيير مفاتيح البيئة + توقّف وجيز) يحتاج قرار المالك. |
| **v10.70** | **علاج جذري لثقل الموقع + "لازم أحدّث ٣ مرات" + إصلاحان UI من صور ناصر**. ناصر بلّغ أن الموقع ثقيل جداً ولا تظهر الحجوزات/الإشعارات/تسجيل الدخول إلا بعد تحديث الصفحة ٢-٣ مرات. **التشخيص الفعلي** (ليس النصائح العامة — العميل أصلاً Singleton، والجداول كلها داخل نشر realtime، وأحجام الجداول صغيرة جداً ٣ مستخدمين/٦ عروض فالـDB ليست العنق): المشكلة ١٠٠٪ في الواجهة — **(أ) عاصفة إعادة بناء قنوات realtime**: الـuseEffect المركزي للـrealtime في `AppContext` كان يعتمد على `[user?.id, showRealTimeAlert, reconcileStatus, addNotification]`؛ و`addNotification`/`showRealTimeAlert` يُعاد إنشاؤهما كلما تبدّل كائن `user` (٣-٤ مرات في كل دخول: null → getCurrentUser → JWT optimistic → صف DB → لغة). كل تبديل = `realtimeService.connect()` → هدم كل القنوات وإعادة اشتراكها أثناء الـhandshake → إشعارات/حجوزات INSERT تضيع حتى يحدّث المستخدم يدوياً. **(ب) جلب مزدوج/ثلاثي**: `initData` + `onAuthStateChange(INITIAL_SESSION)` + `onRefreshAll` (بعد ٥٠٠ms من connect) كلها تجلب نفس البيانات في كل فتح صفحة. **(ج) `onRefreshAll` يُطلق مرتين** عند العودة للتبويب (visibilitychange + focus، عتبة ١s لكليهما = ١٠ استعلامات). **(د)** `getFavorites/setFavorites/getFollowedMerchants` كانت تستدعي `supabase.auth.getUser()` (رحلة شبكة لخادم GoTrue) بدل `getSession()` المحلي. **الإصلاحات: C1** — الـeffect صار يعتمد على `[user?.id]` فقط؛ المعالجات تقرأ أحدث callbacks عبر refs (`showRealTimeAlertRef`/`addNotificationRef`/`reconcileStatusRef`) فالقنوات تُنشأ مرة واحدة لكل جلسة وتبقى مستقرة. **C2** — `onAuthStateChange` تُصفّى بالحدث: الجلب الثقيل يعمل فقط على `SIGNED_IN` صريح ومرة واحدة لكل id (`lastSignInHydratedIdRef`)؛ `INITIAL_SESSION` يملكه `initData` (الذي صار يملأ followed/keywords/smartAlerts/اللغة من نفس الملف بلا طلبات إضافية)؛ `TOKEN_REFRESHED`/`USER_UPDATED` لا تعيد الجلب إطلاقاً. **C3** — `realtimeService` فيه `requestRefresh()` يدمج أي دفعة triggers (visibility/focus/online/bfcache) في تحديث واحد بحد أدنى ٦s بين التحديثات؛ حُذف الـrefresh التلقائي ٥٠٠ms عند connect (يملكه initData)؛ pull-to-refresh الصريح يتجاوز المدمج. **C4** — الثلاث دوال تستخدم `getSession()` المحلي بدل `getUser()`. **النتيجة**: من ١٥-٢٥ طلب مكرر + إعادة اشتراك ٣-٤ مرات في كل تحميل → اشتراك واحد مستقر + تحميل واحد. الإشعارات/الحجوزات تصل فوراً بلا تحديث يدوي. **+ تحصين المقياس (طلب ناصر "ملايين في نفس اللحظة، بدون دين برمجي")**: مستشار أداء Supabase كان فيه **٢١١ تحذير**؛ كل سياسات RLS على الجداول الساخنة (deals/users/bookings/notifications/favorites/booking_messages/store_branches) كانت تعيد تقييم `auth.uid()`/`is_admin()` **لكل صف**. هجرة `v10_70_rls_initplan_hot_tables`: غلّفت كل النداءات بـ`(SELECT …)` (تقييم مرة واحدة لكل استعلام — مكافئ سلوكياً ١٠٠٪) + دمجت السياسات المكرّرة (users: سياستا SELECT/UPDATE → واحدة؛ store_branches: حذف `branches_admin_all` الـALL المتداخلة وطيّ admin عبر `is_admin()` لكل أمر). هجرة `v10_70_covering_fk_indexes`: ١٢ فهرس تغطية للمفاتيح الأجنبية. **النتيجة: ٢١١ → ١٥٦ تحذير، والجداول الساخنة السبعة = صفر تحذيرات** (المتبقي كله على جداول إدارية باردة). **UI-1 (صورة لوحة الطلبات)**: التاجر كان يرى UUID المشتري `1d38446e…` بدل الاسم — جدول `bookings` لا يخزّن اسم المشتري إطلاقاً، وRLS لا يسمح للتاجر بقراءة صف المشتري في `users`. الإصلاح: هجرة `v10_70_bookings_buyer_name` تضيف `user_name`+`user_phone` (denormalized، يُلتقط وقت الحجز) + backfill للحجوزات الموجودة (الطلبات الـ٣ القديمة عُبّئت)؛ `bookingRepository.save/getByUser/getByBarcode` + مُعالج realtime `onBookingChange` يكتبون/يقرؤون الاسم. **UI-2 (صورة الوضع الليلي)**: اسم المتجر واسم المنتج في `DealCard` كانا يستخدمان `var(--accent)`/`var(--dark)` وكلاهما داكن في الوضع الليلي (#1e293b/#334155) فيختفيان على الخلفية الداكنة → بُدّلا إلى `var(--text-primary)` (يتكيّف: داكن نهاراً، أبيض ليلاً) — بلا ألوان مثبّتة ولا دين برمجي. |
| **v10.69** | **إصلاحان حرجان من ناصر بعد اختبار v10.68: routing الإشعارات + الرسالة الثالثة لا تصل**. (١) **routing الإشعارات** — ناصر طلب أن أي إشعار حجز أو تأكيد استلام أو رسالة عند الضغط عليه يفتح **صفحة "حجوزاتي"** حيث تظهر المحادثة، لا صفحة المنتج. **التشخيص**: كل الإشعارات المتعلقة بالحجز (إنشاء، تأكيد استلام، رسائل، إكمال، إلغاء) تستخدم `type='booking'` ويتم تمييزها بـ`meta_data.audience` و`meta_data.isMessage`. قبل v10.69 كان buyer audience يُوجَّه إلى `/deal/{id}?barcode=...` — أي يفتح صفحة المنتج لا صفحة الحجز. **الإصلاح في `Notifications.tsx`**: غيّرت buyer destination إلى `/bookings?barcode={barcode}` + أضفت fallback لأي booking notification بدون audience محدد ليذهب لـ`/bookings?barcode=...`. (٢) **`Bookings.tsx` يحترم query parameter الـbarcode**: أضفت `useLocation` + useEffect يقرأ `?barcode=` من الـURL ويـauto-expand البطاقة المستهدفة + `scrollIntoView({ block: 'center' })` + شارة highlight بـ`var(--secondary)` (برتقالي) تختفي بعد 3.5 ثانية — نفس نمط `highlightedBarcode` في SellerDashboard. أضفت `id="booking-{barcode}"` على كل بطاقة (نشطة وسابقة) ليعمل الـscroll. النتيجة: المشتري يضغط إشعار "💬 رسالة جديدة من التاجر" → يفتح Bookings، البطاقة الصحيحة تتمدد تلقائياً، الصفحة تتمرر إليها، وbookingThread يظهر الرسالة فوراً. (٣) **الرسالة الثالثة لا تصل** — السبب الجذري: **realtime websocket على iOS Safari يموت بصمت** لما التطبيق يـbackgrounded (قفل الشاشة، تبديل تطبيقات، مكالمة)؛ أي `INSERT` على `booking_messages` يحدث أثناء الفجوة يضيع نهائياً — الـchannel يعيد الاشتراك لكن لا يـreplay الأحداث الفائتة. **اختبار حي** عبر Supabase MCP أكّد التشخيص: barcode `SEXKU2HX` يحتوي **٥ رسائل في الـDB** (٣ buyer + ٢ seller)، لكن جهاز التاجر كان يعرض ٤ فقط (counter: "أنت 2/3 — الطرف الآخر 2/3"). الرسالة المفقودة `3ac90efb...` "تفهم انت ولا لا" مخزّنة بصحة 100% في الـDB، RLS يسمح للتاجر بقراءتها، فقط الـrealtime ما وصّلها. **الإصلاح ١: في `BookingThread.tsx`** أضفت listeners لـ`visibilitychange` + `focus` + `pageshow(persisted)` على document/window — كلما الـthread يصبح visible (بعد background → foreground) يستدعي `fetchBookingMessages(barcode)` + `markBookingMessagesRead(barcode)` لسحب أي رسائل فاتت + تمييز رسائل الطرف الآخر كمقروءة. هذا يغلق الـgap بشكل deterministic بدون polling. **الإصلاح ٢: bug جانبي اكتُشف أثناء التشخيص** — `onRefreshAll` في `AppContext.tsx` (يُستدعى من realtimeService على focus/online/bfcache) كان يستبدل `bookings` بالكامل بـ`getByUser(user.id)` الذي لا يجلب `messages`. النتيجة: كل focus event يمسح كل الرسائل المخزّنة في الذاكرة، فالـthread يضطر للـrefetch من الصفر. **الإصلاح**: نسخت pattern الـ`refreshBookings` (يحفظ messages by barcode) إلى `onRefreshAll` فلا تضيع الرسائل عند الـrefresh العام. النتيجة الكلية: التاجر يفقد التركيز ويعود → الرسالة المفقودة تظهر تلقائياً خلال ميلي ثوانٍ، بدون pull-to-refresh يدوي. |
| **v10.68** | **سجل طلبات سابقة للتاجر + موقع زر الرجوع/اللغة في شاشات المصادقة**. (١) **سجل طلبات للتاجر**: قبل v10.68 كان تبويب "الطلبات" يعرض فقط `activeOrders` (status = pending / acknowledged) — أي طلب بعد ما يُكمل أو يُلغى يختفي نهائياً من شاشة التاجر. ناصر طلب أن تكون السجلات السابقة موجودة للرجوع لها لاحقاً. **التنفيذ**: أضفت state جديد `ordersFilter: 'active' \| 'history'` + sub-tabs أعلى تبويب الطلبات بشكل segmented control: **🔔 نشطة (N)** و **📜 السجل (N)**. الـcount badge على كل تبويب يعرض العدد فوراً. لما `ordersFilter === 'history'` يُعرض `pastOrders` (كان محسوباً أصلاً من قبل) مرتباً تنازلياً بـ`bookedAt`، كل طلب في **card قراءة فقط** (لا أزرار، لا scanner، لا chat) يعرض: اسم العرض، التاريخ والوقت (locale عربي/إنجليزي)، **شارة الحالة** (✅ مكتمل أخضر / ❌ ملغي أحمر مع خلفية شفافة)، اسم المشتري، الكمية، الرمز المرجعي (#barcode بـmonospace LTR)، وملاحظات المشتري/التاجر إن وُجدت. opacity 0.95 ليوحي بصرياً بأنها أرشيف. لو كان `order.deal` محذوفاً يعرض "عرض محذوف" بدل crash. **النتيجة**: التاجر يرجع لأي طلب سابق يتأكد من تفاصيله بدون مغادرة لوحة التحكم. (٢) **رفع زر الرجوع وزر اللغة عن الـnotch**: شاشات تسجيل الدخول وإنشاء الحساب في `Register.tsx` كانت `TopHeader` فيها بـ`padding: '16px 20px'` بدون احترام `safe-area-inset-top` — على iPhones مع dynamic island/notch (iPhone 14 Pro+، Pro Max) كانا "يلامسان الشاحن" حسب وصف ناصر. **الإصلاح**: غيّرت الـpadding إلى `calc(env(safe-area-inset-top, 12px) + 14px) 20px 16px` — يستخدم fallback 12px للأجهزة اللي ما تدعم safe-area + يضيف 14px فراغ بصري إضافي. هذا الـpattern موجود أصلاً في 13 مكان آخر في المشروع (DealDetails، Bookings، StoreDetails، Profile، إلخ) فيتسق مع البقية. الـTopHeader يُستعمل في ثلاثة `mode`s: landing / login / type / form — كل ما يظهر يستفيد من الإصلاح. |
| **v10.67** | **٤ إصلاحات UI بطلب ناصر** بناءً على ٤ لقطات. (١) **زر "احجز الآن" + الكمية كانوا fixed في أسفل DealDetails** — ناصر أراد القسم في نهاية الصفحة يـscroll مع المحتوى وليس مثبتاً. **الإصلاح**: أخرجت `<div className="book-cta">` من خارج `.page-content` ووضعته داخل الـscroll area كآخر عنصر، مع `marginBottom: calc(env(safe-area-inset-bottom) + 80px)` لإفساح مكان لـBottomNav الـfixed. شكّلته كـcard بـrounded border بدلاً من شريط ممتد على عرض الشاشة — يندمج بصرياً مع باقي الـcards. الـclassName أزلتها لتفادي قواعد `.book-cta { position:fixed }` الموجودة في styles.css (تركتها لئلا تكسر فئات أخرى مستقبلية). حذفت الـspacer الـ80px اللي كان فوق الـCTA. (٢) **زر تأكيد الحجز في modal "إتمام الحجز" كان مخفياً خلف BottomNav** — `z-index` الـmodal كان 1000، الـBottomNav 1100، فالـBottomNav كان يرسم فوق الزر. **الإصلاح**: رفعت z-index الـmodal إلى 1200، وأضفت `paddingBottom: calc(env(safe-area-inset-bottom) + 28px)` على المحتوى الداخلي + رفعت `maxHeight` من 90vh لـ92vh لإعطاء مساحة أكبر. (٣) **مربع التعليق أسود في الوضع الفاتح + الإشارة لا تظهر**: (٣أ) الـtextarea في `showReviewForm` كان بلا `background` ولا `color` صريحين، فعلى iOS Safari ورث خلفية النظام الافتراضية السوداء. أضفت `background: 'var(--card-bg)'` + `color: 'var(--text-primary)'`. (٣ب) `addRating` في AppContext كان `Promise<void>` يصمت على الفشل (RLS، شبكة، إلخ). `handleReview` ما يـawait. النتيجة: الفورم يُغلق، لا رسالة، التعليق ما يظهر — التاجر يحسبها فاشلة. **الإصلاح**: حولّت `addRating` إلى `Promise<boolean>` — يرجع false لو الـDB رفض، true لو نجح. `handleReview` صار `async` + ينتظر + يعرض "✅ تم إرسال تقييمك — شكراً لمشاركتك!" على النجاح، "❌ تعذّر إرسال التقييم..." على الفشل. زر "إرسال التقييم" صار "⏳ جاري الإرسال..." + disabled أثناء المعالجة. (٤) **القائمة الجانبية: "🏠 الرئيسية" مرتفعة جداً تلامس notch** — كان `paddingTop: env(safe-area-inset-top) + 16px`. **الإصلاح**: زدتها إلى `+ 36px` فيُعطي مسافة طبيعية مثل الـdrawer في تطبيقات iOS الأصلية، يجلس الـheader بمستوى أيقونة البطارية لا تحتها. |
| **v10.66** | **إصلاحان حرجان من ناصر بعد اختبار v10.64**. **(١) التنبيه ما اشتغل على حساب ثاني**: ناصر فعّل تنبيه إلكترونيات + رنج كم على حساب التاجر "تمتومي" لكن ما وصله إشعار. **التشخيص**: اختبرت في DB مباشرة (UPDATE smart_alerts للسيلر) — الـbackfill اشتغل صح وأنتج إشعار سليم بشكل احترافي ("📍 عرض بجانبك تماماً  •  📱 إلكترونيات"). يعني المشكلة في الـclient، لا في الـDB. **السبب الجذري**: `addSmartAlert`/`removeSmartAlert` في `AppContext.tsx` كانوا يستدعون `userRepository.saveProfile(...)` **بدون await** (fire-and-forget). على iOS Safari الـSDK من Supabase يعلق على inTabLock للـauth-refresh لمدة ١٥+ ثانية لما التطبيق يرجع من backgrounding، فـsaveProfile يفشل بـTimeoutError صامت. الـUI أظهر القاعدة (لأن setSmartAlerts ركض)، لكن الـDB ما حفظ شي، فالـtrigger ما عنده قاعدة يطابق ضدها → ولا إشعار. **الإصلاح**: حولّت addSmartAlert/removeSmartAlert إلى async مع `await` + try/catch — على فشل يـrevert الـlocal state ويرجع `false`، على نجاح يرجع `true`. **Profile.tsx handleAdd** صار async ينتظر النتيجة: على فشل يعرض "❌ تعذّر حفظ التنبيه — تحقق من الاتصال وحاول مجدداً"، على نجاح يعرض "✅ تم حفظ التنبيه. سيصلك إشعار فور نزول عرض مطابق (أو أي عرض نشط من آخر ٧ أيام)" — رسالة تشرح للمستخدم سلوك الـbackfill اللي أضفناه في v10.64. زر "إضافة قاعدة تنبيه" صار disabled مع "⏳ جاري الحفظ..." أثناء المعالجة. كذلك زر الـ✕ لحذف قاعدة صار يستخدم `handleRemove` async يعرض رسالة على فشل. **(٢) ضغط مكرر على الكروب → نفس الصورة ٤ مرات**: لما الـupload يبطئ، الـeditor يبقى ظاهراً، فالمستخدم يضغط "تطبيق ومتابعة" أو "تخطّي القص" عدة مرات → الـcallback يتنفّذ ٤ مرات → نفس الـFile يُرفع ٤ مرات. **الإصلاح**: (أ) في `ImageCropEditor.tsx` أضفت `dispatchingRef = useRef(false)` — متغير مرجع قابل للقراءة بشكل sync (بدون انتظار React re-render). أول ضغط يضع `dispatchingRef.current = true`، الضغطات اللاحقة ترجع فوراً. يُعاد ضبطه تلقائياً في `useEffect([file])` عند تقدم الـqueue للصورة التالية. غلّفت `onSkip` و`onCancel` بـ`handleSkip`/`handleCancel` يستخدمان نفس الحارس. (ب) في `SellerDashboard.tsx` غيّرت onApply/onSkip لتقدّم الـqueue **فوراً (sync)** ثم تـupload في الخلفية بدلاً من `await uploadCroppedFile(...)` ثم advance — هذا يخلي الـeditor يـunmount/يتبدّل للصورة التالية فوراً بمجرد الضغط، فما يتاح للمستخدم وقت لإعادة الضغط على نفس الزر. **النتيجة**: ضغطات سريعة متتالية = صورة واحدة فقط تُرفع، وحفظ التنبيه يفشل بصوت عالي وواضح بدلاً من الصمت. حذفت بيانات الاختبار من حساب التاجر (smart_alerts reset + notification deleted) فيـclean state لما يختبر ناصر بنفسه. |
| **v10.65** | **٦ تصنيفات جديدة** بطلب ناصر: 💈 صالون رجالي، 💇‍♀️ صالون نسائي، 🥩 ملحمة، 🧵 خياطة رجالية، 🪡 مشغل نسائي، 🩺 عيادات. **التنفيذ كان في ٤ أماكن لأن قائمة التصنيفات مكرّرة عن قصد** (للأنواع، للسيلر، للبوت، للـDB): (١) `src/data/mock.ts` — أضفت ٦ قيم لـ`Category` union وموقّعتها ثيمياً في `CATEGORIES[]` (الصالونات بعد Beauty، الملحمة بعد Supermarket، الخياطة بعد Laundry، العيادات بعد Pharmacy) عشان المستخدم يلقاها مع الفئات المشابهة. (٢) `src/data/botQuestions.ts` — نفس الـ٦ في قائمتي السيلر (`category`) والمشتري (`buyer_category`) بنفس الـemojis. (٣) `src/pages/DealsList.tsx` — حذف `slice(0, 12)` من شريط الفلاتر؛ كان يخفي أي تصنيف بعد الـ١٢ والآن كل التصنيفات تظهر مع تمرير أفقي (overflow-x: auto موجود أصلاً). (٤) `taki_category_label_ar/_en` في DB — أضفت WHEN clauses للستة الجدد عشان الإشعارات الذكية ترجم IDs لأسماء عربية مع emojis (المنطق في `taki_dispatch_smart_alert` يستدعي هاتين الدالتين مباشرة، فالـ٦ الجدد يشتغلون مع التنبيهات تلقائياً بدون أي تعديل في الترايقر). **النتيجة الفورية**: التاجر يقدر يضيف عرض في أي من الـ٦ تصنيفات، المشتري يقدر يحط أي منها في تنبيهاته الذكية، وإشعار التنبيه يستخدم الاسم العربي مع emoji ('🥩 ملحمة' بدل 'Butcher'). الـ٢٤ تصنيف القديمة تبقى بنفس الـIDs، فما في deals قديمة تأثرت. |
| **v10.64** | **التنبيهات الذكية: دمج كل التطابقات في إشعار واحد + backfill للقواعد الجديدة + تجهيز البوتات**. ناصر اشتكى إن تنبيه "إلكترونيات" ما اشتغل بعد ما فعّله. **التشخيص**: الترايقر `tr_deal_smart_notifications` يشتغل فقط `AFTER INSERT` على deals، فالعروض اللي أُنشئت قبل تفعيل التنبيه ما يُعاد تقييمها. ناصر أيضاً سأل: لو طابقت عدة تنبيهات نفس العرض (متابعة + ٥كم + إلكترونيات)، هل يجي ٣ إشعارات أو واحد؟ **رأيي**: واحد فقط بكل الأسباب مرتّبة بالأولوية — مثل Instagram/TikTok/Booking. **التنفيذ**: (١) **دالة `taki_dispatch_smart_alert(user, deal, geo, skip_existing)` مشتركة** تأخذ زوج واحد (مستخدم، عرض)، تقيّم القواعد (AND داخل القاعدة، OR عبر القواعد) + `followed_merchants`، وتنشئ إشعار واحد مدمج لو في تطابق. (٢) **ترتيب الأولوية في العنوان**: متابعة → نطاق → مول → مدينة → كلمة مفتاحية → تصنيف → منطقة. **chips** بنفس الترتيب في الـbody، محدودة بأعلى ٣ + "+N" لو أكثر. (٣) **تحويل ID التصنيف**: `taki_category_label_ar/_en` يحوّل 'Electronics' → '📱 إلكترونيات'، مع كل ٢٤ تصنيف من `data/mock.ts`. (٤) **شكل الإشعار الجديد (٣ أسطر مقتضبة)**: `{المنتج}  —  خصم X٪` / `🏪 {المتجر}` / `chip1  •  chip2  •  chip3`. مسافات مزدوجة قبل وبعد `•` للتنفس البصري. (٥) **مسافة طبيعية**: < 100 م → "بجانبك تماماً"، < 1 كم → "أقل من 1 كم منك"، ≥ 1 كم → "1.2 كم منك" (يقطع الـtrailing zeros). (٦) **Backfill جديد**: ترايقر `tr_smart_alerts_backfill` على `users AFTER UPDATE OF smart_alerts` يفحص كل قاعدة في `NEW` ضد قواعد `OLD` بـdeep-equality (jsonb_array_elements + NOT EXISTS)؛ لو فيه قاعدة جديدة أو معدّلة، يسحب آخر ٢٠٠ عرض active من آخر ٧ أيام ويعيد `taki_dispatch_smart_alert` مع `skip_existing=TRUE` (يـdedupe ضد الإشعارات القائمة عبر `meta_data.dealId`). الإزالات الصرفة لا تُشغّل replay. (٧) **خطأ مكتشف أثناء الاختبار**: `deals.created_at` نوعه BIGINT (epoch ms) لا timestamptz — الـbackfill كان يفشل بـ"operator does not exist: bigint > timestamp"، صحّحته بحساب `cutoff_ms := EXTRACT(EPOCH FROM NOW() - INTERVAL '7 days') * 1000`. (٨) **تجهيز البوتات (inert)**: أضفت أعمدة `telegram_chat_id`, `whatsapp_chat_id`, `notify_via_telegram`, `notify_via_whatsapp` (default FALSE) على users، + listener جديد في `server/bot.js` يستمع `notifications` INSERT ويرسل لو المستخدم فعّل الـopt-in. الترايقر يـpre-render النص في `meta_data.bot_message_ar/_en` فالبوت يبعثه كما هو بدون escape. **debounce** ٢٠ ثانية لكل user يمنع backfill من إطلاق burst إرسال. (٩) **اختبار حي**: حذف الإشعار القديم لـHbdd، toggle smart_alerts لـ`[]` ورجوع، الترايقر أنتج: title `🔥 تاكي نزّل عرضاً يطابق تنبيهك` + body `Hbdd — خصم 59٪ / 🏪 تاكي / 🔥 متجر تتابعه  •  📍 بجانبك تماماً  •  📱 إلكترونيات` — مطابق تماماً لما طلب ناصر "احترافي جداً جداً". |
| **v10.63** | **شاشة قص الصورة تفتح فوراً بالصورة جاهزة — بلا "صفحة ثانية"**. ناصر كان يشتكي من شاشة "تعذّر عرض الصورة" تطلع بعد ما يضغط Use Photo في كاميرا الآيفون، وبعدها أوضح: "اريد الكروب يظهر لحظة التقاط الصوره مباشره في نفس صفحة use photo وليس كفلتر ثاني وصفحة ثانيه". **السبب الجذري**: `ImageCropEditor` كان يفتح أولاً، وبعدها يطلب `URL.createObjectURL(file)` ويـmount الـ`<img>` وينتظر `onLoad` ليعرف `naturalWidth/Height`. على بعض ملفات iOS (HEIC variants أو race مع revoke الـobject-URL القديم لما القائمة تتقدّم) كان `onLoad` يطلق بـ`naturalWidth=0` → `setLoadFailed(true)` → شاشة الخطأ. وحتى لما تشتغل، حالة "⏳ جاري تحضير الصورة..." + fade-in opacity 0→1 كانت تخلّيها تحس صفحة منفصلة تأتي بعد. **الحل**: نقل decode الصور إلى مرحلة الـingestion في `SellerDashboard` نفسها — `preDecodeFile()` يقرأ الـFile كـdata URL بـFileReader ثم يفك تشفيرها بـ`new Image()` ويتأكد أن `naturalWidth > 0`. ينجح → يُدخل عنصر `{file, dataUrl, naturalW, naturalH}` في `cropQueue`. يفشل → يرفع الأصل مباشرة عبر `uploadCroppedFile(f)` بدلاً من عرض شاشة خطأ. **النتيجة**: `ImageCropEditor` صار يستقبل الـdata URL + الأبعاد كـprops، يرسم الصورة + إطار القص + المقابض على **أول paint** بدون أي state تحميل ولا fade-in ولا fallback خطأ. قلّصت الكود ~175 سطر (حذف load-state machine بالكامل). إضافة بسيطة: `useLayoutEffect` للقياس الأولي للـstage بدل `useEffect` يلغي flicker واحد frame كانت تنطّ فيه المقابض. الكود الآن من ناحية المستخدم: Use Photo → تظهر شاشة سوداء بالصورة الكاملة + إطار قص + 4 مقابض جاهزة للسحب فوراً، إحساس استمرار للـUse Photo لا صفحة ثانية. |
| **v10.62** | **إعادة كتابة كاملة لـCrop Editor** بناءً على تشخيص ناصر: (أ) الصورة ما تظهر بعد التقاطها من كاميرا الآيفون — الشاشة تطلع سوداء فيها إطار القص فقط بدون صورة. (ب) أزرار النسب مربكة و16:9 شكلها معكوس على شاشة بورتريه. (ج) ناصر طلب: احذف أزرار النسب وخلّ القص حرّ بالكامل. **السبب الجذري للمشكلة (أ)**: كان `<img>` في v10.60 يُعرَض بـ`width: auto` قبل ما `onLoad` يضبط `imgNatural` — يعني الصورة تطلع بحجمها الطبيعي (4032×3024 على iPhone) للحظة، تمتد آلاف البكسلات خارج المسرح، والـcrop frame مع الـbox-shadow أسود في كل مكان فيغطّيها. **التنفيذ الجديد**: (١) تغيير نموذج التفاعل من "إطار ثابت + pinch/pan للصورة" (انستغرام) إلى "صورة fit-to-stage + إطار قابل للسحب والتغيير" (مثل تطبيق الصور في الآيفون) — تجربة مألوفة وما عندها bug الـauto-size. (٢) **`fitScale`** يُحسب من `Math.min(stageW/visualNW, stageH/visualNH)` بحيث الصورة دائماً تنطبق داخل المسرح؛ width/height تستخدم `imgNatural.w * fitScale` بدل auto، فما فيه frame واحد قبل الـonLoad. (٣) إطار قص (`crop = {x, y, w, h}` بإحداثيات display-px) يبدأ بـ90% مركزي من الصورة المعروضة. (٤) تفاعلات: touch/mouse على جسم الإطار = move (مع clamp داخل الصورة)، touch/mouse على أي زاوية (radius 36px) = resize من تلك الزاوية بحرّية تامة (لا aspect lock، حد أدنى 60px). (٥) ٤ مقابض دائرية بيضاء على الزوايا — visual cue واضح. (٦) Rule-of-thirds guides داخل الإطار. (٧) **حُذفت جميع أزرار النسب** — رد على طلب ناصر "احذف المقاسات وخلّ الكروب حرّ فقط". (٨) **معالجة أخطاء**: حالة تحميل ("جاري تحضير الصورة..."), حالة فشل decode (يعرض رسالة + زر "رفع بدون قص" + إلغاء). (٩) زر التدوير ↻ ما يزال موجود (يدوّر الصورة بـ90° steps، displayBox يتحدث تلقائياً). (١٠) **Export** = نفس النموذج المُجرّب: temp canvas للـrotation ثم crop من نطاق المعروض بمقياس `fitScale`؛ output capped at 1600px JPEG q=0.9. |
| **v10.61** | **HOTFIX: لوحة التاجر كانت تتعطّل بـReferenceError on load** — ناصر شاف `ReferenceError: Cannot access 'tC' before initialization` (tC هو `locNameOf` بعد minification). السبب: في v10.59 أضفت `mergedLocationChips = useMemo(...)` يستدعي `locNameOf(d)` داخل factory الـuseMemo، لكن `locNameOf` كان مُعرّف **بعد** الـuseMemo في الـsource. الـuseMemo factory يُنفّذ فوراً عند أول render، فيقع في Temporal Dead Zone (TDZ) لأن `const locNameOf = ...` لم يُهيّأ بعد. أي تاجر يدخل لوحته كان يحصل على ErrorBoundary بدل الفورم. **الإصلاح**: نقلت تعريف `locNameOf` لقبل `mergedLocationChips`. تم التأكد بأن لا توجد TDZ ثانية في الـuseMemos الأخرى (`activeLocationsList`/`activeLocationKeys` تستدعي `locKeyOf` المُعرّف قبلها). |
| **v10.60** | **٣ إصلاحات** بناءً على بلاغ ناصر. (١) **تعذّر حفظ العرض: malformed array literal "تصنيفك"** — الـtrigger `handle_deal_smart_notifications` كان يفشل على أي عرض يطابق smart-alert فيه `categories` (أو region/city/mall/keyword/coord). السبب: `rule_parts := rule_parts || 'تصنيفك'` هو `text[] || unknown`، وPostgres عنده overloads متعددة (`anyarray || anyelement` و `anyarray || anyarray`) — مع right operand string literal بدون cast، الـplanner يقدر يختار overload الـarray ويحاول يـparse 'تصنيفك' كـtext[] literal (لازم يبدأ بـ`{`) → boom 22P02. **الإصلاح**: cast صريح `::TEXT` على كل الـ6 instances في الـfunction (regions/cities/malls/categories/keywords/coords) عبر migration `fix_handle_deal_smart_notifications_array_concat_cast`. تم التحقق بإعادة إنشاء deal Electronics في منطقة مطابقة — INSERT نجح بدلاً من الفشل. (٢) **swipe بين صور العرض داخل الـlightbox** — في `ImageZoomViewer` (DealDetails)، أضفت swipe-to-navigate بأسلوب Instagram: لما الصورة بـ scale 1، الـsingle-finger drag الأفقي يحرّك الصورة مع الإصبع ويـsnap للصورة التالية/السابقة عند الإفلات إذا تجاوز commit threshold (`max(60px, viewport*0.22)`) أو سرعة الإفلات > 0.6 px/ms. axis lock بعد 8 px منع التفعيل العفوي للـvertical scrolls. عكس الاتجاه في RTL يطابق ترتيب القراءة العربي. الـtransform بيتأخّر بـ`transition: none` أثناء الـswipe ويرجع `0.18s cubic` عند الإفلات للـspring-back. الـpinch-zoom + pan لما scale > 1 يلغي الـswipe (الـpriority للـzoom). (٣) **Crop editor جديد بعد التصوير/الاختيار** — مكوّن `ImageCropEditor.tsx` modal full-screen يفتح لكل صورة جاية من picker/camera/drag-drop/clipboard في `ingestFiles`. UX: قص بأربع نسب (1:1 افتراضي + 4:3 + 16:9 + حر)، تدوير ↻ بـ90°، pinch-zoom + pan داخل الـframe، rule-of-thirds guides داخل الـcrop window، dark overlay خارجها. الـExport عبر 2-step canvas: (أ) رسم rotated image على temp canvas upright، (ب) crop من الـtemp إلى output canvas. السقف 1600px على الجانب الأطول + JPEG quality 0.9 — يقلّص متوسط الصورة من ~4MB لـ~300KB بدون فقدان جودة ملحوظ. أزرار "تخطّي القص" (يرفع الأصل بدون تعديل) و"إلغاء" (يحذف الصورة من الـqueue). دعم batch — لو اختار 3 صور دفعة واحدة، يفتح الـeditor 3 مرات مع counter "1/3". الـ4-image cap محترم في الـqueue. حُلّ bug ثانوي: input file `e.target.value = ''` بعد كل اختيار حتى يقدر يختار نفس الصورة مرتين متتاليتين. |
| **v10.59** | **اللوكيشن السابق صار DB-backed مع حذف**. ناصر طلب: تأكد كل شي مربوط بالـDB، اللوكيشن السابق لو اضاف لوكيشنين تظهر، ويستطيع الحذف، والتثبيت/الحفظ/الحذف كلها في الـDB. **التنفيذ**: (١) **`branchRepository.ts` جديد** يلف `store_branches` table الموجودة أصلاً في الـDB (محمية بـRLS: select=public، insert/update/delete=own merchant_id فقط، admin bypass) — `listByMerchant`/`upsert`/`remove`. (٢) **AppContext** يحمّل branches للـsellers/admins على تسجيل الدخول (مع clear على SIGNED_OUT/account-switch)، يعرّض `branches` + `saveBranch` + `removeBranch` بـoptimistic updates ورجوع تلقائي على فشل الـDB delete. (٣) **`mergedLocationChips`** يدمج branches المحفوظة في الـDB مع لوكيشنات العروض النشطة (للـbackward-compat) + يـdedupe بنفس `locKeyOf`؛ branches تأخذ الأولوية فعندها `branchId` يفعّل زر ✕. (٤) **chip UI** صار بـ2 buttons داخل pill واحد — جسم الـchip يتبنّى اللوكيشن، و✕ منفصل بفاصل عمودي يحذف بعد customConfirm. (٥) **auto-save branch عند submit عرض**: بعد ما addDeal/updateDeal ينجح، يـupsert الـlocation كـbranch إن مو موجودة أصلاً (نفس الـlocKey dedupe) — fire-and-forget عشان فشل الـbranch upsert ما يفشل العرض. (٦) **`saveShopLocation`** صار يـmirror الموقع كـbranch بـ`is_primary=true`؛ إعادة الحفظ تحدّث نفس الصف (بدل ما تـcreate صف جديد كل مرة). **النتيجة**: التاجر يقدر الحين يحفظ عدة لوكيشنات (deals × N + primary shop)، يضغط chip ليتبنّى لوكيشن سابق فوراً، ويضغط ✕ ليحذف من الـDB — كله بـRLS سليم وحماية للمالك فقط. |
| **v10.58** | **إصلاحان** بناءً على لقطتي ناصر. (١) **زر الحجز كان مخفياً على صفحة المنتج**: المشتري كان يشوف صف "الكمية -/+" فقط فوق الـBottomNav بدون أي زر "🎟️ احجز الآن". السبب: قاعدة CSS قديمة `@supports (height: 100dvh) { .book-cta { bottom: 0 } }` في `styles.css:1581` (أُضيفت قبل v10.33 لأجل keyboard handling) كانت تُعيد `.book-cta` إلى bottom: 0 على كل المتصفحات الحديثة، فيغطّيه `.bottom-nav` (z-index 1100) كلياً — لكن لأن الكمية في **أعلى** الـCTA flex column والزر في **أسفله**، الكمية تطلع فوق الـBottomNav والزر يبقى مدفون تحته. الإصلاح: حذف الـ@supports block بالكامل — القاعدة الأساسية `bottom: calc(env(safe-area-inset-bottom, 0px) + 60px)` تتعامل مع الـdynamic viewport بدون أي override (الـbrowsers الحديثة تربط `bottom` بحافة الـvisual viewport السفلية تلقائياً عند ظهور الكيبورد). (٢) **زر "لوكيشن سابق" في إضافة عرض**: ناصر أراد فوق التلميح المُحاط بالأحمر في الصورة (تلميح "اضغط على الخريطة مباشرة") خانة تعرض لوكيشناته المُستخدَمة سابقاً كـchips، وعند الضغط على chip يملأ التطبيق منطقة + مدينة + نوع المكان + إحداثيات الدبوس تلقائياً ويحدّث الخريطة والفلاتر بضغطة واحدة. **التنفيذ**: (أ) قسم جديد قبل تلميح الخريطة في `SellerDashboard.tsx` يظهر فقط لو `activeLocationsList.length > 0` وغير مخفي بسبب banner تجديد اللوكيشن المحذوف (لتفادي التكرار). (ب) chips دائرية بـborder رمادي تتلوّن بـprimary لما تكون الـchip هي اللوكيشن الحالي المُختار في الفورم. (ج) **حسّنت `adoptLocationFromDeal`** ليضبط `locationType` أيضاً (يقرأ `LOCATIONS[locationId].type` للأماكن المعروفة، أو 'other' للوكيشن المخصص) — قبلها كان dropdown نوع المكان لا يتحدث مع الـchip فيضطر التاجر يغيّره يدوياً. الـchips تستخدم نفس مصدر بيانات الـ"الباقة تسمح بـ3 مواقع — المستخدم حالياً X/3" الموجود فوق الفورم، فما فيها duplication state. |
| **v10.57** | **إصلاح "الزر يلف للأبد"** عند حفظ موقع المتجر وعند إضافة العرض. السبب الحقيقي ما كان الـtimeout الخارجي (كان 30s/40s منذ v10.55 — كافٍ على شبكة سليمة)، بل **awaits داخلية بلا سقف** على `supabase.from(...).upsert(...)` نفسها. عندما تعلق الـSupabase JS SDK على inTabLock للـauth-refresh (يحصل خصوصاً على iOS Safari في الـbackground)، الـpromise الداخلية لا تنتهي أبداً، فالـ`withTimeout` الخارجي ينتظر زمنه الكامل بدون أي feedback خلال تلك المدة. **الإصلاحات**: (١) **`userRepository.saveProfile`** غلّفت الـupsert بـ`withTimeout(..., 15s)` داخلياً. (٢) **`dealRepository.save`** غلّفت كل من الـ3 upsert paths (الرئيسي + الـlegacy fallback + الـnoGeo fallback) بـ`withTimeout(..., 25s)` — الـDeal triggers (smart-notifications + location-cap + guard) قد تأخذ أطول قليلاً من user upsert. (٣) **حذف الـpre-sync** في `addDeal`: كان يستدعي `userRepository.saveProfile(user)` قبل insert الـdeal — مكرر بلا فائدة (handle_new_user trigger ينشئ الـuser row عند signup أصلاً)، ويأكل نصف الـtimeout budget على lock عالق. الـDB سيرمي FK error واضح في الحالة النادرة فعلاً اللي يفقد فيها الـrow. النتيجة: زر "حفظ كموقع دائم" وزر "حفظ وإضافة العرض" يفشلان بسرعة بدلاً من اللف للأبد، والمستخدم يحصل على رسالة قابلة للعمل بدلاً من spinner أبدي. |
| **v10.56** | **تنظيف البيانات الوهمية** بطلب ناصر. (١) **DB**: حُذفت كل العروض الـ5 التجريبية اللي كانت في `public.deals` (تيشرتات، الهلال، مغسلة سيارات، شاورما، منظف عفن) — كلها أنشئها الأدمن للاختبار + كانت كلها expired/paused قبل ما تُمسح. حُذفت أيضاً الجداول المرتبطة (`booking_messages`, `bookings`, `store_analytics_events`) لكل تلك العروض. الـDB الآن صفر deals — جاهزة للاستخدام الحقيقي. (٢) **Code**: حُذف `MOCK_DEALS` array من `src/data/mock.ts` (dead code من قبل v10.47 لما الـapp صارت DB-first). 0 مراجع له في باقي الكود فالحذف آمن. **النتيجة**: الصفحة الرئيسية الآن فاضية شرعياً، تنتظر عروض حقيقية من تجار حقيقيين. لما ناصر أو أي تاجر يضيف عرض من لوحة التاجر، يظهر فوراً. |
| **v10.55** | **٣ إصلاحات** بناءً على بلاغ ناصر. (١) **رابط التحقق كان يفتح localhost:3000**: `signUpWithEmail` ما يمرر `emailRedirectTo`، فالـemail link يستخدم Site URL الافتراضي = `localhost:3000`. الإصلاح: تمرير `emailRedirectTo: window.location.origin` في `signUp` options. الآن الرابط يفتح `https://taki-test-eight.vercel.app/...` تلقائياً. ⚠️ يحتاج إضافة الـURL إلى Supabase Redirect-URL allowlist (انظر تعليمات الـDashboard أدناه). (٢) **السعر العشري ممنوع** كان `.replace(/\D/g, '')` يفلتر النقطة فيمنع الفواصل (16.50 ر.س). الإصلاح: helper جديد `sanitizeDecimalInput` في `helpers.ts` يسمح بالأرقام + نقطة واحدة + سقف خانتين عشريتين (للهللات). type="text" inputMode="decimal" بدل type="tel". DB column كان `numeric` أصلاً (يدعم decimal بدون migration). (٣) **"تأخر الحفظ" تظهر فوراً**: الـtimeouts كانت 15s/20s — قصيرة جداً لحفظ deal يطلق `tr_deal_smart_notifications` + `tr_enforce_location_cap` مع RTT من AP-NE1 إلى السعودية. رفعتها: profile save 15s→30s، deal save 20s→40s. الـtimeout ما يزال موجود (لا يعلق spinner للأبد) لكن نادراً ما يطلق على network سليم. **Email-with-code vs link**: شاشة verify الموجودة في Register أصلاً تقبل كود 6 أرقام عبر `verifyOtp` RPC، لكن الـemail template الافتراضي يحتوي رابط فقط. تعديل التيمبليت ليضمن `{{ .Token }}` يحتاج Supabase Dashboard. |
| **v10.54** | **إصلاح OAuth UX** — في v10.53 الزرين كانا يظهران دائماً، ولما يضغطهم المستخدم بدون تكوين Google Cloud + Supabase Dashboard، الـbrowser كان ينتقل لصفحة `supabase.co` بـJSON: `Unsupported provider: provider is not enabled` ويترك التطبيق! الإصلاح: (١) **`skipBrowserRedirect: true`** في `signInWithGoogle/Apple` — يخلي الـcaller يمسك الـerror قبل ما يحدث redirect. (٢) **`handleOAuthClick(provider)` wrapper في Register**: يستدعي الـauth method ويتحقق من الـerror؛ لو `not enabled` يعرض customAlert عربي ودود: "⚠️ تسجيل الدخول عبر Google غير مفعّل حالياً. استخدم البريد وكلمة المرور." بدون أن يفقد المستخدم سياقه. (٣) **Feature flags في `platform_settings`** — صفّان جديدان `oauth_google_enabled` و `oauth_apple_enabled` (افتراضياً false). AppContext يقرأهما + يستمع للـrealtime تحديثات. (٤) **رندر شرطي**: الزرين والفاصل "أو" يظهرون فقط لو الـflag مفعّل. الآن الصفحة تبدأ بـForm نظيف بدون أزرار معطّلة. لتشغيل Google/Apple فعلياً: ناصر يخبر Claude "فعّل Google" بعد ضبط Google Cloud + Supabase Dashboard، Claude يرفع الـflag بـUPDATE واحد، الزر يظهر فوراً عبر realtime في كل الـclients. |
| **v10.53** | **تسجيل دخول Google + شاشة "أكمل بياناتك" + إصلاح الفضاء الأبيض** على صفحة Register. (١) **زر Google**: `authService.signInWithGoogle()` يستدعي Supabase OAuth بـprovider `google` مع `access_type=offline` للـrefresh token. زر أبيض بـlogo Google الرسمي (SVG inline) فوق زر Apple. (٢) **شاشة CompleteProfile جديدة** `src/pages/CompleteProfile.tsx` تظهر للمستخدمين القادمين من OAuth (Google/Apple) لأنهم لا يعطون phone/user_type/shop. تطلب: الاسم (prefilled من Google)، نوع الحساب (مشتري/تاجر بـcards مرئية)، رقم الجوال (مع validation + uniqueness check)، اسم المحل (إذا تاجر). تحفظ via `updateProfile` (partial-aware) + `supabase.auth.updateUser` لمزامنة metadata. (٣) **AuthRedirector الجديد** يفحص بعد OAuth callback إذا profile incomplete (phone فاضي أو seller بدون shop) ويوجّه لـ`/complete-profile` بدلاً من landing page. الـemail/password signup لا يتأثر لأنه يجمع كل البيانات في Register أصلاً. (٤) **إصلاح الفضاء الأبيض**: iOS Safari rubber-band overscroll كان يكشف `--body-bg` الفاتح (#f1f5f9) خلف الـgradient navy. الإصلاح: useEffect في Register + CompleteProfile يضع `documentElement.style.background = '#050a18'` ويسترجع عند unmount. **⚠️ لتفعيل Google OAuth فعلياً**: ناصر يحتاج (أ) إنشاء OAuth Client ID في Google Cloud Console، (ب) تفعيل Google provider في Supabase Dashboard → Auth → Providers مع الـclient ID/secret. الكود جاهز لكن الزر سيرجع "provider not enabled" حتى يتم التكوين. |
| **v10.52** | **تحصين أمني — جولة ثانية** فحص شامل للـclient + البوتات + headers. **Client side**: تأكيد 0 XSS vectors (لا `dangerouslySetInnerHTML`, لا `eval`, لا `document.write`)، 0 secrets في localStorage (فقط UI cache + Supabase auth tokens المعيارية)، كل CORS proxies في الـCSP مستخدمة فعلاً (fallback chain لـmaps.app.goo.gl). **bot.js — 3 ثغرات حرجة اتصلحت**: (١) WhatsApp HMAC كان يـsignature على `JSON.stringify(req.body)` بعد ما Express parsed الـbody — أي تغيير في whitespace/key-order يفشل المطابقة. الإصلاح: `express.raw({ type: 'application/json' })` على `/webhook/whatsapp` فقط، والـHMAC يُحسب على الـraw bytes الفعلية. (٢) لو `WHATSAPP_APP_SECRET` فاضي، الـsignature check كان **يُتجاوز كلياً** — أي attacker يقدر يرسل POST مزيف. الآن hard-reject بـ503. نفس الإصلاح طُبّق على `/webhook/telegram` مع `TELEGRAM_WEBHOOK_SECRET`. (٣) أُضيف **global rate-limit middleware** (300 req/5min/IP) فوق الـper-chat limits الموجودة. **Headers/CSP**: تأكيد HSTS 2 سنة + preload، X-Frame-Options: DENY، COOP/CORP same-origin، Permissions-Policy محدّد، CSP صارم بـscript-src 'self' وframe-ancestors 'none'. **HIBP**: تبيّن أن الـorganization على **free plan** و"Leaked Password Protection" حصرية لـPro ($25/شهر) — لكن validation الـclient (8 chars + uppercase + lowercase + digit + symbol) أقوى من متطلبات HIBP الافتراضية، فالـwarning compensating-control موجود. |
| **v10.51** | **تحصين أمني شامل** بعد تنبيه Supabase advisor (143 ثغرة). أهم الإصلاحات: (١) **ERROR حرج**: `sa_cities_geo` كان RLS مغلق — أي anon يقدر يحذف 73 صف مدن السعودية ويعطّل `find_nearest_sa_city`. تم enable RLS + قراءة عامة + كتابة admin-only. (٢) **booking_messages_update_recipient** كانت `WITH CHECK = true` — الطرف المستلم يقدر يعيد كتابة sender_role/content/barcode لأي رسالة! حُذفت كلياً (الـRPC `mark_booking_messages_read` SECURITY DEFINER هو الكاتب الوحيد). (٣) **store_analytics_events.an_insert_anyone** سمحت لـanon بحقن أحداث analytics — حُذفت (كل الكتابات تمر عبر RPCs SECURITY DEFINER). (٤) **storage 'deals'** كان أي anon يقدر يرفع أي ملف بأي حجم — قيّدتها لـauthenticated + صور فقط (JPEG/PNG/WebP/GIF) + سقف 10MB + منع الـlisting، مع الحفاظ على CDN reads. (٥) **16 function بـsearch_path قابل للتغيير** (vector لـsearch_path injection) — pin `SET search_path = 'public'`. (٦) **6 functions cron-only بدون admin check** (`purge_expired_accounts`, `expire_trials`, `cleanup_old_activity`, `prune_analytics_events`, `send_trial_ending_notifications`, `send_trial_warnings`) — أي مستخدم authenticated كان يقدر يستدعيها ويحذف الحسابات أو يفرض انتهاء الفترات التجريبية! `REVOKE EXECUTE FROM anon, authenticated, PUBLIC`. الـ7 admin_* functions تركت لأنها فعلاً تحقق `user_type='admin'` داخلياً. **النتيجة:** 143 → 111 (-32)، 1 ERROR → **0**، 16 search_path → 0، 2 always-true RLS → 0، 1 bucket listing → 0. المتبقي 110 SECURITY DEFINER warnings مقصودة (RPCs للـclient كلها فيها admin checks داخلية) + 1 تحتاج Dashboard (تفعيل Leaked Password Protection). ملف الـmigration: `supabase/migration_v10_51_security_hardening.sql`. |
| **v10.50** | إصلاح ٣ مشاكل في صفحة إنشاء الحساب الجديد. (١) **Database error saving new user** عند كل محاولة تسجيل: السبب trigger `handle_new_seller_trial` كان يحاول `UPDATE users SET trial_starts_at, trial_ends_at` لكن الأعمدة **غير موجودة** في `public.users` — كل صف auth.users جديد كان يفشل بـ `SQLSTATE 42703`. الإصلاح: حذف الـUPDATE المُكرّر (التعليق نفسه يقول "redundant trial tracking"؛ `store_profiles.subscription_started_at/expires_at` هي المصدر الموثوق). (٢) **ألوان غامقة على خلفية غامقة** في checklist قوة كلمة المرور — `var(--accent)` يحُل لـ`#1e293b` (slate-800 شبه أسود) على gradient navy فأصبح غير مقروء. الإصلاح: استبدال صريح بألوان فاتحة (`#10b981` للمحقّق، `#94a3b8` لغير المحقّق) في الـchecklist وفي شاشة verify أيضاً (الإيميل + رسالة "افتح بريدك"). (٣) **حذف حقل "موقع المتجر (المدينة/الحي)"** من نموذج تسجيل التاجر — موقع المتجر الفعلي يُحدد لاحقاً من داخل التطبيق عبر اللوكيشن (نظام v10.42-v10.46 للوكيشنات الثلاث). أُستبدِل بسطر توضيحي صغير "📍 سيتم تحديد موقع المتجر لاحقاً من داخل التطبيق". |
| **v10.49** | "معاينة كمشتري" في حساب الأدمن صارت فعلياً تدخّله كمشتري. كان الأدمن لمّا يضغط 🛒 مشتري من الشريط الجانبي، يروح للصفحة الرئيسية بس الـBottomNav يظل بشريط التاجر (لوحتي/صفحتي) وصفحة "حسابي" تعرض شارة "مدير النظام 👑" وقسم بيانات المتجر. السبب: `BottomNav` كان يقرأ `user.userType === 'admin'` مباشرة (يتجاهل `effectiveUserType`)، وصفحة Profile كذلك. الإصلاح: (١) `BottomNav.tsx` صار يعتمد فقط على `effectiveUserType` — `showSellerNav = (effectiveUserType === 'seller' \|\| 'admin')`. (٢) `Profile.tsx` صار يقرأ `effectiveUserType` لكل فروع العرض (اسم/شارة الدور، الإحصائيات بائع/مشتري، قسم "معلومات التواصل للمتجر"). نتيجة: الأدمن في وضع معاينة كمشتري يشوف شريط المشتري كاملاً (الرئيسية/الإشعارات/حولي/حجوزاتي/حسابي) + شارة "مشتري ⭐" + إحصائيات التوفير، تماماً مثل ما يصير لمّا يضغط معاينة كتاجر. |
| **v10.48** | ثلاث مشاكل في نموذج إضافة العرض على الجوال: (١) **الخريطة لا تتحرك** بعد لصق رابط قوقل ماب رغم استجابة فلتر المنطقة/المدينة. السبب: `MapCenterUpdater` كان يستدعي `setView` بـ`animate:true, duration:0.4` داخل `setTimeout(0)` — على iOS Safari عند فتح modal نجاح فوق الخريطة، `invalidateSize()` يقيس tile-grid بصفر ارتفاع فينقطع الـpan. الإصلاح: pan فوري بـ`animate:false` ثم إعادة `invalidateSize+setView` بعد 300ms كـsafety net، كله داخل try/catch. (٢) **زر "حفظ كموقع دائم للمتجر" يعلّق spinner** للأبد. السبب: `await updateProfile()` بدون أي سقف زمني — لو الـSupabase auth-refresh عالق في الـinTabLock، الـawait ينتظر للأبد. الإصلاح: `withTimeout(updateProfile(...), 15000)` + رسالة "⏱️ تأخر الحفظ، تأكد من اتصال الإنترنت" عند تجاوز السقف. (٣) **نفس المشكلة في "حفظ وإضافة العرض" و"إضافة وتكرار"** — `withTimeout(addDeal/updateDeal, 20000)` + رسالة timeout واضحة. الـhelper `withTimeout` + `TimeoutError` مضافين في `utils/helpers.ts` لإعادة الاستخدام. |
| **v10.47** | **DB-first فعلياً** — إزالة أي تخزين تطبيقي محلي. (١) `addDeal` و `updateDeal` ينتظران الـDB قبل أي تحديث على الـUI (لا optimistic update). إذا الـDB رفض → رسالة فشل واضحة + لا يظهر شيء في الواجهة. (٢) إزالة رسالة "تم الحفظ محلياً لكن المزامنة فشلت" نهائياً — رسالة كاذبة لأنه لا يُحفظ شيء فعلياً عند فشل الـDB. (٣) إزالة `TAKI_LAST_PROMO_*` من localStorage — الـDB column `users.last_promo_check_at` هو المصدر الوحيد للـthrottle. (٤) إزالة `TAKI_LAST_PATH` من localStorage في App.tsx — المسار بعد تسجيل الدخول يُحدد من `user.userType` فقط. (٥) إزالة `TAKI_SEASONAL_NOTIFY` — جدول `push_subscriptions` هو الـopt-in الحقيقي. **يبقى ضرورة تقنية:** Supabase auth tokens (لا يمكن إزالتها) و SW versioning. |
| **v10.46** | فرض حد 3 لوكيشنات على مستوى **قاعدة البيانات** (كان client-only). trigger جديد `tr_enforce_location_cap` على deals يطلق BEFORE INSERT/UPDATE OF (status, location_id, map_lat, map_lng) — يحسب الـlocKey بنفس طريقة الـclient (loc:<id> أو geo:<lat>,<lng> مدور 3 منازل) ويرفض بـ `LOCATION_LIMIT_EXCEEDED` إذا تجاوز. admin يتجاوز. status≠active لا يخضع. `addDeal` و `updateDeal` في AppContext يكشفان الـerror code ويعرضان رسالة عربية محددة (بدل "تم الحفظ محلياً لكن المزامنة فشلت"). ملف migration: `supabase/migration_v10_46_enforce_seller_location_cap.sql`. |
| **v10.45** | إضافة عبارة "تم تغيير موقعك السابق" في banner تجديد العرض المنتهي (v10.44). النص الكامل صار: "انتهت كل عروض موقعه السابق فحُذفت الخانة. تم تغيير موقعك السابق — اختر أحد مواقعك الحالية الـ3 لتجديد هذا العرض:". لا تغيير في الـlogic. |
| **v10.44** | حالة خاصة على قاعدة v10.43 — تجديد عرض منتهي بلوكيشن "محذوف" (بمعنى: انتهت كل عروض الموقع وتحررت الخانة قبل ما يحاول التاجر التجديد). الكشف: editing deal بـstatus غير active، locKey الأصلي ليس في activeKeys، و activeKeys.size = 3. النتيجة: banner برتقالي يظهر فوق قسم الموقع برسالة "تم حذف لوكيشن العرض السابق" + chips قابلة للضغط للمواقع الـ3 الحالية. ضغطة على chip → adoptLocationFromDeal يضبط locationId/region/city/mapPos تلقائياً وينظف googleMapsLink. |
| **v10.43** | تعديل قاعدة v10.42 — التعديل يخضع للحد أيضاً. الـ3 لوكيشنات قيد ثابت على كل العمليات (إضافة + تعديل). نقل منتج موجود لـلوكيشن 4 جديد **ممنوع** الآن — التاجر يجب يحذف كل منتجات أحد مواقعه أولاً لتفريغ خانة. counter مرئي حتى في الـedit mode. زر "حفظ التعديل فقط" يصير معطّل عند wouldExceedLimit. admin بدون قيود (كما كان). |
| **v10.42** | حد أقصى 3 لوكيشنات فريدة لمنتجات التاجر النشطة في الباقة الأساسية. الحساب: locationId مفهرس (مول/سوق) أو إحداثيات مدوّرة لـ3 منازل (~110م) للدبابيس المخصصة. **التعديل** على منتج موجود حر بالكامل (يقدر التاجر ينقل أي منتج لأي مكان حتى لو أنتج 4 لوكيشنات). **الإضافة** الجديدة في لوكيشن 4+ مرفوضة مع رسالة وزر "إدارة الاشتراك". counter مرئي في النموذج "X / 3 مواقع" مع تلوين أصفر/أحمر. admin يتجاوز القاعدة. آلية الـupgrade لباقة أعلى موجودة في backlog. |
| **v10.41** | ثلاث مشاكل مترابطة: (١) **DB**: فشل مزامنة المنتج مع السيرفر `new row violates RLS for table notifications` — الـtrigger `handle_deal_smart_notifications` كان يحاول إدراج إشعارات للمتابعين بصلاحيات الـseller بدل صلاحيات النظام. الإصلاح: `SECURITY DEFINER` على الـfunction. ملف migration: `supabase/migration_v10_41_smart_notifications_security_definer.sql`. (٢) **علم أوكرانيا**: Leaflet 1.9+ يحقن العلم في الـattribution badge؛ أُضيف `attributionControl={false}` على كل MapContainer (SellerDashboard + Nearby). (٣) **الخريطة لا تتمركز** بعد حلّ الرابط/الفلتر: `MapCenterUpdater` يستدعي `flyTo` على tile-grid قديم. أُضيف `invalidateSize()` قبل `setView` ليُعيد Leaflet حساب الـviewport. |
| **v10.40** | لوحة البائع: تبويب "إضافة +" كان يفتح صفحة "السكانر" بدل نموذج الإضافة. السبب: فحص الاشتراك كان يقرأ من `storeProfiles.subscription_expires_at` (حقل قديم لا يُحدّث للتجار في فترة التجربة ولا للـadmin) → `isSubscriptionValid=false` → الـform يسقط للـscanner fallback. الإصلاح: (١) admin يتجاوز فحص الاشتراك دائماً. (٢) المصدر الحقيقي للاشتراك صار `merchant_subscriptions` (status + trial_ends_at + current_period_end). (٣) لو فعلاً لا اشتراك للتاجر، تظهر لوحة "الاشتراك مطلوب" مع زر إدارة الاشتراك بدل صفحة السكانر. |
| **v10.39** | **DB fix حرج** — سياستا RLS على جدول users (`users_select_all` و `users_update_admin`) كانتا تستخدمان `EXISTS (SELECT FROM users ...)` داخل تعريفهما → infinite recursion عند أي UPDATE/UPSERT على users. النتيجة: `saveProfile` يفشل بصمت → "حفظ كموقع دائم" + "إضافة وتكرار" + addDeal trigger smart_notifications كلها معطلة. الإصلاح: استبدال الـsubquery بـ `is_admin()` SECURITY DEFINER. ملف الـmigration: `supabase/migration_v10_38_fix_users_rls_infinite_recursion.sql`. |
| **v10.38** | نموذج إضافة العرض — تعليق spinner عند فشل رابط Google Maps: (١) `handleMapLinkUpdate` يسجّل المحاولة في `lastResolvedLink` حتى عند الفشل، فالضغطات التالية على Save/حفظ كموقع دائم لا تكرر الـresolution. (٢) إخفاء تنبيه الفشل عن الـauto-resolver (يبقى فقط لو ضغط المستخدم "تحديد" يدوياً). (٣) re-entrance guard (Ref) يمنع تشغيل resolution موازية. (٤) spinner منفصل لكل زر (`submitMode` state) — لا يدور الزرّان معاً. (٥) زر "حفظ كموقع دائم" له spinner مستقل ولا ينتظر أكثر من 2s. |
| **v10.37** | نموذج إضافة العرض: (١) تنبيه inline أحمر فوري + حدود حمراء + تعطيل أزرار الحفظ لو `discountedPrice >= originalPrice`. (٢) زر "حفظ كموقع دائم" يحل رابط Google Maps أولاً (cap 3s) قبل الحفظ، فلا يحفظ الإحداثي الافتراضي. (٣) submitAction: نقل الـvalidation السريع قبل بدء الـspinner + تقليل link-resolution timeout من 5s إلى 3s + spinner داخل "إضافة وتكرار" أيضاً — لا تعليق ولا انتظار 5 ثوان للـvalidation. |
| **v10.36** | StoreDetails: العرض الزمني (بدون stock cap) كان يظهر في تبويبَي "نشطة" و"سابقة" معاً لأن `quantity<=0` كان يُعتبر sold-out حتى بدون initialQuantity. الفلترة الآن متطابقة مع SellerDashboard: العرض في "سابقة" فقط إذا status=expired/paused أو (active && sold-out-with-cap) أو (active && timed-out). |
| **v10.35** | BottomNav ثابت فعلياً في كل الصفحات — رفع z-index من 50 إلى 1100 (فوق Leaflet) + `isolation: isolate` على `.leaflet-container` + نقل BottomNav خارج PullToRefresh في Home و Bookings (translateY كان يكسر `position: fixed`) |
| **v10.34** | auto-version-check على كل page load: يقارن CACHE_NAME الـserver مع الـcached محلياً ويـapply تلقائياً إذا اختلف |
| **v10.33** | BottomNav على DealDetails + رفع زر الحجز فوقه + خريطة حولي بـ120 px clearance + Sidebar بدون flex:1 |
| **v10.32** | تثبيت BottomNav (إزالة auto-hide) + فراغ تحت خريطة "حولي" في وضع map-only |
| **v10.31** | إزالة Pull-to-Refresh من حولي + BottomNav يكشف على أي scroll-up (مثل X) |
| **v10.30** | Pull-to-Refresh spinner ≤ 700ms cap + fire-and-forget refresh per page |
| **v10.29** | PTR indicator centered + BottomNav auto-hide بنمط X (لاحقاً أُزيل في v10.32) |
| **v10.28** | Pull-to-Refresh component + applySwUpdate نووي (cache nuke + cache-busted reload) |
| **v10.27** | زر "📍 موقعي" في حولي يمسح فلاتر region/city تلقائياً |
| **v10.26** | Profile header غير sticky (override .premium-bar للـscroll الطبيعي) |
| **v10.25** | partial-aware saveProfile (يمنع wipe المتابعة) + StoreDetails notch + UpdateBanner |
| **v10.24** | عرض المنطقة + المدينة على بطاقة الموقع في DealDetails |
| **v10.23** | فلتر sort في صفحة حجوزاتي (newest-first default) |
| **v10.22** | realtime resync threshold 1s + pageshow bfcache handler + live GPS + Bookings header notch |
| **v10.21** | محادثة المشتري ↔ التاجر (٣+٣ رسائل) — جدول DB + RPCs + realtime + UI |
| **v10.20** | atomic booking RPCs (complete/acknowledge/cancel) + await/rollback — إصلاح "الحجز يرجع" |
| **v10.19** | فلتر region/city: deals.region + deals.city columns + DB trigger update + backfill 6 rows |
| **v10.18** | رسالة التاجر تظهر دائماً في حجوزاتي (status-driven fallback) |
| **v10.17** | إزالة inner scroll من صندوق التصنيفات في Smart Alerts (Profile) |
| **v10.16** | Permissions-Policy: camera=(self) — فتح الكاميرا في الباركود + KM label overflow fix |
| **v10.15** | SW cache bump إجباري كل deploy + وقت الوصول مشي/سيارة في حولي |
| **v10.14** | dark mode --primary → emerald `#10b981` (يصلح 33 موقع نص خفي دفعة واحدة) |
| **v10.13** | زر "✅ تم الحجز" → ينقل لـ/bookings + شارة الإشعارات في الدارك مود + distance badge |
| **v10.12** | Vercel SPA rewrites — يصلح صفحة 404 على أي رابط مباشر |
| **v10.0–v10.11** | (سابق للجلسة) — v10.0 SW cache strategy، v10.1 auto-recovery، v10.4–10.11 إصلاحات الإدارة + Vercel deploy |

---

## 🧠 ذاكرة دائمة (gotchas)

- **Parcel preview لا يشتغل في sandbox (EPERM)** — أعتمد على typecheck + Vercel deploy.
- **Custom location_id (`custom_<ts>`)** لا يطابق LOCATIONS array — حُلت في v10.19 عبر deals.region/city columns + find_nearest_sa_city RPC.
- **iOS Safari pauses realtime websocket في الـbackground** — الـpageshow + 1s threshold يحل ذلك (v10.22, v10.28).
- **controllerchange على iOS قد يأخر ثواني** — applySwUpdate الآن لا ينتظره، يمسح الكاش ويعمل `location.replace` فوراً (v10.28).
