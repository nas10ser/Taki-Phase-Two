# ops — نصوص تشغيل الخادم (نسخة للمراجعة)

المستودع وحده لا يُعيد بناء الخادم. هذا المجلّد نسخة **للقراءة والمراجعة** مما
يعمل فعلاً على خادم جدة، أي تعديل يُطبَّق في الموضعين معاً.

⚠️ المستودع **عام** — لا يُرفع هنا أي سرّ ولا أي بيانات عملاء.

| الملف | مكانه الحيّ | ماذا يفعل |
|---|---|---|
| `taki-backup.sh` | `/usr/local/bin/taki-backup.sh` عبر `/etc/cron.d/taki-backup` | النسخة اليومية ٣:٣٠ فجراً: القاعدة **والصور**، ١٤ نسخة من كلٍّ، وحارس حجم يرفض نسخة فاسدة |
| `deleted-orphan-images-2026-09-13.txt` | — | سجلّ الـ١٤١ ملفاً المحذوفة في v14.16 (نسختها في `taki-orphan-images-2026-09-13.tar.gz`) |
